# 重构规范（v1.47）

> 状态：定稿（随实现更新）
> 关联文档：`需求文档.md`（v1.86）、`技术栈规范.md`（v1.82）、`使用手册.md`（v1.130）
> 说明：本文档定义本项目代码重构的目标、触发条件、流程、边界与评审要求，保证代码演进过程结构清晰、行为稳定。

---

## 目录

- [1. 重构目标与原则](#1-重构目标与原则)
- [2. 何时发起重构（触发信号）](#2-何时发起重构触发信号)
- [3. 重构前置条件（安全网）](#3-重构前置条件安全网)
- [4. 重构流程](#4-重构流程)
- [5. 代码组织与分层规范（后端）](#5-代码组织与分层规范后端)
- [6. 前端重构规范](#6-前端重构规范)
- [7. 命名规范（重构时统一执行）](#7-命名规范重构时统一执行)
- [8. 常见坏味道与处理手法对照表](#8-常见坏味道与处理手法对照表)
- [9. 重构边界与红线](#9-重构边界与红线)
- [10. 提交与评审](#10-提交与评审)
- [11. 重构记录表](#11-重构记录表)
- [12. 变更记录](#12-变更记录)

---

## 1. 重构目标与原则

- **目标**：提升代码的可读性、可维护性、可测试性，不改变外部行为。
- **原则**：
  - 小步重构：每次改动可编译、可运行、可回滚；
  - 先测试后重构：重构区域必须有测试基线；
  - 一次只做一件事：重构与功能开发分离，禁止夹带；
  - 优先消除重复与坏味道，而非追求过度抽象；
  - 重构涉及架构/API 契约变化时，必须更新对应文档并知会评审。

---

## 2. 何时发起重构（触发信号）

出现以下任一信号即可立项重构：

| 信号 | 示例 |
| --- | --- |
| 重复代码 | 同一逻辑在 3 处以上复制（如解析、错误处理） |
| 过长函数/类 | 函数 > 50 行、类职责 > 2 个 |
| 过度耦合 | 跨层调用、循环依赖、services 直拼 SQL |
| 魔法值散落 | 状态码、类型字符串、阈值散落各处 |
| 命名失真 | 名称无法表达意图，需读实现才懂 |
| 测试困难 | 依赖无法注入、单测需起全链路 |
| 缺陷高发 | 同一模块频繁修 bug、回归反复出现 |
| 注释解释代码 | 大量「此处不能动」「为什么这样写」注释 |

---

## 3. 重构前置条件（安全网）

1. **测试基线**：先为被重构区域补齐或确认测试（pytest / Vitest），确保重构前后行为可对比。
2. **行为冻结**：重构期间不新增功能、不修无关 bug；确有 bug 需单独提交并说明。
3. **小步可回滚**：每次提交独立可运行；复杂重构拆分为多个 commit。
4. **数据安全**：涉及数据库结构变化必须走 Alembic 迁移，禁止原地改表导致数据丢失。

---

## 4. 重构流程

1. **识别与立项**：明确坏味道、目标结构与验收方式；
2. **建立测试基线**：覆盖被重构区域的核心路径（必要时先补测试）；
3. **设计目标结构**：不越过分层边界（`api → services → repositories → models`）；
4. **小步执行**：抽取/移动/重命名，每步运行对应测试；
5. **验证行为不变**：全部测试通过 + 人工冒烟（页面/接口关键路径）；
6. **提交**：独立 `refactor` commit，说明动机与方法（见 §10）；
7. **文档同步**：若结构/配置/API 变化，同步更新 `技术栈规范.md`、`.env.example` 等。

---

## 5. 代码组织与分层规范（后端）

- 分层：`api（路由）→ services（业务）→ repositories（数据）→ models（模型）`；
- 依赖方向：上层依赖下层，**禁止反向依赖与跨层调用**；
- 依赖注入：数据库会话、LLM 客户端通过参数/构造器注入，禁止服务内直接 `import` 单例创建连接；
- 公共能力（配置、日志、常量）放 `core/`，禁止各模块各自复制；
- 长函数优先「提取子函数」而非「加注释分段」。

---

## 6. 前端重构规范

- 展示组件只收 props、发事件；容器组件管状态与请求；禁止组件内直接调用后端。
- 共享业务请求统一收敛到 `src/api/` 封装，避免各页面重复实现错误处理。
- 状态提升/下放：状态被多组件共享时提升至 Pinia；仅组件内使用则保留局部。
- ECharts 配置与数据分离：图表组件只接收数据与配置项，禁止在组件外拼 ECharts option。
- 大组件拆分：单文件超 400 行时应拆分子组件/组合式函数（composables）。

---

## 7. 命名规范（重构时统一执行）

| 类别 | 规范 |
| --- | --- |
| 后端函数/变量 | `snake_case`，布尔用 `is_/has_/can_` 前缀 |
| 后端类 | `PascalCase` |
| 后端常量 | `UPPER_SNAKE_CASE` |
| 前端组件 | `PascalCase` 多词（`BookShelfCard.vue`） |
| 前端函数 | `camelCase` |
| 前端 store | `useXxxStore` |
| 目录/文件 | 全小写，`-` 或 `_` 分隔，风格全仓一致 |

---

## 8. 常见坏味道与处理手法对照表

| 坏味道 | 处理手法 |
| --- | --- |
| 重复代码 | 提取公共方法/组件/服务/工具 |
| 过长函数 | 拆分职责、提取子函数、提前返回 |
| 过深嵌套 | 卫语句、提前返回、策略拆解 |
| 万能对象（God Object） | 拆分单一职责类 |
| 魔法值/魔法字符串 | 常量或枚举化 |
| 过度耦合 | 接口抽象 + 依赖注入 |
| 长参数列表 | 封装参数对象 |
| 注释解释代码 | 重命名以表达意图，删除冗余注释 |
| 数据与视图混杂 | 前后端分层，组件只做展示 |
| 直接依赖第三方细节 | 通过封装层隔离（如 AI 客户端、解析器） |

---

## 9. 重构边界与红线

- **禁止**：一次性大规模重写；无测试基线的重构；夹带新功能或无关修复；未通知地变更公开 API 契约。
- **禁止**：跳过 Alembic 手工改表；删除/覆盖用户数据（书籍、笔记、画像）相关结构未做迁移与备份。
- **禁止**：重构中把配置/密钥硬编码进代码。
- 涉及外部依赖升级：先看 changelog，评估破坏性变更，升级与重构分离提交。

---

## 10. 提交与评审

- 提交信息遵循 Conventional Commits：`feat` / `fix` / `refactor` / `style` / `docs` / `test` / `chore`。
- `refactor` 提交信息需说明：动机（坏味道）、手法（提取/拆分/重命名）、验证方式（哪些测试/冒烟）。
- 评审 checklist：
  - [ ] 行为未变或变更已被需求认可
  - [ ] 测试基线存在且通过
  - [ ] 未跨层、无循环依赖
  - [ ] 命名符合 §7
  - [ ] 未夹带功能/bug 修复
  - [ ] 涉及配置/API/结构变化已同步文档

---

## 11. 重构记录表

| 日期 | 模块 | 类型 | 动机 | 影响范围 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| 2026-08-11 | backend（repositories/books、rag_router）+ frontend（useStreamSession、RagDetailView）+ docs 五处 | 第 7 轮复审修复（审查报告 §12） | ① 删 repositories/books.py::create_book/add_chapters 死代码（m-6 合并后零引用，AST+rg 验证；5 处手册引用改写为 create_book_with_chapters 单事务口径，B1）；② 补回归测试 test_delete_book_and_asset_clear_selection_cache（删资产/删书→挑选会话缓存清空，B2）；③ rag_router.select_knowledge 缓存键 chapter.id None 防御（B3）；④ useStreamSession.stream() 同步抛错防御（复位 streaming/streamError、不启动轮询、移除空气泡，F1）；⑤ /rag/15 双滚动条修复（.rag-detail 改 min-height:100%，E2E #1）；⑥ 文档 D-01~D-06 | 同左 + backend/tests/test_assets.py（+1 回归）+ frontend/tests/streamSession.test.ts（15 用例） | pytest 291 + ruff 全绿；vitest 124 + vue-tsc 双模式 0 错误 + vite build；E2E 193 项 = 190 PASS / 0 FAIL / 3 WARN；markdown-it 断言通过 |
| 2026-08-11 | frontend（composables/useStreamSession.ts、utils/streamCore.ts 新增；useReaderAi/useGlobalAi 收敛）+ backend（rag_input、ai_context、routes/assets、books_service） | 第 6 轮复审修复（审查报告 §11） | ① MO3 流式内核抽取：useReaderAi/useGlobalAi 的流式差量 / scheduleFlush / pendingText 轮询 / 终态状态机（逐字节重复 66.2%）下沉 useStreamSession（8 项行为注入 + 5 组计时参数，F1/F2/MO2/I-2/I-4/I-13/C-1 契约参数化保留），两调用方降为薄适配层（约 400/330 → 238/164 行）；② 后端死代码清理与接线：删 build_llm_input（零引用）、page_image_data_uri（含测试），clear_session_cache 接入删书/删资产三处调用点 | 同左文件 + frontend/tests（+20 用例）+ backend/tests/test_pdf_media.py（-1 旧测试） | vitest 123 全绿 + vue-tsc 双模式 0 错误 + vite build + E2E 冒烟 14/14（零真实 AI）；pytest 290 + ruff 全绿；health 200 |
| 2026-08-11 | frontend（ReaderChatPanel/MdRender/RagDetailView/ReaderView/ProfileView/theme.css/constants.ts + DoodleToolbar/HomeView/RagView） | 第 5 轮复审修复（审查报告 §10） | ① E2E #1 聊天区列表溢出根因：行内公式 inline 元素不响应 max-width/overflow-x → 改 inline-block + KaTeX MathML 分支全局 overflow:hidden（根因修复，非局部兜底）；② 点击目标 24px 与 --text-secondary 对比度 4.47→5.35（观感基线）；③ 魔法常量收口（AI_TEST_TASK_TIMEOUT_MS/HTTP_TIMEOUT_MS 语义化命名）；④ 6 处未使用声明清理（死函数 mergedCount、未用参数/解构） | 同左文件 + frontend/tests/round5-fixes.test.ts（16 用例） | vitest 103 全绿 + vue-tsc 双模式 0 错误 + vite build + Playwright 复验 10/10（零写） | ① E2E #1 聊天区列表溢出根因：行内公式 inline 元素不响应 max-width/overflow-x → 改 inline-block + KaTeX MathML 分支全局 overflow:hidden（根因修复，非局部兜底）；② 点击目标 24px 与 --text-secondary 对比度 4.47→5.35（观感基线）；③ 魔法常量收口（AI_TEST_TASK_TIMEOUT_MS/HTTP_TIMEOUT_MS 语义化命名）；④ 6 处未使用声明清理（死函数 mergedCount、未用参数/解构） | 同左文件 + frontend/tests/round5-fixes.test.ts（16 用例） | vitest 103 全绿 + vue-tsc 双模式 0 错误 + vite build + Playwright 复验 10/10（零写） |
| 2026-08-11 | frontend（composables/useStreamSession.ts、utils/streamCore.ts 新增；useReaderAi/useGlobalAi 收敛）+ backend（rag_input、ai_context、routes/assets、books_service） | 第 6 轮复审修复（审查报告 §11） | ① MO3 流式内核抽取：useReaderAi/useGlobalAi 的流式差量 / scheduleFlush / pendingText 轮询 / 终态状态机（逐字节重复 66.2%）下沉 useStreamSession（8 项行为注入 + 5 组计时参数，F1/F2/MO2/I-2/I-4/I-13/C-1 契约参数化保留），两调用方降为薄适配层（约 400/330 → 238/164 行）；② 后端死代码清理与接线：删 build_llm_input（零引用）、page_image_data_uri（含测试），clear_session_cache 接入删书/删资产三处调用点 | 同左文件 + frontend/tests（+20 用例）+ backend/tests/test_pdf_media.py（-1 旧测试） | vitest 123 全绿 + vue-tsc 双模式 0 错误 + vite build + E2E 冒烟 14/14（零真实 AI）；pytest 290 + ruff 全绿；health 200 |
| 2026-08-11 | backend（repositories/assets + services/graph/clustering + api/routes/books,folders）+ frontend（utils/highlight,searchHighlight,constants + composables/useReader*,useTaskPoll + api/chat + styles/theme.css） | 四域复审第 4 轮修复与解耦（审查报告 §9） | ① 资产写锁下沉仓储（路由层 `_dedupe_lock` 移除）、per-book 锁表 LRU 有界 128；② 聚类缓存签名含资产版本 + LPA 流行度增量 + 碎片合并单趟；③ 导入建书单事务 `create_book_with_chapters`；④ FK 冲突 404/409 语义统一；⑤ 前端 SSE 空闲超时降级反向桥接、划词高亮归一化匹配（KaTeX 剥离）、高亮防嵌套、useTaskPoll 信号合并、useReaderArchive 快照守卫、划词菜单 ref 注入、语义色 token 化、魔法常量收敛、FolderItem 死代码删除；⑥ m-5 挑选缓存键问题指纹回退（保持 session+chapter，删除 `_CACHE_QUESTION_SENSITIVE`/hashlib） | 同左文件 + tests/test_rag_router.py（回退还原）、tests/test_assets.py、tests/test_settings_reload_env.py、frontend/tests/{highlight,searchHighlight,sse-fallback}.test.ts | 后端 291 pytest + ruff 全绿；前端 87 vitest + vue-tsc + vite build；markdown-it 渲染断言通过 |
| 2026-08-05 | backend（repositories/graph 新建 + 6 路由 + 5 新 service） | 结构性重构（审查 D 组 P0-2~P0-8，完成） | ① 图谱域建仓 `repositories/graph.py`：13 个只读查询（list_books/list_relations/list_knowledge_points/count_* 等）+ 2 个批量删除（clear_relations/clear_book_knowledge_graph），cross_book / clustering / graph_sync / intra_book / graph 路由收敛调用点并清理未用导入；② 路由瘦身：annotations（P0-6）→ annotation_service（read/save_page_annotations，MAX_ELEMENTS=2000）、reading（P0-5）→ reading_service.save_book_progress（LookupError→404）、chat（P0-4）→ chat_service.replay_cached_chat、settings（P0-3）→ settings_service（to_store/submit_connect_test，ValueError→400）、graph（P0-2）→ graph_sync.apply_relation_feedback、books（P0-7）→ upload_service（stream_upload_to_temp/import_uploaded_book）；conftest monkeypatch 目标同步改 services.settings_service | repositories/graph.py（新增）、services/annotation_service.py、services/reading_service.py、services/settings_service.py、services/upload_service.py、services/chat_service.py、services/graph_sync.py、api/routes/{annotations,books,chat,graph,reading,settings}.py、tests/conftest.py | 后端 224 pytest + ruff 全绿 |
| 2026-08-05 | frontend（GraphView + 新组件/工具） | 结构性重构（审查 D 组前端大组件拆分，完成） | GraphView 730→465 行：统计卡片/筛选栏抽 `components/graph/GraphGlobalPanel.vue` 与 `GraphIntraPanel.vue`（事件上抛 cluster-change/level-toggle）；全局/书内 ECharts option 拼装抽 `utils/graphOption.ts`（buildGlobalOption/buildIntraOption/truncateLabel/clusterColorFor/nodeMapOf，纯函数可单测）；方向文案收敛 `utils/graphEdges.ts::edgeDirLabel`（删除视图内重复实现）；`utils/graphLabel.ts::labelRichFormatter` 返回类型改 ECharts 兼容富文本；graph series 的 large/progressive 因 echarts 5.6 类型未声明做整体断言 | views/GraphView.vue、components/graph/GraphGlobalPanel.vue（新增）、components/graph/GraphIntraPanel.vue（新增）、utils/graphOption.ts（新增）、utils/graphEdges.ts、utils/graphLabel.ts | vue-tsc + vite build 通过 |
| 2026-08-04 | backend（repositories/chat、services/chat_service、api/routes/chat）+ frontend（useReaderAi、ReaderChatPanel、ReaderView、api/chat） | P0 落地耦合审查（会话分池/历史注入 + 流式节流，完成） | 耦合审查结论：会话键生成 / 历史窗口截断 / 持久化收敛在 repositories/chat（无跨层 SQL，`list_messages`/`clear_messages`/`persist_chat` 全部支持 mode）；`chat_service.build_messages` 只增 `history` 参数、不感知历史来源（隐私规则在 `prepare_chat_job` 统一判断）；前端流式缓冲、公式感知与模式切换均在 `useReaderAi` 内自洽（`ReaderChatPanel` 纯展示 + `@mode-change` 事件上抛）；本轮为独立文件改动、无新增循环依赖、无重复代码 | repositories/chat.py、services/chat_service.py、api/routes/chat.py、composables/useReaderAi.ts、components/ReaderChatPanel.vue、views/ReaderView.vue、api/chat.ts、tests/test_chat.py | 后端 164 项 pytest + ruff 全绿；前端 vue-tsc + vite build + vitest 43 项通过 |
| 2026-08-04 | backend（api/routes/books、services/rag_service、全部延迟导入）+ frontend（GraphView） | 解耦重构 | 耦合审查产出：① `api/routes/books.py` 路由过厚（11 依赖）——PDF 页图渲染/低清重渲染抽为 `services/book_pages.py::get_or_render_page`，封面文件校验收敛 `media_service.book_cover_file`，tag 清洗收敛 `books_service.clean_tags`，路由降至 8 依赖；② `rag_service._collect_new_material` 绕过仓储直接 `db.query(Note/ChatMessage)` ——改走 `repositories.notes.list_notes` + 新增 `repositories.chat.list_recent_messages`；③ 全后端 20+ 处函数内 `from app.*` 延迟导入提升为模块顶层（依赖图验证无环，统一风格 + fail-fast）；④ 前端 GraphView `edgeEndpoints` 与 `graphEdges.linkEndpoints` 方向判定重复——`edgeEndpoints` 改为复用 `linkEndpoints`（方向判定唯一化） | api/routes/books.py、api/routes/reading.py、api/routes/graph.py、api/routes/notes.py、api/routes/vision.py、services/book_pages.py（新增）、services/rag_service.py、services/graph/cross_book.py、services/import_service.py、services/vision_extract.py、services/profile_service.py、services/recommendation_service.py、services/books_service.py、services/media_service.py、repositories/chat.py、repositories/assets.py、main.py、GraphView.vue | 后端 163 项 pytest + ruff 全绿；前端 vue-tsc + vitest 43 项通过 |
| 2026-08-03 | backend（graph/edges、rag_input、chat_service、core/database、main）+ frontend（useReaderPageCache、useReaderArchive） | 解耦重构 | 消除 `cross_book`↔`llm_score` 惰性循环依赖（`pair_key` 下沉 `graph/edges.py`）；core 层移除 services 依赖（媒体迁移上移 main lifespan）；`lexicon` 私有成员改公共访问器；`rag_service` 拆分纯函数（切块/输入构建/技能归一化）到 `rag_input`；chat 路由编排下沉 `chat_service`（不再直连仓储）；ReaderView 抽页缓存/归档两个组合式函数 | services/graph/*、services/rag_*.py、api/routes/chat.py、core/database.py、main.py、ReaderView.vue | 后端 151 项 pytest + ruff 全绿；前端 vue-tsc + vite build 通过 |
| 2026-08-03 | backend（services/graph_service 删除 + app/ai/parsing）+ 四处服务 | 删除死代码 + 收敛重复解析（完成） | M8 收尾审查发现：`services/graph_service.py`（889 行）在图谱包拆分后仍遗留，与 `services/graph/` 包完全重复且全库无引用（死代码）；LLM 输出 JSON 容忍解析在 graph_sync / llm_score（上一轮新增）/ rag_service / mindmap_service 四处重复；新增 `app/ai/parsing.py` 统一 `parse_llm_json`（去代码围栏 + 取首 { 到末 }），四处调用点收敛，`test_assets` 改引新模块 | services/graph_service.py（删除）、app/ai/parsing.py（新增）、services/graph_sync.py、services/graph/llm_score.py、services/rag_service.py、services/mindmap_service.py、tests/test_assets.py | 后端 135 项 pytest + ruff 全绿；vue-tsc + vite build 通过；真实冒烟 /api/books + /api/graph/books |
| 2026-08-03 | backend/services/graph（llm_score）+ models/graph + frontend GraphView | 新增职责独立成模块 + 消除重复（完成） | M8 收尾落地 LLM 打分/方向/原因时审查：跨书评分链路再叠加一步 LLM 增强，若直接塞进 `cross_book.py` 会继续膨胀；独立 `services/graph/llm_score.py`（`score_pair_llm`/`enrich_pairs_with_llm`/`apply_llm_result`）与 `ai/prompts/graph_edge.py`，`cross_book` 只负责候选收集与结果合并；`_parse_json` 与 `_parse_json`（graph_sync）此前重复出现，本轮新增模块复用同款容忍解析；`BookRelation` 新增 `from_book_id` 走 `_ensure_columns` 增量迁移；前端方向/箭头逻辑收敛为 `edgeEndpoints`/`edgeDirLabel` 两个纯函数，避免 tooltip/详情/links 三处重复计算 | services/graph/llm_score.py、ai/prompts/graph_edge.py、services/graph/cross_book.py、models/graph.py、core/database.py、services/graph_sync.py、GraphView.vue、types.ts、tests/test_graph_llm_score.py | 后端 135 项 pytest + ruff 全绿；vue-tsc + vite build 通过 |
| 2026-08-03 | backend/api/routes/notes + services/note_export.py + frontend | 路由减负 + 导出职责收敛（完成） | M10 导出实现时审查：笔记导出从路由内联逻辑抽为 `services/note_export.py`（`build_notes_markdown`/`build_notes_pdf`），路由只做参数校验与响应包装，Markdown/PDF 两种格式共用同一份数据获取（book/notes/chapters）；`pdf_page_target_width` 页图目标宽度改走 `_target_width_cached`（lru_cache）消除每页请求重开 PDF 的开销；测试环境与真实 `.env` 解耦（conftest 屏蔽 AI/视觉配置），消除后台视觉预提取与测试的竞态 | routes/notes.py、services/note_export.py、parsers/pdf.py、routes/books.py、tests/conftest.py、tests/test_export_m10.py | 后端 128 项 pytest + ruff 全绿；vue-tsc + vite build 通过 |
| 2026-08-02 | backend/repositories/books + api/routes/books + frontend/HomeView | 书架能力收敛（完成） | 书架排序/搜索/换位落地时审查：排序（`list_books`）、批量重排（`reorder_books`）、搜索过滤统一收敛 `repositories/books`，路由只做参数透传；`Book.position` 经 `_ensure_columns` ALTER 增量迁移（不动旧库）；HomeView 重写为组合式状态（搜索/标签/拖拽/标签编辑各司其职，无跨职责耦合） | repositories/books.py、api/routes/books.py、HomeView.vue、types.ts | 后端 125 项 pytest + ruff 全绿；vue-tsc + vite build 通过 |
| 2026-08-02 | backend/services/relatedness.py + profile_service | 相关度判定独立成服务 + 修复 NULL 过滤缺陷（完成） | 决策 20 落地时审查：相关度判定从 `profile_service._is_related_book` 抽出为 `services/relatedness.py`（compute_relatedness/is_related_book），并入暖记忆与行为样本记录解耦；修复复现的 SQLAlchemy `user_feedback != "忽略"` 过滤 NULL 行缺陷（NULL 反馈边被误排除，改用 `or_(is_(None), !=)`，与 graph_sync v1.17 修复一致）；移除 profile_service 遗留的 `RELATED_STRENGTH_THRESHOLD` 常量与未用导入 | services/relatedness.py、services/profile_service.py、services/profile_learning.py | 后端 119 项 pytest + ruff 全绿；vue-tsc + vite build 通过 |
| 2026-08-02 | backend/services/graph/cross_book.py | 提取公共函数（完成） | 新增导入增量更新时发现全量重建（`compute_cross_book_graph`）与增量（`incremental_cross_book_graph`）各有一份「两书评分（关键词共现/笔记加权）」与「边键规范化」逻辑；提取 `_pair_score(db, a, b, ka, kb)`（共现余弦分 + 笔记加权，<1 分返回 None）与 `pair_key(x, y)`，两处复用消除重复 | services/graph/cross_book.py、api/routes/books.py（导入后触发增量更新）、api/routes/graph.py（跨书检索端点） | 后端 111 项 pytest + ruff 全绿；vue-tsc + vite build 通过 |
| 2026-08-02 | backend/services + api/routes + frontend（图谱联动/阈值学习） | 新增职责收敛 + 统一阈值读取（完成） | 图谱联动沉淀与画像阈值学习落地时审查：联动逻辑（存根/LLM 增改/术语补水）独立成 `services/graph_sync.py`，避免塞进 `graph/cross_book.py`；阈值读取由 `profile_service`/`recommendation_service` 各自常量改为统一 `profile_learning.get_thresholds(db)`（魔法值收敛）；修复 SQLAlchemy `column != value` 过滤 NULL 行的缺陷（`user_feedback IS NULL` 的关联被误过滤），改用 `or_(is_(None), !=)`；修复前端「重建图谱」按钮只刷新未重建（`rebuildGraph` 已导入未使用）的失效问题 | services/graph_sync.py、services/profile_learning.py、services/profile_service.py、services/recommendation_service.py、api/routes/graph.py、api/routes/profile.py、GraphView.vue、ProfileView.vue | 后端 107 项 pytest + ruff 全绿；vue-tsc + vite build 通过 |
| 2026-08-02 | backend/services（graph/ai_context）+ frontend ReaderView | 拆分上帝模块与重复代码（完成） | 审查发现 `graph_service.py`（889 行）承载 5 类职责、`ai_context.py` 承载 6 类职责、资产 JSON 解析（4 处）与 tag 清洗（4 处）重复；拆分 `services/graph/` 包（keywords/lexicon/corpus/clustering/cross_book/intra_book）、`ai/factory.py`、`services/citations.py`、`repositories/chat.py`；`repositories/assets.py` 新增 `read_asset_content`/`retrieve_rag_chunks`/`load_skills`，`repositories/books.book_tags` 消除重复；chat 路由改用仓库消除直接 SQL；前端 ReaderView 涂鸦抽 `useReaderDoodle`（813→741） | services/graph/*、ai/factory.py、repositories/chat.py、repositories/assets.py、ReaderView.vue | 后端 97 项 pytest + ruff 全绿；前端 vue-tsc + vite build 通过 |
| 2026-08-02 | backend（M8 实现） | 耦合审查与解耦（完成） | M8 图谱落地时审查：跨书关联/聚类/书内知识点/序列化收敛到 services/graph_service.py（无跨层 SQL），路由 api/routes/graph.py 仅做参数校验与懒构建触发；文本语料与 media_service.page_image_path / local_text 约定保持一致（页文本路径暂未抽公共 helper，待 M9 沉淀时收敛）；修复两处实现缺陷：compute_cross_book_graph 误置 graph_built=True 导致书内图谱跳过懒构建、领域自动聚类空结果全落「其他」 | services/graph_service.py、api/routes/graph.py（含文本语料/页图路径约定相关调用方） | 6 项图谱 pytest + ruff 全绿 |
| 2026-08-02 | backend（M7 实现） | 耦合审查与解耦（完成） | M7 落地时审查：视觉提取/页缓存/滑动窗口/整书重建收敛到 services/vision_extract.py（无跨层 SQL）；页上下文拼装与引用解析复用 services/ai_context.py（新增 build_page_context_block、扩展 extract_citations 支持【第X页】），chat 路由与 mindmap 服务同走该链路避免两处复制；多模态配置收敛 repositories.settings（vision_client_kwargs/vision_configured，未覆盖项取 .env 不回退到文本 AI 配置）；修复页缓存路由字面量路径（rebuild/status）被 /{page_index} 抢先匹配的注册顺序问题；parse_pdf 移除文本型/扫描型双分支（统一按页），删除 _pdf_heading/SCANNED_TEXT_THRESHOLD 死代码 | services/vision_extract.py、services/ai_context.py、repositories/settings、parsers/pdf.py、api/routes（页缓存路由）、chat/mindmap 路由 | 54 项 pytest + ruff + vue-tsc + 23 项 Vitest 全绿 |
| 2026-08-01 | app/parsers | 提取公共解析助手 split_by_headings | md/txt/pdf 三处重复切章逻辑（同类坏味道：重复代码） | backend/parsers（text/pdf/_split）+ tests | ruff 全绿 + pytest 14 项全绿 |
| 2026-08-01 | app/schemas + api/routes | 统一实体序列化 serializers.py | 路由各自 _to_out 重复且 tags 返回 JSON 字符串与前端契约不符 | books/folders/assets 路由 | 新增 tags 往返测试，pytest 全绿 |
| 2026-08-01 | app/api/routes/books | API 层瘦身 | update/delete 业务逻辑与文件 I/O 散在路由（分层违规） | repositories/books.update_book、services/books_service.delete_book | pytest 全绿 |
| 2026-08-01 | app/services/rag_service | 消除重复切块 | chunk_book 在生成与发 LLM 时各调用一次 | rag_service（_build_llm_input 改为接收 chunks） | pytest 全绿 + RAG 冒烟 |
| 2026-08-01 | app/api/routes（reading/notes/assets） | 提取公共依赖 require_book | 三处重复的「取书并抛 404」helper（重复代码 ≥ 3 处） | app/api/deps.py 新增 require_book；三路由复用 | ruff 全绿 + pytest 16 项全绿 + 阅读页冒烟 |
| 2026-08-02 | frontend/src/views/ReaderView.vue | 拆分组合式函数与子组件（完成） | 单文件 1054 行超 §6 的 400 行上限，阅读进度/笔记/高亮/自动读完/AI/脑图逻辑混杂；拆为 5 个组合式函数（selection/notes/progress/mindmap/ai）+ 2 个展示子组件（左栏书架目录、AI 面板），ReaderView 降至 427 行；顺带删除死变量 initialRestore | ReaderView.vue、composables/useReader*、components/ReaderLeftPanel.vue、ReaderChatPanel.vue | vue-tsc 全绿 + vitest 5 项 + 阅读页接口冒烟 |
| 2026-08-02 | frontend/src（HomeView/ReaderView） | 重复代码（完成） | 整书进度百分比公式在 3 处重复；提取 `src/utils/progress.ts` 的 `chapterPercent(read, total)` 与 `percentOf(progress)`，HomeView/ReaderView 统一接入 | utils/progress.ts | vue-tsc 全绿 + vitest |
| 2026-08-02 | backend/api/routes/books + repositories/reading | 性能/查询模式（完成） | `list_books` 逐本调用 `book_reading_summary` 产生 N+1 查询（每本 2 次 SELECT）；新增 `books_reading_summary` 批量聚合（一次查全部最新日志）+ `list_books` selectinload 预载章节 | repositories/reading、repositories/books、routes/books | pytest 39 项全绿 + /api/books 冒烟 |
| 2026-08-02 | frontend（Vitest 已配置无用例） | 测试基线（完成） | §3.1 要求重构前有测试基线；为 progress/content 纯函数补 Vitest 用例 | frontend/tests/utils.test.ts（5 项） | vitest run 5 项通过 |
| 2026-08-02 | backend/app/repositories/reading | 提取共享状态同步助手 | update_book_reading 与 set_chapter_read_flag 各有一套章节已读→书籍状态判断（重复逻辑）；提取 `_refresh_status` 统一「全部读完→读完/部分→在读/全未读→未读」 | repositories/reading.py | ruff 全绿 + pytest 17 项全绿 + 章节开关冒烟 |
| 2026-08-02 | backend/repositories/settings + services/chat_service | 提取配置→客户端参数映射 | M4 接线时发现 `build_client` 用 `LLMClient(**load_ai_overrides)` 传入 `ai_*` 键导致 TypeError（隐藏依赖）；提取 `client_kwargs` 统一映射并剔除 `ai_enable_body_send` | chat_service.build_client/is_configured、settings 路由测试连接 | ruff 全绿 + pytest 23 项全绿 + mock 流式冒烟 |

---

## 12. 变更记录

| 版本 | 日期 | 变更内容 | 说明 |
| --- | --- | --- | --- |
| v1.47 | 2026-08-11 | 四域复审第 7 轮收尾（审查报告 §12）：① 后端死代码清理（repositories/books.py::create_book/add_chapters 删除，B1）+ 挑选会话缓存失效回归测试（B2）+ rag_router 缓存键 chapter.id None 防御（B3）；② 前端流式内核同步抛错防御（F1）+ /rag/15 双滚动条修复（E2E #1）；③ 文档 D-01~D-06；详见 §11 重构记录表新增行；同步 使用手册 v1.130、技术栈 v1.82、待办 v2.21 |
| v1.46 | 2026-08-11 | 四域复审第 6 轮收尾（审查报告 §11）：MO3 流式内核抽取（useStreamSession 249 行 + streamCore 34 行，useReaderAi/useGlobalAi 收敛为薄适配层，重复 66.2%→31.9%）+ 后端死代码清理（build_llm_input / page_image_data_uri 删除、clear_session_cache 接线）+ 台账漂移修正；详见 §11 重构记录表新增行；同步 使用手册 v1.129、技术栈 v1.81、待办 v2.20 |
| v1.45 | 2026-08-11 | 四域复审第 5 轮收尾（审查报告 §10）：前端 7 项修复（观感 5 + 常量/死代码 2）+ 文档 4 项 + 服务重启；详见 §11 重构记录表新增行；同步 使用手册 v1.128、技术栈 v1.80、待办 v2.19 |
| v1.44 | 2026-08-11 | 四域复审第 4 轮收尾（审查报告 §9）：后端 10 项 + 前端 15 项修复与解耦（详见 §11 重构记录表新增行）；m-5 挑选缓存键问题指纹回退（与决策 34/F3 冲突）；同步 使用手册 v1.127、技术栈 v1.79、待办 v2.18 |
| v1.43 | 2026-08-11 | 三审修复收尾轮（审查报告四域复审 §8.5~8.6） | ① useTaskPoll 收敛（RagView/RagDetailView 卸载中止轮询，D10 落地）；② rag_input 四函数 enable_body 透传（隐私开关 DB 覆盖，Major-2）；③ SSE 空闲超时 IdleTimeout（Major-3）；④ 涂鸦只读零写（doodleDirty 脏标记，Minor-3）；⑤ 死代码清理（WARM_TO_COLD_THRESHOLD 等）+ 文档 6 文件修正（Minor-11）；验证：后端 288 pytest + ruff、前端 69 vitest + vue-tsc + build；同步 使用手册 v1.126、技术栈规范 v1.78、待办清单 v2.17 |
| v1.42 | 2026-08-11 | 四域复审修复轮 | ① 图谱域仓储下沉收尾（cross_book feedback 查询、graph/tasks book_ids、clustering folder 查询、graph_sync 按 id 取书、graph 路由取关联全部改走 repositories/graph 或 repositories/books）；② 视觉压缩图链路：prep_page_image 渲染移出 _meta_lock（锁只保护 meta 短临界区）、VISION_TASK_PREFIX 统一导入与普通重建防重键；③ 聚类缓存键语义修正（写库后状态重算，杜绝自失效）；④ 前端流式历史合并收敛到 utils/chatMerge.ts 共享纯函数（为 useStreamSession 抽取预留）；⑤ 任务中心顺序逻辑收敛到 utils/task.ts latestFinishedTasks；记录见 docs/审查报告-20260811-四域复审.md |
| v1.41 | 2026-08-05 | 审查 P0+A 执行（P0 行为缺陷 + A 组低风险项） | ① P0：`rag_router._select_fallback` 直查 `user_feedback != "忽略"` 漏 NULL（v1.63 回归）→ 改走仓储 `list_active_relations`（新增 `book_id`/`limit` 参数，NULL 视为未忽略）+ 回归测试；② A-死代码 7 处删除（limiter/bookmarks/notes/settings/schemas.common/import_service）；③ A-任务包装收敛：新建 `services/graph/tasks.py`，graph.py 4 个 + assets.py 1 个后台包装下沉，路由只留 `_submit_graph_task` 提交幂等；④ A-建仓收尾：新增 `list_post_classified_books`，clustering.py `post_classify_book`/`merge_and_rename_clusters` 直连 `db.query(Book)` 清零；后端 232 pytest + ruff 全绿；关联文档升需求 v1.74、使用手册 v1.112、技术栈不动 |
| v1.40 | 2026-08-05 | 审查 D 组结构性重构实施（P0-2~P0-8 + 前端图谱拆分） | ① 图谱域建仓 repositories/graph.py（13 只读查询 + 2 批量删除），cross_book / clustering / graph_sync / intra_book / graph 路由全部收敛调用点；② 路由瘦身：新增 annotation_service / reading_service / settings_service / upload_service，chat 缓存回放下沉 replay_cached_chat、关联反馈下沉 apply_relation_feedback，六个路由减负；③ 前端 GraphView 730→465 行：面板抽 components/graph/ 两组件、option 拼装抽 utils/graphOption.ts 纯函数、方向文案收敛 graphEdges.edgeDirLabel、graphLabel 富文本返回类型 ECharts 兼容；后端 224 pytest + ruff 全绿，vue-tsc + vite build 通过；关联文档升需求 v1.69、技术栈 v1.72、使用手册 v1.108 |
| v1.39 | 2026-08-05 | 修复 §11 表格渲染 + 关联文档同步（无代码重构） | 删除重构记录表中间残留的分隔行「\| --- \|」（破坏 Markdown 表格渲染，介于 2026-08-04 与 2026-08-03 记录之间）；关联文档升需求 v1.68；聚类改进方案 O1~O8 定稿为 demo 验证（demo/clustering_demo.py），正式版零改动 |
| v1.38 | 2026-08-04 | 关联文档同步（无代码重构） | 关联文档升需求 v1.58、技术栈 v1.70；性能优化路径独立成文（docs/性能优化路径.md），重构时同步补索引 / 缓存（关键词、聚类） |
| v1.37 | 2026-08-04 | 关联文档同步（无代码重构） | 关联文档升需求 v1.57、技术栈 v1.69；异步 / 并行改造计划独立成文（docs/异步并行改造计划.md），重构执行顺序按计划 Phase 1~3 |
| v1.36 | 2026-08-04 | 关联文档同步（无代码重构） | 关联文档升需求 v1.56、技术栈 v1.68；异步 / 并行审查 13 落点登记（需求-决策 §9.3.2），重构时按落点逐一解耦（图谱路由 → 任务服务、导入 → 两段式） |
| v1.35 | 2026-08-04 | 关联文档同步（无代码重构） | 关联文档升需求 v1.55、技术栈 v1.67；整体异步 / 并行改造清单登记（需求-决策 §9.3 第 9 行 + 里程碑 §8.1 第 8 行），与任务系统 / 导入 / 并发同批次 |
| v1.34 | 2026-08-04 | 关联文档同步（无代码重构） | 关联文档升需求 v1.54、技术栈 v1.66；9.3.1 LLM 挑选细节备选与 9.3 进度条需求登记（均待定稿，见 需求-决策 §9.3） |
| v1.33 | 2026-08-04 | 关联文档同步（无代码重构） | 关联文档升需求 v1.53、技术栈 v1.65；决策 34 修订为 LLM 自主挑选（两级混合 + 规则降级，见 需求-决策 §9.1/9.3） |
| v1.32 | 2026-08-04 | 关联文档同步（无代码重构） | 关联文档升需求 v1.52、技术栈 v1.64；缺口备选方案登记见 需求-决策 §9.3（EPUB 排版 / 画像编辑粒度待定稿） |
| v1.31 | 2026-08-04 | 关联文档同步（无代码重构） | 关联文档升需求 v1.51、技术栈 v1.63；登记实现状态审查（需求-里程碑与验收 §8.1）：决策 31/32/33 与跨书复用等缺口待实现、决策 34（跨书 RAG/Skill 路由）定稿 |
| v1.30 | 2026-08-04 | 关联文档同步（无代码重构） | 关联文档升需求 v1.50、技术栈 v1.62；聚类算法方案选型（向量化 vs 共现）登记为待定稿开放项（需求-决策 §9.2 + 知识图谱聚类算法 §10），决策后需同步技术栈 ADR |
| v1.29 | 2026-08-04 | P0 落地登记 + 耦合审查 | 登记会话分池 / 历史注入 / 流式渲染节流实现（详见 §11 记录表新增行与 使用手册 §18）：后端 `repositories/chat.py` `chat_session_id` / `recent_history_texts`（10 轮、8k 截断）、`chat_service` 历史注入、历史 API `?mode=`；前端 `useReaderAi` 80ms 流式节流 + `switchMode` 模式切换；耦合审查结论：独立文件改动、低耦合、无循环依赖；关联文档升需求 v1.44、技术栈 v1.54、使用手册 v1.87 |
| v1.28 | 2026-08-04 | 关联文档同步（无代码重构） | 关联文档升需求 v1.43、技术栈 v1.53、使用手册 v1.86；登记 P0 待落地方案（会话分池/历史注入 + 流式渲染节流）完成后将按 §3 流程做耦合审查，记录表届时补充 |
| v1.27 | 2026-08-03 | 新增章节目录 | 文档头部新增目录（## 两级锚点链接，12 项）；关联文档升需求 v1.42、技术栈 v1.52、使用手册 v1.78 |
| v1.26 | 2026-08-03 | 登记资产页两级结构改造 | 前端资料页收敛：`RagView.vue` 去掉内联资产详情（expanded 展开面板），仅保留列表 + 固定大小提交总结；完整内容迁移到新增 `RagDetailView.vue`，与阅读页共用 `MdRender` 渲染（无重复渲染逻辑），删除/移除操作由列表页下放详情页；vue-tsc + vite build 通过；关联文档升需求 v1.40、技术栈 v1.50、使用手册 v1.74 |
| v1.25 | 2026-08-03 | 文档系统性检查 | 清理重构记录表模板遗留「（示例）」行；全量核对关联文档版本（需求 v1.39 / 技术栈 v1.49 / 使用手册 v1.73）；本轮无代码重构（最近轮次为 AI 多接口格式 / base_url 双写法 / UA 修复，属功能实现，重构审查见 v1.24 及后续轮） |
| v1.24 | 2026-08-03 | 登记解耦重构轮（P1+P2） | 消除 `cross_book`↔`llm_score` 惰性循环依赖（`pair_key` 下沉 `graph/edges.py`，顶层导入）；`core/database.py` 移除 `services.media_service` 依赖（迁移上移 `main.py` lifespan）；`lexicon` 私有 `_GENERIC_DOMAIN_TERMS` 改公共 `generic_domain_terms()`；`rag_service` 拆分纯函数到 `services/rag_input.py`；`chat.py` 编排下沉 `chat_service.prepare_chat_job` 等（路由不再直连仓储）；前端 ReaderView 抽 `useReaderPageCache`/`useReaderArchive`（747→655 行）；后端 151 项 pytest + ruff 全绿、vue-tsc + vite build 通过；关联文档升需求 v1.38、技术栈 v1.45、使用手册 v1.69 |
| v1.23 | 2026-08-03 | 登记死代码清理 + 解析收敛重构 | 删除 `services/graph_service.py`（889 行，图谱包拆分后遗留死代码，`services/graph/` 包已完整替代）；新增 `app/ai/parsing.py`（`parse_llm_json`）收敛 graph_sync/llm_score/rag_service/mindmap_service 四处重复解析；后端 135 项 pytest + ruff 全绿；关联文档升需求 v1.33、技术栈 v1.39、使用手册 v1.63 |
| v1.22 | 2026-08-03 | 登记 M8 收尾重构 | LLM 打分/方向/原因增强独立 `services/graph/llm_score.py` + `ai/prompts/graph_edge.py`（`cross_book` 只做候选收集/合并）；`BookRelation.from_book_id` 增量迁移；前端方向箭头收敛 `edgeEndpoints`/`edgeDirLabel`；新增 7 项 pytest（后端 135 通过、ruff 全绿、vue-tsc + vite build 通过）；关联文档升需求 v1.32、技术栈 v1.38、使用手册 v1.62 |
| v1.21 | 2026-08-03 | 登记 M10 收敛重构 | 笔记导出从路由抽出为 `services/note_export.py`（Markdown/PDF 双格式共用数据获取）；页图目标宽度 `lru_cache` 缓存；测试环境隔离真实 `.env` 的 AI/视觉配置（修复后台预提取与测试竞态等 9 项环境性失败）；后端 128 项 pytest + ruff 全绿、vue-tsc + vite build 通过；关联文档升需求 v1.31、技术栈 v1.37、使用手册 v1.61 |
| v1.20 | 2026-08-02 | 登记书架能力收敛 | `list_books` 排序/搜索与 `reorder_books` 批量重排收敛 `repositories/books`；`Book.position` 增量迁移（`_ensure_columns`）；HomeView 状态拆分（搜索/标签/拖拽/标签编辑）；新增 6 项 pytest（后端 125 通过、ruff 全绿）；关联文档升需求 v1.30、技术栈 v1.36、使用手册 v1.60 |
| v1.19 | 2026-08-02 | 登记 M9 第五轮收敛重构 | 相关度判定从 `profile_service` 抽出为 `services/relatedness.py`（决策 20 落地），暖记忆/行为样本/阈值学习职责分离；修复相关度查询 NULL 反馈边被 `!=` 误过滤缺陷；移除遗留常量与未用导入；新增 8 项 pytest（后端 119 通过、ruff 全绿）；关联文档升需求 v1.29、技术栈 v1.35、使用手册 v1.59 |
| v1.18 | 2026-08-02 | 登记 M8 第 12 轮收敛重构 | `cross_book.py` 提取 `_pair_score`/`pair_key` 消除全量重建与导入增量两份相似评分/边键逻辑；导入链路（`books.py`）与检索端点（`graph.py`）只做触发与参数校验，评分/检索逻辑收敛服务层；新增 4 项 pytest（后端 111 通过、ruff 全绿）；关联文档升需求 v1.28、技术栈 v1.34、使用手册 v1.58 |
| v1.17 | 2026-08-02 | 登记图谱联动/阈值学习收敛重构 | 新增 `services/graph_sync.py`（本地存根 + LLM 联动）与 `services/profile_learning.py`（阈值学习）独立成模块；阈值读取统一走 `get_thresholds(db)`（`profile_service`/`recommendation_service` 魔法值收敛）；修复 SQLAlchemy `!=` 过滤 NULL 行缺陷与前端重建按钮失效；新增 10 项 pytest（后端 107 通过、ruff 全绿）；关联文档升需求 v1.27、技术栈 v1.33、使用手册 v1.57 |
| v1.16 | 2026-08-02 | 登记耦合解耦重构 | 拆分 `graph_service.py` → `services/graph/` 包、`ai_context.py` → `ai/factory.py` + `services/citations.py` + `repositories/chat.py` + `repositories/assets.py`（资产读取/检索）；`book_tags` 消除 tag 清洗重复；chat 路由走仓库；前端抽 `useReaderDoodle`；后端 97 项 pytest + ruff 全绿，前端 vue-tsc + vite build 通过；关联文档升需求 v1.26、技术栈 v1.32、使用手册 v1.56 |
| v1.15 | 2026-08-02 | 同步 M8 实现与耦合审查 | 登记 M8（知识图谱）实现后的耦合审查：图谱计算/聚类/序列化收敛 `services/graph_service.py`，路由瘦身；修复跨书计算误置 graph_built 与领域聚类空结果两个缺陷；新增 6 项图谱 pytest；关联文档升为需求 v1.16、技术栈 v1.21、使用手册 v1.44 |
| v1.14 | 2026-08-02 | 同步 M7 实现与耦合审查 | 登记 M7（PDF 统一按页 + 多模态视觉提取）实现后的耦合审查：视觉提取/缓存/窗口/重建收敛 `services/vision_extract.py`；页上下文与引用解析复用 `ai_context`；多模态配置收敛 `repositories.settings`；修复页缓存路由注册顺序；`parse_pdf` 移除双分支死代码；关联文档升为需求 v1.15、技术栈 v1.16 |
| v1.13 | 2026-08-02 | 登记 AI 上下文解耦重构 | 后端新增 `app/services/ai_context.py` 公共层：消除 `mindmap_service → chat_service` 横向依赖（原 7 项共享工具函数迁出）；统一 `build_messages`/`build_mindmap_messages` 的正文+RAG 上下文拼装为 `build_context_block`；页图路径统一 `media_service.page_image_path`；`rag_service` 改走 `build_client(db)`（设置页覆盖生效）；会话 ID 统一 `chat_session_id`；45 项 pytest / ruff 全绿，接口冒烟通过；关联文档升为需求 v1.14、技术栈 v1.15 |
| v1.12 | 2026-08-02 | 登记涂鸦修复轮解耦 | 新增 `src/utils/annotations.ts` 纯函数（`toPoint`/`normalizeAnnotationPoints`）承接旧数据迁移与点坐标兼容，`PageDoodleCanvas` 不再直接解构 `points`（消除旧数据导致渲染抛错的耦合点）；画板尺寸同步逻辑收敛到 `syncSize`（仅在缓冲区变化时重置，杜绝"清空画布"类时序 bug）；涂鸦工具栏移入阅读页顶部后，`ReaderView` 的 pageMode 分支职责更清晰；新增 6 项 Vitest（共 23 项）；关联文档升为需求 v1.14、技术栈 v1.14 |
| v1.11 | 2026-08-02 | 同步关联文档版本 | 需求文档升 v1.13、技术栈规范升 v1.13；本轮为 M6 实现（书签 + 涂鸦）文档同步，无既有代码重构 |
| v1.10 | 2026-08-02 | 同步关联文档版本 | 需求文档升 v1.12、技术栈规范升 v1.12；本轮为涂鸦划线批注与提问（需求 9.1 决策 26）文档同步，无代码改动 |
| v1.9 | 2026-08-02 | 同步关联文档版本 | 需求文档升 v1.11、技术栈规范升 v1.11；本轮为 M6 起里程碑重新规划（需求 §7）文档同步，无代码改动 |
| v1.8 | 2026-08-02 | 同步关联文档版本 | 需求文档升 v1.10、技术栈规范升 v1.10；本轮为书签组织与涂鸦功能定稿（需求 9.1 决策 24/25）文档同步，无代码改动 |
| v1.7 | 2026-08-02 | 同步关联文档版本 | 需求文档升 v1.9、技术栈规范升 v1.9；本轮为 PDF 统一按页处理（需求 3.2/3.4.11）文档同步，无代码改动 |
| v1.6 | 2026-08-02 | 同步关联文档版本 | 需求文档升 v1.8、技术栈规范升 v1.8；本轮为书签/涂鸦与提取触发细化（需求 3.4.11/3.3/3.5）文档同步，无代码改动 |
| v1.5 | 2026-08-02 | 同步关联文档版本 | 需求文档升 v1.7、技术栈规范升 v1.7；本轮为开放项定稿（决策 17~23）同步，无代码改动 |
| v1.4 | 2026-08-02 | 同步关联文档版本 | 需求文档升 v1.6、技术栈规范升 v1.6；本轮为文档固化（扫描件多模态视觉提取设计），无代码改动 |
| v1.3 | 2026-08-02 | 同步关联文档版本 | 需求文档升 v1.4、技术栈规范升 v1.5；本轮为脑图小步扩展（新增纯函数 `utils/text.ts` 与组件按钮），无既有代码重构 |
| v1.2 | 2026-08-02 | 登记阅读页解耦重构 | 后端 N+1 批量聚合（books_reading_summary + selectinload）；前端抽 utils/progress、utils/content 纯函数；ReaderView 1054→427 行（5 组合式函数 + 2 子组件）；补 Vitest 基线 5 项 |
| v1.1 | 2026-08-01 | 同步文档版本 + 登记 M3 重构 | 关联文档升为 v0.9 / v1.4；重构记录表登记 require_book 公共依赖提取（M3 收尾耦合审查产出） |
| v1.0 | 2026-08-01 | 初稿 | 定义重构原则、触发信号、流程、分层规范、坏味道对照、红线与评审要求 |

# 技术栈规范（v1.44）

> 状态：定稿（随实现微调）
> 关联文档：`需求文档.md`（v1.38）、`使用手册.md`（v1.68）
> 说明：本文档锁定本项目的技术选型与工程规范，所有代码必须遵循；与需求文档冲突时以本文档为准（需求描述除外）。

---

## 1. 技术选型总览

| 层 | 选型 | 版本要求 | 说明 |
| --- | --- | --- | --- |
| 后端语言 | Python | 3.11+ | 类型注解、异步友好 |
| 后端框架 | FastAPI | 最新稳定版 | 异步、自带 OpenAPI 文档、Pydantic 集成 |
| ASGI 服务器 | Uvicorn | 最新稳定版 | 本地运行 `127.0.0.1:8321` |
| 数据库 | SQLite（WAL 模式）+ SQLAlchemy 2.0 | SQLAlchemy ≥ 2.0 | 本地单用户，单文件库 |
| 数据库迁移 | Alembic | 最新稳定版 | 模型变更一律走迁移 |
| PDF 解析 | `pymupdf`（PyMuPDF） | 最新稳定版 | PDF 统一按页渲染为图片阅读（含文本型，需求 v1.9）；本地文本抽取仅作全文检索索引 |
| Markdown 解析 | `markdown`（含 toc 扩展） | 最新稳定版 | 标题层级切分章节 |
| EPUB 解析 | `ebooklib` | 最新稳定版 | 正文+目录解析、保留排版 |
| AI 客户端 | `openai` 官方 SDK | 最新稳定版 | 自定义 `base_url` 兼容多厂商 |
| 配置管理 | `pydantic-settings` | 最新稳定版 | `.env` 管理 api_key 等 |
| 前端框架 | Vue 3 + TypeScript + Vite | Vue ≥ 3.4、TS 严格模式 | SPA，见 ADR-003 |
| UI 组件库 | Element Plus | 最新稳定版 | 中文生态成熟，按需引入 |
| 状态管理 | Pinia | 最新稳定版 | 替代手写全局状态 |
| 路由 | Vue Router 4 | 最新稳定版 | |
| 图表/图谱 | ECharts 5 | 最新稳定版 | 脑图（树图）+ 谱系图（关系图）；复杂交互备选 AntV G6（ADR-004） |
| Markdown/公式渲染 | markdown-it + KaTeX + DOMPurify | 最新稳定版 | 用户输入与 LLM 输出统一渲染；公式用 KaTeX，复杂公式兼容 MathJax；渲染前消毒 |
| 构建/包管理 | Vite + pnpm | 最新稳定版 | 前端依赖统一 pnpm |
| 后端代码质量 | Ruff | 最新稳定版 | lint + format |
| 前端代码质量 | ESLint + Prettier | 最新稳定版 | |
| 测试 | pytest（后端）/ Vitest（前端） | 最新稳定版 | |
| 版本控制 | Git | — | Conventional Commits |

---

## 2. 项目结构（monorepo）

```
LLMnotebook/
├── backend/
│   ├── app/
│   │   ├── main.py            # 应用入口、路由注册、启动任务
│   │   ├── core/              # 配置、日志、安全、常量
│   │   ├── api/               # 路由层（仅做参数校验与响应组装）
│   │   ├── models/            # SQLAlchemy ORM 模型
│   │   ├── schemas/           # Pydantic 请求/响应模型
│   │   ├── services/          # 业务逻辑（导入、画像、图谱、阅读、AI；graph/ 为图谱包，ai_context 为阅读上下文组装）
│   │   ├── repositories/      # 数据访问层（封装 SQLAlchemy）
│   │   ├── parsers/           # PDF / Markdown / TXT / EPUB 解析器
│   │   ├── ai/                # LLM 客户端、提示词、上下文管理、流式输出
│   │   └── tasks/             # 异步任务（解析、图谱构建、画像更新）
│   ├── tests/                 # pytest 测试
│   ├── pyproject.toml         # 依赖 + Ruff 配置
│   ├── alembic/               # 数据库迁移
│   ├── .env.example           # 环境变量模板
│   └── README.md
├── frontend/
│   ├── src/
│   │   ├── views/             # 页面（书架/谱系图/阅读/设置/画像）
│   │   ├── components/        # 组件（阅读器/脑图/图谱/笔记面板等）
│   │   ├── composables/       # 组合式函数（页面级逻辑按域拆分，如阅读进度/笔记/划词/AI/脑图）
│   │   ├── stores/            # Pinia stores
│   │   ├── api/               # 后端接口封装（统一请求/错误处理）
│   │   ├── router/            # 路由
│   │   ├── utils/             # 工具函数
│   │   └── styles/            # 主题变量（护眼/夜间）
│   ├── package.json
│   ├── vite.config.ts
│   └── .eslintrc / .prettierrc
├── data/                      # 运行时数据（书籍文件、SQLite、缓存），不入库
├── docs/                      # 需求文档、本规范、使用手册、决策记录
└── .gitignore
```

---

## 3. 后端规范

### 3.1 代码风格
- 全程类型注解；公共函数必须写 docstring。
- 异步优先：IO（数据库、LLM、文件解析）用 `async`/`await`；CPU 密集解析放 `asyncio.to_thread` 或任务队列。
- 禁止 `except Exception` 裸吞异常；异常必须记录日志或转成业务错误码。

### 3.2 分层与依赖方向
- 严格分层：`api → services → repositories → models`。
- 禁止跨层调用（如 services 直接拼 SQL、api 直接访问 models 字段运算）。
- 依赖注入：数据库会话、LLM 客户端以参数/构造器注入，便于单测替换。
- 公共逻辑放 `core/`，禁止各模块复制粘贴。
- 共享 AI 上下文按职责拆分：客户端构建收敛 `ai/factory.py`（`build_client`/`is_configured`）；引用出处解析收敛 `services/citations.py`；资产内容读取（`read_asset_content`）/RAG 检索/Skill 读取收敛 `repositories/assets.py`；会话历史读写收敛 `repositories/chat.py`；阅读上下文组装（段落编号/PDF 页窗口文本/上下文块）保留 `services/ai_context.py`。chat/mindmap/rag 等服务不得互相横向依赖。

### 3.3 API 规范
- 资源命名：复数名词、小写、`-` 分隔（如 `/api/books`、`/api/books/{id}/chapters`）。
- 响应结构统一：`{ "code": 0, "message": "ok", "data": ... }`；错误时 `code` 非 0。
- 分页：`page`、`page_size` 参数，响应含 `total`。
- 耗时操作（导入、图谱构建）返回 `task_id`，前端轮询任务状态接口。
- 流式 AI 输出走 SSE（`text/event-stream`）。

### 3.4 配置与密钥
- 全部配置走 `pydantic-settings` + `.env`；`.env` 不入库，提供 `.env.example` 模板。
- 配置优先级：**设置页保存值（DB `settings` 表）> `.env`**；需以 `.env` 为准时，清除对应 DB 覆盖项（`DELETE FROM settings WHERE key=...`）后重启后端。
- API Key 禁止硬编码、禁止写入日志、前端不回显明文。
- 数据目录固定 `data/`，路径由配置注入。

### 3.5 异步任务
- 解析、图谱构建、画像更新一律异步执行，提供任务状态（排队/执行中/成功/失败）。
- 任务失败可重试；重试需幂等（同输入重复执行结果一致）。

### 3.6 日志
- 使用标准库 `logging`，按模块 `getLogger(__name__)`。
- 级别规范：DEBUG 调试、INFO 任务完成、WARNING 可恢复问题、ERROR 未捕获异常。
- 日志不得包含书籍正文与 API Key。

---

## 4. 前端规范

### 4.1 代码风格
- Vue 3 组合式 API + `<script setup>`；TypeScript 严格模式。
- 组件命名：PascalCase 多词（`BookShelfCard`）；props/事件定义完整。
- 业务请求禁止散落组件内，统一走 `src/api/` 封装（含错误提示、token 无关的通用处理）。

### 4.2 组件划分
- 容器组件：管理状态与数据获取，渲染子组件。
- 大页面拆分：单文件超 400 行时拆展示子组件（props/events）与组合式函数（`src/composables/`，按域拆分状态与副作用）；共享纯函数放 `src/utils/`。
- 展示组件：只收 props、只发事件，不直接请求后端。
- ECharts 统一封装为通用组件（`MindMapChart.vue`、`BookGraph.vue`），配置与数据分离。

### 4.3 状态管理
- 全局共享状态（当前书、阅读位置、AI 会话）用 Pinia。
- 页面级临时状态不放入全局 store。

### 4.4 主题与样式
- 主题色/字体等用 CSS 变量定义，支持护眼/夜间切换。
- 组件库按需引入，避免全量打包。


### 4.8 阅读/构建性能约定（M10）

- **笔记导出**：`services/note_export.py` 提供 `build_notes_markdown` / `build_notes_pdf`；PDF 使用 PyMuPDF 内嵌 CJK 字体（`pymupdf.Font("cjk")`）生成 A4，正文 Markdown 记号转纯文本、LaTeX 公式以源码保留（服务端不做公式排版）；路由 `GET /api/books/{id}/notes/export?fmt=md|pdf`（默认 md）。
- **页图性能**：`parsers/pdf.py` 的目标宽度按 `(文件, 页号)` 进程内缓存（`lru_cache`，PDF 导入后不可变）；页图响应 `Cache-Control: private, max-age=600`（页图不可变，翻页复用浏览器缓存）；前端 PDF 页模式预加载相邻页（`.page-preload` 隐藏 img）。
- **前端构建分包**：vite `manualChunks` 拆分 vendor-vue / vendor-element / vendor-echarts / vendor-markdown；echarts 仅图谱/脑图路由按需加载；新增 vendor 库时同步更新 `vite.config.ts` 分包列表。
### 4.5 图谱渲染性能
- 谱系图采用「领域聚类 + 分层展开」（需求 3.6.1），避免一次性渲染千节点。
- 大数据集使用 ECharts 采样/增量更新，交互保持流畅。

---


### 4.6 Markdown 与 LaTeX 渲染（输入输出统一）

- 用户输入与 LLM 输出共用统一渲染组件（如 `RichText`），禁止各页面各自实现 Markdown 解析。
- Markdown：`markdown-it`（标题、列表、代码高亮、表格、链接）；代码块语法高亮统一插件。
- LaTeX：`KaTeX` 渲染行内 `$...$` 与块级 `$$...$$`；KaTeX 不支持的复杂公式兼容 `MathJax`。
- 脑图/图谱等非富文本控件：节点显示文本先经 `src/utils/text.ts` 的 `toPlainDisplayText` 清洗（去 `$` 定界、LaTeX 命令/花括号、Markdown 记号）再渲染，避免 ECharts 标签显示原始记号；完整 Markdown/LaTeX 内容（如脑图大纲）以 `.md` 下载或作为笔记正文渲染。
- **安全红线**：任何来源的 Markdown/HTML 在渲染前必须经 `DOMPurify` 消毒；禁止未消毒内容直接 `v-html`。

### 4.7 PDF 页图涂鸦（画板）

- PDF 按页阅读时启用：页图上叠加 `<canvas>` 画板，工具含**笔刷/高亮/橡皮/文本**，颜色、线宽与字号可调；橡皮擦除笔画/文本痕迹。
- **撤销与历史（需求 9.1 决策 25）**：支持**撤销上一步，最多 5 步**；撤销栈仅在**会话内**维护（`undoStack` 上限 5），**不持久化历史版本**，刷新/重开页面即重置。
- **划线批注与提问（需求 9.1 决策 26）**：每条 `stroke` 可**选中**后附加**批注**（`note` 字段，Markdown/LaTeX 文本 + `note_meta` 时间戳）或发起**对划线区域的 AI 提问**；提问走 §6 多模态/页缓存链路（优先注入该页 `page_text` 缓存文本并附带划线区域裁剪图，缓存缺失回退划线区域图片附件或提示先提取），出处标注「第 X 页」，受隐私开关约束。
- 持久化：保存为 `data/books/<书目录>/annotations/page_XXX.json`（元素数组：`stroke`——工具/颜色/线宽/坐标序列/可选 `note`+`note_meta`；`text`——内容/颜色/字号/位置），打开页时按书+页加载重绘；涂鸦不修改原图。

## 5. 数据层规范

- SQLite 开启 WAL 模式与外键约束。
- 高频查询字段建索引：`books.status`、`chapters.book_id`、`notes.book_id`、`kp.book_id`。
- 模型变更必须通过 Alembic 迁移，禁止手工改表。
- 三层画像（M9）：`UserProfile` 表（`layer` cold/warm/hot + `dimension` + `value_json` + `updated_at`）；热=当前书细节（章节脉络/进度/高亮/问题）、暖=近 1~2 本 + 相关领域书（recent_books/related_books/archived_count）、冷=长期偏好（domain_preferences/knowledge_level/long_term_interests）；归档迁移：跨 1 本热→暖、跨 3 本暖→冷、>3 本全部沉淀冷（`WARM_TO_COLD_THRESHOLD=3`、`KEEP_RECENT=2`）；相关领域判定（决策 20 已定稿，v1.35）：`services/relatedness.py` 相关度函数 = max(跨书谱系边强度, 0.5×暖画像术语覆盖率×100 + 0.5×冷画像术语覆盖率×100)，同 post 簇直接判定相关；related = 同簇 或 score ≥ `related_strength`（默认 60，自动学习 45~60、设置页手动覆盖）；画像阈值自动学习（v1.27）：阈值持久化在 `settings` 表 `profile.warm_threshold / profile.related_strength / profile.review_days / profile.learning_state`（JSON 节奏样本），`services/profile_learning.py`（`get_thresholds`/`save_thresholds`/`record_archive`/`learn_thresholds`：归档样本 ≥6 或确认关联边 ≥3 后按归档间隔中位数/节奏/确认关联低分位自动调整，范围 2~5 / 45~60 / 1~7，样本不足回退默认常量），`profile_service`/`recommendation_service` 统一走 `get_thresholds(db)`；API 在 `api/routes/profile.py`（GET /api/profile、POST /api/profile/reset、GET/PATCH /api/profile/thresholds、POST /api/profile/learn），阅读进度/笔记路由回写热画像，归档任务触发迁移并记录节奏样本。
- 缓存策略：章节解析结果、AI 生成结果、图谱计算结果落盘缓存（`data/cache/`），按书籍/任务维度失效；删除书籍时清理对应缓存。
- 书签存 SQLite（`Bookmark` 表：`book_id + chapter_id/page_index/para_pos` 定位；新增 `group_name` 分组/文件夹字段，列表默认按 `created_at` 倒序平铺，支持分组归类；随书删除）。
- PDF 页图涂鸦存书籍目录 `annotations/page_XXX.json`（元素 JSON：`stroke`/`text`；`stroke` 可含 `note` 批注文本与 `note_meta` 时间戳），随书删除清理；仅保存最终元素，不保存历史版本（撤销栈仅会话内，最多 5 步）。
- 书架排序（v1.36）：`Book.position`（INTEGER，默认 0）为书架排序位——新书导入自动取全局最大位 + 1；`list_books` 按 `position asc, created_at desc` 返回并支持 `q`（书名/作者/`tags_json` 子串搜索）；`POST /api/books/reorder`（`repositories.books.reorder_books`）按 `ordered_ids` 批量重排（position 1..N，未知 id 忽略）；增量列由 `database._ensure_columns` ALTER 补齐（旧库无需重建）。
- 知识图谱（M8）：数据表 `BookRelation`（书籍关联：strength 0~100、direction 无/承接/发展/批判、from_book_id 理论源头书（有向边箭头起点）、relation_type、reasons_json、user_feedback 人工反馈）、`KnowledgePoint`（书内知识点：chapter_id+para_pos 定位、importance 1~5、level 章节级/重要段落/用户标记）、`KpRelation`（知识点关系：前置依赖/承接等）；关联强度 = 关键词共现余弦分 + 笔记加权（`services.graph.cross_book._note_weight` 上限 15 分），同聚类书补低分「主题相似」边；聚类规则 tag → 文件夹名 → 领域自动聚类；书内图谱懒构建（`graph_built` 标志）+ 全局/单书手动重建；人工反馈回写 `user_feedback` 作为修正层；前端 ECharts graph（force 布局、聚类筛选、最密切边高亮、方向箭头、层级筛选、导出 PNG）；聚类算法细节见 `docs/知识图谱聚类算法.md`。**导入自动增量更新（v1.28 已实现）**：`upload_book` 导入成功后调用 `cross_book.incremental_cross_book_graph(db, book_id)`（复用 `_pair_score` 评分、同聚类低分「主题相似」边、幂等、不动既有关联），随后 `link_graph_assets` 补本地 RAG 联动存根；**跨书知识检索（v1.28 已实现）**：`GET /api/graph/knowledge/{kp_id}/appears-in`（`cross_book.knowledge_appears_in`：其他书 KnowledgePoint title/summary 关键词重叠 + 其他书 RAG `key_points` 命中，来源书不计）。**LLM 打分与方向/原因增强（v1.32 已实现，M8 收尾）**：跨书重建/导入增量对关键词候选对做**有界 LLM 打分**（`services/graph/llm_score.py`：`MAX_LLM_PAIRS=40` 按关键词分降序截断控制成本；提示词 `ai/prompts/graph_edge.py`）——LLM 输出 strength 0~100、`from_book`（A/B/null）+ direction（承接/发展/批判/无）、relation_type（理论传承/概念共现/主题相似/应用扩展）、3~6 条 reasons；强度取 max(关键词分, LLM 分)，方向/类型/原因以 LLM 为准；未配置 AI / 调用失败 / 非法输出一律回退关键词分，不阻塞构建；`book_relations` 新增 `from_book_id` 列（`_ensure_columns` 增量迁移）；谱系图按 `from_book` 显式画箭头并展示「A → B」方向与原因；`graph_sync.attach_linked_book_stub` 的 `linked_books` 存根补 `direction` 字段。**两阶段分类（M9 基础版已实现）**：pre-classify（tag/文件夹/关键词共现粗略聚类，实时计算）→ 获得 RAG/Skill 资产后 post-classify（基于 `summary/key_points` 后验关键词 top60：簇内一致性校验 `T_POST=3` 维持 / 簇迁移 / 维持 pre 簇，tag/文件夹硬约束优先，落盘 `cluster_name/classify_source/classified_at/classify_version`；`assign_clusters` 懒校验直接采用未失效的 post 结果，资产 version 变化失效重算；簇合并（`T_MERGE=5`：两簇后验关键词并集重叠阈值）与代表性术语重命名（簇内出现书最多的后验术语众数，冲突跳过）**已实现**（`merge_and_rename_clusters`：只处理 `classify_source=post` 的书，归档成功后触发，失败不阻塞归档））。

- **领域命名约定（M9 第四轮已实现）**：领域名（领域自动聚类簇名 / post-classify 自动命名 / 簇重命名）必须是**领域专业术语**。`services.graph.lexicon` 提供泛化词表 `_GENERIC_DOMAIN_TERMS`（定理/定义/证明/方法/理论/概念/教程/讲义/习题/答案/笔记/前言/附录/摘要/引言/目录 等泛化与出版元词，命名时过滤）；候选词由 `_domain_candidates(book, posterior)` 生成：章节标题×3 + 清洗后书名×2 + 正文×1 + RAG 后验（summary+key_points）×5，经 `extract_keywords` 抽取并剔除泛化词（书名/标题先经 `_clean_title_segment` 去掉括号内作者/版本/出版社与下划线段）；`_pick_domain_name` 从簇内成员候选词中按「覆盖书数优先 → 词频次之」挑选；`post_classify_book` 自动命名用 `_first_meaningful_term`，`merge_and_rename_clusters` 重命名跳过泛化词众数。**专业术语词库（M9 第五轮已实现）**：用户可编辑词库文件 `domain_terms_file`（默认 `backend/domain_terms.txt`，与 .env 同级；每行一个术语、`#` 注释；上部【用户区】最高优先级，「系统缓存区」标记行之后为系统自动学习术语）。`load_domain_lexicon` 按 mtime 缓存读取（修改即时生效）；命名优先级：**用户词库命中 → 自动候选（含系统缓存词库，覆盖书数优先→词频次之）**；自动选定的专业术语经 `cache_domain_term` 写入缓存区（泛化词不缓存、去重、缺失自动建文件）；`_first_meaningful_term`/`merge_and_rename_clusters` 同样优先用户词库。

---

## 6. AI 接入规范

- 统一封装 LLM 客户端（`app/ai/client.py`），构造参数：`base_url / api_key / model / mode / timeout / verify_ssl / temperature / max_tokens / top_p / frequency_penalty / presence_penalty / stop / thinking_type / reasoning_effort / enable_thinking / thinking_budget`，全部来自配置（`.env` + 设置页 DB 覆盖）。
- **精细参数与思考模式（按 DeepSeek 官方文档，需求 9.1 决策 27）**：文本 AI 的 `thinking`（`AI_THINKING_TYPE`：`enabled/disabled`）与 `reasoning_effort`（`AI_REASONING_EFFORT`：`low/medium/high/max`，`thinking=disabled` 时不发送）**仅 chat 模式**写入请求体；`AI_MAX_TOKENS`（DeepSeek 默认 32K、最大 64K，含思考 token）；`temperature/top_p/frequency_penalty/presence_penalty` 在思考模式下官方说明「设置不报错但不生效」；**responses 模式**仅把 `max_tokens` 映射为 `max_output_tokens`（DeepSeek responses 命名），thinking 参数待定稿。
- **多模态视觉精细参数（SiliconFlow）**：`vision_temperature / vision_top_p / vision_frequency_penalty / vision_presence_penalty` 为通用 OpenAI 兼容参数；`vision_enable_thinking / vision_thinking_budget`（`enable_thinking`/`thinking_budget`）仅 SiliconFlow DeepSeek/Zhipu 系推理模型可用，Qwen 等非推理模型勿开。
- 上下文策略：整本书建索引，回答按「当前章节/选中段落」取上下文；图谱构建按批次处理，控制 token 成本。
- 所有生成结果带引用出处（书名 + 章节 + 段落位置，需求 3.4.8）。
- 超时与限流：设置超时时间，限流时退避重试；失败降级为本地规则式摘要/关键词共现，并提示用户。
- 隐私开关：关闭时 AI 请求不携带书籍正文，仅用元信息与用户问题。
- 提示词集中存放（`app/ai/prompts/`），按能力分文件，修改走版本记录。
- **RAG/Skill 资产存储与调用约定**：
  - **存储**：统一存 `BookAsset` 表（`book_id` + `kind(rag/skill)` + `content_json` + `version`），以「书」为单位，随书删除；`content_json` 结构由 Pydantic schema 定义并版本化，禁止散落为任意文件。
  - **生成与更新时机**：读完归档（需求 3.4.9）生成——**M9 已实现**：`POST /api/books/{id}/archive` 提交 `rag_service.archive_book_task` 后台任务，PDF 书先经 `vision_extract.rebuild_book_caches` 视觉通读全书补齐页缓存（命中不重复调用），再以缓存全文（`page_chunks`/`_build_page_input`，RAG 片段与出处按「第 X 页」）由文本模型总结（`generate_rag_skill(..., page_texts=...)`），随后章节全部标记已读、状态=读完；非 PDF 按章节正文总结；再次阅读结束、跨书图谱更新（需求 3.4.7 / 3.6.1）时**在原条目上增改**，每次更新 `version + 1`，**保留至少 3 个版本，回滚粒度为整条目**（需求 9.1 决策 19）。**增量增改（M9 已实现）**：再次归档时 `generate_rag_skill` 检测到已有 `rag` 资产即走增量提示词（`rag_skill.py` `INCREMENTAL_SYSTEM_PROMPT` + `build_incremental_user_prompt(book_title, old_rag, old_skill, new_material, body_text)`），素材由 `rag_service._collect_new_material` 收集（该书全部笔记/划线/「不理解」+ 最近 20 条对话），输出仍为同结构 JSON 整体覆盖落库，原条目 id 不变。
  - **RAG 调用约定**：问答前按相关性检索 `kind=rag` 资产（当前书优先；跨书问答可全库检索），命中片段连同出处注入上下文；注入片段必须保留「书名 + 章节 + 段落」出处，符合需求 3.4.8。
  - **Skill 调用约定**：按任务类型/领域匹配 `kind=skill` 资产（结合暖画像与跨书谱系关联度排序），匹配到的技能以指令形式注入系统提示词；未匹配则不注入，避免无关技能污染回答（**AI 自动按任务匹配调用**，需求 9.1 决策 18）。
  - **联动一致性**：图谱更新、相关领域书写入暖画像等事件，必须触发受影响书籍 RAG/Skill 的增量更新（或标记「待重算」），保证资产不过期。**已实现（v1.27）**：`services/graph_sync.py`——跨书谱系重建/懒构建/关联反馈（确认/修改）自动补本地 RAG 存根（`attach_linked_book_stub` 写 `rag.linked_books`、`link_domain_terms` 写 `domain_terms/linked_terms`，无 AI 也可执行，内容未变化不写库不 bump 版本，避免 post-classify 频繁失效）；`POST /api/graph/sync` 显式联动时 `sync_assets_for_relations`对强度 ≥50（`LINK_MIN_STRENGTH`）且未忽略的关联按 `ai/prompts/rag_link.py` 增量提示词执行 LLM 增改（旧资产概要 + 关联描述 + `rag_book_input` 轻量素材 → 合并 JSON 整体覆盖，保留 `chunks/linked_books/domain_terms`，version+1，失败回滚不阻塞，随后 post-classify）；未配置 AI 时仅本地存根不报错。前端谱系图页「💾 联动沉淀」按钮（`api/graph.ts syncGraphAssets`）。
  - **回收与清理**：删除书籍时级联删除其 `BookAsset` 与相关缓存；提供「重建单书 / 重建全部」入口，用于版本损坏或模型升级后的恢复。**手动删除（v1.40 已实现）**：`DELETE /api/books/{id}/asset?kind=rag|skill` 删除整条资产、`DELETE /api/books/{id}/asset/{kind}/{section}/{index}` 删除单条条目（`rag/key_points`、`rag/chunks`、`skill/skills`，0 基索引），单条删除视为资产变更 `version + 1` 并触发 post-classify 失效重算；前端资料页提供整条/单条删除按钮（二次确认）。**去重合并（v1.41 已实现，v1.43 判定基准更新）**：条目级——写入 `upsert_asset` 时对列表字段按 `content_hash`（规范化 JSON 指纹，剔除 `merged_book_ids`）去重；资产级——`POST /api/assets/dedupe` 执行 `merge_duplicate_assets`，判定基准为**书籍内容 hash**（`Book.content_hash` = 原文件 sha256，导入时计算、旧书懒回填，只与书籍本身内容相关、与 LLM 生成内容无关），hash 相同的多本书资产合并为一条主资产（保留最新），被合并书 id 写入主资产 `content.merged_book_ids`，`get_asset`/`list_assets`/RAG 检索/Skill 读取反查透明共享（`read_asset_content` 剔除元数据保持幂等可比）；**「删除资产」语义 = 移除知识库（rag/skill）及其对应书籍**：`DELETE /api/books/{id}` 级联删除资产与文件（`books_service.delete_book` 走仓储 `delete_assets`，共享引用成员解除、主书转移）。
- **PDF 页图的多模态视觉提取与缓存约定**（需求 3.4.11，统一适用于全部 PDF）：
  - **视觉入口**：PDF 页图通过**独立配置的多模态 LLM 客户端**（复用 `app/ai/client.py` 的 `LLMClient`，配置项 `vision_base_url / vision_api_key / vision_model / vision_timeout / vision_verify_ssl / vision_max_tokens` 及精细参数 `vision_temperature / vision_top_p / vision_frequency_penalty / vision_presence_penalty / vision_enable_thinking / vision_thinking_budget`，由 `repositories.settings.vision_client_kwargs/vision_configured` 提供；**独立于文本模型配置、无需额度管理**，需求 9.1 决策 23）逐页提取完整页面信息（Markdown 文本，含公式/表格/图注描述），收敛在 `services/vision_extract.py`。
  - **接口对齐（SiliconFlow）**：视觉客户端**强制 `mode="chat"`**（仅支持 `POST /chat/completions`；`responses` 模式会 404）；请求体按设置携带 `max_tokens`（`vision_max_tokens`，默认 4096）与精细参数（`temperature/top_p/frequency_penalty/presence_penalty`，以及推理模型专用 `enable_thinking/thinking_budget`）；页图与对话附件（页图/划线区域裁剪图）的 `image_url` 统一带 `detail="high"`，部件格式 `{"type":"image_url","image_url":{"url":..., "detail":"high"}}`（`url` 支持 base64 data URI，前端裁剪图统一输出 `image/jpeg`）；`LLMClient` 支持以上构造参数（chat 分支写入请求体，responses 分支不受影响）。
  - **页级缓存**：提取结果写入 `data/books/<书目录>/page_text/page_XXX.md`（UTF-8 Markdown，文件名 = 页号）；**存在且非空即命中，不再调用多模态 API**；提取任务异步执行（`tasks.submit` 后台线程），单页失败可重试、不中断整书重建。
  - **触发时机**：手动导入 PDF（含文本型）作为知识库时**批量预提取**（导入服务后台提交）；阅读时不预提取，**用户在当前页对 AI 提问**时按需提取；**读完归档时全书批量提取**（补全缺失页缓存，作为 RAG/Skill 总结的全文输入，命中页不重复调用）；窗口 `[P-1,P,P+1]`（第 1 页/末页裁剪），仅补提取缺失页（增量缓存，需求 9.1 决策 22）。
  - **消费约定**：文本大模型解读 PDF 按页章节时注入页缓存文本（带「第 X 页」出处，符合需求 3.4.8；引用解析支持【第X页】）；缓存缺失时回退当前页原图附件（chat 模式）或提示先提取；禁止把原图直接交给文本模型。
  - **生命周期**：随书删除级联清理；阅读页提供「重提本页 / 重建页缓存 / 缓存 x/y 页」入口（`api/routes/vision.py`）；页缓存文本可作为 RAG 片段来源（复用 3.4.9 资产链路）；受隐私开关约束（关闭不提取、不发送）。
  - **本地抽取文本**：PDF 导入时把 `page.get_text()` 按页写入 `local_text/page_XXX.txt`，仅作全文检索索引，不用于正文展示与 AI 上下文。

---

## 7. 环境与运行

| 项目 | 命令/说明 |
| --- | --- |
| 后端启动 | `uvicorn app.main:app --host 127.0.0.1 --port 8321` |
| 前端开发 | `pnpm dev`（Vite 代理 `/api` 到后端） |
| 前端构建 | `pnpm build` |
| 测试 | 后端 `pytest`；前端 `pnpm test` |
| 迁移 | `alembic upgrade head` |
| 一键启动 | 根目录 `start.bat`：检查依赖后启动后端（8321）+ 前端（5173）并打开浏览器；前端命令按 pnpm → node vite.js → 本地 pnpm → 运行时 pnpm 兜底（解决系统 PATH 无 pnpm） |

环境变量清单（`.env.example`）：`APP_HOST / APP_PORT / DATA_DIR / DB_URL`；文本 AI `AI_BASE_URL / AI_API_KEY / AI_MODEL / AI_MODE / AI_TIMEOUT / AI_VERIFY_SSL / AI_TEMPERATURE / AI_ENABLE_BODY_SEND`（隐私开关）与精细参数 `AI_MAX_TOKENS / AI_THINKING_TYPE / AI_REASONING_EFFORT / AI_TOP_P / AI_FREQUENCY_PENALTY / AI_PRESENCE_PENALTY / AI_STOP`（DeepSeek 思考模式）；多模态视觉 `VISION_BASE_URL / VISION_API_KEY / VISION_MODEL / VISION_TIMEOUT / VISION_VERIFY_SSL / VISION_MAX_TOKENS` 及 `VISION_TEMPERATURE / VISION_TOP_P / VISION_FREQUENCY_PENALTY / VISION_PRESENCE_PENALTY / VISION_ENABLE_THINKING / VISION_THINKING_BUDGET`（独立配置，需求 3.4.11）等。

---

## 8. 任务收尾规范（使用手册 + 耦合审查）

### 8.1 使用手册同步（每轮任务必做）

- **文件**：`docs/使用手册.md`，全项目唯一使用手册入口；函数数量增长后按模块分节维护。
- **触发时机**：每执行完一轮任务（完成一批功能开发/修复）后，作为收尾步骤**必须**更新本手册。
- **记录范围**：本轮**新实现**与**发生变更**的每个函数/方法（含后端、前端、AI 提示词入口等）。
- **记录格式**（每个函数一个小节）：

```
### 函数名（`模块路径`）
- 功能：一句话说明做什么
- 输入：参数列表与类型
- 输出：返回值与类型
- 调用示例：最小可运行示例
- 使用说明：使用场景与前置条件
- 修改指南：修改入口、涉及依赖、注意事项（如不可随意改动的约定、测试要求）
```

- 示例：

```
### parse_book(file_path: str) -> BookParseResult
- 功能：按扩展名分发到 PDF/MD/EPUB 解析器并返回解析结果
- 输入：file_path（str，书籍文件绝对路径）
- 输出：BookParseResult（章节列表、元信息、警告列表）
- 调用示例：result = await parse_book("/data/books/xx.pdf")
- 使用说明：仅由导入服务调用；解析在后台任务中执行
- 修改指南：新增格式在 parsers/ 下添加解析器并在本函数注册；需同步更新单测与使用手册
```

- 未收录的新函数视为「未完成收尾」：下一轮任务不允许直接复用，避免出现无人知晓的「黑盒函数」。

### 8.2 耦合度审查与解耦重构

- **触发时机**：每实现若干函数（建议每 5~10 个，或每轮任务结束）执行一次耦合度审查。
- **审查检查点**：
  - 循环依赖（模块间互相 import）；
  - 跨层调用（services 直接拼 SQL、api 直操数据层）；
  - 重复逻辑 ≥ 3 处（需提取公共模块/工具/服务）；
  - 职责过重（一个类/模块职责 > 2 个、全局单例滥用）；
  - 长参数列表与隐藏依赖。
- **处理方式**：发现耦合过高 → 立即进行**解耦与复用重构**：
  - 提取公共模块/工具/服务，遵循分层与依赖方向（`api → services → repositories → models`）；
  - 用依赖注入替换硬编码依赖与全局单例；
  - 重构执行遵循 `重构规范.md`（先补测试基线、小步提交、不夹带功能）。
- **记录**：审查结论与重构内容登记到 `重构规范.md` §11 重构记录表，保证可追溯。

---

## 9. 技术决策记录（ADR 摘要）

- **ADR-001 单仓库（monorepo）**：前后端同仓、文档同仓，便于本地单用户项目协作与备份。
- **ADR-002 SQLite 而非 PostgreSQL**：本地单用户、无并发写压力；WAL + 外键 + Alembic 足够；后续迁移成本可控。
- **ADR-003 前端 Vue 3 而非 React**：中文生态、上手成本、与 Element Plus/ECharts 配合成熟；需求文档原「Vue 或 React」在此定稿。
- **ADR-004 ECharts 为主，AntV G6 备选**：谱系图/脑图以 ECharts 关系图+树图实现；若出现复杂交互（多选联动、大量自定义布局）再引入 G6，封装层需隔离。
- **ADR-005 openai SDK 兼容多厂商**：统一走 OpenAI 兼容协议，DeepSeek/通义等通过 `base_url` 切换，避免多 SDK。
- **ADR-006 仅本地单用户**：服务绑定 `127.0.0.1`，无鉴权体系；如需共享再引入认证（届时另行评审）。
- **ADR-007 PDF 页图多模态视觉提取 + 页级缓存（统一适用于全部 PDF）**：本地不做 OCR；页图经独立配置的多模态 LLM 提取为文本，按页缓存（`data/books/<书目录>/page_text/page_XXX.md`），命中不重复调用；文本模型基于缓存解读，缓存缺失回退页图附件（需求 3.4.11）。
- **ADR-008 PDF 页图涂鸦 = 页图 + Canvas 叠加层（PDF 按页阅读时）**：涂鸦不修改原图，作为独立标注层持久化（`annotations/page_XXX.json`，`stroke`/`text` 元素），读取时叠加显示；支持会话内撤销（最多 5 步）、擦除与文本输入，不保存历史版本（需求 9.1 决策 25）；**每条划线可选中后附加批注（`note`）或发起对划线区域的 AI 提问**（走页缓存/多模态链路，需求 9.1 决策 26）；位置书签存 SQLite（`Bookmark` 表，含分组字段，需求 9.1 决策 24），与涂鸦分离存储。

---

## 10. 变更记录

| 版本 | 日期 | 变更内容 | 说明 |
| --- | --- | --- | --- |
| v1.44 | 2026-08-03 | 联动存根不 bump 版本；空存根总结全量 | §6 补充：`graph_sync` 存根写入用 `repositories.assets.save_asset_content`（新增，不递增版本）；`generate_rag_skill` 增量判定以**实质内容**为准（summary/key_points 非空），空存根先删后全量生成（新书首次总结 version=1）；后端 151 项 pytest + ruff 全绿；关联文档升需求 v1.38、使用手册 v1.68 |
| v1.43 | 2026-08-03 | 资产 hash 基准改为书籍内容；删除语义明确 | §6 AI 接入规范「回收与清理」更新：合并判定基准 = `Book.content_hash`（原文件 sha256，导入计算/旧书懒回填）；「删除资产」= 移除知识库及其书籍（`DELETE /api/books/{id}` 级联资产与文件）；后端 150 项 pytest + ruff 全绿、vue-tsc + vite build 通过；关联文档升需求 v1.37、使用手册 v1.67 |
| v1.42 | 2026-08-03 | 一键启动幂等化 | §8 启动约定补 `start.bat` 端口占用检测：启动前 `netstat` 检查 8321/5173，已在运行则跳过并提示，杜绝重复启动 `WinError 10048`；后端 147 项 pytest + ruff 全绿；关联文档升需求 v1.36、使用手册 v1.66 |
| v1.41 | 2026-08-03 | 资产去重合并（hash 相同合并为一个资产） | §6 AI 接入规范「回收与清理」补去重合并约定：`content_hash` 指纹（剔除 `merged_book_ids` 元数据）、条目级写入去重、跨书整条合并（`merge_duplicate_assets` + `POST /api/assets/dedupe`，主资产 `content.merged_book_ids` 记录共享书、读取反查透明共享）、共享引用删除/删书处理（成员解除、主书转移，`books_service.delete_book` 改走仓储 `delete_assets`）；后端 147 项 pytest + ruff 全绿、vue-tsc + vite build 通过；关联文档升需求 v1.35、使用手册 v1.65 |
| v1.40 | 2026-08-03 | 资产手动删除能力 | §6 AI 接入规范「回收与清理」补：`DELETE /api/books/{id}/asset?kind=`（整条）与 `DELETE /api/books/{id}/asset/{kind}/{section}/{index}`（单条 key_points/chunks/skills，version+1、post-classify 失效重算）；仓储 `delete_asset`/`delete_asset_item`；前端资料页删除按钮（二次确认）；后端 140 项 pytest + ruff 全绿；关联文档升使用手册 v1.64 |
| v1.39 | 2026-08-03 | 重构轮：删除死代码 + 统一 LLM JSON 解析 | 删除 `services/graph_service.py`（图谱包拆分后遗留，`services/graph/` 包已完整替代）；新增 `app/ai/parsing.py`（`parse_llm_json`），§6 AI 接入相关服务（graph_sync/llm_score/rag_service/mindmap_service）统一走该解析，不再各自维护一份；后端 135 项 pytest + ruff 全绿；关联文档升需求 v1.33、使用手册 v1.63、重构 v1.23 |
| v1.38 | 2026-08-03 | 同步 M8 完成（LLM 打分与方向/原因增强） | §5 知识图谱约定补：`services/graph/llm_score.py`（有界 LLM 打分 `MAX_LLM_PAIRS=40`、`ai/prompts/graph_edge.py` 提示词、强度取 max、失败回退）、`book_relations.from_book_id` 列（`_ensure_columns` 增量迁移）、谱系图按 from_book 画箭头、`graph_sync` 存根补 direction；关联文档升需求 v1.32、使用手册 v1.62、重构 v1.22 |
| v1.37 | 2026-08-03 | 同步 M10 打磨（笔记 PDF 导出 + 性能优化 + 验收测试） | §3.4 新增笔记导出格式约定（`GET /api/books/{id}/notes/export?fmt=md|pdf`，`services/note_export.py`：Markdown 结构化导出 + PyMuPDF 内嵌 CJK 生成 A4 PDF，LaTeX 源码保留）；§4.5/§3.5 补性能约定（页图目标宽度 `parsers.pdf._target_width_cached` 进程内缓存、页图 `Cache-Control: private, max-age=600`、前端相邻页预加载 `.page-preload`、vite vendor 分包 vue/element-plus/echarts/katex+markdown-it）；§8.2 登记 M10 收敛重构（笔记导出逻辑从路由抽出为服务）；验收测试：conftest 隔离真实 .env 的 AI/视觉配置（后台视觉预提取与测试竞争、未配置断言、思考模式默认参数 9 项环境性失败修复），后端 128 通过、ruff 全绿、vue-tsc + vite build 通过；关联文档升需求 v1.31、使用手册 v1.61、重构 v1.21 |
| v1.36 | 2026-08-02 | 同步书架 UI 打磨（拖拽换位/标签/搜索） | §5 数据层补 `Book.position` 排序位约定（导入自动追加末尾、列表按 position 排序、`POST /api/books/reorder` 批量重排、`_ensure_columns` ALTER 迁移）；`GET /api/books` 支持 `?q=` 搜索；关联文档升需求 v1.30、使用手册 v1.60、重构 v1.20 |
| v1.35 | 2026-08-02 | 同步 M9 第五轮（相关度阈值函数，决策 20 落地） | §5 画像约定补 `services/relatedness.py` 相关度函数（谱系主信号 + 暖/冷画像覆盖率辅助 + 同簇直判）、行为样本学习（`record_relatedness_sample` → related_samples，learn_thresholds 取行为样本/确认边低分位 min 学习 related_strength）；关联文档升需求 v1.29、使用手册 v1.59、重构 v1.19 |
| v1.34 | 2026-08-02 | 同步 M8 第 12 轮（导入增量更新 + 跨书知识检索） | §5 知识图谱约定补：`cross_book.incremental_cross_book_graph`（`upload_book` 导入成功后触发，只补新书边、幂等、复用 `_pair_score`、补本地 RAG 联动存根）与跨书知识检索端点 `GET /api/graph/knowledge/{kp_id}/appears-in`（`knowledge_appears_in`：其他书知识点关键词重叠 + RAG key_points 命中，来源书不计）；关联文档升需求 v1.28、使用手册 v1.58、重构 v1.18 |
| v1.33 | 2026-08-02 | 同步图谱联动 RAG/Skill 沉淀与画像阈值自动学习 | §5 画像阈值学习约定（`settings` 表 `profile.*` 键、`profile_learning` 服务与学习规则、阈值 API）；§6 联动一致性补 `services/graph_sync.py` 落地（本地存根 linked_books/domain_terms、`/api/graph/sync` LLM 增量增改、`rag_link` 提示词、幂等不 bump 版本）；关联文档升需求 v1.27、使用手册 v1.57、重构 v1.17 |
| v1.32 | 2026-08-02 | 登记耦合解耦重构（图谱包拆分 + AI 上下文拆分） | §2/§3 同步新结构：`services/graph_service.py`（889 行）拆为 `services/graph/` 包（keywords/lexicon/corpus/clustering/cross_book/intra_book/rebuild，`__init__` 保持公共 API 兼容）；`ai_context.py` 拆为 `ai/factory.py`（客户端工厂）、`services/citations.py`（引用解析）、`repositories/assets.py`（`read_asset_content` + `retrieve_rag_chunks` + `load_skills`）、`repositories/chat.py`（会话历史，chat 路由同步改用仓库消除直接 SQL）；新增 `repositories/books.book_tags` 消除 tag 清洗重复；后端 97 项 pytest + ruff 全绿；前端 ReaderView 813→741 行（涂鸦抽 `useReaderDoodle`），vue-tsc + vite build 通过；关联文档升需求 v1.26、使用手册 v1.56、重构 v1.16 |
| v1.31 | 2026-08-02 | 同步专业术语词库 | §5 补词库约定：`domain_terms_file`（默认 `backend/domain_terms.txt`，与 .env 同级）用户可编辑；用户区最高优先级，系统缓存区自动追加学习术语；命名优先级：用户词库命中 → 自动候选（含缓存词库，覆盖书数优先）；`load_domain_lexicon`/`cache_domain_term`/`_lexicon_hits` 已实现；关联文档升需求 v1.26、使用手册 v1.55 |
| v1.30 | 2026-08-02 | 同步领域专业术语命名 | §5 补领域命名约定：`_GENERIC_DOMAIN_TERMS` 泛化词过滤、`_domain_candidates` 加权候选（章节×3+书名×2+正文×1+后验×5）、`_pick_domain_name` 覆盖书数优先→词频次之；post-classify 自动命名/簇重命名/领域自动聚类统一使用（重命名跳过泛化词）；关联文档升需求 v1.25、使用手册 v1.54 |
| v1.29 | 2026-08-02 | 同步自动总结领域清洗 | §5 聚类约定补：LLM 自动总结的 skill `domains`（tags）保存前清洗；领域自动聚类的领域名不再带 `#书id` 标点后缀（重名关键词书并入同一簇）；关联文档升需求 v1.24、使用手册 v1.53 |
| v1.28 | 2026-08-02 | 同步聚类 tag 清洗 | §5 聚类约定补 `sanitize_cluster_name`（只保留汉字/英文字母/数字与单词间空格，去特殊标点、全角字母/数字转半角；保存 tag、post_classify/assign_clusters/merge_and_rename_clusters 写入簇名前统一清洗）；关联文档升需求 v1.23、使用手册 v1.52 |
| v1.27 | 2026-08-02 | 同步 M9 第三轮（增量增改/簇合并/阅读建议）实现 | §5 两阶段分类补簇合并与重命名已实现（`merge_and_rename_clusters`：`T_MERGE=5` 并集重叠合并 + 众数重命名，仅 post 书，归档成功后触发）；§6 资产生成时机补增量增改约定（增量提示词 + `_collect_new_material` 素材 + 原条目 id 不变）；关联文档升需求 v1.22、使用手册 v1.51 |
| v1.26 | 2026-08-02 | 同步 M9 第二轮（三层画像）实现 | §5 数据层补三层画像约定（UserProfile 结构、归档迁移阈值、相关领域判定初值、服务与 API 位置）；关联文档升需求 v1.21、使用手册 v1.50 |
| v1.25 | 2026-08-02 | 同步 M9 第一轮实现 | §6 资产生成时机标注 M9 已实现（`archive_book_task`：PDF 视觉通读补齐页缓存 → 页缓存全文总结 → 标记读完，`generate_rag_skill` 支持 `page_texts`）；§5 两阶段分类标注基础版已实现（`T_POST=3`、落盘字段、`assign_clusters` 懒校验采用 post）；关联文档升需求 v1.20、使用手册 v1.49 |
| v1.24 | 2026-08-02 | 同步需求补登记决策 27 | 需求文档 v1.19 补登记 9.1 决策 27（文本思考模式与精细参数），本规范 §6「需求 9.1 决策 27」引用现已有出处；需求 9.2 开放项补「responses 模式 thinking 参数待定稿」，与本规范 §6 responses 模式备注一致；关联文档升需求 v1.19、使用手册 v1.48 |
| v1.23 | 2026-08-02 | 固化「读完归档 → 视觉通读全文 → 文本模型总结 RAG/Skill」 | §6 资产生成时机补链路：PDF 书归档时先经多模态视觉通读全书（`vision_extract` 全书提取、补全缺失页缓存）再以缓存全文由文本模型总结；§6 多模态触发时机补「读完归档时全书批量提取」（命中页不重复调用）；关联文档升需求 v1.18、使用手册 v1.47 |
| v1.22 | 2026-08-02 | 固化两阶段聚类（pre/post-classify） | §5 知识图谱约定补两阶段分类：pre-classify 实时粗略聚类，RAG 资产就绪后 post-classify 精细聚类（校验/迁移/合并/重命名、硬约束、失效重算与落盘字段）；关联文档升需求 v1.17、使用手册 v1.46 |
| v1.21 | 2026-08-02 | 同步 M8 知识图谱 | §5 数据层补知识图谱约定（BookRelation/KnowledgePoint/KpRelation、关联强度=共现余弦+笔记加权、聚类规则、懒构建与重建、人工反馈回写、ECharts graph 渲染）；关联文档升需求 v1.16、使用手册 v1.44、重构 v1.15 |
| v1.20 | 2026-08-02 | 多模态读图对齐 SiliconFlow 多模态文档 | §6 接口对齐补请求格式：多模态统一 `POST /chat/completions` + `messages[].content` parts（文本+图片）；图片部件 `image_url` 参数 `url`（支持 base64 data URI）+ `detail`（auto/low/high），页提取与对话页图/划线裁剪图附件统一 `detail="high"`；关联文档升使用手册 v1.42 |
| v1.19 | 2026-08-02 | 文本/多模态模型精细参数 | §6 补 DeepSeek 思考模式（`thinking/reasoning_effort` 仅 chat 模式，responses 仅 `max_output_tokens`）与 SiliconFlow 精细参数约定（`temperature/top_p/...` 通用、`enable_thinking/thinking_budget` 仅推理模型）；§7 环境变量清单更新；关联文档升使用手册 v1.41 |
| v1.18 | 2026-08-02 | 配置迁移到 backend/.env | §3.4 补配置优先级（设置页 DB 覆盖 > `.env`）；正式库 AI/视觉参数已迁移进 `backend/.env` 并清除 DB 覆盖，`.env` 为唯一配置源；关联文档升使用手册 v1.40 |
| v1.17 | 2026-08-02 | 多模态接口对齐 SiliconFlow 文档 | §6 视觉约定补：客户端强制 chat 模式（SiliconFlow 仅支持 `/chat/completions`）、`max_tokens`（`vision_max_tokens` 默认 4096）全链路、页图 `image_url.detail="high"`、`.env.example` 默认 SiliconFlow 端点与模型；关联文档升需求 v1.15、使用手册 v1.39 |
| v1.16 | 2026-08-02 | 同步 M7 实现 | §6 多模态视觉约定更新为实际实现（`services/vision_extract.py` + `LLMClient` + `vision_client_kwargs/vision_configured`，本地抽取文本 `local_text/`）；PDF 解析统一按页（`parse_pdf` 全部返回按页章节）；新增页缓存 API 与阅读页入口；关联文档升为需求 v1.15 |
| v1.15 | 2026-08-02 | 登记 AI 上下文解耦重构 | 新增 `services/ai_context.py` 收敛 LLM 客户端构建、页图附件、段落编号、引用解析、RAG 检索、Skill 读取、上下文组装与会话落库；chat/mindmap/rag 服务改为共享该模块（消除 mindmap→chat 横向耦合与两处上下文拼装重复）；页图路径统一走 `media_service.page_image_path`；rag 总结改用运行时配置客户端；关联文档升为需求 v1.14、重构 v1.13 |
| v1.14 | 2026-08-02 | 同步 M6 修复轮 | §4.7 涂鸦补坐标格式兼容（`[x,y]`/`{x,y}` 统一，前端读取时迁移）与画板尺寸同步约定（以页图渲染尺寸为基准、仅尺寸变化时重置缓冲区，避免清空画布）；涂鸦工具栏位于阅读页顶部工具栏（第 X 页 与 第 X 章/共 XX 章 之间），仅 PDF 按页阅读显示；关联文档升为需求 v1.14 |
| v1.13 | 2026-08-02 | 同步 M6 实现 | §4.7 涂鸦、§5 数据层（`Bookmark` 表 / `annotations/`）与 ADR-008 已落地；chat 扩展 `crop_image/crop_label`；关联文档升为需求 v1.13 |
| v1.12 | 2026-08-02 | 同步涂鸦划线批注与提问 | 4.7 涂鸦补划线选中后批注（`note`+`note_meta`）与区域提问（走 §6 页缓存/多模态链路，出处「第 X 页」）；数据层与 ADR-008 同步决策 26；关联文档升为需求 v1.12 |
| v1.11 | 2026-08-02 | 同步 M6 起里程碑重新规划 | 需求 §7 重排为 M6 阅读体验补全 / M7 PDF 统一处理与多模态提取 / M8 知识图谱 / M9 个性化与沉淀 / M10 打磨；多模态视觉提取（技术栈 §6）归入 M7，涂鸦/书签（§4.7/§5）归入 M6；关联文档升为需求 v1.11 |
| v1.10 | 2026-08-02 | 同步书签组织与涂鸦功能定稿 | 4.7 涂鸦补撤销（最多 5 步，会话内）/擦除/文本工具与 `stroke`+`text` 元素持久化；数据层 `Bookmark` 表补 `group_name` 分组字段、列表按时间倒序；ADR-008 同步决策 24/25；关联文档升为需求 v1.10 |
| v1.9 | 2026-08-02 | 同步 PDF 统一按页处理 | 4.7/5 涂鸦范围改「PDF 按页阅读」；6 多模态视觉约定扩展为全部 PDF（含文本型，避免数学乱码）、触发时机同步决策 22；ADR-007/008 标题同步为「PDF 页图」；关联文档升为需求 v1.9 |
| v1.8 | 2026-08-02 | 同步书签/涂鸦与提取触发细化 | 前端新增 4.7 扫描件页图涂鸦（Canvas 画板 + `annotations/` JSON 持久化）；数据层补 `Bookmark` 表与涂鸦文件约定；多模态触发时机改为「当前页提问触发 + [P-1,P,P+1] 滑动窗口增量缓存」（决策 22）；ADR-008；关联文档升为需求 v1.8 |
| v1.7 | 2026-08-02 | 同步开放项定稿 | RAG/Skill 版本约定补「保留≥3 版本、整条目回滚」（决策 19）；Skill 调用明确自动按任务匹配（决策 18）；多模态视觉约定补独立配置无需额度管理（决策 23）与触发时机（决策 22）；关联文档升为需求 v1.7 |
| v1.6 | 2026-08-02 | 新增多模态视觉提取与缓存规范 | AI 接入规范补充扫描版页图多模态提取与页级缓存约定（独立视觉客户端、`page_text/` 页缓存、命中不重复调用、消费/生命周期与隐私约束）；关联文档升为需求 v1.6 |
| v1.5 | 2026-08-02 | 同步需求 v1.4 + 脑图显示文本规范 | 关联文档升为 v1.4；4.6 渲染规范补充脑图节点显示文本清洗（`src/utils/text.ts` `toPlainDisplayText`）与脑图大纲下载/插入批注（笔记正文 Markdown/LaTeX 渲染） |
| v1.4 | 2026-08-01 | 同步需求 v0.9 + 一键启动 | 关联文档升为 v0.9；环境与运行补充 start.bat 一键启动（含 pnpm 兜底链）；对应 M3 已实现（阅读闭环/渲染组件）与需求文档进度对齐 |
| v1.3 | 2026-08-01 | 补充渲染规范 | 前端新增 4.6 Markdown/LaTeX 统一渲染与 XSS 消毒（markdown-it + KaTeX + DOMPurify） |
| v1.2 | 2026-08-01 | 补充 RAG/Skill 资产约定 | AI 接入规范新增 RAG/Skill 存储（BookAsset）、生成/联动更新、检索与技能调用、出处与清理约定 |
| v1.1 | 2026-08-01 | 新增任务收尾规范 | 每轮任务后更新 `docs/使用手册.md`；每实现若干函数执行耦合度审查，耦合过高即解耦重构并登记到重构记录表 |
| v1.0 | 2026-08-01 | 初稿 | 定稿技术选型、项目结构、前后端/数据/AI 规范、ADR |

# 需求文档 · 里程碑与验收（分册）

> 本文档是《需求文档》拆分出的分册（里程碑计划 / 验收标准（原 §7~§8））。
> 总纲（概述 / 目录 / 定位表 / 变更记录）见 [需求文档.md](需求文档.md)。
> 章节标题**沿用原编号**（如 3.1），正文交叉引用可按原编号在本分册或总纲定位表中查找。

## 本分册目录

| 原编号 | 标题 |
| --- | --- |
| 7. 里程碑计划（建议） |
| 8. 验收标准（示例） |

---

## 7. 里程碑计划（建议）

- **M1 需求冻结**：本文档评审通过。**✅ 已完成（v0.8 冻结，v1.x 为增量修订）**。
- **M2 基础框架**：项目脚手架、数据库、书籍导入解析（PDF/MD/TXT/EPUB）、书架与阅读页静态布局。**✅ 已完成**（解析器含 PDF/EPUB/MD/TXT 切章；书架主页、三栏阅读页、设置/资料/画像/谱系图页骨架）。
- **M3 阅读闭环**：章节切换、进度记录、笔记/高亮/「不理解」标记（双入口）、笔记导出。**✅ 已完成（v1.31）**：正文 Markdown/LaTeX 渲染、进度保存与恢复、已读标记、笔记 CRUD 与 **Markdown 导出**已上线并通过冒烟；**PDF 导出随 M10 上线**（`?fmt=pdf`，PyMuPDF 内嵌 CJK，见 M10）；**位置书签（全格式）+ PDF 页图涂鸦标注随 M6 完成**（见 3.3/3.5、9.1 决策 22~25）。
- **M4 AI 接入**：API 配置、对话流式输出、按章节上下文问答、引用出处标注。**✅ 已完成**（设置页 API 配置与测试连接；SSE 流式输出；按章节上下文 + RAG/Skill 注入问答；【第X章 第Y段】引用出处解析与展示；对话历史按书保存/清空；隐私开关生效；**M4 补充：PDF/EPUB 封面渲染、扫描版按页读图、提问附带当前页图片、PDF 统一按页处理（含文本型）**；**PDF 统一按页处理（含文本型）与多模态视觉提取/页级缓存移至 M7**（见 3.2/3.4.11、9.1 决策 2/21~23））。
- **M5 AI 增强**：解读/概论/脑图（图像组件含定理层）/思考逻辑四类生成、划词操作。**✅ 已完成**（四类预设；脑图 ECharts 树组件三层展示大纲/细节/重要定理 + 出处标注 + 节点点击跳转原文 + 复制大纲/下载大纲 .md/导出 PNG/插入为批注；脑图节点名自动清洗 LaTeX/Markdown 记号；划词 AI 操作：解释选中段/该段脑图/加入思考清单；笔记正文 Markdown/LaTeX 渲染）。
- **M6 阅读体验补全**：位置书签（全格式）+ PDF 页图涂鸦。**✅ 已完成**（书签：`bookmarks` 表 + CRUD/分组 API + 阅读页书签抽屉倒序平铺/分组视图/跳转定位；涂鸦：`annotations/page_XXX.json` 读写 API + `PageDoodleCanvas` 画板（笔刷/高亮/橡皮/文本、撤销最多 5 步会话内）+ `DoodleToolbar` + 划线选中批注/划线区域提问（chat 携带 `crop_image`）；随书删除级联清理）。**修复轮（v1.14）**：画板尺寸改为以页图实际渲染尺寸同步且仅在尺寸变化时重置缓冲区（修复旧数据导致 mounted 异常、画布卡 1×1 无法绘制/文本不渲染）；涂鸦点坐标兼容旧版 `{x,y}` 对象与新版 `[x,y]` 数组（读取时自动迁移并回写）；涂鸦工具栏由页底移入阅读页顶部工具栏，置于「第 X 页」与「第 X 章/共 XX 章」之间，仅 PDF 按页阅读时显示；撤销栈切页时清空。
  - 位置书签：任意格式书籍可添加书签并跳转（PDF 整页 / 文本书章节+段落）；书签列表**默认按时间倒序平铺**，支持**分组/文件夹归类**（命名/重命名/删除、拖拽归组）；书签可命名/备注，随书删除。
  - PDF 页图涂鸦：按页阅读页图叠加画板（笔刷/高亮/橡皮/**文本输入**，颜色/线宽/字号可调）；支持**撤销上一步（最多 5 步，会话内）**与**擦除痕迹**；**每条划线可选中后附加批注（Markdown/LaTeX）或发起对划线区域的 AI 提问**（结合页缓存文本/划线区域图片，出处标注「第 X 页」）；标注随页持久化（`annotations/page_XXX.json`）、再次打开叠加显示，随书删除。
  - 验收对应：验收 22、23。
- **M7 PDF 统一处理与多模态视觉提取**：PDF（含文本型）统一按页处理 + 多模态视觉提取与页级缓存。**✅ 已完成（v1.15）**（承接 M4 补充规划，见 3.2/3.4.11、9.1 决策 2/21~23）。
  - PDF 统一按页处理：不再区分扫描版/文本型，统一按原始页切章（`第 N 页`，content 为空）、阅读页直接展示原图（按内嵌原图分辨率渲染）；本地抽取文本落盘 `local_text/page_XXX.txt` 仅作全文检索索引，不用于正文展示与 AI 上下文；封面/页图端点对新旧 PDF 一致生效（旧版文本型 PDF 仍按旧行为展示，可删除后重新导入升级）。
  - 多模态视觉提取：设置页新增独立多模态配置（`vision_base_url / vision_api_key / vision_model / vision_timeout / vision_verify_ssl`，无需额度管理，含「测试视觉连接」）；新增 `services/vision_extract.py` 提取服务与 `page_text/page_XXX.md` 页级缓存；**手动导入批量预提取**（后台任务，受隐私开关约束）；阅读时**当前页对 AI 提问**触发按需提取，`[P-1,P,P+1]` 滑动窗口增量缓存（第 1 页/末页裁剪、仅补缺失页，命中不重复调用）；文本大模型基于缓存解读并标注「第 X 页」出处（对话与脑图链路均已接入，缓存缺失回退当前页原图附件）；阅读页提供「重提本页 / 重建页缓存 / 缓存 x/y 页」入口；随书删除清理。
  - 受「发送书籍内容至模型」隐私开关约束：关闭时不触发提取、不发送缓存文本。
  - 验收对应：验收 1、7、21。
- **M8 知识图谱**：跨书关联计算与书籍级谱系图（含 tag+领域聚类分层）、书籍内部知识图谱（章节级+重要段落+用户标记级）、节点联动阅读区与跨书跳转、图谱缓存与重建、**图谱更新联动 RAG/Skill 沉淀与暖画像写入**。**✅ 已完成（v1.32）**：跨书关联计算（关键词共现余弦分 + 笔记加权 + 同聚类低分「主题相似」边）、聚类分层（tag → 文件夹名 → 领域自动聚类）、书内知识图谱（章节级 / 重要段落 / 用户标记三层，章节顺序「前置依赖」+ 同章重要段落「承接」）、懒构建（`graph_built` 标志）与重建（全局 / 单书）、关联人工反馈（确认 / 忽略 / 修改强度回写）、前端谱系图与书内图谱 ECharts 渲染（聚类筛选、最强关联边高亮、方向箭头、层级筛选、跳转阅读、导出 PNG）已实现。**新书导入自动增量更新（v1.28）**：`upload_book` 导入成功后调用 `incremental_cross_book_graph`（`services/graph/cross_book.py`）只补新书与既有书关联边（同聚类低分「主题相似」边、幂等、不动既有关联），随后补本地 RAG 联动存根；**跨书知识检索（v1.28）**：`GET /api/graph/knowledge/{kp_id}/appears-in` 返回「该知识点还出现在哪些书」（其他书内知识点 title/summary 关键词重叠 + 其他书 RAG `key_points` 命中，来源书不计），前端书内图谱节点点击打开「知识点详情」弹窗（跨书列表 + 跳转对应书图谱 / 阅读原文）。**图谱联动 RAG/Skill 沉淀（v1.27）**：跨书谱系重建/懒构建/关联反馈（确认/修改）时自动补本地 RAG 存根（`linked_books` + `domain_terms`，无 AI 也可执行、内容未变化不写库）；`POST /api/graph/sync` 显式联动对强度 ≥50 且未忽略的关联执行 LLM 增量增改（`ai/prompts/rag_link.py`，RAG 补跨书条目、Skill 融合新方法、version+1、失败回滚不阻塞、随后 post-classify）；谱系图页新增「💾 联动沉淀」按钮，并修复「重建图谱」按钮原先仅刷新未重建的问题。**LLM 打分与方向/原因增强（v1.32，M8 收尾）**：新增 `services/graph/llm_score.py`（提示词 `ai/prompts/graph_edge.py`）——跨书重建与新书导入增量时，对关键词候选对做**有界 LLM 打分**（`MAX_LLM_PAIRS=40`，按关键词分降序截断，控制数百本书的成本），LLM 输出关联强度（0~100）、理论传承方向（`from_book`：A/B/null，配合 承接/发展/批判）、关联类型（理论传承/概念共现/主题相似/应用扩展）与 3~6 条关联原因；强度取 max(关键词分, LLM 分)，方向/类型/原因以 LLM 为准；未配置 AI / 调用失败 / 非法输出一律回退关键词分，不阻塞图谱构建；新增 `book_relations.from_book_id` 列（`_ensure_columns` 增量迁移），谱系图按 `from_book` 显式画箭头并展示「A → B」方向与原因；RAG 联动存根 `linked_books` 补 `direction` 字段；新增 7 项 pytest（后端 135 通过、ruff 全绿、vue-tsc + vite build 通过）。**暖画像写入已实现**（相关领域书入暖记忆，见 M9）。
- **M9 个性化与沉淀**：冷/暖/热三层画像、书籍 RAG 与 Skill 沉淀（读完归档与增量增改）、**归档视觉通读（读完归档时图像模型通读 PDF 全书并缓存，再以缓存全文由文本模型总结 RAG/Skill，见 3.4.9/3.4.11）**、阅读建议、复习提醒。**✅ 已完成（v1.29）**：第一轮——读完归档链路（`POST /api/books/{id}/archive`：PDF 视觉通读补齐页缓存→缓存全文总结 RAG/Skill→标记读完）+ post-classify 基础版（见 v1.20）。第二轮——**三层画像已实现**：`profile_service`（热=当前书细节、暖=近 1~2 本+相关领域、冷=长期偏好）；**归档迁移**（跨 1 本热→暖、跨 3 本暖→冷、>3 本全部沉淀冷，阈值常量 `WARM_TO_COLD_THRESHOLD=3`/`KEEP_RECENT=2`，后续按用户习惯学习）；**相关领域书入暖记忆**（同 post 簇或跨书关联边 strength ≥60 判定，阈值待决策 20 定稿）；**阅读行为实时回写**（阅读进度/章节脉络、笔记/划线/「不理解」→ 热画像）；画像 API（`GET /api/profile`、`POST /api/profile/reset`）与前端画像页（/profile 三层卡片+重置）。第三轮——**资产增量增改**：再次归档时已有 RAG/Skill 资产走增量提示词（`rag_skill.py` `INCREMENTAL_SYSTEM_PROMPT`/`build_incremental_user_prompt`：旧资产概要 + 本轮新增笔记/划线/「不理解」/最近对话 + 全书正文），原资产 `version+1`、id 不变（见 v1.22）；**post-classify 簇合并与重命名**：`merge_and_rename_clusters`（两簇后验关键词并集重叠 ≥ `T_MERGE=5` 合并进书更多的主簇、簇名取簇内出现书最多的后验术语众数、冲突跳过，只处理 `classify_source=post` 的书，归档成功后触发，失败不阻塞）；**阅读建议基础版**：`recommendation_service`（习惯统计 stats / 薄弱概念「不理解」关键词聚合 top3 / 复习提醒「归档 ≥1 天标 due」/ 节奏文案 start·building·stable 三档本地规则），`GET /api/profile/recommendations`，画像页新增「阅读建议」卡片。第四轮——**画像阈值自动学习（v1.27）**：`services/profile_learning.py`（阈值持久化键 `profile.warm_threshold / profile.related_strength / profile.review_days / profile.learning_state`，归档时记录节奏样本 `record_archive`，样本 ≥6 或确认边 ≥3 后 `learn_thresholds` 自动调整：归档间隔中位数 → `review_days`、节奏快慢 → `warm_threshold`、确认关联低分位 → `related_strength`，样本不足回退默认常量 `WARM_TO_COLD_THRESHOLD=3`/`RELATED_STRENGTH_THRESHOLD=60`/`REVIEW_AFTER_DAYS=1`）；`profile_service`/`recommendation_service` 统一走 `get_thresholds(db)`；**图谱更新联动 RAG/Skill 增量增改（v1.27）**：`services/graph_sync.py` 本地存根 + LLM 增量增改（见 M8/3.4.7/3.4.9）；画像 API 补 `GET/PATCH /api/profile/thresholds`、`POST /api/profile/learn`，画像页新增「画像阈值」卡片（查看/保存/立即学习）。**第五轮——相关度阈值函数（决策 20 落地，v1.29）**：新增 `services/relatedness.py`——score = max(跨书谱系关联强度, 0.5×暖画像术语覆盖率×100 + 0.5×冷画像术语覆盖率×100)，同 post 簇直接判定相关（分数抬升到阈值）；`profile_service` 归档入暖记忆改走该函数（`related_books` 条目含 `score`）；归档时记录相关度行为样本（`record_relatedness_sample` → `related_samples`，保留 48 条），`learn_thresholds` 取行为样本 25% 低分位与确认边 25% 低分位之**较小者**学习 `related_strength`（样本 ≥6 触发，裁剪 45~60）；阈值 API `learning` 返回 `related_sample_count/related_samples_min`，画像页展示。**待办已清空**：相关度阈值函数已以自动学习 + 手动覆盖落地，阈值随更多样本继续收敛（样本持续累积）。
- **M10 打磨**：导出能力（含笔记 PDF）、性能优化、验收测试。**✅ 已完成（v1.31）**：
  - **笔记导出 Markdown + PDF**：`GET /api/books/{id}/notes/export?fmt=md|pdf`（默认 md）；新增 `services/note_export.py`（Markdown 结构化导出沿用 M3 格式；PDF 用 PyMuPDF 内嵌 CJK 字体生成 A4，保留章节定位/引文/笔记正文，LaTeX 公式以源码保留）；前端笔记抽屉新增「导出 PDF」入口。对应验收 3/13。
  - **性能优化**：后端页图目标宽度按 (书, 页) 进程内缓存（`parsers/pdf.py` `_target_width_cached`，避免每次翻页重开 PDF）；页图响应 `Cache-Control: private, max-age=600`（页图不可变，10 分钟内翻页走浏览器缓存）；前端 PDF 阅读预加载相邻页（`.page-preload` 隐藏 img 预热浏览器缓存）；vite vendor 分包（vue/element-plus/echarts/katex+markdown-it 独立 chunk，echarts 仅图谱/脑图页按需加载，ReaderView 主 chunk 189KB→56KB）。
  - **验收测试**：新增 `tests/test_export_m10.py`（MD 默认导出/PDF 内容含中文与 LaTeX 源码/格式参数校验）；`tests/conftest.py` 隔离真实 `.env` 的 AI/视觉配置（修复 9 项环境性失败：后台视觉预提取与测试竞争、未配置 API 断言、思考模式默认参数），后端全量 **128 通过**、ruff 全绿、vue-tsc + vite build 通过。

---

## 8. 验收标准（示例）

1. 可导入 PDF、Markdown、TXT、EPUB 书籍并正常解析为章节；PDF/EPUB 提取封面并在书架渲染；**PDF（含文本型）统一按原始页数切章**、阅读页直接展示原图（按内嵌原图分辨率渲染）；不做本地 OCR，可选「多模态 LLM 视觉提取」生成页文本缓存供 AI 解读（见 3.4.11）。
2. 主页左右两栏正确展示近期阅读与书架，可搜索、筛选、设置分类 tag。
3. 阅读页三栏布局符合 3.3 描述，进度记录准确，重新打开回到上次位置；笔记（高亮/批注/思考/不理解）可用，可导出 Markdown 与 PDF 两种格式；「不理解」标记可通过划词菜单与阅读区侧边按钮两个入口触发。
4. AI 助手可对指定章节/选中段落生成解读、概论、脑图、思考逻辑；脑图以图像组件展示大纲、细节与重要定理；结果均可复制/导出并**标注章节段落出处**。
5. 画像按冷/暖/热三层维护：热画像随当前书阅读实时更新，暖画像随读新书切换，三层均可查看/编辑/重置。
6. API 配置可切换，失败时提示友好，流式输出正常。
7. 隐私开关生效：关闭后不向模型发送书籍正文；对 PDF（按页处理）提问时可按设置附带当前页图片（chat 模式，需模型支持视觉输入），关闭隐私开关时不附带页图。
8. 可基于全部投喂书籍生成**书籍级谱系图**：节点为书、边带关联强度与原因，能自动标注「与哪本书关系最密切」；强度计算融合 LLM 打分、关键词共现与**用户笔记加权**，人工反馈生效；**有向关系以箭头显式展示**；新书导入后增量更新。
9. 书籍数量大时，谱系图**先按用户 tag、无 tag 按领域自动聚类**，再分层展开。
10. 点击书籍可查看**内部知识点分布谱系**：覆盖章节级知识点、重要段落、用户笔记/「不理解」段落；点击知识点可联动阅读区定位原文，并可跨书跳转到其他书的相关章节。
11. 图谱支持用户反馈（确认/忽略/修改关联）且反馈生效；图谱可重建与导出。
12. 三层画像按「跨书数」自动迁移（跨越 1 本 → 热转暖、3 本 → 暖转冷、>3 本 → 沉淀冷画像），阈值可由用户习惯学习调整，可在设置页查看与手动调整。
13. EPUB 以保留原排版（图文混排）形式阅读展示。
14. 结束一本书的对话后，可自动生成该书 **RAG 与 Skill 资产**（含出处标注）；再次阅读同一本书并结束后，**在原资产上增改**（版本可追溯），不产生重复条目。
15. RAG 资产可被后续（跨书）问答检索引用；Skill 资产由 AI 在后续相关阅读/任务中**自动按任务匹配调用**。
16. 跨书谱系图构建/更新后，受影响书籍的 **RAG 与 Skill 资产同步增改**（含跨书关联条目，版本可追溯）；阅读与已读领域相关的书籍后，该书关键内容**自动存入暖画像**（相关领域按跨书谱系关联强度判定，决策 20）。
17. 用户输入与 LLM 输出支持 **Markdown**（标题/列表/代码块/表格）与 **LaTeX 公式**渲染（行内与块级）；笔记同样支持，渲染无 XSS 风险。
18. 书架支持**文件夹归类**（新建/重命名/删除、拖拽移动，**支持多级嵌套**），可按文件夹浏览；支持**批量移动、批量设 tag**，书架/阅读页可调整书籍与文件夹位置；同一文件夹内无显式 tag 的书**默认继承文件夹 tag**，并用于谱系图聚类。
19. 提供 start.bat **一键启动**：检查依赖后同时拉起后端与前端并打开浏览器；系统 PATH 无 pnpm 时仍能正常启动。
20. 用户可将外部 Markdown/书籍文件直接投喂（资料页 /rag），AI 将其总结为 RAG 与 Skill 资产（含出处），无需先导入书架或读完；资产可被后续问答检索与技能调用。
21. PDF（含文本型）在配置多模态 LLM 后，**用户在当前页对 AI 提问**时按 `[P-1,P,P+1]` 滑动窗口（第 1 页/末页裁剪）提取页面完整信息并缓存到书籍目录；窗口内已缓存页直接读取、仅补提取缺失页，缓存命中不重复调用多模态 API；文本大模型基于页缓存解读并标注「第 X 页」出处；关闭隐私开关时不触发提取与发送。
22. 任意格式书籍均支持**位置书签**并可从书签跳转：PDF 为整页书签（页号）、文本书为「章节 + 段落位置」；书签可命名/备注；书签列表**默认按时间倒序平铺**，支持**分组/文件夹归类**，随书删除。
23. PDF 按页阅读页支持**画板式涂鸦标注**（笔刷/高亮/橡皮/文本输入，颜色、线宽与字号可选），支持**撤销上一步（最多 5 步）**与**擦除痕迹**；**每条划线可选中后附加批注（Markdown/LaTeX）或发起对划线区域的 AI 提问**（结合页缓存文本/划线区域图片，出处标注「第 X 页」）；标注随页持久化、再次打开叠加显示，随书删除清理。
24. 设置页可切换文本 AI **接口格式**（`responses` / `chat` / `anthropic` 三选一，见 3.4.12/决策 28）；`base_url` 支持**基础地址自动补全**（按格式补全路径）与**完整接口 URL 直接使用**两种写法；请求自动携带浏览器 User-Agent，经 Cloudflare 等前置防护的服务商可正常调用。
25. AI 输出涉及数学符号**只能使用 LaTeX 或 Markdown**（行内 `$...$`、块级 `$$...$$`），**禁止 Unicode 数学字符**（如 `Λ^n V`、`∈`、`ℝ`）与无定界符裸 LaTeX；对话应结合**读者三层画像**调整讲解深度与偏好（隐私开关控制）；「解读/概论/思考逻辑」为**结构化输出**；Skill 资产**按任务相关性**自动匹配调用（见 3.4.10/决策 18、29）。

---

### 8.1 实现状态审查（v1.51，2026-08-05 复核）

> 2026-08-04 全量代码审查：以验收标准与已定稿决策为基线，逐项核对实现。下表为**实现状态清单**（原 8 项缺口经 v1.62 异步/并行与 2026-08-05 C 组全部关闭）。

| # | 功能 / 承诺 | 文档依据 | 现状（代码事实） | 状态 |
| --- | --- | --- | --- | --- |
| 1 | 跨书 RAG 检索引用 + Skill 自动按任务匹配（含跨书） | 验收 15、决策 18 | **已实现（决策 34，v1.63）**：`services/rag_router.py` LLM 自主挑选（独立挑选器配置 + session_id 会话缓存 + 领域分组目录 + 规则降级），`prepare_chat_job` 跨书注入，出处【《书名》第X章 第Y段】；暖画像+谱系关联用于挑选输入与降级排序 | ✅ 已实现（2026-08-04） |
| 2 | 两段式导入（上传秒回 + 后台解析 + 进度） | 决策 32、技术栈 §3.3/3.5 | **已实现（决策 35，v1.62）**：上传分块流式写盘秒回 `task_id`，后台任务解析/页图渲染/视觉预提取带 `progress`/`stage`（导入 40/40/20 权重），任务中心面板轮询 | ✅ 已实现（2026-08-04） |
| 3 | 并发落地（视觉提取 / LLM 打分 / 任务系统） | 决策 33、ADR-009 | **已实现（决策 35，v1.62）**：全局线程池（`TASK_WORKERS`）+ 同类型配额信号量（`TASK_QUOTA_*`，0=不限制），`rebuild_book_caches`/`enrich_pairs_with_llm` 并发化 | ✅ 已实现（2026-08-04） |
| 4 | Markdown 内嵌本地图片（复制到 `images/` + 媒体端点） | 决策 31、技术栈 §3.3 | **已实现（2026-08-05，C 组）**：导入扫描 `![](src)` 复制到该书 `images/` + 白名单媒体端点 + 章节返回重写 + 聊天附件（media_service / import_service / books 路由 / chat_service） | ✅ 已实现（2026-08-05） |
| 5 | EPUB 保留原排版（图文混排）阅读 | 验收 13、决策 15 | **已实现（2026-08-05，方案 A）**：spine 解析 + zip 图片提取重写 + 服务端消毒（`scrub_html`）+ 前端 DOMPurify `v-html` 渲染（epub 分支）；AI/RAG/图谱/搜索消费点统一 `html_to_text` | ✅ 已实现（2026-08-05） |
| 6 | 三层画像可查看/编辑/重置 | 验收 5 | **已实现（2026-08-05，方案 A）**：查看 + 重置 + 阈值调整 + 冷画像编辑（`PATCH /api/profile/cold`：领域偏好/知识水平/语言风格/长期兴趣，热/暖保持自动管理） | ✅ 已实现（2026-08-05） |
| 7 | 任务系统（持久化 / 进度 / 同类型配额） | 决策 32/33 | **已实现（决策 35，v1.62）**：`tasks` 表落库（保留 7 天）+ 全局线程池 + 同类型配额信号量 + `progress`/`stage` 上报（500ms 节流） | ✅ 已实现（2026-08-04） |
| 8 | 整体异步 / 并行 | 决策 33、技术栈 §3.5 | **已实现（决策 35，v1.62）**：页图渲染并发复用已打开文档、测试连接后台化、图谱重建/增量与 post-classify 后台化、长操作全部入任务系统；SQLite WAL 读写分离 | ✅ 已实现（2026-08-04） |

> 其余验收 1~25 已核对为已实现或已登记开放项（DeepSeek responses 思考参数待官方支持；聚类算法路线已定稿——先 demo 验证暂不入正式版，见 需求-决策 §9.2-2）；「用户输入与 LLM 输出支持 Markdown/LaTeX」（验收 17）已满足（输入框提示 + 历史消息按 Markdown/LaTeX 渲染）。
# 需求文档 · 非功能与数据（分册）

> 本文档是《需求文档》拆分出的分册（非功能需求 / 数据模型 / 页面与交互（原 §4~§6））。
> 总纲（概述 / 目录 / 定位表 / 变更记录）见 [需求文档.md](需求文档.md)。
> 章节标题**沿用原编号**（如 3.1），正文交叉引用可按原编号在本分册或总纲定位表中查找。

## 本分册目录

| 原编号 | 标题 |
| --- | --- |
| 4. 非功能需求 |
| 4.1 技术栈（建议，待技术评审） |
| 4.2 API 接入要求 |
| 4.3 数据存储 |
| 4.4 安全与隐私 |
| 4.5 性能与可用性 |
| 4.6 一键启动与外部输入 |
| 5. 数据模型（草案） |
| 6. 页面与交互（概要） |

---

## 4. 非功能需求

### 4.1 技术栈（建议，待技术评审）

- 后端：Python（FastAPI/Flask）+ SQLite（本地存储）。
- 前端：单页应用（Vue 3 或 React），书架/阅读器/图谱组件。
- 文本解析：PDF 用 `pypdf`/`pdfplumber`，Markdown 用原生解析，EPUB 用 `ebooklib`。
- 图谱与脑图可视化：**ECharts**（常用、成熟、中文文档全，关系图/树图能力足够）；如复杂交互超出其能力，可升级为 AntV G6。
- AI 接入：OpenAI 兼容 API（可配置 base_url、api_key、model）。
- 部署形态：**仅本地单用户**——本地运行（如 `127.0.0.1:8321`），无账号体系、无局域网共享；数据不出本机（除模型 API 调用外）。

### 4.2 API 接入要求

- **可配置**：`base_url`、`api_key`、`model`、`temperature` 等参数在设置页可改，支持多厂商（OpenAI、DeepSeek、通义等兼容接口）。
- **上下文管理**：长文需做分块/摘要策略（如整本书先建索引，回答按章节取上下文）；图谱构建按批次处理，控制 token 成本。
- **失败处理**：API 不可用/限流时给出明确提示，支持重试与降级（本地规则式摘要/关键词共现兜底）。
- **流式输出**：AI 回答建议流式展示，提升体验。
- **异步任务**：书籍解析、图谱构建、画像更新等耗时任务后台异步执行，前端展示进度。
- **多模态视觉入口（PDF，统一含文本型）**：PDF 的页面信息由独立配置的多模态 LLM 逐页提取为文本并**按页缓存到书籍目录**；缓存命中不重复调用；文本大模型基于缓存解读（见 3.4.11）。

### 4.3 数据存储

- 书籍文件：本地目录存储（保留原文件）。
- 元数据/进度/笔记/画像/图谱：SQLite。
- 解析缓存：章节文本、AI 生成结果、图谱计算结果可落盘缓存，避免重复解析/重复付费。
- 扫描页文本缓存：`data/books/<书目录>/page_text/`，按页 Markdown 缓存多模态提取结果，随书删除清理（见 3.4.11）。
- Markdown 内嵌图片：`data/books/<书目录>/images/`，导入时从原位置复制（相对 / 绝对 / `file://` 均支持），章节引用改写为该相对路径；随书删除清理（见 3.2/3.4.10、9.1 决策 31）。
- 图谱重建：提供「重建全部图谱」「重建单书图谱」操作。

### 4.4 安全与隐私

- API Key 本地保存，不写进代码仓库，不在前端明文回显。
- 书籍与笔记均为用户私有数据；AI 请求仅发送必要上下文（当前章节/选中段落/图谱抽取所需片段），并提供「是否将书籍内容发送至模型」的开关。
- 提供「清除全部数据」「删除单书（连同 AI 记录与图谱节点）」能力。

### 4.5 性能与可用性

- 千页级 PDF 打开与章节切换应在秒级内完成（解析异步+缓存）。
- **整体并发约定（v1.47，见 9.1 决策 33）**：多模态全书提取（归档 / 手动提取 / 重建）与跨书 LLM 打分（`MAX_LLM_PAIRS` 候选对）由**串行改为并发**（4~8 并发线程，`.env` 可配 `VISION_CONCURRENCY` / `AI_CONCURRENCY`，配合限流退避重试）；失败页/失败对不中断、回退既有策略（页缓存缺失回退原图、打分回退关键词分）；后台任务全局线程池 + 同类型并发配额，防线程失控与 API 额度争抢。
- **导入性能（v1.46，见 9.1 决策 32）**：文件**流式写盘**（分块 `copyfileobj`，不整读内存）；PDF 页图**后台并发渲染**（4~8 线程）+ 阅读时缺失页**按需渲染回退** + 渲染宽度上限 1600px；书架导入不自动触发多模态全书预提取（改为手动，避免导入被 API 耗时与额度拖慢）。
- 阅读滚动流畅，长章节可分段懒加载。
- 书架加载百本书无卡顿。
- 图谱渲染：百节点级图谱交互流畅；千节点级采用「领域聚类 + 分层展开」（3.6.1），避免一次性渲染全图。
- 图谱构建、画像更新为后台任务，不阻塞阅读操作。

### 4.6 一键启动与外部输入

- 提供根目录 start.bat **一键启动**：自动检查依赖（Node / 后端 venv / 前端 node_modules）→ 启动后端（8321）与前端（5173）→ 自动打开浏览器；前端启动命令按 pnpm → node node_modules\vite\bin\vite.js → 本地 pnpm → 运行时 pnpm 顺序解析，系统 PATH 无 pnpm 时也能正常启动。


---

## 5. 数据模型（草案）

```
Folder        id, name, parent_id(可空，支持嵌套), created_at
Book          id, title, author, cover, format(pdf/md/txt/epub), file_path, status(未读/在读/读完),
              progress, total_chapters, created_at, last_opened_at, graph_built(图谱状态),
              tags_json(用户分类 tag 列表), folder_id(可空，所属文件夹)
Chapter       id, book_id, index, title, content_path/content_text, word_count, read_flag
ReadingLog    id, book_id, chapter_id, position, updated_at
Note          id, book_id, chapter_id, quote_text, note_text, type(高亮/批注/思考/不理解), created_at
Bookmark      id, book_id, chapter_id(可空，文本书定位), page_index(可空，PDF 整页), para_pos(可空，段落位置),
              title, note, created_at
ChatMessage   id, session_id, role(user/assistant), content, ref_book_id, ref_chapter_id,
              ref_para_pos(出处标注), created_at
UserProfile   id, layer(cold/warm/hot), dimension, value_json, updated_at   # 三层画像
Setting       key, value                                    # api 配置、阅读偏好、画像迁移阈值（可学习）等
BookAsset     id, book_id, kind(rag/skill), content_json, version, created_at, updated_at  # 读完归档资产；跨书图谱更新时联动增改

# —— 图谱相关 ——
BookRelation    id, book_a_id, book_b_id, strength(0~100), direction(无/承接/发展/批判),
                relation_type(主题相似/概念共现/理论传承), reasons_json(关联原因列表),
                user_feedback(确认/忽略/修改), updated_at
KnowledgePoint  id, book_id, chapter_id, para_pos(段落位置), title, summary, importance,
                level(章节级/重要段落/用户标记), aliases_json, created_at
KpRelation      id, from_kp_id, to_kp_id, relation_type(前置依赖/总分/承接/并列), strength, note
```

页图涂鸦标注（PDF 按页阅读）：`data/books/<书目录>/annotations/page_XXX.json`（元素 JSON：`stroke` 笔画——工具/颜色/线宽/坐标序列，可选 `note` 批注文本与 `note_meta`（创建/更新时间、Markdown/LaTeX）；`text` 文本——内容/颜色/字号/位置），随书删除清理；**撤销仅在会话内生效（最多 5 步），不持久化历史版本**；划线批注随元素持久化。

---

## 6. 页面与交互（概要）

| 页面 | 布局 | 关键交互 |
| --- | --- | --- |
| 主页（书架） | 左：近期阅读；右：书架 | 搜索、筛选、文件夹归类、设置 tag、导入、进入阅读 |
| 主页（谱系图） | 全屏关系图 | 领域聚类/分层展开、缩放拖拽、点节点进书、点边看关联详情 |
| 书籍内部图谱 | 知识图谱 + 知识点分布条 | 点知识点联动阅读区、跨书跳转、导出 |
| 阅读页 | 左：竖向书架；中：阅读区；右：AI 助手 | 章节切换、划词操作、笔记/不理解标记、进度记录、AI 生成、图谱入口 |
| 设置页 | 单栏表单 | API 配置（文本 / 多模态）、画像查看/重置、图谱重建、数据管理 |
| 画像页 | 三层卡片 | 查看/编辑冷·暖·热三层画像，手动调整 |

---



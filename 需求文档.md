# 读书阅读助手 —— 需求文档（v1.73）

> 状态：**开发中**（需求已冻结）
> 更新日期：2026-08-05
> 说明：本文档用于锁定产品需求与功能边界，后续开发、UI 设计与测试均以本文档为准。原待确认问题已评审确认（见 9.1），开放项登记见 9.2（当前 1 条待定稿：DeepSeek responses 模式思考参数；决策 36/37 已定稿并实现——附件统一经视觉模型提取为文本、主页全局 AI 对话；聚类算法路线已定稿——先 demo 验证暂不入正式版，见 9.2）。
>
> **当前开发进度**（详见 §7 里程碑）：
> - ✅ M1 需求冻结；✅ M2 基础框架（含书架/阅读页静态布局与解析）；✅ M3 阅读闭环（Markdown / PDF 导出已上线，见 M10；位置书签 + 页图涂鸦移至 M6 后已完成）
> - ✅ M4 AI 接入（设置页配置 + 对话流式 + 章节上下文问答 + 引用出处标注；补充：PDF 封面 / 扫描版按页阅读 / 页图附件 / 页图高清原图渲染 + 阅读缩放；PDF 统一按页处理含文本型与多模态视觉提取移至 M7）；✅ M5 AI 增强（脑图图像组件 + 划词 AI 操作 + 脑图节点清洗/下载大纲/插入批注）→ ✅ M6 阅读体验补全（书签全格式+分组、页图涂鸦含撤销/擦除/文本/划线批注/划线提问）→ ✅ M7 PDF 统一处理与多模态提取 → ✅ M8 知识图谱（跨书谱系 + 聚类分层 + 书内知识图谱 + 重建/人工反馈 + LLM 打分与理论传承方向/关联原因 + 图谱联动 RAG/Skill 沉淀 + 新书导入增量更新 + 跨书知识检索，见 §7）→ ✅ M9 个性化与沉淀（三层画像 + 归档迁移 + 阈值自动学习 + 相关度阈值函数已实现、待办已清空，见 §7）→ ✅ M10 打磨（笔记 PDF 导出 + 性能优化 + 验收测试，见 §7）
> - ✅ M10+ 打磨轮（v1.39）：AI 多接口格式（`responses | chat | anthropic` 三选一 + base_url 两种写法，见 3.4.12/决策 28）、文本模型精细参数（thinking/reasoning_effort 等）、LLM 请求浏览器 UA（修复 Cloudflare 403）、资产链路实测（上传→总结→单条删除→去重合并→移除资产，见 v1.38 变更记录）；前后端文本模型 API 实测通过（SSE 流式 + 引用标注正常）
> - ✅ M10+ 打磨轮（v1.40）：RAG/Skill 资产文本统一 LaTeX/Markdown 渲染（复用 MdRender）；资料页改为**两级结构**——资产列表页仅显示「有无 + 简略摘要 + 详情入口」，下级 `/rag/{bookId}` 完整内容页的 RAG 摘要 / 关键知识点 / 知识分块 / Skill 技能可**折叠展开**；上传总结完成后在列表页**上侧**以**固定大小**卡片展示该次提交摘要（无滚动条），见 3.4.9
> - ✅ M10+ 打磨轮（v1.41）：Prompt 能力补全——AI 对话注入**三层画像**（隐私开关控制：关闭正文发送时不注入，见 3.4.1）、「解读/概论/思考逻辑」预设改为**后端结构化 system 模板**（前端预设按钮携带 `mode` 发送）、Skill 资产**按任务相关性排序截断注入**（见 3.4.10/决策 18）、全部 LLM 提示词统一「数学只能用 LaTeX/Markdown、禁止 Unicode」硬性规则（见 3.4.10/决策 29）
> - ✅ M10+ 打磨轮（v1.45）：Markdown 内嵌本地图片处理方案定稿——导入时复制本地图片到书籍目录 `images/` 并改写相对路径、后端媒体接口、前端渲染与 AI 附件联动（见 3.2/3.4.10/4.3、9.1 决策 31）
> - ✅ M10+ 打磨轮（v1.46）：导入性能与进度方案定稿——两段式导入（上传秒回 + 后台任务带 progress/stage）、PDF 页图并发渲染 + 缺失页按需回退、书架导入不再自动全书预提取（改为手动触发）、前端上传真实进度 / 批量队列 / 导入任务卡片（见 3.2/3.4.11/4.5、9.1 决策 32）
> - ✅ M10+ 打磨轮（v1.47）：整体流畅度方案定稿——多模态全书提取与跨书 LLM 打分并发化（4~8 并发、`.env` 可配）、任务系统升级（全局线程池 + progress/stage + 同类型并发配额）、前端请求合并、测试连接异步化（见 3.4.11/4.5、9.1 决策 33）
> - ✅ 异步/并行改造已实施（v1.60，决策 35 落地）：任务系统落库（tasks 表 7 天保留 + 重启中断标记）、导入/图谱/测试连接后台化（两段式导入 + building/task_id + 全局任务中心 1s 轮询）、页图渲染/视觉全书提取/跨书 LLM 打分并发化、进度权重（导入 40/40/20、归档 60/40、重建 20/50/30），见 docs/异步并行改造计划.md
> - ✅ 性能优化第一梯队 + 前端并行收尾已实施（v1.61）：SQLite PRAGMA 调优（synchronous=NORMAL / cache_size=20MB / mmap=256MB）、外键与热点查询索引幂等补齐（models index=True + `_ensure_indexes`，含组合索引）、关键词内容寻址缓存（聚类/相关度/推荐/画像共享）、聚类结果落盘缓存（群体签名失效，打开谱系图不再全量重算）、上传分块流式写盘（1MB 分块 + 增量 sha256）；前端 ProfileView/RagDetailView 串行请求改并行（需求-决策 §9.3.2 #10/#11）；后端 181 pytest + ruff 全绿，vue-tsc + vite build 通过，见 docs/性能优化路径.md
> - ✅ 决策 34 已实现（v1.63）：LLM 自主挑选 RAG/Skill 注入（验收 15 / 里程碑 §8.1-1）——`services/rag_router.py` 跨书知识路由（独立挑选器模型配置 `RAG_SELECT_*`、session_id 会话内缓存、领域分组折叠候选目录、规则降级）、`prepare_chat_job` 跨书注入（出处【《书名》第X章 第Y段】）、页模式同样注入跨书 chunks、隐私关闭仅注入 Skill；§9.3.1 四项实现细节已敲定，见 需求-决策 9.1 决策 34
> - ✅ UI 打磨轮（v1.64）：真实 API 冒烟测试通过——决策 34 挑选链路端到端验证（候选目录按领域分组折叠、跨书 chunks 注入带【《书名》第X章 第Y段】出处、同 session 同章节二次调用命中会话缓存 `source=cache`），SSE 流式与引用标注正常；书架页 / 资产页 UI 重新设计（theme.css 明暗两套设计变量、书架状态徽标与统计 chips、资产行卡片与折叠面板图标），vue-tsc + vite build 通过
> - ✅ UI 打磨轮（v1.65）：资产页「外部资料 → RAG/Skill」摘要窗口化——最近提交总结由固定高度截断改为**固定大小窗口完整展示全部细节**（RAG 摘要全文 / 关键知识点 / Skill 技能 / 知识分块全文，均无内部滚动条），头部新增 RAG/Skill 版本徽标与「✕ 关闭」，见 3.4.9
> - ✅ M10+ 打磨轮（v1.67）：审查 C 组实施——EPUB 方案 A（保留图文混排，验收 13 达成：spine 解析 + 图片提取重写 + 服务端消毒 + 前端 DOMPurify 渲染 + 文本消费点 html_to_text）、画像编辑方案 A（仅冷画像可编辑）、决策 31 Markdown 内嵌图片落地；rag_router 会话缓存上限 200、聚类 GET 只读、chat 占位符泄漏断言；见 需求-决策 §9.3 第 3/4/5 行
> - 已提前实现：RAG/Skill 资产总结与展示页（资料页 /rag，需求 3.4.9 外部资料直投）、一键启动（start.bat，需求 4.6）

---


## 目录

- [1. 项目概述](#1-项目概述)
- [2. 目标用户与核心场景](#2-目标用户与核心场景)
- [3. 分册体系（v1.49 拆分说明）](#3-分册体系v149-拆分说明)
  - [3.1 快速定位表](#31-快速定位表)
- [4. 需求变更记录](#4-需求变更记录)

---

## 1. 项目概述
## 1. 项目概述

构建一个本地优先的「读书阅读助手」桌面 Web 应用，核心目标：

1. **多书管理**：以书架形式管理用户导入的书籍（支持 PDF、Markdown、EPUB 等），记录阅读进度。
2. **沉浸式阅读**：打开书籍后提供三栏式阅读工作台——左侧竖向书架、中间阅读区、右侧 AI 助手。
3. **AI 阅读增强**：通过接入外部大模型 API（可配置），基于用户已投喂的书籍构建**三层个性化画像**，并针对任意指定章节/段落生成**解读、概论、脑图、思考逻辑**，帮助用户提升阅读能力。
4. **跨书串联（谱系化）**：LLM 将用户已投喂的书籍互相串联，生成**书籍级谱系图**；点击某本书后，进一步展示该书**内部知识点的分布谱系与串联关系**，边标注与哪本书关系最密切。
5. **书籍沉淀（RAG + Skill）**：每本**结束对话**的书籍，由 LLM 总结为可复用的 **RAG（检索增强知识）** 与 **Skill（技能能力）** 资产；若再次阅读同一本书并结束对话，则**直接在原书的 RAG 与 Skill 资产内增改**，不重复创建。

**核心价值主张**：AI 不是简单问答，而是「读过你的书、懂你的水平、按你的习惯讲」，并且能帮你把读过的书连成一张知识网络。

---

## 2. 目标用户与核心场景

## 2. 目标用户与核心场景

| 场景 | 用户诉求 |
| --- | --- |
| 知识型读者 | 想高效理解一本难书，需要逐章解读与逻辑梳理 |
| 主题研究者 | 需要跨书建立知识关联，快速生成脑图与概论 |
| 备考/输出者 | 需要把书中知识转化为可复述、可提问的思考逻辑 |
| 系统学习者 | 想看清自己读过的一批书之间的渊源与脉络，建立体系化认知 |

---


---

## 3. 分册体系（v1.49 拆分说明）

v1.49 将原单文件需求文档按功能域拆分为 4 个分册 + 本总纲。**分册章节沿用原编号**（如 3.1、9.1），正文交叉引用（「见 3.4.x」「见 9.1 决策 N」）可按原编号在对应分册定位；总纲只维护概述、分册索引与变更记录。

| 分册 | 内容域 | 原章节 |
| --- | --- | --- |
| [需求-功能需求.md](需求-功能需求.md) | 书架 / 导入解析 / 阅读视图 / AI 助手能力 / 数据记录 / 跨书谱系与书内知识图谱 | 3.1~3.6（含 3.4.1~3.4.13、3.6.1~3.6.2） |
| [需求-非功能与数据.md](需求-非功能与数据.md) | 非功能需求（技术栈 / API / 存储 / 安全 / 性能 / 启动）/ 数据模型 / 页面交互 | 4.1~4.6、5、6 |
| [需求-里程碑与验收.md](需求-里程碑与验收.md) | 里程碑计划（M1~M10+）/ 验收标准 | 7、8 |
| [需求-决策.md](需求-决策.md) | 已确认决策 1~37 / 开放项 | 9.1、9.2 |

### 3.1 快速定位表

| 想找什么 | 分册 | 原编号 |
| --- | --- | --- |
| 书架 / 近期阅读 / 文件夹 / 排序搜索 | 功能需求 | 3.1 |
| 书籍导入与解析（含 Markdown 图片、两段式导入） | 功能需求 | 3.2 |
| 阅读视图（三栏 / 进度 / 笔记 / 涂鸦 / 书签） | 功能需求 | 3.3 |
| AI 助手（画像/解读/概论/脑图/思考逻辑/跨书串联/引用/RAG/渲染/视觉提取/接口/会话） | 功能需求 | 3.4.1~3.4.13 |
| 数据与记录（进度/笔记/书签/涂鸦/资产/画像/图谱数据约定） | 功能需求 | 3.5 |
| 跨书谱系图与书内知识图谱 | 功能需求 | 3.6.1~3.6.2 |
| 非功能需求（技术栈/API/存储/安全/性能/一键启动） | 非功能与数据 | 4.1~4.6 |
| 数据模型草案与页面交互 | 非功能与数据 | 5、6 |
| 里程碑（M1~M10+） | 里程碑与验收 | 7 |
| 验收标准 | 里程碑与验收 | 8 |
| 已确认决策 1~37 与开放项 | 决策 | 9.1、9.2 |

---

## 4. 需求变更记录

| 版本 | 日期 | 变更内容 | 说明 |
| --- | --- | --- | --- |
| v1.75 | 2026-08-05 | 待办清单建档 + 词库预置 + 聚类 demo 同义词归一化与真实数据实测 | ① 新建 `docs/待办清单.md`：登记 B 组（超长函数 TOP5 / ReaderView 719 行 / PageDoodleCanvas 430 行）、C 组（run_in_session 统一 / 三大视图拆分）与需求开放项，按紧急度排序；② `backend/domain_terms.txt` 用户区预置 122 个常见领域关键词与同义词组规范词（.example 同步说明），正式代码加载验证通过；③ 聚类 demo 增补：`canonical_terms` 同义词归一化（41 组规范词=别名，折叠+长词内部子串×0.3 抑制）、`--input` 实际语料、`demo/export_real_corpus.py` 从真实库导出 11 本 → `real_corpus.json`；④ 真实数据实测结论入册（改进方案 §4.1）：同义词折叠与多字命名生效（变分→变分法），D1 误吸主因是泛化词高 df（IDF+tau 可解），中英混合书需 tag/LLM 修正层；正式版聚类仍零改动；同步 待办清单、知识图谱聚类算法-改进方案 §4.1 |
| v1.74 | 2026-08-05 | 审查 P0+A：降级挑选回归修复 + 代码清理重构 | ① P0 行为缺陷：`rag_router._select_fallback` 谱系关联候选漏 `user_feedback IS NULL` 边（决策 34 落地回归，导致 LLM 挑选降级时几乎拿不到谱系关联书）→ 改走 `repositories/graph.py::list_active_relations`（`book_id` 按强度降序 + `limit`），补回归测试；② A 组：死代码 7 处删除、图谱后台任务包装收敛到 `services/graph/tasks.py`（5 处）、clustering.py 直查改仓储（`list_post_classified_books`）；后端 232 pytest + ruff 全绿；同步 重构规范 v1.41、使用手册 v1.112 |
| v1.73 | 2026-08-05 | 全局 AI 会话历史载入与删除会话 | 主页全局 AI 按 session_id 载入原对话历史：前端 localStorage 持久化会话键（`global_ai_session_id`），展开面板时合并拉取历史（与本地流式消息按角色+内容去重）；新增**删除会话**：后端 `DELETE /api/ai/chat/session`（清空历史 + `rag_router.clear_global_selection_cache` 清除该会话挑选缓存），前端面板「清空」（保留会话）与「删除会话」（重置会话键，重开为全新对话）双按钮，删除前二次确认；后端 `test_global_chat.py` 新增用例；vue-tsc + vite build 通过；同步 需求-功能需求 3.4.14、使用手册 前端分册 |
| v1.72 | 2026-08-05 | 决策 37 定稿并实现：主页全局 AI 对话（阅读之外） + 手写 SVG 渲染增补 | 主页右下角全局 AI 对话浮窗（不绑定书籍/章节）：会话 `global:{session_id}` + 多轮历史；注入冷/暖画像 + 全局 Skill（按任务相关性 top8）+ RAG 跨书（LLM 自主挑选全库目录，降级规则检索，session_id 会话缓存）；隐私关闭仅注入 Skill；SSE + stream_key 滚动落库 + 2s 轮询；后端 `api/routes/ai_chat.py`（POST /api/ai/chat + 消息 GET/DELETE）、`chat_service.prepare_global_job`、`rag_router.select_global_knowledge`、`repositories/assets.load_all_skills`；前端 `GlobalChatPanel.vue` + `useGlobalAi.ts`。手写 SVG：MdRender markdown-it html 透传 + DOMPurify svg profile（决策 31 增补）；同步 需求-决策 9.1（决策 37 + 决策 31 增补）、需求-功能需求 3.4.10/3.4.14 |
| v1.71 | 2026-08-05 | 决策 36 定稿并实现：附件统一走视觉模型提取为文本 | 未缓存的附件（划线区域裁剪图 / Markdown 正文插图）在「发送页图」开启时统一经多模态视觉模型提取为文本（内容寻址缓存 `data/cache/attachment_text/`，命中不重复调用），主模型只收文本——彻底消除纯文本端点 `unknown variant image_url` HTTP 400；PDF 页模式提问不再回退直发页图（纯文本降级）；`chat_service.build_messages` / `mindmap_service` 删除 image_url 组装分支，入口 `vision_extract.extract_image_attachment`；前端无改动；相关 25 项 pytest + 全量 226 pytest + ruff 全绿；同步 需求-决策 9.1（新增决策 36）/9.2（开放项 2→1）、需求-功能需求 3.4.11、使用手册 v1.109 |
| v1.70 | 2026-08-05 | 固化待解决问题：PDF 解读 HTTP 400（image_url 不支持） | 现象：chat 模式下对 PDF 提问/解读，页图附件以 image_url 块发送给不支持视觉输入的模型/端点 → HTTP 400 `unknown variant 'image_url', expected 'text'`；根因：`ai_send_page_image` 无模型视觉能力声明、页缓存不可用/未配置多模态时回退仍走主模型发图、chat 模式原样透传 content 数组；备选方案 A~E 与推荐组合（A 视觉开关 + D 错误友好化）见 需求-决策 §9.2-3，待定稿；同步 需求-决策 9.2（开放项 2 条） |
| v1.69 | 2026-08-05 | 审查 D 组结构性重构实施（无功能改动） | 图谱域建仓 repositories/graph.py + 六个路由瘦身（annotations/reading/chat/settings/graph/books 抽服务层）+ 前端 GraphView 拆分（components/graph/ 面板组件 + utils/graphOption.ts 纯函数）；后端 224 pytest + ruff 全绿、vue-tsc + vite build 通过；同步 重构规范 v1.40、使用手册 v1.108、技术栈 v1.72 |
| v1.68 | 2026-08-05 | 检查收尾：聚类路线定稿（demo 验证）+ 里程碑状态复核 + 文档修复 | ① 聚类算法路线定稿：改进方案 O1~O8 先 demo 验证（demo/clustering_demo.py：现行贪心吸收 vs IDF 余弦+连通分量 vs 加权 LPA + O3 LLM 允许降分融合，示例语料复现 D1 误吸场景），正式版聚类零改动；② 里程碑 §8.1 八项缺口全部回写已实现（v1.62 异步/并行 + C 组）；③ 重构规范 §11 表格分隔行残留修复；④ 需求-决策 §9.2 开放项 2→1、§9.3 第 7 行状态同步；同步 重构规范 v1.39 |
| v1.67 | 2026-08-05 | 审查 C 组实施：EPUB 方案 A + 画像编辑 + 决策 31 落地 | ① EPUB 保留图文混排（验收 13）：parsers/epub.py 按 spine 解析、zip 图片提取到 images/ 并重写引用（复用媒体白名单端点）、服务端消毒（html_util.scrub_html）、content_text 存消毒后 HTML、ReaderView epub_render 分支（DOMPurify v-html 渲染）；LLM/RAG/图谱/搜索文本消费点统一 html_to_text；② 画像编辑方案 A：仅冷画像可编辑（PATCH /api/profile/cold + 前端表单）；③ 决策 31 Markdown 内嵌图片落地（导入复制 / 章节重写 / 聊天附件 / 媒体正则兼容 HTML 引号）；④ 审查 B/C 收尾：rag_router 会话缓存上限 200、聚类 GET 只读（persist=False）、chat 占位符泄漏断言、前端生命周期 dispose/AbortController；后端 223 pytest + ruff 全绿、vue-tsc + vite build 通过；同步 使用手册 v1.107、README |
| v1.66 | 2026-08-04 | 资产页上传区优化（拖拽/点击 + 去滚动条）+ 上传返回结构修复 | RagView 上传卡片改 `.upload-zone`（拖拽/点击选择、虚线边框、hover/已选态）；`.rag-page` 移除页面级滚动条（`min-height:100%`，仅文档级滚动）；`UploadResult` 改平铺类型 `BookItem & { task_id }`，`startIngest` 用 `res.id` 触发总结、HomeView 用 `res.title` 提示；浏览器实测上传区与提交窗口均无内部滚动条；vue-tsc + vite build 通过；同步 使用手册 v1.99、README |
| v1.65 | 2026-08-04 | 资产页「外部资料 → RAG/Skill」总结窗口化 | 最近提交总结由固定高度截断改为**固定大小窗口完整展示全部细节**：RAG 摘要全文 / 关键知识点有序列表 / Skill 技能卡片（名称 + applicable + usage + 领域 chip）/ 知识分块全文，均无内部滚动条；头部新增 RAG/Skill 版本徽标与「✕ 关闭」；vue-tsc + vite build 通过；同步 使用手册 v1.98、README |
| v1.64 | 2026-08-04 | 真实 API 冒烟 + 书架/资产页 UI 重新设计 | 冒烟（决策 34 链路）：候选目录领域分组、跨书 chunks 出处【《书名》第X章 第Y段】、会话缓存 `source=cache`、SSE 流式正常；前端：`theme.css` 新增设计变量（明暗两套）、HomeView 重设计（近期阅读封面缩略图 + 进度条、书架状态徽标 读完/在读/未读 + 统计 chips 共N本·X读完·Y在读、无封面渐变占位首字母+格式、hover 上浮、空状态，交互保留拖拽换位/标签编辑/删除）、RagView 重设计（上传卡片、统计 共N本·M本已总结、绿色 rail 已总结标识、AI 总结/查看完整按钮、提交摘要固定卡片）、RagDetailView（折叠面板图标、sticky 头部、卡片 hover）；vue-tsc + vite build 通过；同步 使用手册 v1.97、README |
| v1.63 | 2026-08-04 | 决策 34 落地：LLM 自主挑选 RAG/Skill（验收 15 / 里程碑 §8.1-1） | `services/rag_router.py` + `ai/prompts/rag_select.py` + `prepare_chat_job` 改挑选版（跨书注入、页模式注入、隐私仅注入 Skill）；§9.3.1 四项细节定稿：① 书级挑选 + 书内规则检索；② session_id 会话内缓存（键=会话+章节，TTL 60 分钟可配）；③ 挑选器独立模型配置（`RAG_SELECT_*` 未填回退主模型，默认 max_tokens=512 / 禁思考 / 低温）；④ 候选目录按领域分组折叠（tag→文件夹→聚类→未分类）+ 冷画像偏好优先；前端阅读页会话 `session_id` 生成与传递；跨书出处【《书名》第X章 第Y段】citations 解析扩展；新增 `tests/test_rag_router.py` 7 项，后端 196 pytest + ruff 全绿、vue-tsc + vite build 通过；同步 需求-决策 v1.63、里程碑-验收 §8.1-1、技术栈 ADR-011、使用手册 |
| v1.62 | 2026-08-04 | 性能优化第二/三梯队 + 任务中心 UI | 决策 1 聊天历史只留最近 N 条（`chat_history_limit` 默认 200，0=不限）；决策 2 LLM 结果缓存（脑图/解读/概论/思考逻辑内容寻址缓存，TTL 30 天 + 容量 300 淘汰最旧，命中回放 `cached=true`）；决策 3 FTS5 全书搜索（trigram 中文子串 + 触发器同步 + `GET /api/books/search`，短词 LIKE 回退）；前端封面懒加载、正文解析缓存（`cachedSplitBlocks` LRU 30）、谱系图渐进渲染（>200 节点）、任务中心 UI/交互重设计；`tests/test_perf_tier2.py` 8 项，后端 189 pytest + ruff 全绿、vue-tsc + vite build 通过；详见 docs/性能优化路径.md §4/§7；使用手册 v1.95 同步 |
| v1.61 | 2026-08-04 | 性能优化第一梯队 + 前端并行收尾实施 | SQLite PRAGMA 调优（`synchronous=NORMAL` / `cache_size=-20000` / `mmap_size=256MB`）；外键/热点查询索引补齐（models `index=True` + `_ensure_indexes` 幂等迁移，含 `ix_book_assets_book_kind`、`ix_bookmarks_book_created` 组合索引）；关键词内容寻址缓存（`keywords.py`，聚类/相关度/推荐/画像共享，`book_keywords` 整书入口）；聚类结果落盘缓存（`clustering.py` `data/cache/graph_clusters.json`，**群体签名**失效）；上传分块流式写盘（路由 1MB 分块 + 增量 sha256 → `import_book_file`）；前端 `ProfileView`（Promise.all）/ `RagDetailView`（Promise.allSettled）并行化（需求-决策 §9.3.2 #10/#11）；新增 `tests/test_perf_tier1.py` 6 项，后端 181 pytest + ruff 全绿，vue-tsc + vite build 通过；详见 docs/性能优化路径.md §4；使用手册 v1.94 同步 |
| v1.60 | 2026-08-04 | 异步/并行改造实施完成（决策 35） | 3 个 commit 分批落地：①任务系统（tasks 表落库 7 天保留、全局线程池、同类型配额信号量 0=不限、progress/stage 500ms 节流、重启 mark_interrupted、SQLite busy timeout 30s）；②后台化（导入两段式秒回 {book,task_id}、图谱懒构建/重建/联动/单书重建 submit 化 + find_active 幂等、测试连接后台化 60s、前端全局任务中心右下角 1s 轮询任务结束即停）；③并发化（render_pdf_pages worker 级 doc 复用、rebuild_book_caches 独立会话并发 + 主线程进度聚合、enrich_pairs_with_llm 按 ai_concurrency 并发打分、进度权重导入 40/40/20 归档 60/40 重建 20/50/30）；后端 175 pytest + ruff 全绿，vue-tsc + vite build 通过，真实环境冒烟（PDF 6 页并发渲染 / md 两段式 / 任务状态透出）；决策 35 状态改已实现 | 需求-决策 §9.1 新增决策 35（任务落库 7 天 / 并发参数 .env 可配 0=不限 / 配额信号量分池 B 方案 / SQLite 写锁+重试 / 进度权重自适应默认值 / 统一任务中心 / 测试连接异步 60s / 3 commit 分批）；§9.3 第 1/2/8/9 行状态改已定稿；实施细节见 docs/异步并行改造计划.md §8；备份：文件快照 + codex/async-parallel 分支；技术栈 v1.71 同步 |
| v1.58 | 2026-08-04 | 性能优化路径独立成文 | 新建 docs/性能优化路径.md（3 个已核实问题：外键索引缺失 / 上传整读 / 关键词重复抽取；6 维度全景与三梯队；5 个决策项）；需求-决策 §9.3.2 引用同步；技术栈 v1.70、重构规范 v1.38 |
| v1.57 | 2026-08-04 | 异步 / 并行改造计划独立成文 | 新建 docs/异步并行改造计划.md（13 落点、原则边界、Phase 1~3、验收、风险）；需求-决策 §9.3.2 保留落点清单并引用；技术栈 v1.69、重构规范 v1.37 同步 |
| v1.56 | 2026-08-04 | 异步 / 并行审查落点清单（9.3.2） | 代码审查 13 个具体落点：图谱路由三处同步阻塞（懒构建 / 重建 / 联动 sync 逐关系 LLM）、导入同步增量、页图渲染、测试连接、前端三处串行请求、tasks 基础；与 9.3 第 1/2/8/9 项合并实现；技术栈 v1.68、重构规范 v1.36 同步 |
| v1.55 | 2026-08-04 | 登记整体异步 / 并行改造 | 9.3 新增第 9 行（全链路耗时点异步化 / 并行化改造清单：页图渲染并发、测试连接异步化等，方案 A 全覆盖推荐）；里程碑 §8.1 补第 8 行现状审查；技术栈 v1.67、重构规范 v1.35 同步 |
| v1.54 | 2026-08-04 | 备选方案固化：LLM 挑选细节 + 进度条需求 | 9.3 新增第 8 行（进度反馈：必要操作进度条，含覆盖清单与方案 A/B/C）；9.3.1 新增 LLM 挑选实现细节备选表（粒度/频率/配置/目录组织，待定稿）；技术栈 v1.66、重构规范 v1.34 同步 |
| v1.53 | 2026-08-04 | 修订决策 34：RAG/Skill 注入改为 LLM 自主挑选 | 需求方向变更：由 LLM 结合冷/暖画像与当前需求从知识库自主挑选 RAG/Skill（两级混合：书级挑选 + 书内规则检索），规则化候选保留为降级；9.3 第 6 行同步；技术栈 v1.65（ADR-011 修订）、重构规范 v1.33 同步 |
| v1.52 | 2026-08-04 | 登记缺口备选方案（9.3） | 9.3 新增缺口需求与备选方案表（两段式导入 / 并发 / MD 图片 / EPUB 排版 / 画像编辑 / 跨书复用 / 聚类 7 项，含推荐倾向与状态）；9.2 开放项补充引用 9.3；技术栈 v1.64、重构规范 v1.32 同步 |
| v1.51 | 2026-08-04 | 跨书 RAG/Skill 复用定稿 + 全量实现状态审查 | 9.1 新增决策 34（跨书 RAG/Skill 路由方案，含候选书/打分/预算/出处/降级，待实现）；§8.1 新增实现状态审查清单（两段式导入、并发、MD 图片、EPUB 排版、画像编辑、任务系统等缺口）；技术栈 v1.63 更正 ADR-009 并发落地声称并登记 ADR-011 |
| v1.50 | 2026-08-04 | 登记聚类算法方案选型开放项 | 9.2 开放项新增第 2 条：词语向量化 vs 文本共现法（含术语归一化增强）待统一决定；方案对比固化于 docs/知识图谱聚类算法.md §10；关联文档升技术栈 v1.62、重构规范 v1.30 |
| v1.49 | 2026-08-04 | 需求文档拆分：总纲 + 4 分册 | 按功能域拆分为 需求-功能需求 / 非功能与数据 / 里程碑与验收 / 决策 4 个分册；分册沿用原编号；总纲新增分册体系与快速定位表；README 文档索引同步；关联文档升技术栈 v1.60 |
| v1.48 | 2026-08-04 | 文档审查：修复目录锚点断链 | 系统审查全部文档：目录锚点按 GitHub slugger 规则校验，修复 3.4.3 / 3.4.10 两处目录链接锚点缺 `-` 问题（`Overview / Summary`、`Markdown + LaTeX` 中的 `+`、`/` 在 slugger 中删除导致双连字符）；复核决策引用 1~33 无悬空、表格管道数无断裂、`.env.example` 与 `config.py` 键一致、后端 167 项 pytest 与文档一致；README 同步 v1.49 版本表（技术栈 v1.59） |
| v1.47 | 2026-08-04 | 整体并发与流畅度方案定稿（文档固化，待实现） | 3.4.11 提取链路补并发执行；4.5 补整体并发约定；9.1 新增决策 33（视觉/打分并发、任务系统升级、前端合并、测试连接异步）；README 版本表同步；关联文档升技术栈 v1.57 |
| v1.46 | 2026-08-04 | 导入性能与进度方案定稿（文档固化，待实现） | 3.2 补两段式导入与进度；3.4.11 触发时机修订（书架导入不自动预提取、改为手动，知识库投喂仍自动）；4.5 补导入性能约定；9.1 决策 22 修订注 + 新增决策 32；README 版本表同步；关联文档升技术栈 v1.56 |
| v1.45 | 2026-08-04 | Markdown 内嵌本地图片方案定稿（文档固化，待实现） | 3.2 补导入处理（扫描 `![...](src)` → 复制到 `images/` → 改写相对路径）；3.4.10 补渲染与媒体接口约定；4.3 补存储路径；9.1 新增决策 31；README 版本表同步；关联文档升技术栈 v1.55 |
| v1.44 | 2026-08-04 | P0 落地：会话分池 + 历史注入 + 流式渲染节流 | 3.4.13 与决策 30 由「方案已评审」转「已实现」：后端 repositories/chat.py 新增 `chat_session_id`（`book:{id}:{mode}` 分池）与 `recent_history_texts`（最近 10 轮、8k 截断窗口注入），chat_service.build_messages 增 history 参数、prepare_chat_job 隐私开启时加载历史，聊天历史 API 支持 ?mode=；前端 useReaderAi 新增 chatMode/switchMode + 80ms 流式渲染节流（hasUnclosedMath 公式感知、end/error/中断强制 flush），ReaderChatPanel 顶部模式 tab；后端 164 项 pytest + ruff 全绿，前端 vue-tsc + vite build + vitest 43 项通过；关联文档升技术栈 v1.54、使用手册 v1.87、重构 v1.29 |
| v1.43 | 2026-08-04 | 对话会话与上下文管理方案评审 + 并发实验结论收录 | 新增 3.4.13 与决策 30：现状（按书会话 `book:{id}`、历史不回传、多轮无记忆）→ 方案（会话键加 mode 分池、注入最近 6~10 轮并按约 8k 字符截断、不引入 user_id）；使用手册新增 §17 并发大模型实验（v1.84，真实 API 并发加速 3.4x、异构并行可行，主应用落地需先补限流+重试） |
| v1.42 | 2026-08-03 | 新增章节目录 | 文档头部新增目录（## + ### + #### 三级 GitHub 兼容锚点链接，38 项），便于长文定位；关联文档升技术栈 v1.52、使用手册 v1.78、重构 v1.27 |
| v1.41 | 2026-08-03 | Prompt 能力补全（画像注入 + 模式模板 + Skill 相关性 + 数学硬规则） | 3.4.10 补数学表达硬规则（决策 29：只能 LaTeX/Markdown、禁 Unicode，对话/脑图/关联/联动/页提取全链路）；3.4.1 画像接入对话（隐私开关控制）；「解读/概论/思考逻辑」改为后端结构化模板（前端 `mode` 透传）；Skill 按任务相关性排序截断注入；验收标准补第 25 条；头部进度补 v1.41 打磨轮；关联文档升技术栈 v1.51、使用手册 v1.77 |
| v1.40 | 2026-08-03 | RAG/Skill 渲染与资产页两级结构 | 新增 3.4.9 展示形态条目：RAG/Skill 文本统一 LaTeX/Markdown 渲染；资料页两级结构（`/rag` 列表仅「有无 + 简略摘要 + 入口」，`/rag/{bookId}` 下级完整内容页可折叠展开）；上传总结固定大小置顶展示（无滚动条）；头部进度补 v1.40 打磨轮；关联文档升技术栈 v1.50、重构 v1.26、使用手册 v1.74 |
| v1.39 | 2026-08-03 | AI 多接口格式与 base_url 双写法 | 新增 3.4.12（AI 接口接入多格式）与 9.1 决策 28：`responses \| chat \| anthropic` 三选一 + base_url 基础地址自动补全/完整 URL 直接用 + 浏览器 UA 规避 Cloudflare 403；9.2 开放项措辞同步（anthropic 已定稿，responses 思考参数仍待定稿）；头部进度补 M10+ 打磨轮；关联文档升技术栈 v1.49、重构 v1.25、使用手册 v1.73 |
| v1.38 | 2026-08-03 | 资产链路实测与版本语义修复 | 前端全链路实测通过：上传→总结 RAG/Skill（带出处）→单条删除→去重合并（同内容多本共享一份资产）→移除资产（书籍+资产级联，共享转移/解除）；修复图谱联动存根反复 bump 版本与空存根误触发增量总结（存根 v1.68 起不 bump、空存根总结走全量 version=1）；关联文档升技术栈 v1.44、使用手册 v1.68 |
| v1.37 | 2026-08-03 | 资产 hash 与删除语义定稿 | 资产合并 hash = **书籍内容指纹**（原文件 sha256，`Book.content_hash`，导入时计算、旧书懒回填）；相同内容重复导入的书籍共享一个资产；「删除资产」= 从资产库移除不需要的知识库（rag/skill）**及其对应书籍**（`DELETE /api/books/{id}` 级联资产与文件）；关联文档升技术栈 v1.43、使用手册 v1.67 |
| v1.36 | 2026-08-03 | 一键启动幂等化（端口占用检测） | `start.bat` 重复启动不再报端口占用：检测 8321/5173 已在运行则跳过并提示；本次问题根因是旧后端实例未关闭占用端口；关联文档升技术栈 v1.42、使用手册 v1.66 |
| v1.35 | 2026-08-03 | 资产去重合并（hash 相同合并为一个资产） | 资产（RAG/Skill）支持删除（v1.34）且自动去重合并：条目级按内容 hash 去重（写入时合并 key_points/chunks/skills/domains 重复项）；跨书整条资产按内容 hash 合并为一条共享资产（`POST /api/assets/dedupe` 手动触发，被合并书透明读取同一资产，删除/删书时共享引用自动解除或转移主书）；资料页「合并重复资产」按钮与「共享 N 本书」标记；关联文档升技术栈 v1.41、使用手册 v1.65 |
| v1.34 | 2026-08-03 | 资产手动清理（删除不需要的 RAG/Skill） | 资料页资产详情支持删除：整条 RAG/Skill 资产（二次确认）或单条条目（关键知识点 / RAG 检索片段 / 技能条目），单条删除视为资产变更 version+1、post-classify 失效重算；后端 `DELETE /api/books/{id}/asset?kind=` 与 `/asset/{kind}/{section}/{index}`；关联文档升技术栈 v1.40、使用手册 v1.64 |
| v1.33 | 2026-08-03 | 重构轮：删除死代码 + 统一 LLM JSON 解析 | 删除 `services/graph_service.py`（889 行，图谱包拆分后遗留死代码，全库无引用，`services/graph/` 包已完整替代）；新增 `app/ai/parsing.py`（`parse_llm_json`：容忍代码围栏与前后杂文的 JSON 提取），统一 `graph_sync`/`llm_score`/`rag_service`/`mindmap_service` 四处重复解析；后端 135 项 pytest + ruff 全绿；关联文档升技术栈 v1.39、使用手册 v1.63、重构 v1.23 |
| v1.32 | 2026-08-03 | M8 完成：LLM 打分与理论传承方向/关联原因增强 | §7 M8 里程碑改为 ✅ 已完成（v1.32）：新增 `services/graph/llm_score.py` 与 `ai/prompts/graph_edge.py`，跨书重建/导入增量对候选对做有界 LLM 打分（MAX_LLM_PAIRS=40），输出强度/方向（from_book+承接发展批判）/关联类型/原因，强度取 max、方向类型原因以 LLM 为准，失败回退关键词分；`book_relations` 新增 `from_book_id` 列（增量迁移），谱系图按 from_book 画箭头并展示 A→B 方向与原因；RAG 联动存根补 direction；前端 GraphView 方向箭头/详情/tooltip 升级；新增 7 项 pytest（后端 135 通过、ruff 全绿、vue-tsc + vite build 通过）；关联文档升技术栈 v1.38、使用手册 v1.62、重构 v1.22 |
| v1.31 | 2026-08-03 | M10 打磨：笔记导出（Markdown+PDF）+ 性能优化 + 验收测试 | 笔记 PDF 导出（`services/note_export.py`，PyMuPDF 内嵌 CJK，LaTeX 源码保留，`?fmt=md\|pdf`，前端抽屉「导出 PDF」）；性能优化（页图目标宽度缓存、页图 Cache-Control 10 分钟、前端相邻页预加载、vite vendor 分包）；验收测试（新增 test_export_m10.py 3 项；conftest 隔离真实 .env 的 AI/视觉配置修复 9 项环境性失败；后端 128 通过、ruff 全绿、vue-tsc + vite build 通过）；关联文档升技术栈 v1.37、使用手册 v1.61、重构 v1.21 |
| v1.30 | 2026-08-02 | 书架 UI 打磨：拖拽换位 + 手动标签 + 搜索/筛选 | §3.1 书架支持拖拽交换位置（`books.position` 排序位，`POST /api/books/reorder` 批量重排，新书自动追加末尾）；书架卡片手动编辑标签（弹层多选 + 自由输入，PATCH tags 清洗去重）；搜索（书名/作者/标签 `?q=`）+ 标签下拉/卡片点击筛选；列表按 position 升序返回；新增 6 项 pytest（后端 125 通过、ruff 全绿、vue-tsc + vite build 通过）；关联文档升技术栈 v1.36、使用手册 v1.60、重构 v1.20 |
| v1.29 | 2026-08-02 | M9 第五轮：相关度阈值函数（决策 20 落地） | 新增 `services/relatedness.py`（score = max(谱系强度, 0.5×暖画像覆盖率×100 + 0.5×冷画像覆盖率×100)、同簇直判、signals 可解释）；`profile_service` 归档入暖记忆改走相关度函数（related_books 含 score）并记录行为样本（related_samples）；`profile_learning.learn_thresholds` 学习 related_strength 取行为样本低分位与确认边低分位之 min（样本 ≥6，45~60）；修复相关度查询 SQLAlchemy `!=` 过滤 NULL 行缺陷（`or_(is_(None), !=)`）；阈值 API `learning` 返回 related_sample_count/related_samples_min，画像页展示；§3.4.1/M9 里程碑/9.1 决策 20 同步；新增 8 项 pytest（后端 119 通过、ruff 全绿、vue-tsc + vite build 通过）；关联文档升技术栈 v1.35、使用手册 v1.59、重构 v1.19 |
| v1.28 | 2026-08-02 | M8 第 12 轮：新书导入增量更新 + 跨书知识检索 | §3.6.1 新书导入增量更新落地（`upload_book` 触发 `services/graph/cross_book.py` `incremental_cross_book_graph`：只补新书与既有书边、幂等、补本地 RAG 联动存根，失败回滚不阻塞导入）；§3.6.2 跨书知识检索落地（`GET /api/graph/knowledge/{kp_id}/appears-in`：其他书内知识点关键词重叠 + 其他书 RAG key_points 命中，来源书不计；前端书内图谱节点点击打开「知识点详情」弹窗，可跳转对应书图谱/阅读原文）；里程碑 M8 更新为第 12 轮，待办仅剩 LLM 打分与传承方向/关联原因增强；新增 4 项 pytest（后端 111 通过、ruff 全绿、vue-tsc + vite build 通过）；关联文档升技术栈 v1.34、使用手册 v1.58、重构 v1.18 |
| v1.27 | 2026-08-02 | 图谱联动 RAG/Skill 增量增改 + 画像阈值自动学习 | §3.4.7/3.4.9 跨书联动沉淀落地：`services/graph_sync.py`（本地存根 `linked_books`/`domain_terms` 补水、`POST /api/graph/sync` LLM 增量增改、`ai/prompts/rag_link.py`），谱系重建/懒构建/关联反馈自动补存根，谱系图页新增「💾 联动沉淀」按钮并修复「重建图谱」按钮；§3.4.1 画像阈值自动学习落地：`services/profile_learning.py`（归档节奏样本 + 确认关联低分位 → `review_days/warm_threshold/related_strength`，样本不足回退默认常量），新增 `GET/PATCH /api/profile/thresholds`、`POST /api/profile/learn`，画像页新增阈值卡片；新增 10 项 pytest（后端 107 通过、ruff 全绿）；关联文档升技术栈 v1.33、使用手册 v1.57、重构 v1.17 |
| v1.26 | 2026-08-02 | 专业术语词库（用户可编辑，优先匹配） | 新增与 .env 同级的用户可编辑词库文件 `backend/domain_terms.txt`（每行一个术语，# 注释）：用户区术语命名时最高优先级；系统自动学习选定的专业术语写入【系统缓存区】作为以后优先备选（按覆盖书数优先竞争）；命名优先级：用户词库 → 自动候选（含系统缓存词库）；新增 5 项 pytest（后端 97 通过、ruff 全绿）；关联文档升技术栈 v1.31、使用手册 v1.55 |
| v1.25 | 2026-08-02 | 自动总结领域名改为专业术语 | 领域名（自动聚类簇名 / post-classify 自动命名 / 簇重命名）必须是领域专业术语，剔除泛化词（定理/定义/方法/教程/讲义/习题等）与作者/元数据噪声（如「茆诗」）；实现：`_GENERIC_DOMAIN_TERMS` 泛化词过滤 + `_domain_candidates` 加权候选（章节×3+书名×2+正文×1+后验×5）+ `_pick_domain_name` 命名；新增 2 项 pytest（后端 92 通过、ruff 全绿）；关联文档升技术栈 v1.30、使用手册 v1.54 |
| v1.24 | 2026-08-02 | 自动总结的领域清洗 | LLM 总结的 skill `domains`（tags）保存前经 `sanitize_cluster_name` 清洗（空项丢弃、去重）；`assign_clusters` 领域自动聚类不再追加 `#书id` 标点后缀（重名关键词书并入同一簇）；新增 2 项 pytest（后端 90 通过、1 项既有失败）；关联文档升技术栈 v1.29、使用手册 v1.53 |
| v1.23 | 2026-08-02 | 聚类 tag 清洗：只保留汉字/英文词组 | 聚类名与 tag 统一清洗（`graph_service.sanitize_cluster_name`：去除特殊标点、全角字母/数字转半角、中文之间不留空格，保留汉字/英文字母/数字与单词间空格）；保存 tag 时清洗（books PATCH，空 tag 丢弃、重复项去重）；`post_classify_book`/`assign_clusters`/`merge_and_rename_clusters` 的 tag、文件夹、簇名写入点统一清洗，并兜底清洗历史遗留脏簇名；新增 5 项 pytest；关联文档升技术栈 v1.28、使用手册 v1.52 |
| v1.22 | 2026-08-02 | M9 第三轮：增量增改 + 簇合并/重命名 + 阅读建议基础版 | `rag_service` 再次归档走增量提示词（`INCREMENTAL_SYSTEM_PROMPT`/`build_incremental_user_prompt`，素材=`_collect_new_material` 全书笔记/划线/「不理解」+最近20条对话），原资产 version+1、id 不变；`graph_service.merge_and_rename_clusters`（T_MERGE=5 两簇后验关键词并集重叠合并进主簇、簇名取书最多的后验术语众数，仅 post 书，归档成功后触发）；新增 `services/recommendation_service.py`（stats/weak_concepts/review/rhythm）与 `GET /api/profile/recommendations`，画像页新增阅读建议卡片；修复簇合并未把被并入书并入主簇成员列表导致重命名漏书的问题；新增 4 项 pytest（后端 83 项通过，仅 1 项既有失败）、ruff/vue-tsc/vitest 全绿；关联文档升技术栈 v1.27、使用手册 v1.51 |
| v1.21 | 2026-08-02 | M9 第二轮：三层画像实现 | §3.4.1 三层画像（热=当前书细节、暖=近 1~2 本+相关领域、冷=长期偏好）；归档迁移跨 1 本热→暖、跨 3 本暖→冷、>3 本全部沉淀冷（阈值初值 `WARM_TO_COLD_THRESHOLD=3`/`KEEP_RECENT=2`，后续按用户习惯学习）；相关领域书入暖记忆（同 post 簇或关联边 strength ≥60 初值）；阅读行为实时回写（进度/章节脉络、笔记/划线/「不理解」→ 热画像）；画像 API 与前端画像页（/profile 三层卡片+重置）；M9 里程碑标记部分完成（第二轮）；关联文档升技术栈 v1.26、使用手册 v1.50 |
| v1.20 | 2026-08-02 | M9 第一轮：读完归档链路 + post-classify 基础版 | 新增归档端点 `POST /books/{id}/archive`（`assets.py`，后台任务 `archive_book_task`）：PDF 先视觉通读全书补齐页缓存（`rebuild_book_caches`，命中不重复调用）再以缓存全文由文本模型总结 RAG/Skill（`page_chunks`/`_build_page_input`，出处「第 X 页」）；非 PDF 按章节正文；成功后章节全部标记已读、状态=读完；`generate_rag_skill` 支持 `page_texts` 参数并成功触发 post-classify；`graph_service` 落地两阶段分类基础版（`T_POST=3` 一致性校验/簇迁移、落盘 `cluster_name/classify_source/classified_at/classify_version`、`assign_clusters` 采用未失效 post 结果、停用词表扩充防「本书介绍」噪音）；前端 `rag.ts` 新增 `archiveBook`、阅读页「归档并总结 RAG/Skill」按钮；新增 5 项 pytest；M9 标记部分完成；关联文档升技术栈 v1.25、使用手册 v1.49 |
| v1.19 | 2026-08-02 | 补登记决策 27 + 修正过时引用 | 9.1 新增决策 27（文本模型思考模式与精细参数，DeepSeek：chat 模式 thinking/reasoning_effort、AI_MAX_TOKENS 32K/64K、responses 模式仅 max_output_tokens）；9.2 开放项登记 1 条（responses 模式 thinking 参数待定稿）；§3.4.1「阈值见 9.2 开放项」过时引用修正为「9.1 决策 20」；技术栈 v1.23 原「需求 9.1 决策 27」引用现已有出处；关联文档升技术栈 v1.24、使用手册 v1.48 |
| v1.18 | 2026-08-02 | 固化「读完归档 → 视觉通读全文 → 文本模型总结 RAG/Skill」 | §3.4.9 首次归档补链路：PDF 书归档时先由**多模态图像模型通读全书**（后台按页批量提取、补全缺失页缓存、命中不重复调用），再以缓存全文为输入由**文本模型**总结 RAG 与 Skill；§3.4.11 触发时机补「读完归档时全书批量提取」；9.1 决策 22 同步；M9 里程碑补「归档视觉通读」；关联文档升技术栈 v1.23、使用手册 v1.47 |
| v1.17 | 2026-08-02 | 固化两阶段聚类（pre/post-classify） | §3.6.1 聚类与分层展开补充**两阶段分类**：pre-classify（tag/文件夹/关键词共现粗略聚类）→ 获得 RAG/Skill 资产后 post-classify（基于 summary/key_points/Skill 用法精细聚类：细化/合并/迁移簇、代表性术语重命名，tag/文件夹硬约束优先，资产版本变化失效重算）；M8 待办补充 post-classify；设计细节入 `docs/知识图谱聚类算法.md` §9；关联文档升技术栈 v1.22、使用手册 v1.46 |
| v1.16 | 2026-08-02 | M8 知识图谱第一轮实现 | 新增 `services/graph_service.py`（关键词抽取 / 跨书关联=关键词共现余弦分+笔记加权 / 聚类分层=tag→文件夹→领域自动聚类 / 书内知识图谱=章节级+重要段落+用户标记 / 懒构建与重建）与 `api/routes/graph.py`（全局谱系、书内图谱、重建、人工反馈，注册到 main.py）；前端 GraphView 重写为 ECharts 关系图（聚类筛选、最密切边高亮、方向箭头、层级筛选、关联详情与反馈、跳转阅读、导出 PNG）+ `api/graph.ts`；新增 6 项 pytest；里程碑 M8 标记部分完成（待办：LLM 打分/传承方向、跨书跳转、图谱联动 RAG/Skill 与暖画像、导入自动增量更新）；关联文档升技术栈 v1.21、使用手册 v1.44、重构 v1.15 |
| v1.15 | 2026-08-02 | M7 PDF 统一处理与多模态视觉提取完成 | PDF（含文本型）统一按页切章与渲染（`parse_pdf` 全部返回按页章节 + `local_text/` 本地抽取文本）；设置页新增多模态独立配置（含测试视觉连接）；新增 `services/vision_extract.py`（逐页提取/滑动窗口增量缓存/全书重建后台任务）与 `api/routes/vision.py`（缓存状态/读取/重提本页/重建）；对话与脑图链路优先注入 `[P-1,P,P+1]` 页缓存文本（出处「第 X 页」），缓存缺失回退页图附件；导入 PDF 后台批量预提取；阅读页页缓存入口；引用解析支持【第X页】；新增 9 项 pytest（提取/缓存命中/滑动窗口/隐私/随书清理/路由）；里程碑 M7 标记完成，M8 为下一步 |
| v1.14 | 2026-08-02 | M6 修复轮：涂鸦渲染与工具栏重排 | 修复 PDF 页图涂鸦：旧数据 `{x,y}` 点坐标兼容并自动迁移（新增 `src/utils/annotations.ts` 纯函数）；画板尺寸以页图渲染尺寸为准、仅变化时重置缓冲区（修复 canvas 卡 1×1 导致笔刷/文本不渲染、无绘制反馈）；涂鸦工具栏移入阅读页顶部（第 X 页 与 第 X 章/共 XX 章 之间），仅 PDF 按页阅读显示；撤销栈切页清空；新增 6 项 Vitest（点兼容/迁移） |
| v1.13 | 2026-08-02 | M6 阅读体验补全完成 | 位置书签（全格式 + 分组归类 + 跳转）与 PDF 页图涂鸦（画板工具/撤销 5 步/擦除/文本/划线批注/划线区域提问）已实现；里程碑 M6 标记完成，M7 为下一步 |
| v1.12 | 2026-08-02 | 涂鸦划线批注与提问 | 每条划线可选中后附加批注（Markdown/LaTeX，随划线持久化）或发起对划线区域的 AI 提问（结合页缓存文本/划线区域图片，出处标注「第 X 页」，受隐私开关约束）；9.1 新增决策 26；3.3/3.5/5/3.4.11/验收 23/M6 同步 |
| v1.11 | 2026-08-02 | 重新规划 M6 及以后里程碑 | 任务变动（书签/涂鸦定稿 + PDF 统一按页处理/多模态提取待实现）后重排：M6 阅读体验补全（书签+涂鸦）、M7 PDF 统一处理与多模态视觉提取（知识图谱语料前置）、M8 知识图谱、M9 个性化与沉淀、M10 打磨；头部进度块与 M3/M4 描述同步指向新编号 |
| v1.10 | 2026-08-02 | 书签组织与涂鸦功能定稿 | 书签列表默认按时间倒序平铺，支持分组/文件夹归类；涂鸦支持撤销上一步（最多 5 步）、擦除痕迹、输入文本，不保留历史版本（会话内撤销栈）；9.1 新增决策 24/25，9.2 开放项清零；3.3/3.5/5/验收 22/23 同步 |
| v1.9 | 2026-08-02 | PDF 统一按页处理（含文本型） | 文本型 PDF 数学符号抽取乱码，改为与扫描件一致：统一按页切章、原图阅读、多模态视觉提取；本地文本抽取仅作全文检索索引；3.2/3.3/3.4.11/3.5/5/决策 2 与 22/验收 1/7/21/23/9.2 措辞/里程碑同步 |
| v1.8 | 2026-08-02 | 细化多模态提取触发 + 书签/涂鸦需求 | 3.4.11 按需提取改为「当前页对 AI 提问时触发」+ `[P-1,P,P+1]` 滑动窗口增量缓存（第 1 页/末页裁剪、仅补缺失页）；新增位置书签（全格式：扫描件整页/文本书章节+段落）与扫描件页图涂鸦标注（画板式）；3.3/3.5/5/决策 22/验收 21~23/里程碑/开放项同步 |
| v1.7 | 2026-08-02 | 开放项全部定稿 | 9.1 新增决策 17~23（结束对话三组合优先主动归档 / Skill 自动按任务匹配 / RAG/Skill 保留≥3 版本整条目回滚 / 相关领域按跨书谱系关联强度 / 文件夹多级嵌套+批量移动/设tag+可调位置 / 多模态导入批量预提取+首读按需前后页 / 多模态独立 API 配置无需额度管理）；9.2 开放项清空；3.1/3.4.9/3.4.11/验收标准同步 |
| v1.6 | 2026-08-02 | 扫描件多模态视觉提取（文档固化，待实现） | 新增 3.4.11：扫描版页图经多模态 LLM 提取完整页面信息、按页缓存到书籍目录（命中不重复调用），文本大模型基于缓存解读；多模态 API 独立配置、受隐私开关约束；4.2/4.3 补约定；验收标准补第 21 条；开放项补 6/7 条 |
| v1.5 | 2026-08-02 | 扫描版页图高清化 + 阅读缩放 | 扫描版页图改为按 PDF 内嵌原图分辨率渲染（替代 72 DPI 低清），旧低清页图按需重渲染；阅读页页图支持「适配宽度 / 原始大小 / ＋/－」缩放与横向滚动 |
| v1.4 | 2026-08-02 | 标记 M5 收尾（脑图渲染/导出/批注） | 脑图节点名清洗：新增 `src/utils/text.ts` `toPlainDisplayText`（去 LaTeX/Markdown 记号后再进 ECharts 标签）；脑图工具栏新增「下载大纲」（.md）与「插入为批注」（写入当前章节批注，复用笔记保存逻辑）；笔记抽屉正文改为 Markdown/LaTeX 渲染（MdRender）；M5 里程碑补充上述能力 |
| v1.3 | 2026-08-02 | 标记 M5 完成 | 里程碑 M5（AI 增强）完成：脑图 ECharts 图像组件（大纲/细节/重要定理三层 + 出处 + 跳转 + 导出 PNG）、四类生成预设、划词 AI 操作（解释选中段/该段脑图/加入思考清单）；M6 知识图谱为下一步 |
| v1.2 | 2026-08-02 | 封面修复 + 书架删除 | 修复 PDF/EPUB 封面跨书共享覆盖：每书独立子目录存储 + 启动自动迁移旧书并回填封面；书架支持删除书籍（级联清理笔记/对话/资产/图谱与本地文件） |
| v1.1 | 2026-08-02 | PDF 封面 / 扫描版按页阅读 / LLM 页图附件 | 导入 PDF/EPUB 提取封面并在书架渲染；扫描版按页切章、阅读页直接读原图；设置页「发送页面图片」开关，扫描书提问自动附带当前页图片（chat 模式、受隐私开关约束）；验收标准与里程碑同步更新 |
| v1.0 | 2026-08-02 | 标记 M4 完成 | 里程碑 M4（AI 接入）完成：API 配置、对话流式、按章节上下文问答、引用出处标注；M5 AI 增强为下一步 |
| v0.9 | 2026-08-01 | 标记开发进度 + 补充需求 | 状态改为开发中；里程碑标记 M1✅/M2✅/M3◐（PDF 导出移至 M8）；补充需求 3.4.9 外部资料直投（资料页投喂即总结 RAG/Skill）、4.6 一键启动（start.bat）；验收标准补 19/20 条 |
| v0.8 | 2026-08-01 | 新增文件夹归类 | 书架支持文件夹归类；同文件夹内无显式 tag 的书默认继承文件夹 tag（用于谱系图聚类） |
| v0.7 | 2026-08-01 | 新增渲染格式 | 用户输入与 LLM 输出支持 Markdown 与 LaTeX 渲染（含笔记），渲染前 XSS 消毒 |
| v0.6 | 2026-08-01 | 补充图谱联动与暖记忆 | 跨书谱系构建/更新时同步增改对应书籍 RAG/Skill；阅读相关领域书籍时将其关键内容存入暖画像 |
| v0.5 | 2026-08-01 | 新增书籍沉淀 | 结束对话的书由 LLM 总结为 RAG 与 Skill 资产，再次阅读并结束后在原资产上增改；补充数据模型/里程碑/验收/开放项 |
| v0.4 | 2026-08-01 | 细化项全部确认 | 画像迁移规则（跨书数 1/3/>3 + 习惯学习）、笔记导出 MD+PDF、不理解双入口、EPUB 保留排版、边方向箭头 |
| v0.3 | 2026-08-01 | 评审确认 11 项决策 | 本地单用户、无 OCR、脑图图像组件、三层画像、支持 EPUB、引用出处、关联强度三合一+笔记加权、图谱粒度三层覆盖、ECharts、tag+领域聚类 |
| v0.2 | 2026-08-01 | 新增跨书串联 | 固化书籍级谱系图、书籍内部知识图谱、关联强度与「关系最密切」标注 |
| v0.1 | 2026-08-01 | 初稿 | 固化主页/阅读页/AI 助手/画像等核心需求 |
> - ✅ UI 打磨轮（v1.66）：资产页上传区优化——「外部资料 → RAG/Skill」上传卡片改为**拖拽/点击选择框**（虚线边框、hover 高亮、已选文件态），资产页移除页面级滚动条（仅保留文档最外层滚动），并修复上传返回结构（后端平铺返回 `{...book, task_id}`，前端 `UploadResult = BookItem & { task_id }`，书架页/资产页同步适配），见 3.4.9

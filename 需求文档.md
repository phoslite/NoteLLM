# 读书阅读助手 —— 需求文档（v1.33）

> 状态：**开发中**（需求已冻结）
> 更新日期：2026-08-03
> 说明：本文档用于锁定产品需求与功能边界，后续开发、UI 设计与测试均以本文档为准。原待确认问题已评审确认（见 9.1），开放项登记见 9.2（当前仅剩 1 条待定稿：DeepSeek responses 模式思考参数，见 9.2）。
>
> **当前开发进度**（详见 §7 里程碑）：
> - ✅ M1 需求冻结；✅ M2 基础框架（含书架/阅读页静态布局与解析）；✅ M3 阅读闭环（Markdown / PDF 导出已上线，见 M10；位置书签 + 页图涂鸦移至 M6 后已完成）
> - ✅ M4 AI 接入（设置页配置 + 对话流式 + 章节上下文问答 + 引用出处标注；补充：PDF 封面 / 扫描版按页阅读 / 页图附件 / 页图高清原图渲染 + 阅读缩放；PDF 统一按页处理含文本型与多模态视觉提取移至 M7）；✅ M5 AI 增强（脑图图像组件 + 划词 AI 操作 + 脑图节点清洗/下载大纲/插入批注）→ ✅ M6 阅读体验补全（书签全格式+分组、页图涂鸦含撤销/擦除/文本/划线批注/划线提问）→ ✅ M7 PDF 统一处理与多模态提取 → ✅ M8 知识图谱（跨书谱系 + 聚类分层 + 书内知识图谱 + 重建/人工反馈 + LLM 打分与理论传承方向/关联原因 + 图谱联动 RAG/Skill 沉淀 + 新书导入增量更新 + 跨书知识检索，见 §7）→ ✅ M9 个性化与沉淀（三层画像 + 归档迁移 + 阈值自动学习 + 相关度阈值函数已实现、待办已清空，见 §7）→ ✅ M10 打磨（笔记 PDF 导出 + 性能优化 + 验收测试，见 §7）
> - 已提前实现：RAG/Skill 资产总结与展示页（资料页 /rag，需求 3.4.9 外部资料直投）、一键启动（start.bat，需求 4.6）

---

## 1. 项目概述

构建一个本地优先的「读书阅读助手」桌面 Web 应用，核心目标：

1. **多书管理**：以书架形式管理用户导入的书籍（支持 PDF、Markdown、EPUB 等），记录阅读进度。
2. **沉浸式阅读**：打开书籍后提供三栏式阅读工作台——左侧竖向书架、中间阅读区、右侧 AI 助手。
3. **AI 阅读增强**：通过接入外部大模型 API（可配置），基于用户已投喂的书籍构建**三层个性化画像**，并针对任意指定章节/段落生成**解读、概论、脑图、思考逻辑**，帮助用户提升阅读能力。
4. **跨书串联（谱系化）**：LLM 将用户已投喂的书籍互相串联，生成**书籍级谱系图**；点击某本书后，进一步展示该书**内部知识点的分布谱系与串联关系**，边标注与哪本书关系最密切。
5. **书籍沉淀（RAG + Skill）**：每本**结束对话**的书籍，由 LLM 总结为可复用的 **RAG（检索增强知识）** 与 **Skill（技能能力）** 资产；若再次阅读同一本书并结束对话，则**直接在原书的 RAG 与 Skill 资产内增改**，不重复创建。

**核心价值主张**：AI 不是简单问答，而是「读过你的书、懂你的水平、按你的习惯讲」，并且能帮你把读过的书连成一张知识网络。

---

## 2. 目标用户与核心场景

| 场景 | 用户诉求 |
| --- | --- |
| 知识型读者 | 想高效理解一本难书，需要逐章解读与逻辑梳理 |
| 主题研究者 | 需要跨书建立知识关联，快速生成脑图与概论 |
| 备考/输出者 | 需要把书中知识转化为可复述、可提问的思考逻辑 |
| 系统学习者 | 想看清自己读过的一批书之间的渊源与脉络，建立体系化认知 |

---

## 3. 功能需求

### 3.1 主页（书架首页）

布局为左右两栏，顶部提供视图切换：

- **顶部视图切换**：「书架 / 谱系图」两种视图。
  - 书架视图：见下方左右两栏。
  - 谱系图视图：展示全部书籍的**书籍级谱系图**（见 3.6.1）。
- **左侧：近期阅读**
  - 展示最近读过的书（按最近打开时间倒序）。
  - 每本书显示：封面/标题、最近阅读进度百分比、最近阅读时间。
  - 点击书目直接进入阅读视图（定位到上次阅读位置）。
  - 可折叠/展开；默认显示最近 5 本。
- **右侧：书架**
  - 全部书籍网格/列表视图，支持按书名/作者/标签搜索、按标签筛选（**已实现 v1.30**：顶部搜索框 + 标签下拉筛选 + 卡片标签点击即筛）。
  - 每个书格显示：封面、书名、作者、分类 tag、阅读进度、阅读状态（未读/在读/读完）。
  - 支持用户为书籍设置**初步分类 tag**（用于谱系图聚类，见 3.6.1）；未设置显式 tag 的书**默认继承所在文件夹的 tag**，仍无 tag 的书由系统按领域自动归类。
  - 支持**文件夹归类**：新建/重命名/删除文件夹，将书籍拖入/移出文件夹，书架支持按文件夹浏览与筛选；删除文件夹时其中书籍转为「未归类」（不删除书籍）；**文件夹支持多级嵌套**；支持**批量移动、批量设 tag**；在书架/阅读页可调整书籍与文件夹位置（见 9.1 决策 21）。**书籍拖拽换位（已实现 v1.30）**：书架卡片可直接拖动交换位置，顺序持久化到 `books.position`（`POST /api/books/reorder` 批量重排）。
  - 支持导入新书（PDF / Markdown / TXT / EPUB / 拖拽上传）。
  - 支持对书籍的编辑、删除、重新导入。
  - 每本书提供「查看本书知识图谱」入口（见 3.6.2）。

### 3.2 书籍导入与解析

- 支持格式：`.pdf`、`.md` / `.markdown`、`.txt`、`.epub`。
- 导入流程：上传 → 解析文本 → 抽取元信息（标题、作者、目录结构）→ 入库 → 可阅读。
- PDF：**统一按页处理（不再区分文本型/扫描型）**——按原始页切章（每页一章、目录「第 N 页」），阅读页直接展示原图；AI 解读统一走「多模态 LLM 视觉提取」（见 3.4.11）。原因：文本型 PDF 直接抽取文本时数学公式/符号会乱码，统一按页渲染 + 视觉提取可保证正文与解读的公式完整正确；本地文本抽取仅保留为**全文检索索引**，不用于正文展示与 AI 上下文。
- EPUB：解包解析正文与目录（toc），**保留原排版（图文混排）** 进行阅读展示；AI 分析基于提取的文本内容。
- Markdown/TXT：按标题层级（`#`/`##`/`###`）切分为章节；无标题层级时按段落/长度切分。
- 导入的书籍会同时作为 AI 个性化画像与**跨书图谱**的语料来源（见 3.4、3.6）。
- 新书导入后，**自动增量更新**书籍谱系图与内部知识图谱（后台异步执行）。
- **笔记功能为标配**：阅读区支持高亮、批注、思考清单与「不理解」标记（见 3.3）。

### 3.3 阅读视图（三栏工作台）

打开一本书后呈现三栏布局：

- **左栏：竖向书架**
  - 本书全部章节以竖向列表排布（书名固定顶部，下方为章节列表）。
  - 显示每章阅读完成状态；当前章节高亮。
  - 支持快速切换章节、回到书架首页。
- **中栏：阅读区**
  - 正文展示，支持字号、行距、主题（护眼/夜间）调节。
  - 阅读进度自动记录（按章节+滚动位置），支持手动标记「已读完」；标记读完或结束对话后触发「本书归档」，生成/更新 RAG 与 Skill 资产（见 3.4.9）。
  - 支持选中文本（划词）触发 AI 操作：解释该段、生成脑图、加入思考清单等。
  - **笔记功能**：高亮 + 批注 + 思考清单，内容支持 **Markdown 与 LaTeX** 语法，支持导出（Markdown 与 PDF 两种格式）；「标记为不理解」提供**两个入口**：划词菜单按钮 + 阅读区侧边按钮，标记内容自动进入图谱与画像（见 3.6.2、3.4.1）。
  - 提供「本书知识图谱」入口：查看本书知识点分布与串联，点击知识点可跳回对应原文位置（见 3.6.2）。
  - **位置书签（适配所有格式）**：可将当前阅读位置保存为书签并跳转——PDF（按页阅读）为「整页书签」（页号），文本书为「章节 + 段落位置」书签；书签支持命名/备注，点击跳转回原位置；书签列表**默认按时间倒序平铺**，支持**分组/文件夹归类**（见 3.5、9.1 决策 24）。
  - **页图涂鸦（PDF 按页阅读时）**：页图上提供画板式标注（笔刷/高亮/橡皮、可选颜色与线宽，支持**输入文本**），支持**撤销上一步（最多 5 步）**与**擦除痕迹**，标注随页保存、再次打开叠加显示（见 3.5、9.1 决策 25）。**每一条划线（stroke）可选中后附加批注或发起提问**：批注内容支持 Markdown/LaTeX，随划线持久化保存；对划线区域提问时，AI 结合该页页缓存文本/划线区域图片进行解读，引用出处标注「第 X 页」（见 3.5、9.1 决策 26）。
- **右栏：AI 助手**
  - 与当前书籍、当前章节上下文绑定。
  - 提供预设能力入口：**解读 / 概论 / 脑图 / 思考逻辑**（见 3.4）。
  - 对话式交互，支持追问与多轮对话。
  - 生成结果支持：复制、导出 Markdown、导出图片（脑图）、插入阅读区旁注；**引用出处**（标注出自哪一章哪一段，见 3.4.8）。
  - 支持跨书问答：当问题涉及本书以外的书时，可调取其他书的知识点并在回答中标注来源。

### 3.4 AI 助手能力（核心）

#### 3.4.1 个性化画像（三层架构）

- 画像采用**三层架构**，由系统随阅读行为自动维护：

| 层 | 名称 | 内容 | 更新频率 |
| --- | --- | --- | --- |
| 冷画像 | 长期层 | 重要但不常调用的长期特征：领域偏好、知识水平、语言风格、长期兴趣 | 低频（稳定） |
| 暖画像 | 近书层 | 近期 1~2 本书的重要内容 + **相关领域书籍的关键内容**：近期主题、常用概念、阅读节奏 | 中频（随读新书/相关领域书更新） |
| 热画像 | 当前层 | 当前这本书的大部分细节：章节脉络、划线/笔记/「不理解」点、进行中的问题 | 高频（随阅读实时更新） |

- **读取优先级**：热 > 暖 > 冷；按任务类型决定注入粒度（当前书解读注入热画像全量+暖画像摘要；跨书问答再按需取冷画像）。
- **相关领域书入暖记忆**：阅读与画像/跨书谱系中已有领域**相关**的书籍时，该书关键内容（主题、概念、要点）自动**存入暖画像**，与近期 1~2 本书共同构成暖记忆；**相关度判定已落地（v1.29，决策 20 定稿）**：相关度 = max(跨书谱系关联强度, 0.5×暖画像术语覆盖率 + 0.5×冷画像术语覆盖率)，同 post 簇直接判定相关；阈值由自动学习产出（45~60）、设置页可手动覆盖。
- 画像用于：
  - 调整解读的**深度与措辞**（避免简单问题重复解释、复杂概念深入拆解）；
  - 推荐相关章节、相关书籍（辅助阅读能力提升）；
  - 生成内容时引用用户「已读过的书」做类比与知识串联；
  - 图谱个性化：用户做笔记/标记多的书籍与知识点在谱系图中权重更高。
- 用户行为（做笔记、标记不理解、提问、划线）都会**实时回写画像**。
- 画像可查看、可编辑、可重置；隐私说明见 4.4。
- **归档迁移规则（系统自动管理，阈值可学习）**：
  - 初步界定条件：以「对话/阅读内容跨越的书籍数」为依据——跨越 **1 本** → 热画像归档至暖画像；跨越 **3 本** → 暖画像归档至冷画像；**大于 3 本** → 全部沉淀为冷画像（保留长期低频特征）。
  - 迁移阈值（跨书数、保留时长）由系统**根据用户习惯自动学习调整**（如阅读节奏快慢、复习频率、跨书速度），初期采用上述默认值，可在设置页查看并手动调整。
  - **已实现（v1.27）画像阈值自动学习**：阈值按归档节奏（相邻归档间隔中位数 → `review_days`）与确认关联边强度分布低分位（→ `related_strength`）、阅读节奏（→ `warm_threshold`）自动学习调整（归档样本 ≥6 条或确认边 ≥3 条后触发；范围 `warm_threshold 2~5` / `related_strength 45~60` / `review_days 1~7`）；画像页可查看并手动覆盖（`GET/PATCH /api/profile/thresholds`、`POST /api/profile/learn`）。**相关度阈值函数（v1.29，决策 20 落地）**：新增 `services/relatedness.py`（谱系强度主信号 + 暖/冷画像术语覆盖率辅助信号 + 同簇），归档时记录相关度行为样本（`related_samples`），`learn_thresholds` 取行为样本低分位与确认边低分位之**较小者**学习 `related_strength`（行为样本 ≥6）；阈值 API `learning` 返回 `related_sample_count/related_samples_min`，画像页展示。

#### 3.4.2 指定部分的解读（Interpretation）

- 输入：当前章节/选中的段落/用户指定的页码范围。
- 输出：该部分的
  - 核心观点提炼；
  - 逐段/逐要点解释（含术语、背景、例证）；
  - 作者意图与论证结构分析；
  - 与用户已读内容的关联点（可引用其他书，标注出处）。
- 支持按需调整深度：入门/进阶/专业三档（结合画像自动推荐一档）。

#### 3.4.3 概论（Overview / Summary）

- 输入：整本书、某个章节或指定页码范围。
- 输出：结构化概论，包含
  - 一句话主旨；
  - 分层摘要（全书→章节→小节）；
  - 关键概念清单；
  - 结论与启示。
- 支持生成「速读版」「精读版」两种长度。

#### 3.4.4 脑图（图像组件）

- 输入：整本书、章节或选中段落。
- 输出：层级化脑图数据，前端以**图像组件（图形可视化）**呈现，同一张图中展示三层内容：
  - **大纲层**：主题 → 章节 → 分支观点；
  - **细节层**：展开节点可查看要点、例证、关键段落摘录；
  - **定理层**：重要定理、定义、公式以特殊样式节点突出显示。
- 支持缩放、折叠/展开、定位跳转阅读区；支持导出为图片或 Markdown 大纲。
- 技术实现采用 ECharts 树图（见 4.1）。

#### 3.4.5 思考逻辑（Thinking Logic）

- 面向「提升阅读与思考能力」的输出，包括：
  - 论证链还原：前提 → 推理 → 结论；
  - 苏格拉底式提问：针对该部分提出 3~5 个引导性问题；
  - 批判性思考：指出可能的偏见、假设与反例；
  - 输出式学习：生成「用自己的话复述」提示与自测题。
- 生成的思考问题可加入「思考清单」，供用户后续复习作答。

#### 3.4.6 阅读能力改善机制

- 基于画像与历史互动，向用户提供个性化阅读建议：
  - 阅读节奏建议（何时精读、何时略读）；
  - 薄弱概念补强（推荐相关章节/书籍）；
  - 复习提醒（基于间隔重复的要点回顾）；
  - 阅读习惯统计（时长、章节完成率、提问深度）。
- 该模块属于增强项，可先实现画像+建议的基础版（见 7. 里程碑）。

#### 3.4.7 跨书串联与谱系图生成（全局）

- LLM 分析**全部已投喂书籍**，抽取每本书的主题、核心概念、方法/理论流派，计算书与书之间的关联：
  - **主题相似度**：两书主题/领域的重合程度；
  - **概念共现**：两书共享的核心概念与术语数量；
  - **理论传承**：A 书概念/方法论被 B 书继承、发展或批判（可带方向）。
- **关联强度计算（三者结合 + 笔记加权）**：
  - 基础分 = LLM 自动打分 × 关键词共现统计；
  - **笔记加权**：用户在相关概念上做过笔记/划线/标记「不理解」的书籍，其关联权重提高（结合用户笔记修正强度）；
  - 人工标注/反馈（确认/忽略/修改强度）作为最终修正层，反馈结果回写并影响后续计算。
- 输出**书籍级谱系图**（详见 3.6.1）：
  - 节点 = 书籍；边 = 两书关系；
  - 每条边标注**关联强度**（高/中/低或 0~100 分）与**关联原因**（如「共同核心概念 3 个」「理论承接自」）；
  - **边缘高亮标注**：自动识别并标注与每本书「关系最密切」的其他书（最高关联强度边加粗/特殊样式，并显示「与《XX》关系最密切」）。
  - **边方向显式展示**：理论传承等有向关系（承接/发展/批判）以**箭头**显式呈现（A→B 表示 B 承接/发展 A）；主题相似、概念共现等无方向关系为无箭头边。
- 支持点击节点进入该书内部知识图谱；点击边查看两书关联详情（共同概念清单、交叉引用位置）。
- **图谱联动沉淀**：跨书知识谱系构建/更新时，同步**增改受影响书籍的 RAG 与 Skill 资产**（RAG 补入跨书关联与共同概念条目、Skill 融合跨书对比/串联得出的新方法，版本可追溯，见 3.4.9）。
  - **已实现（v1.27）**：跨书谱系重建/懒构建/关联反馈（确认/修改）时自动为受影响书籍补**本地 RAG 联动存根**（`linked_books` 关联条目 + `domain_terms` 术语补水，无 AI 也可执行、内容未变化不写库）；`POST /api/graph/sync` 显式联动时对强度 ≥50 且未忽略的关联执行 **LLM 增量增改**（RAG 补跨书关联与共同概念、Skill 融合跨书新方法，version+1，失败回滚不阻塞）；谱系图页新增「💾 联动沉淀」按钮。

#### 3.4.8 引用出处（统一要求）

- 所有 AI 生成内容（解读/概论/脑图/思考逻辑/跨书问答）中，凡涉及书中内容，必须**标注出处**：书名 + 章节 + 段落位置（如「《XX》第 3 章 · 第 2 节 · 第 5 段」）。
- 脑图/图谱节点与原文位置关联，可点击跳转原文。


#### 3.4.9 书籍沉淀：RAG 与 Skill 能力（读完归档）

- **触发时机**：三者组合触发——手动标记读完 / AI 检测对话完结 / 用户主动发起归档，**优先以用户主动发起归档为触发方式**（见 9.1 决策 17）。
- **首次归档（总结为 RAG 与 Skill）**：触发归档后，若该书为 PDF（统一按页处理，见 3.4.11），**先由多模态图像模型通读全书**——后台按页批量提取并补全缺失页缓存（命中页直接读取、不重复调用），得到完整页缓存；随后以**缓存全文**（叠加章节要点、笔记、「不理解」标记、对话历史）为输入，由**文本模型**总结生成两类资产：
  - **RAG（检索增强知识）**：本书可检索的知识条目——核心概念、章节要点、关键段落片段、术语表，片段保留章节段落出处（符合 3.4.8）；入库后供后续问答与跨书问答检索引用。
  - **Skill（技能能力）**：从本书提炼的可复用技能——方法、步骤、规则、启发式、适用场景描述；入库后由 **AI 自动按任务匹配调用**（结合暖画像与跨书谱系关联度排序，见 9.1 决策 18），无需用户手动指定。
- **再次阅读同一本书并结束**：**直接在原书的 RAG 与 Skill 资产上增改**——合并新笔记、新理解、新对话内容，不创建重复条目；每次更新记录版本；**保留至少 3 个版本，回滚粒度为整条目**（见 9.1 决策 19）。
- 资产以「书」为单位归属：随书删除而删除；用户可查看、编辑、重建。
- **跨书图谱联动**：跨书知识谱系构建/更新时（新增书籍关联、知识点串联），同步增改对应书籍的 RAG 与 Skill 资产——RAG 补入跨书关联与共同概念条目，Skill 融合跨书对比/串联得出的新方法；版本记录与回滚规则与「再次阅读增改」一致。
  - **已实现（v1.27）**：`services/graph_sync.py`（`link_graph_assets`/`link_relation_stubs`/`attach_linked_book_stub`/`link_domain_terms`/`sync_assets_for_relations` + `ai/prompts/rag_link.py`），无 RAG 资产的书先生成轻量存根（`rag_book_input`），LLM 联动输出整体覆盖并保留 `chunks/linked_books/domain_terms`。
- **暖记忆联动**：当阅读到与画像/谱系中已有领域相关的书籍时，该书关键内容自动存入暖画像（见 3.4.1），形成跨书联动的记忆沉淀；「相关领域」按**跨书谱系关联强度**判定（阈值函数形式确定后再定阈值，见 9.1 决策 20）。
- **外部资料直投（不进入阅读流程）**：用户可把需要的 Markdown 文件或书籍直接作为外部输入投喂（资料页 /rag），由 AI 直接总结为 **RAG 与 Skill 资产**，无需先导入书架或读完；与「读完归档」共用同一资产存储（BookAsset）与版本增改规则，可被后续问答检索与技能调用。
#### 3.4.10 输入输出与渲染格式（Markdown + LaTeX）

- **用户输入**：AI 助手输入框支持 **Markdown 语法**（标题、列表、代码块、表格、链接、引用等）与 **LaTeX 数学公式**（行内 `$...$`、块级 `$$...$$`）。
- **LLM 输出**：AI 回答默认以 **Markdown 渲染**，代码块带语法高亮、表格/列表正常展示；**LaTeX 公式正确渲染**（KaTeX/MathJax）。
- **笔记与脑图/图谱导出**：笔记内容支持 Markdown 与 LaTeX；导出 Markdown/PDF 时保留结构与公式。
- **渲染安全**：所有 Markdown/HTML 渲染前做 XSS 消毒，禁止直接执行渲染内容中的脚本；公式渲染不允许注入可执行内容。


#### 3.4.11 PDF 页面的多模态视觉提取与页级缓存（统一适用于全部 PDF）

- **定位**：解决「PDF 无可靠文本」的问题——扫描件无文本层、文本型 PDF 数学公式/符号抽取乱码；本地不做 OCR，改用**多模态 LLM API 作为视觉入口**，按页提取完整页面信息后供文本大模型使用（统一适用于全部 PDF，含文本型）。
- **提取链路**：PDF 书按页触发（触发时机见下）→ 把该页高清原图发送给**多模态 LLM** → 模型输出该页的**完整结构化文本**（正文、公式、表格、图注的文本化描述，Markdown 格式，保留页号）→ 落盘缓存。
- **触发时机**：手动导入 PDF（含文本型）作为知识库时**批量预提取**；阅读时不主动预提取，**仅当用户在当前页对 AI 提问**时触发按需提取；**书籍读完归档时全书批量提取**（补全缺失页缓存，作为 3.4.9 RAG/Skill 总结的全文输入，命中页不重复调用）；页缓存缺失时可按页手动重建（见 9.1 决策 22）。
- **滑动窗口与增量缓存**：提问页 P 的上下文窗口为 `[P-1, P, P+1]` 三页，按边界裁剪——第 1 页提问取 `[1,2]`，最后一页提问取 `[末-1,末]`；窗口内已缓存页直接读取，**仅提取缺失页并缓存**：例如第 5 页提问提取 4、5、6 页后，第 6 页提问只需补提取第 7 页并读取 5、6、7 页。
- **页级缓存**：提取结果**单独缓存到该书籍文件夹**（`data/books/<书目录>/page_text/page_XXX.md`），以页为单位独立缓存；**命中（文件存在且非空）则不再调用多模态 API**，不重复付费。
- **文本模型解读链路**：阅读页 AI 助手、脑图、解读/概论/思考逻辑等能力对 PDF 按页章节提问时，优先使用页缓存文本作为上下文（引用出处标注「第 X 页」，符合 3.4.8）；缓存缺失时回退为「当前页原图附件」（chat 模式）或提示先触发提取。**涂鸦划线区域提问（决策 26）同样走该链路**：优先注入该页缓存文本并附带划线区域裁剪图（chat 模式），缓存缺失时回退为划线区域图片附件或提示先提取。
- **缓存管理**：随书删除一并清理；提供「重新提取本页 / 重建本书页缓存」入口（模型升级、提取质量差或缓存损坏时重建）；页缓存文本可作为该书 RAG 资产的片段来源（见 3.4.9）。
- **配置与隐私**：多模态 API 使用**独立的一套配置**（`vision_base_url / vision_api_key / vision_model`），与文本 API 分开，**无需额度管理**（见 9.1 决策 23）；受「发送书籍内容至模型」隐私开关约束——关闭时不触发多模态提取，也不向文本模型发送缓存文本。

### 3.5 数据与记录

- 阅读进度：按「书 → 章节 → 位置」三级记录。
- 笔记：高亮、批注、思考清单、「不理解」标记，关联到具体章节与段落，可导出。
- 位置书签：按「书 → 位置」保存（PDF 为页号、文本书为章节 + 段落位置），支持命名/备注与跳转；书签列表**默认按时间倒序平铺**，支持**分组/文件夹归类**（可命名/重命名/删除分组、拖拽归组），随书删除。
- PDF 页图涂鸦：按「书 → 页」保存画板标注（笔刷/高亮/橡皮/文本，颜色、线宽与字号可调），支持**撤销上一步（最多 5 步）**与**擦除痕迹**；**每条划线可附加批注（Markdown/LaTeX）或发起对划线区域的 AI 提问**（AI 结合页缓存文本/划线区域图片解读，出处标注「第 X 页」）；不保留历史版本（会话内撤销栈），随书删除。
- AI 历史：每次对话/生成记录（含输入上下文、输出、时间、出处标注），支持查看与删除。
- 图谱数据：书籍关联、知识点及知识点关系持久化存储，支持重建（见 3.6、5）。
- 笔记与「不理解」标记会**同时回写画像与图谱**（作为图谱节点的纳入条件与关联强度加权依据）。
- 书籍沉淀资产：每本书的 RAG 与 Skill 条目（含版本历史），随书删除而删除。

### 3.6 跨书谱系图与书籍内部知识图谱

#### 3.6.1 书籍级谱系图（全局视图）

- 入口：主页顶部「谱系图」视图。
- 展示：全部书籍为节点，书籍间关联为边，边粗细/颜色表示关联强度。
- 每条边显示：关联强度 + 一句话关联原因；与某书关联最强的边高亮，并显示「与哪本书关系最密切」；**有向关系（理论传承/承接/发展/批判）以箭头显式展示方向**。
- **聚类与分层展开**（书籍数量大时的组织方式）：
  - 第一层：按**用户设置的初步分类 tag** 聚类（用户显式分类优先）；
  - 无显式 tag 的书：**默认继承所在文件夹的 tag**（同一文件夹默认同 tag）；
  - 仍无 tag 的书：按**领域自动聚类**（LLM/共现分析推断领域）；**自动聚类/自动生成的领域名必须是领域专业术语**（剔除「定理/定义/方法/教程/讲义/习题/答案/笔记/前言」等泛化词与作者/元数据噪声，如「茆诗」，设计见 `docs/知识图谱聚类算法.md` §4/§9）；用户可在 `backend/domain_terms.txt`（与 .env 同级）维护**专业术语词库**：用户区术语命名时**优先匹配**，系统自动学习的术语写入缓存区作为以后优先备选；
  - **两阶段分类（pre-classify / post-classify）**：先以 tag/文件夹/关键词共现做**粗略 pre-classify**（导入/图谱构建时）；得到该书 **RAG/Skill 资产**（读完归档 / 外部资料直投 / 图谱联动更新，见 3.4.9）后，基于资产 `summary / key_points / Skill 用法` 做**更精细的 post-classify**——细化 / 合并 / 迁移簇、以资产代表性术语重命名簇；用户 tag/文件夹为硬约束仍优先；资产版本变化时自动失效重算（设计细节见 `docs/知识图谱聚类算法.md` §9）；
  - 领域内再展示书籍与书籍间关联；数百本规模时先显示领域层，点入领域后展开书籍层。
- 交互：
  - 点击节点 → 进入该书内部知识图谱（3.6.2），或弹出该书卡片（进度、状态、tag）。
  - 点击边 → 展示两书关联详情（共同概念、交叉引用位置、传承方向）。
  - 支持以某本书为中心展开（仅显示其相邻节点）、缩放与拖拽。
- **新书导入后增量更新（已实现 v1.28）**：导入成功后自动调用 `incremental_cross_book_graph` 只补新书与既有书的关联边（同聚类低分「主题相似」边，幂等、不动既有关联），并补本地 RAG 联动存根；用户可对单条关联进行「确认/忽略/修改强度」，人工反馈回灌计算（见 3.4.7）；**每次图谱更新同步增改受影响书籍的 RAG/Skill 资产，并将相关领域书籍内容存入暖画像**（见 3.4.9、3.4.1）。

#### 3.6.2 书籍内部知识图谱（点击书籍后）

- 入口：谱系图中点击书节点、书架中「查看本书知识图谱」、阅读区入口。
- **知识点覆盖范围（三层粒度）**：
  - **章节级**（必含）：每章的核心概念、方法、论点，按章节组织；
  - **重要段落级**：重要定理、定义、关键例证（自动识别并纳入）；
  - **用户标记级**：用户标记「不理解」或做过笔记/高亮的段落，**自动纳入图谱**并在节点上标注（如「有笔记」「用户不理解」）。
- 展示该书**内部知识点的分布谱系与串联**：
  - 节点 = 知识点，按重要性分级展示；节点沿**章节/阅读顺序**分布，形成知识点分布谱系——可看到知识点密度、重点章节与演进脉络；
  - 边 = 知识点间关系：前置依赖（前置→后置）、总-分、承接发展、对比并列；
  - 时间轴/分布条：概览全书各章节知识点密度，点击密度块跳转到对应章节。
- 交互：
  - 点击知识点 → 联动阅读区定位到原文位置，高亮该知识点段落；
  - 右键/侧栏：让 AI 讲解该知识点（结合个性化画像），并显示「该知识点在哪些其他书中也出现/被发展」——**跨书跳转**至对应书籍的对应章节；**跨书检索（已实现 v1.28）**：书内图谱点击知识点节点打开「知识点详情」弹窗，展示「还出现在 N 本书」（书名 / 命中知识点 / RAG 要点），可跳转对应书的书内图谱或阅读原文；
  - 支持导出该书知识图谱为图片或 Markdown 大纲。
- 图谱由 LLM 解析生成并入库缓存；新书导入或书籍内容更新后，相关子图自动增量重建。

---

## 4. 非功能需求

### 4.1 技术栈（建议，待技术评审）

- 后端：Python（FastAPI/Flask）+ SQLite（本地存储）。
- 前端：单页应用（Vue 3 或 React），书架/阅读器/图谱组件。
- 文本解析：PDF 用 `pypdf`/`pdfplumber`，Markdown 用原生解析，EPUB 用 `ebooklib`。
- 图谱与脑图可视化：**ECharts**（常用、成熟、中文文档全，关系图/树图能力足够）；如复杂交互超出其能力，可升级为 AntV G6。
- AI 接入：OpenAI 兼容 API（可配置 base_url、api_key、model）。
- 部署形态：**仅本地单用户**——本地运行（如 `127.0.0.1:8321`），无账号体系、无局域网共享；数据不出本机（除模型 API 调用外）。

### 4.2 API 接入要求

- **可配置**：`base_url`、`api_key`、`model`、`temperature` 等参数在设置页可改，支持多厂商（OpenAI、DeepSeek、通义等兼容接口）。
- **上下文管理**：长文需做分块/摘要策略（如整本书先建索引，回答按章节取上下文）；图谱构建按批次处理，控制 token 成本。
- **失败处理**：API 不可用/限流时给出明确提示，支持重试与降级（本地规则式摘要/关键词共现兜底）。
- **流式输出**：AI 回答建议流式展示，提升体验。
- **异步任务**：书籍解析、图谱构建、画像更新等耗时任务后台异步执行，前端展示进度。
- **多模态视觉入口（PDF，统一含文本型）**：PDF 的页面信息由独立配置的多模态 LLM 逐页提取为文本并**按页缓存到书籍目录**；缓存命中不重复调用；文本大模型基于缓存解读（见 3.4.11）。

### 4.3 数据存储

- 书籍文件：本地目录存储（保留原文件）。
- 元数据/进度/笔记/画像/图谱：SQLite。
- 解析缓存：章节文本、AI 生成结果、图谱计算结果可落盘缓存，避免重复解析/重复付费。
- 扫描页文本缓存：`data/books/<书目录>/page_text/`，按页 Markdown 缓存多模态提取结果，随书删除清理（见 3.4.11）。
- 图谱重建：提供「重建全部图谱」「重建单书图谱」操作。

### 4.4 安全与隐私

- API Key 本地保存，不写进代码仓库，不在前端明文回显。
- 书籍与笔记均为用户私有数据；AI 请求仅发送必要上下文（当前章节/选中段落/图谱抽取所需片段），并提供「是否将书籍内容发送至模型」的开关。
- 提供「清除全部数据」「删除单书（连同 AI 记录与图谱节点）」能力。

### 4.5 性能与可用性

- 千页级 PDF 打开与章节切换应在秒级内完成（解析异步+缓存）。
- 阅读滚动流畅，长章节可分段懒加载。
- 书架加载百本书无卡顿。
- 图谱渲染：百节点级图谱交互流畅；千节点级采用「领域聚类 + 分层展开」（3.6.1），避免一次性渲染全图。
- 图谱构建、画像更新为后台任务，不阻塞阅读操作。

### 4.6 一键启动与外部输入

- 提供根目录 start.bat **一键启动**：自动检查依赖（Node / 后端 venv / 前端 node_modules）→ 启动后端（8321）与前端（5173）→ 自动打开浏览器；前端启动命令按 pnpm → node node_modules\vite\bin\vite.js → 本地 pnpm → 运行时 pnpm 顺序解析，系统 PATH 无 pnpm 时也能正常启动。


---

## 5. 数据模型（草案）

```
Folder        id, name, parent_id(可空，支持嵌套), created_at
Book          id, title, author, cover, format(pdf/md/txt/epub), file_path, status(未读/在读/读完),
              progress, total_chapters, created_at, last_opened_at, graph_built(图谱状态),
              tags_json(用户分类 tag 列表), folder_id(可空，所属文件夹)
Chapter       id, book_id, index, title, content_path/content_text, word_count, read_flag
ReadingLog    id, book_id, chapter_id, position, updated_at
Note          id, book_id, chapter_id, quote_text, note_text, type(高亮/批注/思考/不理解), created_at
Bookmark      id, book_id, chapter_id(可空，文本书定位), page_index(可空，PDF 整页), para_pos(可空，段落位置),
              title, note, created_at
ChatMessage   id, session_id, role(user/assistant), content, ref_book_id, ref_chapter_id,
              ref_para_pos(出处标注), created_at
UserProfile   id, layer(cold/warm/hot), dimension, value_json, updated_at   # 三层画像
Setting       key, value                                    # api 配置、阅读偏好、画像迁移阈值（可学习）等
BookAsset     id, book_id, kind(rag/skill), content_json, version, created_at, updated_at  # 读完归档资产；跨书图谱更新时联动增改

# —— 图谱相关 ——
BookRelation    id, book_a_id, book_b_id, strength(0~100), direction(无/承接/发展/批判),
                relation_type(主题相似/概念共现/理论传承), reasons_json(关联原因列表),
                user_feedback(确认/忽略/修改), updated_at
KnowledgePoint  id, book_id, chapter_id, para_pos(段落位置), title, summary, importance,
                level(章节级/重要段落/用户标记), aliases_json, created_at
KpRelation      id, from_kp_id, to_kp_id, relation_type(前置依赖/总分/承接/并列), strength, note
```

页图涂鸦标注（PDF 按页阅读）：`data/books/<书目录>/annotations/page_XXX.json`（元素 JSON：`stroke` 笔画——工具/颜色/线宽/坐标序列，可选 `note` 批注文本与 `note_meta`（创建/更新时间、Markdown/LaTeX）；`text` 文本——内容/颜色/字号/位置），随书删除清理；**撤销仅在会话内生效（最多 5 步），不持久化历史版本**；划线批注随元素持久化。

---

## 6. 页面与交互（概要）

| 页面 | 布局 | 关键交互 |
| --- | --- | --- |
| 主页（书架） | 左：近期阅读；右：书架 | 搜索、筛选、文件夹归类、设置 tag、导入、进入阅读 |
| 主页（谱系图） | 全屏关系图 | 领域聚类/分层展开、缩放拖拽、点节点进书、点边看关联详情 |
| 书籍内部图谱 | 知识图谱 + 知识点分布条 | 点知识点联动阅读区、跨书跳转、导出 |
| 阅读页 | 左：竖向书架；中：阅读区；右：AI 助手 | 章节切换、划词操作、笔记/不理解标记、进度记录、AI 生成、图谱入口 |
| 设置页 | 单栏表单 | API 配置（文本 / 多模态）、画像查看/重置、图谱重建、数据管理 |
| 画像页 | 三层卡片 | 查看/编辑冷·暖·热三层画像，手动调整 |

---

## 7. 里程碑计划（建议）

- **M1 需求冻结**：本文档评审通过。**✅ 已完成（v0.8 冻结，v1.x 为增量修订）**。
- **M2 基础框架**：项目脚手架、数据库、书籍导入解析（PDF/MD/TXT/EPUB）、书架与阅读页静态布局。**✅ 已完成**（解析器含 PDF/EPUB/MD/TXT 切章；书架主页、三栏阅读页、设置/资料/画像/谱系图页骨架）。
- **M3 阅读闭环**：章节切换、进度记录、笔记/高亮/「不理解」标记（双入口）、笔记导出。**✅ 已完成（v1.31）**：正文 Markdown/LaTeX 渲染、进度保存与恢复、已读标记、笔记 CRUD 与 **Markdown 导出**已上线并通过冒烟；**PDF 导出随 M10 上线**（`?fmt=pdf`，PyMuPDF 内嵌 CJK，见 M10）；**位置书签（全格式）+ PDF 页图涂鸦标注随 M6 完成**（见 3.3/3.5、9.1 决策 22~25）。
- **M4 AI 接入**：API 配置、对话流式输出、按章节上下文问答、引用出处标注。**✅ 已完成**（设置页 API 配置与测试连接；SSE 流式输出；按章节上下文 + RAG/Skill 注入问答；【第X章 第Y段】引用出处解析与展示；对话历史按书保存/清空；隐私开关生效；**M4 补充：PDF/EPUB 封面渲染、扫描版按页读图、提问附带当前页图片、PDF 统一按页处理（含文本型）**；**PDF 统一按页处理（含文本型）与多模态视觉提取/页级缓存移至 M7**（见 3.2/3.4.11、9.1 决策 2/21~23））。
- **M5 AI 增强**：解读/概论/脑图（图像组件含定理层）/思考逻辑四类生成、划词操作。**✅ 已完成**（四类预设；脑图 ECharts 树组件三层展示大纲/细节/重要定理 + 出处标注 + 节点点击跳转原文 + 复制大纲/下载大纲 .md/导出 PNG/插入为批注；脑图节点名自动清洗 LaTeX/Markdown 记号；划词 AI 操作：解释选中段/该段脑图/加入思考清单；笔记正文 Markdown/LaTeX 渲染）。
- **M6 阅读体验补全**：位置书签（全格式）+ PDF 页图涂鸦。**✅ 已完成**（书签：`bookmarks` 表 + CRUD/分组 API + 阅读页书签抽屉倒序平铺/分组视图/跳转定位；涂鸦：`annotations/page_XXX.json` 读写 API + `PageDoodleCanvas` 画板（笔刷/高亮/橡皮/文本、撤销最多 5 步会话内）+ `DoodleToolbar` + 划线选中批注/划线区域提问（chat 携带 `crop_image`）；随书删除级联清理）。**修复轮（v1.14）**：画板尺寸改为以页图实际渲染尺寸同步且仅在尺寸变化时重置缓冲区（修复旧数据导致 mounted 异常、画布卡 1×1 无法绘制/文本不渲染）；涂鸦点坐标兼容旧版 `{x,y}` 对象与新版 `[x,y]` 数组（读取时自动迁移并回写）；涂鸦工具栏由页底移入阅读页顶部工具栏，置于「第 X 页」与「第 X 章/共 XX 章」之间，仅 PDF 按页阅读时显示；撤销栈切页时清空。
  - 位置书签：任意格式书籍可添加书签并跳转（PDF 整页 / 文本书章节+段落）；书签列表**默认按时间倒序平铺**，支持**分组/文件夹归类**（命名/重命名/删除、拖拽归组）；书签可命名/备注，随书删除。
  - PDF 页图涂鸦：按页阅读页图叠加画板（笔刷/高亮/橡皮/**文本输入**，颜色/线宽/字号可调）；支持**撤销上一步（最多 5 步，会话内）**与**擦除痕迹**；**每条划线可选中后附加批注（Markdown/LaTeX）或发起对划线区域的 AI 提问**（结合页缓存文本/划线区域图片，出处标注「第 X 页」）；标注随页持久化（`annotations/page_XXX.json`）、再次打开叠加显示，随书删除。
  - 验收对应：验收 22、23。
- **M7 PDF 统一处理与多模态视觉提取**：PDF（含文本型）统一按页处理 + 多模态视觉提取与页级缓存。**✅ 已完成（v1.15）**（承接 M4 补充规划，见 3.2/3.4.11、9.1 决策 2/21~23）。
  - PDF 统一按页处理：不再区分扫描版/文本型，统一按原始页切章（`第 N 页`，content 为空）、阅读页直接展示原图（按内嵌原图分辨率渲染）；本地抽取文本落盘 `local_text/page_XXX.txt` 仅作全文检索索引，不用于正文展示与 AI 上下文；封面/页图端点对新旧 PDF 一致生效（旧版文本型 PDF 仍按旧行为展示，可删除后重新导入升级）。
  - 多模态视觉提取：设置页新增独立多模态配置（`vision_base_url / vision_api_key / vision_model / vision_timeout / vision_verify_ssl`，无需额度管理，含「测试视觉连接」）；新增 `services/vision_extract.py` 提取服务与 `page_text/page_XXX.md` 页级缓存；**手动导入批量预提取**（后台任务，受隐私开关约束）；阅读时**当前页对 AI 提问**触发按需提取，`[P-1,P,P+1]` 滑动窗口增量缓存（第 1 页/末页裁剪、仅补缺失页，命中不重复调用）；文本大模型基于缓存解读并标注「第 X 页」出处（对话与脑图链路均已接入，缓存缺失回退当前页原图附件）；阅读页提供「重提本页 / 重建页缓存 / 缓存 x/y 页」入口；随书删除清理。
  - 受「发送书籍内容至模型」隐私开关约束：关闭时不触发提取、不发送缓存文本。
  - 验收对应：验收 1、7、21。
- **M8 知识图谱**：跨书关联计算与书籍级谱系图（含 tag+领域聚类分层）、书籍内部知识图谱（章节级+重要段落+用户标记级）、节点联动阅读区与跨书跳转、图谱缓存与重建、**图谱更新联动 RAG/Skill 沉淀与暖画像写入**。**✅ 已完成（v1.32）**：跨书关联计算（关键词共现余弦分 + 笔记加权 + 同聚类低分「主题相似」边）、聚类分层（tag → 文件夹名 → 领域自动聚类）、书内知识图谱（章节级 / 重要段落 / 用户标记三层，章节顺序「前置依赖」+ 同章重要段落「承接」）、懒构建（`graph_built` 标志）与重建（全局 / 单书）、关联人工反馈（确认 / 忽略 / 修改强度回写）、前端谱系图与书内图谱 ECharts 渲染（聚类筛选、最强关联边高亮、方向箭头、层级筛选、跳转阅读、导出 PNG）已实现。**新书导入自动增量更新（v1.28）**：`upload_book` 导入成功后调用 `incremental_cross_book_graph`（`services/graph/cross_book.py`）只补新书与既有书关联边（同聚类低分「主题相似」边、幂等、不动既有关联），随后补本地 RAG 联动存根；**跨书知识检索（v1.28）**：`GET /api/graph/knowledge/{kp_id}/appears-in` 返回「该知识点还出现在哪些书」（其他书内知识点 title/summary 关键词重叠 + 其他书 RAG `key_points` 命中，来源书不计），前端书内图谱节点点击打开「知识点详情」弹窗（跨书列表 + 跳转对应书图谱 / 阅读原文）。**图谱联动 RAG/Skill 沉淀（v1.27）**：跨书谱系重建/懒构建/关联反馈（确认/修改）时自动补本地 RAG 存根（`linked_books` + `domain_terms`，无 AI 也可执行、内容未变化不写库）；`POST /api/graph/sync` 显式联动对强度 ≥50 且未忽略的关联执行 LLM 增量增改（`ai/prompts/rag_link.py`，RAG 补跨书条目、Skill 融合新方法、version+1、失败回滚不阻塞、随后 post-classify）；谱系图页新增「💾 联动沉淀」按钮，并修复「重建图谱」按钮原先仅刷新未重建的问题。**LLM 打分与方向/原因增强（v1.32，M8 收尾）**：新增 `services/graph/llm_score.py`（提示词 `ai/prompts/graph_edge.py`）——跨书重建与新书导入增量时，对关键词候选对做**有界 LLM 打分**（`MAX_LLM_PAIRS=40`，按关键词分降序截断，控制数百本书的成本），LLM 输出关联强度（0~100）、理论传承方向（`from_book`：A/B/null，配合 承接/发展/批判）、关联类型（理论传承/概念共现/主题相似/应用扩展）与 3~6 条关联原因；强度取 max(关键词分, LLM 分)，方向/类型/原因以 LLM 为准；未配置 AI / 调用失败 / 非法输出一律回退关键词分，不阻塞图谱构建；新增 `book_relations.from_book_id` 列（`_ensure_columns` 增量迁移），谱系图按 `from_book` 显式画箭头并展示「A → B」方向与原因；RAG 联动存根 `linked_books` 补 `direction` 字段；新增 7 项 pytest（后端 135 通过、ruff 全绿、vue-tsc + vite build 通过）。**暖画像写入已实现**（相关领域书入暖记忆，见 M9）。
- **M9 个性化与沉淀**：冷/暖/热三层画像、书籍 RAG 与 Skill 沉淀（读完归档与增量增改）、**归档视觉通读（读完归档时图像模型通读 PDF 全书并缓存，再以缓存全文由文本模型总结 RAG/Skill，见 3.4.9/3.4.11）**、阅读建议、复习提醒。**✅ 已完成（v1.29）**：第一轮——读完归档链路（`POST /api/books/{id}/archive`：PDF 视觉通读补齐页缓存→缓存全文总结 RAG/Skill→标记读完）+ post-classify 基础版（见 v1.20）。第二轮——**三层画像已实现**：`profile_service`（热=当前书细节、暖=近 1~2 本+相关领域、冷=长期偏好）；**归档迁移**（跨 1 本热→暖、跨 3 本暖→冷、>3 本全部沉淀冷，阈值常量 `WARM_TO_COLD_THRESHOLD=3`/`KEEP_RECENT=2`，后续按用户习惯学习）；**相关领域书入暖记忆**（同 post 簇或跨书关联边 strength ≥60 判定，阈值待决策 20 定稿）；**阅读行为实时回写**（阅读进度/章节脉络、笔记/划线/「不理解」→ 热画像）；画像 API（`GET /api/profile`、`POST /api/profile/reset`）与前端画像页（/profile 三层卡片+重置）。第三轮——**资产增量增改**：再次归档时已有 RAG/Skill 资产走增量提示词（`rag_skill.py` `INCREMENTAL_SYSTEM_PROMPT`/`build_incremental_user_prompt`：旧资产概要 + 本轮新增笔记/划线/「不理解」/最近对话 + 全书正文），原资产 `version+1`、id 不变（见 v1.22）；**post-classify 簇合并与重命名**：`merge_and_rename_clusters`（两簇后验关键词并集重叠 ≥ `T_MERGE=5` 合并进书更多的主簇、簇名取簇内出现书最多的后验术语众数、冲突跳过，只处理 `classify_source=post` 的书，归档成功后触发，失败不阻塞）；**阅读建议基础版**：`recommendation_service`（习惯统计 stats / 薄弱概念「不理解」关键词聚合 top3 / 复习提醒「归档 ≥1 天标 due」/ 节奏文案 start·building·stable 三档本地规则），`GET /api/profile/recommendations`，画像页新增「阅读建议」卡片。第四轮——**画像阈值自动学习（v1.27）**：`services/profile_learning.py`（阈值持久化键 `profile.warm_threshold / profile.related_strength / profile.review_days / profile.learning_state`，归档时记录节奏样本 `record_archive`，样本 ≥6 或确认边 ≥3 后 `learn_thresholds` 自动调整：归档间隔中位数 → `review_days`、节奏快慢 → `warm_threshold`、确认关联低分位 → `related_strength`，样本不足回退默认常量 `WARM_TO_COLD_THRESHOLD=3`/`RELATED_STRENGTH_THRESHOLD=60`/`REVIEW_AFTER_DAYS=1`）；`profile_service`/`recommendation_service` 统一走 `get_thresholds(db)`；**图谱更新联动 RAG/Skill 增量增改（v1.27）**：`services/graph_sync.py` 本地存根 + LLM 增量增改（见 M8/3.4.7/3.4.9）；画像 API 补 `GET/PATCH /api/profile/thresholds`、`POST /api/profile/learn`，画像页新增「画像阈值」卡片（查看/保存/立即学习）。**第五轮——相关度阈值函数（决策 20 落地，v1.29）**：新增 `services/relatedness.py`——score = max(跨书谱系关联强度, 0.5×暖画像术语覆盖率×100 + 0.5×冷画像术语覆盖率×100)，同 post 簇直接判定相关（分数抬升到阈值）；`profile_service` 归档入暖记忆改走该函数（`related_books` 条目含 `score`）；归档时记录相关度行为样本（`record_relatedness_sample` → `related_samples`，保留 48 条），`learn_thresholds` 取行为样本 25% 低分位与确认边 25% 低分位之**较小者**学习 `related_strength`（样本 ≥6 触发，裁剪 45~60）；阈值 API `learning` 返回 `related_sample_count/related_samples_min`，画像页展示。**待办已清空**：相关度阈值函数已以自动学习 + 手动覆盖落地，阈值随更多样本继续收敛（样本持续累积）。
- **M10 打磨**：导出能力（含笔记 PDF）、性能优化、验收测试。**✅ 已完成（v1.31）**：
  - **笔记导出 Markdown + PDF**：`GET /api/books/{id}/notes/export?fmt=md|pdf`（默认 md）；新增 `services/note_export.py`（Markdown 结构化导出沿用 M3 格式；PDF 用 PyMuPDF 内嵌 CJK 字体生成 A4，保留章节定位/引文/笔记正文，LaTeX 公式以源码保留）；前端笔记抽屉新增「导出 PDF」入口。对应验收 3/13。
  - **性能优化**：后端页图目标宽度按 (书, 页) 进程内缓存（`parsers/pdf.py` `_target_width_cached`，避免每次翻页重开 PDF）；页图响应 `Cache-Control: private, max-age=600`（页图不可变，10 分钟内翻页走浏览器缓存）；前端 PDF 阅读预加载相邻页（`.page-preload` 隐藏 img 预热浏览器缓存）；vite vendor 分包（vue/element-plus/echarts/katex+markdown-it 独立 chunk，echarts 仅图谱/脑图页按需加载，ReaderView 主 chunk 189KB→56KB）。
  - **验收测试**：新增 `tests/test_export_m10.py`（MD 默认导出/PDF 内容含中文与 LaTeX 源码/格式参数校验）；`tests/conftest.py` 隔离真实 `.env` 的 AI/视觉配置（修复 9 项环境性失败：后台视觉预提取与测试竞争、未配置 API 断言、思考模式默认参数），后端全量 **128 通过**、ruff 全绿、vue-tsc + vite build 通过。

---

## 8. 验收标准（示例）

1. 可导入 PDF、Markdown、TXT、EPUB 书籍并正常解析为章节；PDF/EPUB 提取封面并在书架渲染；**PDF（含文本型）统一按原始页数切章**、阅读页直接展示原图（按内嵌原图分辨率渲染）；不做本地 OCR，可选「多模态 LLM 视觉提取」生成页文本缓存供 AI 解读（见 3.4.11）。
2. 主页左右两栏正确展示近期阅读与书架，可搜索、筛选、设置分类 tag。
3. 阅读页三栏布局符合 3.3 描述，进度记录准确，重新打开回到上次位置；笔记（高亮/批注/思考/不理解）可用，可导出 Markdown 与 PDF 两种格式；「不理解」标记可通过划词菜单与阅读区侧边按钮两个入口触发。
4. AI 助手可对指定章节/选中段落生成解读、概论、脑图、思考逻辑；脑图以图像组件展示大纲、细节与重要定理；结果均可复制/导出并**标注章节段落出处**。
5. 画像按冷/暖/热三层维护：热画像随当前书阅读实时更新，暖画像随读新书切换，三层均可查看/编辑/重置。
6. API 配置可切换，失败时提示友好，流式输出正常。
7. 隐私开关生效：关闭后不向模型发送书籍正文；对 PDF（按页处理）提问时可按设置附带当前页图片（chat 模式，需模型支持视觉输入），关闭隐私开关时不附带页图。
8. 可基于全部投喂书籍生成**书籍级谱系图**：节点为书、边带关联强度与原因，能自动标注「与哪本书关系最密切」；强度计算融合 LLM 打分、关键词共现与**用户笔记加权**，人工反馈生效；**有向关系以箭头显式展示**；新书导入后增量更新。
9. 书籍数量大时，谱系图**先按用户 tag、无 tag 按领域自动聚类**，再分层展开。
10. 点击书籍可查看**内部知识点分布谱系**：覆盖章节级知识点、重要段落、用户笔记/「不理解」段落；点击知识点可联动阅读区定位原文，并可跨书跳转到其他书的相关章节。
11. 图谱支持用户反馈（确认/忽略/修改关联）且反馈生效；图谱可重建与导出。
12. 三层画像按「跨书数」自动迁移（跨越 1 本 → 热转暖、3 本 → 暖转冷、>3 本 → 沉淀冷画像），阈值可由用户习惯学习调整，可在设置页查看与手动调整。
13. EPUB 以保留原排版（图文混排）形式阅读展示。
14. 结束一本书的对话后，可自动生成该书 **RAG 与 Skill 资产**（含出处标注）；再次阅读同一本书并结束后，**在原资产上增改**（版本可追溯），不产生重复条目。
15. RAG 资产可被后续（跨书）问答检索引用；Skill 资产由 AI 在后续相关阅读/任务中**自动按任务匹配调用**。
16. 跨书谱系图构建/更新后，受影响书籍的 **RAG 与 Skill 资产同步增改**（含跨书关联条目，版本可追溯）；阅读与已读领域相关的书籍后，该书关键内容**自动存入暖画像**（相关领域按跨书谱系关联强度判定，决策 20）。
17. 用户输入与 LLM 输出支持 **Markdown**（标题/列表/代码块/表格）与 **LaTeX 公式**渲染（行内与块级）；笔记同样支持，渲染无 XSS 风险。
18. 书架支持**文件夹归类**（新建/重命名/删除、拖拽移动，**支持多级嵌套**），可按文件夹浏览；支持**批量移动、批量设 tag**，书架/阅读页可调整书籍与文件夹位置；同一文件夹内无显式 tag 的书**默认继承文件夹 tag**，并用于谱系图聚类。
19. 提供 start.bat **一键启动**：检查依赖后同时拉起后端与前端并打开浏览器；系统 PATH 无 pnpm 时仍能正常启动。
20. 用户可将外部 Markdown/书籍文件直接投喂（资料页 /rag），AI 将其总结为 RAG 与 Skill 资产（含出处），无需先导入书架或读完；资产可被后续问答检索与技能调用。
21. PDF（含文本型）在配置多模态 LLM 后，**用户在当前页对 AI 提问**时按 `[P-1,P,P+1]` 滑动窗口（第 1 页/末页裁剪）提取页面完整信息并缓存到书籍目录；窗口内已缓存页直接读取、仅补提取缺失页，缓存命中不重复调用多模态 API；文本大模型基于页缓存解读并标注「第 X 页」出处；关闭隐私开关时不触发提取与发送。
22. 任意格式书籍均支持**位置书签**并可从书签跳转：PDF 为整页书签（页号）、文本书为「章节 + 段落位置」；书签可命名/备注；书签列表**默认按时间倒序平铺**，支持**分组/文件夹归类**，随书删除。
23. PDF 按页阅读页支持**画板式涂鸦标注**（笔刷/高亮/橡皮/文本输入，颜色、线宽与字号可选），支持**撤销上一步（最多 5 步）**与**擦除痕迹**；**每条划线可选中后附加批注（Markdown/LaTeX）或发起对划线区域的 AI 提问**（结合页缓存文本/划线区域图片，出处标注「第 X 页」）；标注随页持久化、再次打开叠加显示，随书删除清理。

---

## 9. 需求决策与开放项

### 9.1 已确认决策（评审结论）

| # | 问题 | 决策 |
| --- | --- | --- |
| 1 | 部署形态 | 仅本地单用户，无多用户/局域网共享 |
| 2 | PDF 处理方式 | 不需要本地 OCR；**PDF 统一按页渲染原图 + 多模态视觉提取**（含文本型，避免数学符号抽取乱码）；本地文本抽取仅作全文检索索引；笔记功能为标配 |
| 3 | 脑图呈现形式 | 图像组件（ECharts 树图），同时展示大纲、细节与重要定理 |
| 4 | 画像持久化 | 三层画像：冷（长期低频）/ 暖（近 1~2 本书）/ 热（当前书细节） |
| 5 | 移动端适配 | 不需要，仅桌面端 |
| 6 | 书籍格式 | 支持 PDF、Markdown、TXT、EPUB |
| 7 | AI 引用出处 | 需要，标注书名+章节+段落位置 |
| 8 | 关联强度计算 | LLM 打分 + 关键词共现 + 人工反馈，并加权用户笔记 |
| 9 | 图谱粒度 | 章节知识点必含；重要段落、用户笔记/不理解段落也纳入 |
| 10 | 图谱组件 | 常用组件：ECharts（复杂场景备选 AntV G6） |
| 11 | 大数量聚类 | 先按用户分类 tag，无 tag 按领域自动聚类，再分层展开 |
| 12 | 画像迁移规则 | 系统自动管理、阈值随用户习惯学习；初步条件：对话内容跨越 1 本→热转暖、3 本→暖转冷、>3 本→沉淀冷画像 |
| 13 | 笔记导出格式 | Markdown 与 PDF 两者都要 |
| 14 | 「不理解」交互形式 | 划词菜单按钮 + 阅读区侧边按钮，两个入口 |
| 15 | EPUB 排版 | 保留原排版（图文混排） |
| 16 | 边方向展示 | 谱系图中显式展示箭头（理论传承/承接/发展/批判方向） |
| 17 | 「结束对话」触发 | 三者组合（手动标记读完 / AI 检测对话完结 / 用户主动发起归档），**优先以用户主动发起归档为触发方式** |
| 18 | Skill 调用方式 | **AI 自动按任务匹配调用**（结合暖画像与跨书谱系关联度排序），无需用户手动指定 |
| 19 | RAG/Skill 版本策略 | **保留至少 3 个版本**；回滚粒度为**整条目**（不按分块） |
| 20 | 相关领域判定 | 基于**跨书谱系关联强度**判定（主信号）+ 暖/冷画像术语覆盖率辅助信号 + 同 post 簇；**阈值函数已定稿（v1.29）**：score = max(谱系强度, 0.5×暖画像覆盖率 + 0.5×冷画像覆盖率)，related = 同簇 或 score ≥ related_strength；阈值由自动学习（行为样本/确认边低分位取 min，45~60）产出、设置页手动覆盖 |
| 21 | 文件夹层级与交互 | 支持**多级嵌套**；支持**批量移动、批量设 tag**；书架/阅读页可调整书籍与文件夹位置 |
| 22 | 多模态提取触发 | 手动导入 PDF（含文本型）作为知识库时**批量预提取**；阅读时不预提取，**用户在当前页对 AI 提问**时按需提取；按 `[P-1,P,P+1]` 滑动窗口（第 1 页/末页裁剪），仅补提取缺失页（增量缓存）；**读完归档时全书批量提取**（补全缺失页缓存，作为文本模型总结 RAG/Skill 的全文输入） |
| 23 | 多模态配置 | 多模态使用**独立的一套 API 配置**（base_url/api_key/model），**无需额度管理** |
| 24 | 书签列表组织 | 平铺列表（默认，按时间倒序），**支持分组/文件夹归类**（可命名/重命名/删除、拖拽归组） |
| 25 | 涂鸦撤销与历史 | 支持**撤销上一步（最多 5 步）**、**擦除痕迹**、**输入文本**等基础功能；**不保留历史版本**（撤销栈仅会话内） |
| 26 | 涂鸦划线批注与提问 | 每条划线可选中后**附加批注**（Markdown/LaTeX，随划线持久化）或**发起对划线区域的 AI 提问**；提问结合该页页缓存文本或划线区域图片，出处标注「第 X 页」，受隐私开关约束 |
| 27 | 文本模型思考模式与精细参数（DeepSeek） | 文本 AI 的 `thinking`（`AI_THINKING_TYPE`：enabled/disabled）与 `reasoning_effort`（`AI_REASONING_EFFORT`：low/medium/high/max，`thinking=disabled` 时不发送）**仅 chat 模式**写入请求体；`AI_MAX_TOKENS` 默认 32K、最大 64K（含思考 token）；思考模式下 `temperature/top_p/frequency/presence` 官方说明「设置不报错但不生效」；**responses 模式**仅将 `max_tokens` 映射为 `max_output_tokens`，thinking 参数待定稿（见 9.2 开放项） |

### 9.2 开放项

（上一版开放项 1~7 已全部定稿，见 9.1 决策 17~23；书签列表组织与涂鸦撤销/历史已定稿，见 9.1 决策 24、25；文本思考模式已定稿，见 9.1 决策 27。）当前开放项：**1 条**——DeepSeek **responses 模式**下思考模式参数（`thinking/reasoning_effort`）的传递方式待定稿（chat 模式已定稿，见 9.1 决策 27；待 DeepSeek 官方 responses 接口支持后再定）。

---

## 10. 需求变更记录

| 版本 | 日期 | 变更内容 | 说明 |
| --- | --- | --- | --- |
| v1.33 | 2026-08-03 | 重构轮：删除死代码 + 统一 LLM JSON 解析 | 删除 `services/graph_service.py`（889 行，图谱包拆分后遗留死代码，全库无引用，`services/graph/` 包已完整替代）；新增 `app/ai/parsing.py`（`parse_llm_json`：容忍代码围栏与前后杂文的 JSON 提取），统一 `graph_sync`/`llm_score`/`rag_service`/`mindmap_service` 四处重复解析；后端 135 项 pytest + ruff 全绿；关联文档升技术栈 v1.39、使用手册 v1.63、重构 v1.23 |
| v1.32 | 2026-08-03 | M8 完成：LLM 打分与理论传承方向/关联原因增强 | §7 M8 里程碑改为 ✅ 已完成（v1.32）：新增 `services/graph/llm_score.py` 与 `ai/prompts/graph_edge.py`，跨书重建/导入增量对候选对做有界 LLM 打分（MAX_LLM_PAIRS=40），输出强度/方向（from_book+承接发展批判）/关联类型/原因，强度取 max、方向类型原因以 LLM 为准，失败回退关键词分；`book_relations` 新增 `from_book_id` 列（增量迁移），谱系图按 from_book 画箭头并展示 A→B 方向与原因；RAG 联动存根补 direction；前端 GraphView 方向箭头/详情/tooltip 升级；新增 7 项 pytest（后端 135 通过、ruff 全绿、vue-tsc + vite build 通过）；关联文档升技术栈 v1.38、使用手册 v1.62、重构 v1.22 |
| v1.31 | 2026-08-03 | M10 打磨：笔记导出（Markdown+PDF）+ 性能优化 + 验收测试 | 笔记 PDF 导出（`services/note_export.py`，PyMuPDF 内嵌 CJK，LaTeX 源码保留，`?fmt=md|pdf`，前端抽屉「导出 PDF」）；性能优化（页图目标宽度缓存、页图 Cache-Control 10 分钟、前端相邻页预加载、vite vendor 分包）；验收测试（新增 test_export_m10.py 3 项；conftest 隔离真实 .env 的 AI/视觉配置修复 9 项环境性失败；后端 128 通过、ruff 全绿、vue-tsc + vite build 通过）；关联文档升技术栈 v1.37、使用手册 v1.61、重构 v1.21 |
| v1.30 | 2026-08-02 | 书架 UI 打磨：拖拽换位 + 手动标签 + 搜索/筛选 | §3.1 书架支持拖拽交换位置（`books.position` 排序位，`POST /api/books/reorder` 批量重排，新书自动追加末尾）；书架卡片手动编辑标签（弹层多选 + 自由输入，PATCH tags 清洗去重）；搜索（书名/作者/标签 `?q=`）+ 标签下拉/卡片点击筛选；列表按 position 升序返回；新增 6 项 pytest（后端 125 通过、ruff 全绿、vue-tsc + vite build 通过）；关联文档升技术栈 v1.36、使用手册 v1.60、重构 v1.20 |
| v1.29 | 2026-08-02 | M9 第五轮：相关度阈值函数（决策 20 落地） | 新增 `services/relatedness.py`（score = max(谱系强度, 0.5×暖画像覆盖率×100 + 0.5×冷画像覆盖率×100)、同簇直判、signals 可解释）；`profile_service` 归档入暖记忆改走相关度函数（related_books 含 score）并记录行为样本（related_samples）；`profile_learning.learn_thresholds` 学习 related_strength 取行为样本低分位与确认边低分位之 min（样本 ≥6，45~60）；修复相关度查询 SQLAlchemy `!=` 过滤 NULL 行缺陷（`or_(is_(None), !=)`）；阈值 API `learning` 返回 related_sample_count/related_samples_min，画像页展示；§3.4.1/M9 里程碑/9.1 决策 20 同步；新增 8 项 pytest（后端 119 通过、ruff 全绿、vue-tsc + vite build 通过）；关联文档升技术栈 v1.35、使用手册 v1.59、重构 v1.19 |
| v1.28 | 2026-08-02 | M8 第 12 轮：新书导入增量更新 + 跨书知识检索 | §3.6.1 新书导入增量更新落地（`upload_book` 触发 `services/graph/cross_book.py` `incremental_cross_book_graph`：只补新书与既有书边、幂等、补本地 RAG 联动存根，失败回滚不阻塞导入）；§3.6.2 跨书知识检索落地（`GET /api/graph/knowledge/{kp_id}/appears-in`：其他书内知识点关键词重叠 + 其他书 RAG key_points 命中，来源书不计；前端书内图谱节点点击打开「知识点详情」弹窗，可跳转对应书图谱/阅读原文）；里程碑 M8 更新为第 12 轮，待办仅剩 LLM 打分与传承方向/关联原因增强；新增 4 项 pytest（后端 111 通过、ruff 全绿、vue-tsc + vite build 通过）；关联文档升技术栈 v1.34、使用手册 v1.58、重构 v1.18 |
| v1.27 | 2026-08-02 | 图谱联动 RAG/Skill 增量增改 + 画像阈值自动学习 | §3.4.7/3.4.9 跨书联动沉淀落地：`services/graph_sync.py`（本地存根 `linked_books`/`domain_terms` 补水、`POST /api/graph/sync` LLM 增量增改、`ai/prompts/rag_link.py`），谱系重建/懒构建/关联反馈自动补存根，谱系图页新增「💾 联动沉淀」按钮并修复「重建图谱」按钮；§3.4.1 画像阈值自动学习落地：`services/profile_learning.py`（归档节奏样本 + 确认关联低分位 → `review_days/warm_threshold/related_strength`，样本不足回退默认常量），新增 `GET/PATCH /api/profile/thresholds`、`POST /api/profile/learn`，画像页新增阈值卡片；新增 10 项 pytest（后端 107 通过、ruff 全绿）；关联文档升技术栈 v1.33、使用手册 v1.57、重构 v1.17 |
| v1.26 | 2026-08-02 | 专业术语词库（用户可编辑，优先匹配） | 新增与 .env 同级的用户可编辑词库文件 `backend/domain_terms.txt`（每行一个术语，# 注释）：用户区术语命名时最高优先级；系统自动学习选定的专业术语写入【系统缓存区】作为以后优先备选（按覆盖书数优先竞争）；命名优先级：用户词库 → 自动候选（含系统缓存词库）；新增 5 项 pytest（后端 97 通过、ruff 全绿）；关联文档升技术栈 v1.31、使用手册 v1.55 |
| v1.25 | 2026-08-02 | 自动总结领域名改为专业术语 | 领域名（自动聚类簇名 / post-classify 自动命名 / 簇重命名）必须是领域专业术语，剔除泛化词（定理/定义/方法/教程/讲义/习题等）与作者/元数据噪声（如「茆诗」）；实现：`_GENERIC_DOMAIN_TERMS` 泛化词过滤 + `_domain_candidates` 加权候选（章节×3+书名×2+正文×1+后验×5）+ `_pick_domain_name` 命名；新增 2 项 pytest（后端 92 通过、ruff 全绿）；关联文档升技术栈 v1.30、使用手册 v1.54 |
| v1.24 | 2026-08-02 | 自动总结的领域清洗 | LLM 总结的 skill `domains`（tags）保存前经 `sanitize_cluster_name` 清洗（空项丢弃、去重）；`assign_clusters` 领域自动聚类不再追加 `#书id` 标点后缀（重名关键词书并入同一簇）；新增 2 项 pytest（后端 90 通过、1 项既有失败）；关联文档升技术栈 v1.29、使用手册 v1.53 |
| v1.23 | 2026-08-02 | 聚类 tag 清洗：只保留汉字/英文词组 | 聚类名与 tag 统一清洗（`graph_service.sanitize_cluster_name`：去除特殊标点、全角字母/数字转半角、中文之间不留空格，保留汉字/英文字母/数字与单词间空格）；保存 tag 时清洗（books PATCH，空 tag 丢弃、重复项去重）；`post_classify_book`/`assign_clusters`/`merge_and_rename_clusters` 的 tag、文件夹、簇名写入点统一清洗，并兜底清洗历史遗留脏簇名；新增 5 项 pytest；关联文档升技术栈 v1.28、使用手册 v1.52 |
| v1.22 | 2026-08-02 | M9 第三轮：增量增改 + 簇合并/重命名 + 阅读建议基础版 | `rag_service` 再次归档走增量提示词（`INCREMENTAL_SYSTEM_PROMPT`/`build_incremental_user_prompt`，素材=`_collect_new_material` 全书笔记/划线/「不理解」+最近20条对话），原资产 version+1、id 不变；`graph_service.merge_and_rename_clusters`（T_MERGE=5 两簇后验关键词并集重叠合并进主簇、簇名取书最多的后验术语众数，仅 post 书，归档成功后触发）；新增 `services/recommendation_service.py`（stats/weak_concepts/review/rhythm）与 `GET /api/profile/recommendations`，画像页新增阅读建议卡片；修复簇合并未把被并入书并入主簇成员列表导致重命名漏书的问题；新增 4 项 pytest（后端 83 项通过，仅 1 项既有失败）、ruff/vue-tsc/vitest 全绿；关联文档升技术栈 v1.27、使用手册 v1.51 |
| v1.21 | 2026-08-02 | M9 第二轮：三层画像实现 | §3.4.1 三层画像（热=当前书细节、暖=近 1~2 本+相关领域、冷=长期偏好）；归档迁移跨 1 本热→暖、跨 3 本暖→冷、>3 本全部沉淀冷（阈值初值 `WARM_TO_COLD_THRESHOLD=3`/`KEEP_RECENT=2`，后续按用户习惯学习）；相关领域书入暖记忆（同 post 簇或关联边 strength ≥60 初值）；阅读行为实时回写（进度/章节脉络、笔记/划线/「不理解」→ 热画像）；画像 API 与前端画像页（/profile 三层卡片+重置）；M9 里程碑标记部分完成（第二轮）；关联文档升技术栈 v1.26、使用手册 v1.50 |
| v1.20 | 2026-08-02 | M9 第一轮：读完归档链路 + post-classify 基础版 | 新增归档端点 `POST /books/{id}/archive`（`assets.py`，后台任务 `archive_book_task`）：PDF 先视觉通读全书补齐页缓存（`rebuild_book_caches`，命中不重复调用）再以缓存全文由文本模型总结 RAG/Skill（`page_chunks`/`_build_page_input`，出处「第 X 页」）；非 PDF 按章节正文；成功后章节全部标记已读、状态=读完；`generate_rag_skill` 支持 `page_texts` 参数并成功触发 post-classify；`graph_service` 落地两阶段分类基础版（`T_POST=3` 一致性校验/簇迁移、落盘 `cluster_name/classify_source/classified_at/classify_version`、`assign_clusters` 采用未失效 post 结果、停用词表扩充防「本书介绍」噪音）；前端 `rag.ts` 新增 `archiveBook`、阅读页「归档并总结 RAG/Skill」按钮；新增 5 项 pytest；M9 标记部分完成；关联文档升技术栈 v1.25、使用手册 v1.49 |
| v1.19 | 2026-08-02 | 补登记决策 27 + 修正过时引用 | 9.1 新增决策 27（文本模型思考模式与精细参数，DeepSeek：chat 模式 thinking/reasoning_effort、AI_MAX_TOKENS 32K/64K、responses 模式仅 max_output_tokens）；9.2 开放项登记 1 条（responses 模式 thinking 参数待定稿）；§3.4.1「阈值见 9.2 开放项」过时引用修正为「9.1 决策 20」；技术栈 v1.23 原「需求 9.1 决策 27」引用现已有出处；关联文档升技术栈 v1.24、使用手册 v1.48 |
| v1.18 | 2026-08-02 | 固化「读完归档 → 视觉通读全文 → 文本模型总结 RAG/Skill」 | §3.4.9 首次归档补链路：PDF 书归档时先由**多模态图像模型通读全书**（后台按页批量提取、补全缺失页缓存、命中不重复调用），再以缓存全文为输入由**文本模型**总结 RAG 与 Skill；§3.4.11 触发时机补「读完归档时全书批量提取」；9.1 决策 22 同步；M9 里程碑补「归档视觉通读」；关联文档升技术栈 v1.23、使用手册 v1.47 |
| v1.17 | 2026-08-02 | 固化两阶段聚类（pre/post-classify） | §3.6.1 聚类与分层展开补充**两阶段分类**：pre-classify（tag/文件夹/关键词共现粗略聚类）→ 获得 RAG/Skill 资产后 post-classify（基于 summary/key_points/Skill 用法精细聚类：细化/合并/迁移簇、代表性术语重命名，tag/文件夹硬约束优先，资产版本变化失效重算）；M8 待办补充 post-classify；设计细节入 `docs/知识图谱聚类算法.md` §9；关联文档升技术栈 v1.22、使用手册 v1.46 |
| v1.16 | 2026-08-02 | M8 知识图谱第一轮实现 | 新增 `services/graph_service.py`（关键词抽取 / 跨书关联=关键词共现余弦分+笔记加权 / 聚类分层=tag→文件夹→领域自动聚类 / 书内知识图谱=章节级+重要段落+用户标记 / 懒构建与重建）与 `api/routes/graph.py`（全局谱系、书内图谱、重建、人工反馈，注册到 main.py）；前端 GraphView 重写为 ECharts 关系图（聚类筛选、最密切边高亮、方向箭头、层级筛选、关联详情与反馈、跳转阅读、导出 PNG）+ `api/graph.ts`；新增 6 项 pytest；里程碑 M8 标记部分完成（待办：LLM 打分/传承方向、跨书跳转、图谱联动 RAG/Skill 与暖画像、导入自动增量更新）；关联文档升技术栈 v1.21、使用手册 v1.44、重构 v1.15 |
| v1.15 | 2026-08-02 | M7 PDF 统一处理与多模态视觉提取完成 | PDF（含文本型）统一按页切章与渲染（`parse_pdf` 全部返回按页章节 + `local_text/` 本地抽取文本）；设置页新增多模态独立配置（含测试视觉连接）；新增 `services/vision_extract.py`（逐页提取/滑动窗口增量缓存/全书重建后台任务）与 `api/routes/vision.py`（缓存状态/读取/重提本页/重建）；对话与脑图链路优先注入 `[P-1,P,P+1]` 页缓存文本（出处「第 X 页」），缓存缺失回退页图附件；导入 PDF 后台批量预提取；阅读页页缓存入口；引用解析支持【第X页】；新增 9 项 pytest（提取/缓存命中/滑动窗口/隐私/随书清理/路由）；里程碑 M7 标记完成，M8 为下一步 |
| v1.14 | 2026-08-02 | M6 修复轮：涂鸦渲染与工具栏重排 | 修复 PDF 页图涂鸦：旧数据 `{x,y}` 点坐标兼容并自动迁移（新增 `src/utils/annotations.ts` 纯函数）；画板尺寸以页图渲染尺寸为准、仅变化时重置缓冲区（修复 canvas 卡 1×1 导致笔刷/文本不渲染、无绘制反馈）；涂鸦工具栏移入阅读页顶部（第 X 页 与 第 X 章/共 XX 章 之间），仅 PDF 按页阅读显示；撤销栈切页清空；新增 6 项 Vitest（点兼容/迁移） |
| v1.13 | 2026-08-02 | M6 阅读体验补全完成 | 位置书签（全格式 + 分组归类 + 跳转）与 PDF 页图涂鸦（画板工具/撤销 5 步/擦除/文本/划线批注/划线区域提问）已实现；里程碑 M6 标记完成，M7 为下一步 |
| v1.12 | 2026-08-02 | 涂鸦划线批注与提问 | 每条划线可选中后附加批注（Markdown/LaTeX，随划线持久化）或发起对划线区域的 AI 提问（结合页缓存文本/划线区域图片，出处标注「第 X 页」，受隐私开关约束）；9.1 新增决策 26；3.3/3.5/5/3.4.11/验收 23/M6 同步 |
| v1.11 | 2026-08-02 | 重新规划 M6 及以后里程碑 | 任务变动（书签/涂鸦定稿 + PDF 统一按页处理/多模态提取待实现）后重排：M6 阅读体验补全（书签+涂鸦）、M7 PDF 统一处理与多模态视觉提取（知识图谱语料前置）、M8 知识图谱、M9 个性化与沉淀、M10 打磨；头部进度块与 M3/M4 描述同步指向新编号 |
| v1.10 | 2026-08-02 | 书签组织与涂鸦功能定稿 | 书签列表默认按时间倒序平铺，支持分组/文件夹归类；涂鸦支持撤销上一步（最多 5 步）、擦除痕迹、输入文本，不保留历史版本（会话内撤销栈）；9.1 新增决策 24/25，9.2 开放项清零；3.3/3.5/5/验收 22/23 同步 |
| v1.9 | 2026-08-02 | PDF 统一按页处理（含文本型） | 文本型 PDF 数学符号抽取乱码，改为与扫描件一致：统一按页切章、原图阅读、多模态视觉提取；本地文本抽取仅作全文检索索引；3.2/3.3/3.4.11/3.5/5/决策 2 与 22/验收 1/7/21/23/9.2 措辞/里程碑同步 |
| v1.8 | 2026-08-02 | 细化多模态提取触发 + 书签/涂鸦需求 | 3.4.11 按需提取改为「当前页对 AI 提问时触发」+ `[P-1,P,P+1]` 滑动窗口增量缓存（第 1 页/末页裁剪、仅补缺失页）；新增位置书签（全格式：扫描件整页/文本书章节+段落）与扫描件页图涂鸦标注（画板式）；3.3/3.5/5/决策 22/验收 21~23/里程碑/开放项同步 |
| v1.7 | 2026-08-02 | 开放项全部定稿 | 9.1 新增决策 17~23（结束对话三组合优先主动归档 / Skill 自动按任务匹配 / RAG/Skill 保留≥3 版本整条目回滚 / 相关领域按跨书谱系关联强度 / 文件夹多级嵌套+批量移动/设tag+可调位置 / 多模态导入批量预提取+首读按需前后页 / 多模态独立 API 配置无需额度管理）；9.2 开放项清空；3.1/3.4.9/3.4.11/验收标准同步 |
| v1.6 | 2026-08-02 | 扫描件多模态视觉提取（文档固化，待实现） | 新增 3.4.11：扫描版页图经多模态 LLM 提取完整页面信息、按页缓存到书籍目录（命中不重复调用），文本大模型基于缓存解读；多模态 API 独立配置、受隐私开关约束；4.2/4.3 补约定；验收标准补第 21 条；开放项补 6/7 条 |
| v1.5 | 2026-08-02 | 扫描版页图高清化 + 阅读缩放 | 扫描版页图改为按 PDF 内嵌原图分辨率渲染（替代 72 DPI 低清），旧低清页图按需重渲染；阅读页页图支持「适配宽度 / 原始大小 / ＋/－」缩放与横向滚动 |
| v1.4 | 2026-08-02 | 标记 M5 收尾（脑图渲染/导出/批注） | 脑图节点名清洗：新增 `src/utils/text.ts` `toPlainDisplayText`（去 LaTeX/Markdown 记号后再进 ECharts 标签）；脑图工具栏新增「下载大纲」（.md）与「插入为批注」（写入当前章节批注，复用笔记保存逻辑）；笔记抽屉正文改为 Markdown/LaTeX 渲染（MdRender）；M5 里程碑补充上述能力 |
| v1.3 | 2026-08-02 | 标记 M5 完成 | 里程碑 M5（AI 增强）完成：脑图 ECharts 图像组件（大纲/细节/重要定理三层 + 出处 + 跳转 + 导出 PNG）、四类生成预设、划词 AI 操作（解释选中段/该段脑图/加入思考清单）；M6 知识图谱为下一步 |
| v1.2 | 2026-08-02 | 封面修复 + 书架删除 | 修复 PDF/EPUB 封面跨书共享覆盖：每书独立子目录存储 + 启动自动迁移旧书并回填封面；书架支持删除书籍（级联清理笔记/对话/资产/图谱与本地文件） |
| v1.1 | 2026-08-02 | PDF 封面 / 扫描版按页阅读 / LLM 页图附件 | 导入 PDF/EPUB 提取封面并在书架渲染；扫描版按页切章、阅读页直接读原图；设置页「发送页面图片」开关，扫描书提问自动附带当前页图片（chat 模式、受隐私开关约束）；验收标准与里程碑同步更新 |
| v1.0 | 2026-08-02 | 标记 M4 完成 | 里程碑 M4（AI 接入）完成：API 配置、对话流式、按章节上下文问答、引用出处标注；M5 AI 增强为下一步 |
| v0.9 | 2026-08-01 | 标记开发进度 + 补充需求 | 状态改为开发中；里程碑标记 M1✅/M2✅/M3◐（PDF 导出移至 M8）；补充需求 3.4.9 外部资料直投（资料页投喂即总结 RAG/Skill）、4.6 一键启动（start.bat）；验收标准补 19/20 条 |
| v0.8 | 2026-08-01 | 新增文件夹归类 | 书架支持文件夹归类；同文件夹内无显式 tag 的书默认继承文件夹 tag（用于谱系图聚类） |
| v0.7 | 2026-08-01 | 新增渲染格式 | 用户输入与 LLM 输出支持 Markdown 与 LaTeX 渲染（含笔记），渲染前 XSS 消毒 |
| v0.6 | 2026-08-01 | 补充图谱联动与暖记忆 | 跨书谱系构建/更新时同步增改对应书籍 RAG/Skill；阅读相关领域书籍时将其关键内容存入暖画像 |
| v0.5 | 2026-08-01 | 新增书籍沉淀 | 结束对话的书由 LLM 总结为 RAG 与 Skill 资产，再次阅读并结束后在原资产上增改；补充数据模型/里程碑/验收/开放项 |
| v0.4 | 2026-08-01 | 细化项全部确认 | 画像迁移规则（跨书数 1/3/>3 + 习惯学习）、笔记导出 MD+PDF、不理解双入口、EPUB 保留排版、边方向箭头 |
| v0.3 | 2026-08-01 | 评审确认 11 项决策 | 本地单用户、无 OCR、脑图图像组件、三层画像、支持 EPUB、引用出处、关联强度三合一+笔记加权、图谱粒度三层覆盖、ECharts、tag+领域聚类 |
| v0.2 | 2026-08-01 | 新增跨书串联 | 固化书籍级谱系图、书籍内部知识图谱、关联强度与「关系最密切」标注 |
| v0.1 | 2026-08-01 | 初稿 | 固化主页/阅读页/AI 助手/画像等核心需求 |
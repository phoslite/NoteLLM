# 审查报告-20260811-四域复审（问题与解决方案台账）

> 状态：四域独立审查 + E2E 前端实测（2026-08-11 完成）——全部问题已复现并修复，测试全绿（后端 288 pytest + ruff；前端 69 vitest + vue-tsc + build）。
> 基线：后端 277 pytest + ruff 全绿；前端 61 vitest + vue-tsc 0 错 + vite build 通过（2026-08-11 HEAD）。
> 原则：先复现再修改；修后补回归测试；修复为后续扩展留接口（常量/组合式函数/仓储函数）。

## 1. A 域（图谱/聚类）

| 编号 | 问题（已复现） | 解决方案（已确定） | 优先级 |
| --- | --- | --- | --- |
| A-C1 | 聚类落盘缓存键用「写库前签名」：签名含 persist 回写的 classify_source/cluster_name/classify_version，写库后缓存键与下次 GET 从 DB 计算的签名不一致 → 缓存永不命中，每次打开谱系图全量重算 | persist 循环赋值后、`db.commit()` 前用内存态（=写库后状态）重算签名再存缓存；补「命中探测」回归测试（monkeypatch `_build_sim_graph` 断言 0 次调用） | Critical |
| A-I1 | `persist=False` 只读链路仍写 `domain_terms.txt`（`cache_domain_term` 在 `if persist:` 外） | 移入 `if persist:` 分支；补文件无副作用测试 | Important |
| A-I2 | `_algo_params_signature` 未覆盖词库状态（泛词集/用户区/同义词区），改词库后缓存不失效 | 签名并入：泛词集 hash + 用户区术语 hash + 同义词映射 hash（系统缓存区是自动输出，不计入，避免自失效）；补失效测试 | Important |
| A-I3 | 增量导入用「子集人口」调 `assign_clusters`（默认 persist=True）：子集 IDF 与全量矛盾、会写坏全量缓存 | 增量路径改 `assign_clusters(db, list_books(db), persist=False)` 只读全量、不落盘；补测试 | Important |
| A-I4 | 仓储下沉不彻底：`cross_book.py:102`、`tasks.py:29`、`clustering.py:184`、`graph_sync.py:118/217`、`routes/graph.py:114` 直连查询 | 补仓储函数（`list_feedback_relations`/`get_relation` 等），替换直连点 | Important |
| A-I5 | `_weighted_lpa` 与 `cluster_use_lpa=True` 路径零测试 | 补 LPA 单测（星形/防吞并/确定性）+ 集成测试（monkeypatch settings） | Important |
| A-M2 | IDF 计算域不一致（聚类过滤泛词后算 IDF，建边未过滤） | 建边侧沿用现设计，`similarity.py` 加注释说明刻度差异（行为不改） | Minor |

## 2. B 域（后端核心）

| 编号 | 问题（已复现） | 解决方案（已确定） | 优先级 |
| --- | --- | --- | --- |
| B-I1 | `prep_page_image` 把 CPU 密集的 `_render_cropped` + 每页两次 glob 放在进程级 `_meta_lock` 内 → 4 线程并行被串行化、整批 O(N²) | 锁只保护 meta 校验/purge/写 meta 短临界区；渲染移出锁外（同参同源幂等输出）；补并发冒烟测试 | Important |
| B-I2 | 视觉提取防重键不对称：import 侧 `name_prefix="vision-"`，重建侧 `"vision-rebuild"` → 导入预提取中重建会并发第二路全书提取 | tasks 抽共享常量 `VISION_TASK_PREFIX="vision-"`，非 force 重建与导入共用；force 保留独立前缀（用户显式全量）；补测试 | Important |
| B-I3 | `responses` 模式静默丢弃 temperature/top_p（默认 AI_MODE=responses，挑选器确定性意图落空） | responses 分支补发 temperature/top_p（DeepSeek responses 支持）；补请求体断言测试 | Important |
| B-I5 | `update_progress` 写库失败（SQLite busy）会把已完成业务任务判 failed | `update_progress` 内 try/except 吞掉写失败仅记日志，与业务结果解耦；补测试 | Important |
| B-M1 | `chat()` 未捕 `resp.read()` 阶段的 OSError/TimeoutError → 调用方拿底层英文错误 | `chat()` 内统一 `except (OSError, TimeoutError)` 转 LLMError | Minor |
| B-M2 | `annotations.py` `page_index` 无上限，可写任意页文件 | 服务层校验 ≤ `book.page_count` | Minor |

## 3. C 域（前端）

| 编号 | 问题（已复现） | 解决方案（已确定） | 优先级 |
| --- | --- | --- | --- |
| C-I1 | `useReaderAi.switchMode` 不清空消息 → 旧模式池消息按「角色+内容去重」追加到新模式历史尾部，四能力池串台 | `switchMode` 先清空 `chatMessages` 再 `loadChatHistory`（replace 语义）；补单测 | Important |
| C-I2 | `useGlobalAi.loadHistory`/`useReaderAi.loadChatHistory` 合并时 `local` 消息恒保留，流式进行中后端已落库的部分行被整体加入 → 折叠再展开出现半截+完整重复 | 新增共享纯函数 `mergeChatHistory(local, rows)`（utils/chatMerge.ts）：按 `stream_key` 排除在途 DB 行；两处共用（为未来 `useStreamSession` 抽取留缝）；补单测 | Important |
| C-I3 | RagView/RagDetailView `waitForTask` 对 `failed` 正常返回 → 任务失败仍提示「总结完成」；轮询无 signal，卸载后继续轮询 | 调用侧检查 `t.status === 'failed'` 抛错；组件加 AbortController + `onBeforeUnmount` 中止；补单测 | Important |
| C-I4 | TaskCenter「最近 3 条已完成」顺序反转（`all` 按 created_at desc，unshift+slice(0,3) 保留最旧 3 条） | 倒序遍历终态任务（oldest→newest）再 unshift，保留最新 3 条；补单测 | Important |
| C-M1 | `vision.ts getPageText` 前端零调用死代码 | 删除 | Minor |
| C-M2 | `DoodleToolbar.vue` 未使用 `computed` import | 清理 | Minor |
| C-M3 | `ReaderView.vue:279` 无章节空书 `loadChapter(target!, true)` 非空断言掩盖空值 → 请求 `/chapters/null` | 空章书显示空态，不发起请求 | Minor |
| C-M4 | `chat.ts` 流式 fetch 无超时，服务端挂起时 streaming 永久为 true | 加 `AbortSignal.timeout` 兜底 | Minor |

## 4. D 域（文档）

| 编号 | 问题（已复现） | 解决方案（已确定） | 优先级 |
| --- | --- | --- | --- |
| D-C1 | README L151-158 + 使用手册-AI与资产 §1.1/§4.6/§17.2 引用不存在的 `demo/chat_demo.py`/`_mock_llm.py`/`parallel_llm.py`（已移出 git） | 改写为「历史产物已移除」；README 联调段改为真实路径（start.bat + 设置页测试连接） | Critical |
| D-C2 | demo 评估证据链（gold_clusters/real_corpus/cluster_report）被 `.gitignore` 整目录忽略，与 6 份文档「已保留/可复现」表述矛盾 | 遵循用户决策「demo 不入库」：文档统一加「仅本地留存、不入库」声明，措辞降级为「本地可复现」 | Critical |
| D-I1 | README L72「导入不再自动预提取」与代码（import_service 仍自动提交 vision 预提取）及决策 32 矛盾 | README 改回「导入 PDF 自动批量预提取（独立 vision 任务、不阻塞导入、可取消）」 | Important |
| D-I3 | 待办清单 §7「并查集化」未勾选但已由 L4 引擎 v2 实现；速览「剩余 3 项」过时 | 勾选并注明；速览改「剩余 2 项」 | Important |
| D-I4 | 使用手册总纲模块地图缺 similarity/terms/thresholds/term_alias/viewport.ts/task.ts | 补齐模块行 | Important |
| D-M1 | 使用手册/技术栈/待办变更记录表顺序混乱 | 按「最新在前」重排 | Minor |
| D-M2 | 使用手册头部「更新日期 2026-08-05」过时 | 更新为 2026-08-11 | Minor |
| D-M3 | 待办清单标题无版本号 | 标题带版本号（本轮 v2.13） | Minor |
| D-M4 | 待办 §2.1 字面 `\n` | 改为真实换行 | Minor |
| D-M5 | 图谱与画像函数表未标模块归属 | 加「模块」列 | Minor |
| D-M6 | 根残留 `.pytest_tmp2/`、`.pytest_tmp_l4/` | 删除 | Minor |

## 5. E2E（前端浏览器模拟用户操作 · 非视觉方法）

按目标要求派发子代理用 Playwright/DOM 检查模拟真实用户操作（非视觉）：
- 首页：书架加载/封面/搜索/拖拽/tag；主页全局 AI 浮窗收发消息（流式、历史恢复、删除会话）
- 阅读页：章节加载、正文渲染（乱码检测：替换字符 U+FFFD/�、mojibake 模式）、AI 面板四模式切换不串台、书签/涂鸦/进度
- 资产页：上传总结、失败提示、折叠展开、无滚动条区域
- 任务中心：最近 3 条顺序、进度条
- 设置页：API 配置保存、强制载入 .env
- 控制台错误收集 + 网络 4xx/5xx 收集

## 6. 修复记录

| 日期 | 修复项 | 验证 | 状态 |
| --- | --- | --- | --- |
| 2026-08-11 | A-C1 聚类缓存命中（写库后状态重算签名） | 命中探测回归测试通过；286 pytest 全绿 | ✅ |
| 2026-08-11 | A-I1 persist=False 不写词库 | 词库文件无副作用测试通过 | ✅ |
| 2026-08-11 | A-I2 词库输入态并入算法签名 | 词库变更失效测试通过 | ✅ |
| 2026-08-11 | A-I3 增量补边改全量人口 | 既有图谱套件全绿 | ✅ |
| 2026-08-11 | A-I4 仓储下沉收尾（5 处直连点） | ruff 全绿；286 pytest 全绿 | ✅ |
| 2026-08-11 | A-I5 LPA 单测 + 集成测试 | 新增 2 例通过 | ✅ |
| 2026-08-11 | A-M2 IDF 刻度差异注释（聚类剔泛词 vs 跨书全量，行为不改） | similarity.py 模块注释已补，ruff 全绿 | ✅ |
| 2026-08-11 | D-M6 根残留临时目录清理 | `.pytest_tmp3` 已删；`.pytest_tmp2/.pytest_tmp_l4` 因 ACL 权限拒绝（需管理员），`.gitignore` 的 `.pytest_tmp*/` 已覆盖，不影响仓库 | ⚠ 部分 |
| 2026-08-11 | B-I1 页图渲染移出全局锁 | 锁外渲染回归测试通过 | ✅ |
| 2026-08-11 | B-I2 视觉防重键统一 VISION_TASK_PREFIX | 防重键回归测试 + 路由断言更新通过 | ✅ |
| 2026-08-11 | B-I3 responses 透传 temperature/top_p | 请求体断言测试通过 | ✅ |
| 2026-08-11 | B-I5 进度写库失败不拖垮任务 | 失败注入测试通过 | ✅ |
| 2026-08-11 | B-M1 chat 读取阶段 OSError 包装 / B-M2 批注页号上限 | ruff + 既有套件全绿 | ✅ |
| 2026-08-11 | C-I1 切模式清空旧池消息 | switchMode 回归测试通过 | ✅ |
| 2026-08-11 | C-I2 共享 mergeChatHistory（stream_key 去重） | 3 例工具单测 + 流式刷新回归通过 | ✅ |
| 2026-08-11 | C-I3 Rag 页失败报错 + 卸载中止轮询 | vue-tsc + build 通过 | ✅ |
| 2026-08-11 | C-I4 任务中心最近完成顺序（latestFinishedTasks） | 2 例单测通过 | ✅ |
| 2026-08-11 | C-M1~M4 死代码/空章守卫/流式超时 | vue-tsc + build + 68 vitest 全绿 | ✅ |
| 2026-08-11 | D-C1 demo 引用改写 / D-C2 仅本地留存声明 / D-I1 口径统一 / D-I3 / D-I4 / M1~M6 | 文档扫描无残留失效引用 | ✅ |
| 2026-08-11 | E2E-C-1 阅读页 AI「清空」加确认框（范围=本书当前模式池，不可逆提示）+ **book 12「解读」池误删数据已从主库快照恢复（14 条）** | Playwright 实测：点清空弹确认、取消可关闭；恢复后池 14 条完整 | ✅ |
| 2026-08-11 | E2E-M-1 任务中心「最近完成」pushRecent 改为按 created_at 降序 | 实测 UI 最新在前（17:41→17:37→17:37）与 API 一致 | ✅ |
| 2026-08-11 | E2E-M-2 手动 tag 保留原样（clean_tags 只去空白/去重；聚类消费端再清洗） | test_tags_kept_raw_on_save + test_clean_tags_keeps_user_punctuation；API 实测连字符原样保存 | ✅ |

## 7. E2E 验证（2026-08-11，Playwright 非视觉实测，报告：.e2e_logs/e2e-report.md）

> 覆盖 9 场景：首页书架 / 阅读页（PDF+md）/ 阅读 AI 四模式 / 资产页 / 任务中心 / 设置页 / 全局 AI 浮窗 / 乱码-LaTeX / 网络巡检。全程 0 pageerror、0 console.error、0 4xx/5xx。

| 场景 | 结果 | 说明 |
| --- | --- | --- |
| S1 首页书架 | PASS（2 ⚠） | 渲染/搜索/拖拽/tag 通过；⚠ tag 清洗、无文件夹 UI（后者登记待办 D8） |
| S2 阅读页 | PASS（1 ⚠） | 布局达标（左 25.0%/右 30.0%/单滚动条）；⚠ md 工具栏缺页码与宽度调节（登记待办 D9） |
| S3 阅读 AI 四模式 | 功能 PASS + Critical | 模式隔离/流式正确；「清空」无确认导致 book 12「解读」池 14 条用户历史被误删 → **已从主库快照恢复** + 加确认框修复 |
| S4 资产页 | PASS | 上传/总结/失败路径/删除闭环全通过 |
| S5 任务中心 | 修复后 PASS | 「最近完成」倒序修复（pushRecent 按 created_at 降序） |
| S6 设置页 | PASS | 42 字段/保存/强制载入 env 正常 |
| S7 全局 AI 浮窗 | PASS | 会话按 session 载入、删除会话生效 |
| S8 乱码/LaTeX | PASS | 0 mojibake、0 未渲染公式、KaTeX 标题正常 |
| S9 网络巡检 | PASS | 6 页零错误零失败请求 |

**修复闭环**：E2E-C-1（确认框 + 数据恢复）、E2E-M-1（排序）、E2E-M-2（tag 原样）已全部修复并回归；新增/更新后端测试 3 例，全量 287 pytest + ruff、68 vitest + vue-tsc + build 全绿。

## 8. 二轮复审 + E2E 二轮（2026-08-11，子代理：代码复审 Descartes + 前端 E2E Herschel）

> 结论：0 Critical / 1 Major（已修复）/ 9 组 Minor（功能类已修复，观感类已修复，登记类入待办 D10~D14）。
> 二轮 E2E：143 项检查，115 PASS + 28 阈值类 ⚠（对比度/字号/按钮尺寸）；9 脚本合并 0 pageerror / 0 console.error / 0 4xx/5xx / 0 requestfailed；0 次真实 AI 调用；测试数据全部还原。

### 8.1 修复记录（二轮）

| 修复项 | 方案 | 验证 | 状态 |
| --- | --- | --- | --- |
| C-I3 卸载中止轮询（RagView/RagDetailView 导入未注册） | 两个组件补 `onBeforeUnmount(() => abort.abort())` | vue-tsc + build | ✅ |
| `_book_domain` tag 未清洗（挑选器目录） | 复用 `sanitize_cluster_name`（空则兜底原值） | ruff + 单测 | ✅ |
| clearChat abortChat 先于确认执行 | 移到确认成功后（取消不误杀在途流） | vue-tsc | ✅ |
| 资产详情 chunk-pos 原始 LaTeX 泄漏 | 章节标题改 `<MdRender inline />` 渲染 | vue-tsc + E2E 复核 | ✅ |
| vision.py 提示语口径 | force/非 force/导入预提取分文案 | ruff | ✅ |
| 死代码：NOTE_TYPES、WARM_TO_COLD_THRESHOLD | 删除（测试改 DEFAULT_WARM_THRESHOLD） | pytest | ✅ |
| relatedness.py 尾部 15 行空行 | 清理 | ruff | ✅ |
| 任务中心浮窗遮挡右侧 AI 面板 | 右下→左下锚定 + 按钮 min 26x24px | E2E 复核 | ✅ |
| 低对比度（TOC 1.44:1 等 10 处） | TOC active 白字 / 图谱绿 #529b2e / 占位符 #8a9199 / 设置 tab #2f6fb0 | E2E 复核 | ✅ |
| 辅助字号 <13px（5 处） | panel-count/recent-line/shelf-progress/head-sub → 13px | E2E 复核 | ✅ |
| 小按钮 <24px（4 处） | 任务中心按钮/阅读页 ✓/查看完整 RAG min 尺寸 | E2E 复核 | ✅ |
| 使用手册-AI与资产 §4.2 函数清单过期 | 改为「职责 → 现属模块」索引 | 文档核对 | ✅ |
| .env.example 缺 3 项 | 补 APP_NAME / AI_TEMPERATURE / MAX_UPLOAD_BYTES | 与 config.py 对照 | ✅ |
| 手册细节 | assign_clusters 补 persist、_is_related_book 引用、render_pdf_pages 补 workers | 文档核对 | ✅ |

### 8.2 登记（待办 D10~D14）

D10 useTaskPoll 收敛 / D11 prepare_chat_job 拆分 / D12 TAU_POST·TAU_MERGE 接线跟踪 / D13 画像层下沉 repositories/profile.py / D14 保留函数说明（is_related_book、book_corpus、clear_keyword_cache、percentOf 均有测试引用；FolderItem 与 D8 一并处理）。

### 8.3 E2E 二轮亮点（143 项）

- 阅读页深度交互 40/40：书签增删/倒序/跳转、划词高亮/批注/思考清单、脑图打开+下载+插入批注、笔记导出 md/pdf 200、已读往返、涂鸦绘制/撤销/清除（不破坏数据）、进度还原（原值 PATCH 回）、清空确认框回归。
- 谱系图 9/9：echarts series=20/links=120 与 API 一致、节点详情、层级筛选、零错误。
- 画像页 5/5：三层结构、统计卡、冷画像前 12 与 API 一致。
- 设置持久化 12/12：修改→保存→刷新保持→还原；强制载入 env 确认框 + 200。
- 全局 AI 浮窗 8/8（0 次真实 AI）：空态/多轮 mock 渲染/删除会话确认（取消不删数据）。
- 多视口 7/7：1024x768 无横向滚动、左右栏零重叠。
### 8.4 二轮收尾验证（2026-08-11，终验）

| 项 | 结果 | 说明 |
| --- | --- | --- |
| test_profile.py L179 语法错误 | ✅ 修复 | `from app.services.profile_service import COLD, WARM, _save` 误顶格（应为函数内缩进），恢复缩进后单测 11 项通过 |
| vision.py rebuild 未返回 msg | ✅ 修复 | 防重改 submit_dedupe 后遗漏 return；补 ok({task_id, msg})（F841 消除，前端兼容） |
| ruff 6 项 | ✅ 全绿 | F841×1（vision.py）+ I001×4（notes.py / rag_router.py / test_profile.py×2）+ F401×1（test_profile.py 函数内冗余 import）；--fix 后复查全过 |
| 后端全量 | ✅ 287 passed | pytest tests（.pytest_tmp3，无缓存）47s 全绿，含 ruff 改动后二次全量回归 |
| 前端全量 | ✅ 68 tests + 0 err | vitest 68/68、vue-tsc --noEmit 0 错误、vite build 成功（CSS // 行注释改 /* */ 后确认） |
| 浏览器复验 | ✅ 4/4 | TaskCenter 固定左下（left:16/bottom:16）不再遮挡 AI 面板清空按钮（683,93 无重叠）；TOC active 白字 rgba(255,255,255,.92)；chunk-pos 章节标题 KaTeX 渲染（book 12 hasKaTeX=true、无原始 \\ 泄漏）；0 pageerror / 0 console.error |
| 后端重启 | ✅ health 200 | 发现 10048 根因之一：残留僵尸 uvicorn（PID 33868 绑定失败未退出）；runtime python 无 uvicorn 模块 → 用 .venv\\Scripts\\python.exe 重启（PID 14100） |
| 验证脚本 | .e2e_logs\\verify-round2-after-fix.js/json | 复验脚本与结果留存（仅本地）；restart-backend.ps1 已清理 |

**结论：二轮修复全部收尾，0 未修复功能问题**。已登记待办缺口不受影响：D8（书架文件夹 UI）、D9（md 书页码/宽度调节）。

### 8.5 三审修复记录（2026-08-11，代码复审 + E2E 复验）

> 结论：1 Major×3（已修复）/ 8 Minor（已修复）/ 后端死代码 7 处 + 任务包装收敛（A 组，本轮完成）。浏览器复验 7/7 全绿。
> 基线修正：`useReaderDoodle.ts` 工作区版本含未提交修复（终审 §6.9/F8/I-2/Minor-3），误执行 `git checkout` 覆盖后按「交接摘要 + 早先完整输出」重建，并补本轮根因修复（见下）。

| 编号 | 问题（已复现） | 解决方案 | 验证 | 状态 |
| --- | --- | --- | --- | --- |
| Major-1 | RagView/RagDetailView 的 `onBeforeUnmount` 写在事件处理器函数体内（Vue 静默丢弃）→ 组件卸载后轮询不中止 | 新建 `useTaskPoll.ts`（顶层注册 onBeforeUnmount → abort signal；`run()` 包装 waitForTask），两视图改用 | vitest + vue-tsc | ✅ |
| Major-2 | 隐私开关 `ai_enable_body_send` 在 RAG 总结链路失效（rag_input 四函数只读 env，无 DB 覆盖） | `rag_input` 四函数加 `enable_body: bool \| None = None` 透传；`rag_service.generate_rag_skill` 与 `import_service` 视觉预提取门控统一走 `load_ai_overrides(db)`；补回归测试 | pytest test_archive_m9（16 项含新增） | ✅ |
| Major-3 | SSE 用 `AbortSignal.timeout(120000)` 总时长，长流被误杀；`useReaderAi` 把超时 AbortError 当用户中断静默吞 | `chat.ts` 重写：共享 `streamSse` 内核 + `IdleTimeout` 类（每次收包 touch 重置 120s；`fired` 标志 + `AbortSignal.any`），`streamChat`/`streamGlobalChat` 统一走内核 | vitest chatStream 4 项 + 浏览器长公式复验 | ✅ |
| Minor-3 | 只读访问回写涂鸦：`loadDoodle` 赋值触发的 deep-watch 回调异步执行时 `doodleLoading` 已复位 → 打开/翻页产生 800ms 后空 PUT | 脏标记 `doodleDirty` + 加载赋值抑制 `doodleSuppressSave`（nextTick 复位）+ `scheduleDoodleSave` 用 `wasDirty` 快照拦非脏落盘 + switchPage/dispose 条件保存 | 浏览器：打开+翻页 0 PUT；vitest doodle-switch 5 项 | ✅ |
| Minor-4 | 谱系图普通边白底对比度不足（alpha 0.55 → 2.6~3.0:1） | `edgeStrokeColor` 基线 alpha 0.68 起（0.68+0.27×强度，白底 ≥3.6:1） | vitest graphEdges（期望同步 0.71/0.82/0.95） | ✅ |
| Minor-5 | 节点/面板色在浅色背景区分度不足 | PALETTE 10 色 + LEVEL_COLOR（章节蓝/重要段红/用户标记橙）+ GraphGlobalPanel 统计卡同步 | vitest + 截图 | ✅ |
| Minor-6 | 辅助字号 <13px（HomeView/GraphView/TaskCenter 共 11 处） | 全部提到 12~13px | E2E 复核 | ✅ |
| Minor-7 | 文本对比度不足（--primary-color/--text-secondary） | 亮色 `#2f6fb0`（5.09:1）/`#6b7280`（4.71:1）；暗色 `#9aa3b2` | E2E 复核 | ✅ |
| Minor-8 | 点击目标 <24px（App 导航 21px） | `.app-nav a` padding 6px 2px（高 33px） | E2E 复核 | ✅ |
| Minor-9 | Element Plus 未中文化 | `main.ts` 注入 zh-cn locale | build | ✅ |
| Minor-10 | 搜索命中无高亮 | HomeView snippet 用 `highlightSnippet`（转义 + `<mark>` 替换）+ CSS 高亮 | 浏览器：11 个 mark 命中 | ✅ |
| Minor-11 | 后端死代码 7 处 + 文档过期 | profile_service 删 WARM_TO_COLD_THRESHOLD；vision.rebuildPageText 补返回类型；文档 6 文件修正 | ruff + pytest | ✅ |

### 8.6 三审收尾验证（2026-08-11）

| 项 | 结果 | 说明 |
| --- | --- | --- |
| 后端全量 | ✅ 288 passed | pytest tests（.pytest_tmp3，无缓存）45s 全绿（含新增隐私覆盖回归 1 条）；ruff check app tests 全绿 |
| 前端全量 | ✅ 69 tests + 0 err | vitest 69/69（graphEdges 期望更新 0.71/0.82/0.95、doodle-switch 按 Minor-3 语义增改）；vue-tsc --noEmit 0 错误；vite build 成功 |
| 浏览器复验 | ✅ 7/7 | ① 搜索命中 11 个 mark；② RAG chunk-pos 无原始 `$` 泄漏；③ 要点正文无 `$` 泄漏；④ 任务中心（左下）不遮挡 AI 清空按钮（x:16,y:709 vs x:1382,y:774）；⑤ AI 消息容器 overflow-x=auto；⑥ 打开+翻页 0 条涂鸦写请求（Minor-3 核心）；⑦ AI 长公式回复 77 个 KaTeX 渲染、0 原始 TeX |
| 后端重启 | ✅ health 200 | 先杀 PID 14100 再用 .venv python 重启（PID 31856），后端改动（rag_input 等）生效 |
| 验证脚本 | .e2e_logs\\r4_verify.js/json、r4_diag_*.js | 复验脚本与结果留存（仅本地，gitignore） |

**结论：三审修复全部收尾，7/7 浏览器复验 + 前后端全量测试全绿，0 未修复功能问题。**

## 9. 第 4 轮复审（2026-08-11，子代理：Galileo 后端 / Lovelace 前端 / Socrates 文档 / Copernicus E2E）

### 9.1 审查结论

- 后端（Galileo，`review-round4-backend.md`）：**0 Critical / 0 Major / 3 Moderate / 8 Minor**，收敛到可修复的干净状态；隐私开关链路、I-3 原子防重、F3/F5/F7、死代码、任务生命周期均核验无问题。
- 前端（Lovelace，`review-round4-frontend.md`）：**0 Critical / 1 Major（SSE 空闲超时降级失效）/ 4 Moderate / 10 Minor / 4 组文档**；编码扫描全库干净、生命周期顶层注册正确、vue-tsc 0 错误。
- 文档（Socrates，`review-round4-docs.md`）：**0 Critical / 4 Major / 6 Moderate / 14 Minor**；22 项「已完成」声称全部有代码证据，0 项找不到实现，4 项实现存在但未记录。
- E2E（Copernicus，`e2e-report-round4.md`）：173 项检查 **154 PASS / 18 FAIL / 1 WARN**；0 Critical / 2 产品 Major（含 KaTeX 划词高亮静默失败、首页文件夹切换缺失）+ 1 审计 Major（真实 AI 调用 3 次已清理，备份 `.recovery/llmnotebook_precleanup_round4.db`）/ 4 Minor / 3 Info；全页面无乱码。

### 9.2 修复记录（后端 10 项 + 前端 15 项 + 文档 4 Major）

| 域 | 编号 | 修复 | 验证 |
| --- | --- | --- | --- |
| 后端 | M-1 | 聚类缓存签名 parts 增资产版本（`clustering.py:148-171`，复用 `asset_classify_versions` 批量查询；签名变化使 `graph_clusters.json` 失效重算一次） | test_graph 缓存命中/失效全绿 |
| 后端 | M-2 | 资产写锁下沉仓储：per-book 锁表改 OrderedDict LRU 有界 128（`repositories/assets.py`），`merge_duplicate_assets` 内部取锁，路由层 `_dedupe_lock` 移除 | 新增 `test_asset_write_locks_bounded` |
| 后端 | M-3 | `_weighted_lpa` 流行度预计算 + 增量维护（O(iter×n²×deg)→O(iter×n×deg)） | LPA 星形/确定性/集成测试全绿 |
| 后端 | m-1 | `book_corpus` / `clear_keyword_cache` 标注 legacy（仅测试用） | perf_tier1 全绿 |
| 后端 | m-2 | `CLUSTER_USE_LPA` 核销（.env.example 已存在） | — |
| 后端 | m-4 | 聚类「孤立书」注释与实现一致化（行为未动） | 纯注释 |
| 后端 | m-5 | 挑选缓存键含问题指纹 → **已回退**（违反决策 34 §9.3.1-② 与 F3 回归，见 §9.4） | — |
| 后端 | m-6 | 导入原子化：`create_book_with_chapters` 单事务（flush 取 id → 批量 Chapter → 一次 commit） | test_import_service 无孤儿残留全绿 |
| 后端 | m-7 | FK 冲突语义统一：`update_book` IntegrityError 拆 404（folder 并发删除）/409（引用冲突）；`delete_folder` 文案统一 | 新增 folder 404 测试 |
| 后端 | m-8 | `_merge_fragments` 单趟批量并入 O(n³)→O(n²) | 聚类输出与旧实现一致 |
| 后端 | E2E #9 | `rag_select_mode` 行内注释残留清洗（config `field_validator` + settings `_strip_inline_comment` 读/写双路径 + .env 注释另起一行） | 新增 2 测试；`/api/settings/ai` 返回空串闭环 |
| 前端 | M1 | SSE 空闲超时降级修复：无 `AbortSignal.any` 时 idle→controller 反向桥接 + 循环内 `fired` 兜底（`api/chat.ts`） | 新增 `sse-fallback.test.ts` 2 用例（模拟旧内核） |
| 前端 | MO1 | 语义色 token 化：`--status-ok/err/warn/--accent` 明暗两套，TaskCenter/ProfileView/MindMapPanel/Doodle 5 文件收敛 | rg 硬编码仅剩装饰渐变 |
| 前端 | MO2 | `streamSeq++` 终态补全：useReaderAi/useGlobalAi 的 catch 与 F2 兜底各补 2 处 | chatStream 4 用例 |
| 前端 | m1 | useReaderArchive 收敛 useTaskPoll + `submittedBookId` 快照守卫（切书迟到完成不刷新新书） | review-fixes 10 用例 |
| 前端 | m2 | useTaskPoll.run() 信号合并（AbortSignal.any 合并 / 降级双向桥接），不再静默覆盖调用方 signal | vue-tsc + 轮询测试 |
| 前端 | m3 | 划词菜单定位改模板 ref 注入（去全局选择器，支持多实例） | E2E 复验菜单定位正常 |
| 前端 | m4 | 高亮防嵌套：collectHlNodes 跳过 `.note-hl` 内部文本，已高亮引文再次高亮静默跳过 | highlight 单测 + E2E 场景 B2（markCount 保持 1） |
| 前端 | m5 | `utils/searchHighlight.ts` 实体化字符归一化命中（&amp; 等） | 新增 searchHighlight 8 用例 |
| 前端 | m6/m7 | 字号收敛（残留 22 处均为徽标/图标类 ≥11px 达标）、点击目标 ≥24px 残余 4 处修正 | vue-tsc + build |
| 前端 | m8 | FolderItem 死代码删除（types.ts + 引用） | vue-tsc |
| 前端 | m9 | `--primary-soft` 双源收敛 + 暗色 primary 覆盖 | vue-tsc |
| 前端 | m10 | 魔法常量 180000/120000 收敛 `utils/constants.ts` | vue-tsc |
| 前端 | E2E #1 | 含 KaTeX 段落划词高亮静默失败：`utils/highlight.ts` 归一化匹配（剥离 KaTeX/已有 mark 节点跨文本节点匹配）+ `wrapQuoteInElement` 整段回退 + `quoteParaIdx` 快照防误伤 | 新增 highlight 8 用例；E2E 复验 `verify_round4_highlight.js` PASS（1 mark、5 公式全部在 mark 外、无嵌套） |
| 前端 | E2E #4~#7 | 对比度/点击目标/字号/横向溢出观感修正 | 同 MO1/m6/m7 |
| 文档 | Major-1 | 版本台账登记：使用手册 v1.126 / 技术栈 v1.78 / 重构 v1.43（本轮再升 v1.127 / v1.79 / v1.44） | 五处同版本登记核验 |
| 文档 | Major-2 | 重构规范 §11 重构记录表补表头分隔行（27 行恢复渲染） | markdown-it 断言 `<table>` ✅ |
| 文档 | Major-3 | 需求文档 v1.78~v1.75 合并行拆 4 行 + v1.60 多单元格修正 + 重复标题清理 | markdown-it 断言 ✅ |
| 文档 | Major-4 | 聚类「先 demo 验证、暂不入正式版」口径 3 处过期同步为「正式版 L0~L4 已落地」 | 需求-决策/需求文档/里程碑 3 处核销 |

### 9.3 验证数据（主控复核）

| 项 | 结果 | 说明 |
| --- | --- | --- |
| 后端全量 | ✅ 291 passed | pytest tests（.pytest_tmp_r4main2）44s 全绿；ruff check app tests 全绿 |
| 前端全量 | ✅ 87 tests + 0 err | vitest 87/87（含新增 highlight 8 / searchHighlight 8 / sse-fallback 2）；vue-tsc --noEmit 0 错误；vite build 成功 |
| 文档渲染 | ✅ ALL_DOCS_OK | markdown-it 实测：重构规范 §11 表格、需求文档 v1.78~v1.75 行、README 更新记录均渲染为 `<table>`；五处版本登记一致（v1.127 / v1.79 / v1.44 / v2.18 / 需求 v1.86） |
| 浏览器复验 | ✅ 划词高亮 PASS | 书 12 含 KaTeX 段落：整段高亮 1 mark、5 公式在 mark 外、无嵌套；纯文本段落精确命中；零 console error；零后端写（拦截 notes POST/progress） |
| 报告 | .e2e_logs/fix-round4-{backend,frontend}.md、review-round4-*.md、e2e-report-round4.md | 仅本地留存（gitignore） |

### 9.4 偏离与回退记录

1. **m-5 挑选缓存键含问题指纹 → 已回退**。修复 agent 将 `_cache_key` 改为 session+chapter+问题指纹并改写 `test_session_cache_reuses_within_chapter`，导致 F3 回归测试失败（`test_f3_session_cache_recomputes_chunks_for_new_question`）且偏离决策 34 §9.3.1-②（键=会话+章节）。回退依据：缓存仅存挑选结果（book_ids/skill_refs），命中后 chunks 仍按当前问题重取（F3 语义），注入内容不陈旧，仅省一次挑选 LLM 调用；换问题重挑的代价是每问一次 LLM。恢复 `_cache_key = session + chapter`，删除 `_CACHE_QUESTION_SENSITIVE` 与 hashlib 导入，测试恢复原始断言（calls==3/4）。回退后 F3 + rag_router 10 passed，全量 291 passed。
2. **m-2 核销**：`CLUSTER_USE_LPA` 在 .env.example 已存在（103 行），无需补写。
3. **审计数据清理**：Copernicus E2E 产生的真实 AI 聊天消息 id 125~130（book7）已清除，DB 备份 `.recovery/llmnotebook_precleanup_round4.db`；后续 E2E 一律 mock AI/vision 端点。

## 10. 第 5 轮复审（2026-08-11，子代理：Arendt 后端 / Ptolemy 前端代码 / Feynman 前端 E2E / Jason 文档；修复：Curie 前端）

### 10.1 审查结论

- 后端（Arendt，`review-round5-backend.md`）：**0 Critical / 0 Major / 0 Moderate / 6 Minor**（文档/运维类）；第 4 轮修复全部真实落地，m-5 回退核验通过（`_cache_key`=session+chapter、无 hashlib、F3 双测试断言「同章换问题命中 cache 且 chunks 按新问题重取」）；ruff 全绿 + 全量 291 passed。
- 前端代码（Ptolemy，`review-round5-frontend.md`）：**0 Critical / 0 Major / 0 Moderate / 2 组 Minor**；第 4 轮 1 Major + 全部 MO/m 级修复无伪修复、无回退；MO3 流式内核重复 66.2%（useGlobalAi 305 行中 202 行与 useReaderAi 逐字节相同）仍为已登记 P2（待办清单.md:23），两阶段低风险抽取方案已给出；vitest 87 + vue-tsc 0 错误 + build 通过。
- 前端 E2E（Feynman，`e2e-report-round5.md`）：**154 项 = 146 PASS / 6 FAIL / 2 WARN**；0 Critical / 0 产品 Major；乱码专项 8 页全清；声称对照 8/8 相符；基线 13/13 无漂移；cl-03 划词菜单与 cl-04 笔记抽屉两个 FAIL 经真实鼠标划选/正确交互路径复现后判定为**脚本问题非产品缺陷**（修正后 PASS）。
- 文档（Jason，`review-round5-docs.md`）：**0 Critical / 2 Major / 1 Moderate / 1 Minor**——Major-1 待办清单变更记录表分隔行错插在 v2.18 行之后（v2.18 被渲染成表头）、Major-2 P3 行断行且含 form feed（0x0C）字符、Moderate-3 README「功能总览」空置且 7 条要点悬浮于更新记录之后、Minor-4 需求文档目录缺 §5。五处版本登记（v1.127/v1.79/v1.44/v2.18）一致。

### 10.2 修复记录

| 域 | 编号 | 修复 | 验证 |
| --- | --- | --- | --- |
| 前端 | E2E #1 | 阅读页横向溢出根因修正：`ReaderChatPanel.vue:171` 行内公式改 inline-block + max-width/overflow-x（聊天区 Markdown 列表被撑破）+ `MdRender.vue:121` KaTeX MathML 可访问性分支全局 overflow:hidden（目录省略号第 4 轮已生效，复验确认） | fx-01~04 PASS；全页溢出 0 |
| 前端 | E2E #2 | 点击目标 ≥24px：`ReaderView.vue:679` toolbar 按钮 min-height:24px、`ReaderChatPanel.vue:184` .chat-copy min-height:24px | fx-05 PASS（重提/重建/复制均 24px） |
| 前端 | E2E #5 | /profile `el-input-number` 步进按钮 22→24px（ProfileView.vue:488 scoped deep 覆盖，6 处） | fx-10 PASS（25×24） |
| 前端 | E2E #3 | /rag/15 KaTeX 行内公式窄容器溢出：`RagDetailView.vue:291` .chunk-text/.read-area 内 .katex max-width+overflow-x；MathML 分支由 MdRender 全局规则消除 | fx-06/07 PASS |
| 前端 | E2E #4 | `--text-secondary` #6b7280→#5f6672（theme.css:12，注释同步）：纯白 5.78:1 / 浅灰 #f5f6f7 5.35:1，均 ≥4.5 | fx-08/09 PASS + 单测锁定旧值 <4.5 |
| 前端 | n1 | 魔法常量 120000 收口：constants.ts 新增 `AI_TEST_TASK_TIMEOUT_MS`（设置页任务轮询）/`HTTP_TIMEOUT_MS`（axios），SettingsView 3 处 + client.ts 1 处替换；rg 仅剩常量定义 | 单测 16 用例含源码残留扫描 |
| 前端 | n2 | 6 处未使用声明清理：DoodleToolbar defineProps、HomeView onDragLeave 参数、RagView 死函数 mergedCount、ReaderView 2 处解构 | vue-tsc --noUnusedLocals 0 错误 |
| 文档 | Major-1 | 待办清单变更记录表分隔行移到表头后（v2.18 恢复为数据行） | markdown-it：v2.18 出现在 `<td>` 非 `<th>` |
| 文档 | Major-2 | P3 行断行合并 + form feed 清除（node + frontend 文本修复） | 全文件无 U+000C；P3 行单行完整 |
| 文档 | Moderate-3 | README 7 条功能要点移入空置的「功能总览」，更新记录表格后悬浮块删除 | markdown-it tables=6 正常 |
| 文档 | Minor-4 | 需求文档目录补 §5 已知不足与待办 | 锚点可跳转 |
| 文档 | E2E 声称 #1 | README「文件夹多级归类与拖拽排序规划中」→「文件夹多级归类规划中（书籍卡片拖拽排序已实现）」（E2E 实测 POST /books/reorder 已工作） | 与前端 DOM/网络证据一致 |
| 运维 | — | 后端服务重启（旧进程早于 m-5 回退，仍运行问题指纹缓存键；已停 2 个 uvicorn 后以 venv python 重启，PID 9300） | health 200 |

### 10.3 验证数据（主控复核）

| 项 | 结果 | 说明 |
| --- | --- | --- |
| 后端全量 | ✅ 291 passed | pytest tests 全绿（Arendt 复核）；ruff 全绿；服务重启后 health 200 |
| 前端全量 | ✅ 103 tests + 0 err | vitest 103/103（新增 round5-fixes 16 用例）；vue-tsc 常规与 --noUnusedLocals 双模式 0 错误；vite build 通过 |
| E2E 复验 | ✅ 10/10 | r5_fix_verify.js：溢出/省略号/按钮 24px/对比度/input-number 全部 PASS，零真实写（aiPosts=0、blockedWrites=0） |
| 文档渲染 | ✅ ALL_DOCS_OK | markdown-it 断言：待办清单/README/需求文档表格正常、无 form feed、v2.18 为数据行 |
| 报告 | .e2e_logs/review-round5-{backend,frontend,docs}.md、e2e-report-round5.md、fix-round5-frontend.md、r5_*.json/js | 仅本地留存（gitignore） |

### 10.4 遗留与登记

1. **MO3 流式内核抽取（useStreamSession）**：重复 66.2% 属结构性架构债，已登记待办清单.md:23 P2（两阶段低风险抽取方案已给出，含 4 处行为差异参数化与行为契约测试），本轮不强行实施以控制回归风险；**第 6 轮已实施完成，见 §11.1**。
2. **后端 Minor 收口**：`assign_clusters` 124 行（B 组已跟踪）、`_cache_key` 兼容形参（有意保留）、httpx/StarletteDeprecationWarning（依赖告警）均为登记项，无需代码改动。
3. **工具链审计**：诊断探针 `r5_probe_settings.js` 曾真实写库 1 次（settings timeout 600→599），已最小写还原为 600 并复核；后续探针一律走拦截。
4. **全库文档 form feed 专项清理**：待办清单 P3 行、审查报告 §8.4 旧段落、使用手册-AI与资产 2 处路径（`frontend/src/views/SettingsView.vue` 的 `f` 被替换为 0x0C）均为同类复制粘贴损坏，已全部清除并恢复完整文本；全仓库 md 扫描 FF=0、残缺路径=0。
5. **E2E #1 口径修正**：第 5 轮报告将 443/383 溢出归因「目录」，实测该测量属于聊天面板历史消息 Markdown 列表；目录省略号第 4 轮已生效。修复按根因实施（行内公式 inline-block + KaTeX MathML 分支全局 overflow:hidden），两处现均 0 溢出。
---

## 11. 第 6 轮复审收尾（2026-08-11，修复：主控 + Boyle）

### 11.1 本轮内容

1. **前端 MO3 流式内核抽取（P2 · 待办清单.md:23 核销）**：新增 `frontend/src/utils/streamCore.ts`（34 行：FLUSH_INTERVAL_MS=80 / MATH_MAX_WAIT_MS=500 / POLL_INTERVAL_MS=2000 / THINKING_FLUSH_MS=150 / SSE_ACTIVE_GUARD_MS=500 + hasUnclosedMath / isAbortError / uuid 纯函数）与 `frontend/src/composables/useStreamSession.ts`（249 行：StreamSessionOptions 注入 fetchStream / pollHistory / scrollToBottom / onFinal / onDelta / onError / removeAssistant + 计时参数；F1/F2/MO2/I-2/I-4/I-13/C-1 行为契约全部参数化保留）；`useReaderAi.ts` 约 400→238 行、`useGlobalAi.ts` 约 330→164 行（薄适配层）；流式缓冲/节流/轮询/终态状态机零重复（逐字节重复 66.2%→31.9%），第三个流式调用方可直接复用。
2. **后端死代码清理 + 缓存失效接线**：删除 `rag_input.build_llm_input`（全库零引用）；删除 `ai_context.page_image_data_uri`（+ `base64`/`page_image_path` 未用导入，ruff --fix）+ `test_pdf_media.py::testpage_image_data_uri`；`clear_session_cache()` 接线 `books_service.delete_book`、`routes/assets.py` 的 `delete_book_asset` 与 `delete_book_asset_item`（删书/删资产时失效会话缓存）。
3. **台账漂移修正**：待办 L23（流式内核抽取）→ 完成；L24（资产页「移除该书资产」入口）→ 实际已实现（RagDetailView.vue:136 按钮 + deleteBook 接线），状态更正；L26（输入面死代码清理/接线）→ 部分完成（build_llm_input 已删、clear_session_cache 已接、page_image_data_uri 已删），剩余参数校验补强仍待办。

### 11.2 验证数据（主控复核）

| 项 | 结果 | 说明 |
| --- | --- | --- |
| 前端全量 | ✅ 123 tests + 0 err | vitest 123/123（基线 103 + 新增 20）；vue-tsc 常规与 --noUnusedLocals 双模式 0 错误；vite build 通过 |
| E2E 冒烟 | ✅ 14/14 | r5_mo3_smoke.json：阅读页 + 主页全局 AI 流、停止/重发、KaTeX；零真实 AI 调用、零 console error |
| 后端全量 | ✅ 290 passed | pytest tests 全绿（-1 = 删除的旧测试 page_image_data_uri，属预期）；ruff 全绿；后端重启后 health 200 |
| 文档渲染 | ✅ ALL_DOCS_OK | markdown-it 断言：README/各文档表格 + 版本登记一致（使用手册 v1.129 / 技术栈 v1.81 / 重构 v1.46 / 待办 v2.20 / 需求 v1.86） |
| 报告 | .e2e_logs/fix-round6-mo3.md | Boyle 补写（行数对比、契约逐条、冒烟数据），仅本地留存（gitignore） |

### 11.3 遗留与登记

1. 待办 L25（参数校验补强：ProgressIn 无 ge/le）、L27（前端交互细节：主页搜索跳章/预设按钮未选章提示/划词关闭清残留）仍待办。
2. D8 文件夹 UI、D9 md 页码、C-2 大视图拆分、B 组后端拆分（assign_clusters 等 TOP5）、C-1 run_in_session 统一工具维持原优先级，见待办清单。
---

## 12. 第 7 轮复审收尾（2026-08-11，修复：主控 + 四域 subagent）

### 12.1 审查结论

- 后端（Arendt，`review-round6-backend.md`）：**0 Critical / 0 Major / 3 Minor / 5 观察项**；第 6 轮改动全部核验通过（死代码零残留、clear_session_cache 三处接线正确、无循环依赖）。
- 前端代码（Ptolemy，`review-round6-frontend.md`）：**0 Critical / 0 Major / 0 Moderate / 3 Minor + 3 Info**；MO3 内核重复度独立复算一致（66.2%→31.9%），F1/F2/MO2/I-2/I-4/I-13/C-1 契约零回归；63 文件依赖图 0 环。
- 前端 E2E（Feynman，`e2e-report-round6.md`）：**193 项 = 190 PASS / 0 FAIL / 3 WARN**；第 5 轮 6 个 FAIL 全部核销；2 项 Minor 观感 + 1 项工具链审计（DELETE 未拦截事故已从 .recovery 还原，12/12 收尾复核基线无漂移）。
- 文档（Jason，`review-round6-docs.md`）：**0 Critical / 2 Major / 2 Moderate / 2 Minor**——D-01 变更记录行序倒置（第 6 轮登记引入）、D-02 MO3 未登记分册、D-03 技术栈 §5 声称无实、D-04 分册 4 处死引用、D-05 六/五处口径、D-06 超长行。

### 12.2 修复记录

| 域 | 编号 | 修复 | 验证 |
| --- | --- | --- | --- |
| 后端 | B1 | 删除 `repositories/books.py::create_book`/`add_chapters`（m-6 合并后死代码，AST+rg 零引用）；5 处手册引用同步改写为 `create_book_with_chapters` 单事务口径 | 全量 291 pytest（+1 B2）+ ruff 全绿 |
| 后端 | B2 | 补回归测试 `test_delete_book_and_asset_clear_selection_cache`：删资产/删书后挑选会话缓存清空（对应三处接线） | 该用例 + 缓存套件 3 passed |
| 后端 | B3 | `rag_router.select_knowledge` 缓存键 `chapter.id` 加 None 防御（与 `_select_llm` 对称；chat 路由已兜底，当前不可达） | 全量回归通过 |
| 文档 | D-01 | 五处变更记录「最新行移至顶部」（使用手册/技术栈/重构/待办/README），恢复「首行=文档版本」口径 | markdown-it `<td>` 断言（首行=最新） |
| 文档 | D-02 | MO3 内核登记：前端分册 §7.2（streamCore）/§7.3（useStreamSession）+ 总纲 §2.2 模块地图 | 渲染断言 |
| 文档 | D-03 | 技术栈 §5 新增 5.3「前端流式内核约定」（v1.81 变更行声称的内容落正文） | markdown-it |
| 文档 | D-04 | 分册死引用清零：AI与资产 4 行（_parse_llm_json/_normalize_skills/_build_llm_input/build_llm_input）、阅读与PDF 2 处（_page_image_data_uri + §9.5 回退口径改决策 36 现行语义） | rg 零残留 |
| 文档 | D-05 | 待办 v2.20 行「六处登记」→「五处版本登记 + 审查报告 §11 同步」 | — |
| 文档 | D-06 | 需求-里程碑与验收 M9（2268 字符）/M8（1577 字符）超长行拆分 | 最长行 2268→660 |
| 前端 | F1 | `useStreamSession.stream()` 同步抛错防御：复位 streaming/streamError、不启动轮询、移除空气泡（不再永久卡 true） | streamSession.test.ts 15 用例（+1） |
| 前端 | F3 | C-2 台账行数改 script 块口径（实测：PageDoodleCanvas 430 / ReaderView 401 / SettingsView 370 / GraphView 302 / HomeView 265 / ProfileView 156 / RagView 151） | 独立实测复核 |
| 前端 | E2E #1 | `/rag/15` 双滚动条修复：`.rag-detail` 由 `height:100%+overflow-y:auto` 改 `min-height:100%`（仅文档级滚动），顺带合并重复 CSS 规则 | r6f_visual 42/42 PASS |
| 前端 | E2E #2 | KaTeX 3px 垂直测量差：视觉不可见，审查建议无需处理 → 登记核销 | r6f_reader_pdf WARN 记录 |

### 12.3 验证数据（主控复核）

| 项 | 结果 | 说明 |
| --- | --- | --- |
| 后端全量 | ✅ 291 passed | pytest tests（+1 B2 回归测试）；ruff 全绿 |
| 前端全量 | ✅ 124 tests + 0 err | vitest 124/124（+1 F1 同步抛错用例）；vue-tsc 常规与 --noUnusedLocals 双模式 0 错误；vite build 通过 |
| E2E 复验 | ✅ 20/20 + 42/42 | r6f_rag.js 与 r6f_visual.js 重跑全 PASS，双滚动条修复生效；零真实写（DELETE 已拦截） |
| 文档渲染 | ✅ ALL_DOCS_OK | markdown-it：五处表格渲染、首行=最新、FF=0、死引用 rg 零残留 |
| 报告 | .e2e_logs/review-round6-{backend,frontend,docs}.md、e2e-report-round6.md | 仅本地留存（gitignore） |

### 12.4 遗留与登记

1. 待办 L25（参数校验补强：ProgressIn ge/le）、L27（前端交互：主页搜索跳章/预设提示/划词关闭清残留）维持待办。
2. E2E #2 KaTeX 3px 测量差登记为已知观感项（视觉不可见，无需处理）；#3 CJK-? 为正常中文问号（非乱码）。
3. C-1 `run_in_session`（12 处独立会话候选）、B 组超长函数拆分（assign_clusters 124 行等）、C-2 大视图拆分维持原优先级。

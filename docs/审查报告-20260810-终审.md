# 全库终审报告（2026-08-10 · 四域并行复审）

> 方法：superpowers `requesting-code-review` + `dispatching-parallel-agents`——4 个只读审查子代理并行（后端核心服务层 / 后端接口数据层 / 前端逻辑层 / 前端视图+文档体系），父进程对全部关键项做交叉复核（逐行阅读 + rg 引用核实），并运行测试基线。
> 基线：前端 `vitest run` **47/47 全绿**（含 chatStream F1/F2 回归 4 例）；后端 `pytest tests -q` **259/259 全绿**（含 F3~F7 回归）。
> 审查对象：当前工作区（含未提交的 F1~F7 修复与文档改动）。本报告为只读审查产物，未修改任何代码。

## 0. 总体结论

- **F1~F7 修复完整、无回归**：四域一致确认修复正确且回归测试到位（前端 chatStream.test.ts 4 例 + 后端 test_review_fixes_20260810.py 5 例）。
- **新增 1 个 P0 功能缺陷**：responses 模式（默认 `ai_mode`）多轮对话丢失 assistant 历史 + thinking 参数未映射。
- **新增 4 个 P1 后端问题 + 4 个 P1 前端问题**，以及一批 P2/Minor 与文档滞后项。
- 昨日「未复核」项本轮全部复核：F11/F13/F15 **确认存在**，F16 裁定「机制存在、UI 不可达」（防御式加固），F17 已修复，D4~D7 **全部属实**。

## 1. Critical（P0，建议立即修）

### C-1 responses 模式多轮历史丢失（新发现，默认配置路径）— ✅ 已复核确认
- `backend/app/ai/client.py:226-227`：`_build_body` 的 responses 分支只拼接 `role=="user"` 消息为单个 `input` 字符串，**assistant 历史整体丢弃**；thinking/`reasoning_effort`/`enable_thinking` 也全部未写入请求体。
- `ai_mode` 默认值就是 `"responses"`（`core/config.py:22`），本地 `.env` 用 chat 才绕开。
- 影响：多轮追问（“第二题呢？”）无上一轮上下文；挑选器「低 max_tokens 禁思考」设计在 responses 模式失效；与技术栈-AI.md「注入最近 6~10 轮历史」、需求 3.4.13 直接矛盾。
- 修法：responses 分支将 messages 转官方 `input` 数组（保留 assistant 轮次）、system 放 `instructions`；thinking 按 DeepSeek responses 规范（`reasoning: {effort}`）补映射或降级文档口径；补测试（现 `test_ai_params.py:42-47` 把「responses 不带 thinking」断言为预期，需同步改）。

## 2. Important（P1，应修）

### 后端
- **I-1 F6 同根因残留**：`services/graph/clustering.py:156-170` `post_classify_book` 的 `_cluster_repr`/`_posterior_keywords` 未剔除 `_GENERIC_DOMAIN_TERMS`，仅共享 3 个泛词（定理/定义/引理）即触发 T_POST 簇迁移；`archive_book_task` 每次归档都触发，发生概率不低于 F6。修法同 `_cluster_keywords`，补姊妹回归测试。✅ 已复核确认
- **I-2 归档 hot 画像跨书污染**：`services/profile_service.py:200-201` 归档条目无条件取 `hot.highlights/questions`，不校验 `hot.current_book_id == book.id`；书架直接归档非当前热书时，档案带上别本书划线/问题，进而污染挑选器与相关度。修法：`current_book_id != book.id` 时置空。✅ 已复核确认
- **I-3 `find_active`→`submit` 非原子（TOCTOU）**：`api/routes/assets.py:26-29,43-46` 并发请求可同时通过防重检查，重复提交总结/归档任务，资产 version/内容互相覆盖（F4 只收窄了窗口）。修法：任务表偏唯一索引（`status IN ('queued','running')`）或进程内锁。✅ 已复核确认
- **I-4 OCR 引擎/语言不在 meta 签名**：`services/vision_image.py:54-60` `_signature()` 仅含 max_width/quality/trim；切换 `VISION_OCR_ENGINE/LANG` 后 `_meta_valid` 仍 True → 不 purge → 旧引擎/旧语言 `page_*.txt` 继续进 RAG（F7 在 OCR 维度复发）。修法：签名纳入 OCR 参数。✅ 已复核确认
- **I-5 PATCH /books/{id} 无效 folder_id → 500**：`api/routes/books.py:113-121` 无 folder 存在性校验，FK 开启下直接 IntegrityError。修法：路由或 `update_book` 校验并返回 400/404。✅ 已复核确认
- **I-6 损坏 PDF 上传 → 500 而非 400**：`parsers/pdf.py:20` `pymupdf.open()` 未包装异常；epub 路径已正确处理（`epub.py:107-108`），PDF 不一致。✅ 已复核确认
- **I-7 `get_db()` 无 commit 隐式契约**：`core/database.py:144-149` 仅 yield+close；持久化全靠「仓储内部 commit」约定，路由/服务层未来直接 `db.add` 会被静默回滚。修法：请求级 commit 或落文档写明契约。✅ 已复核确认
- **I-8 测试缺口（高风险路径）**：responses 多轮历史与 thinking 参数；`select_global_knowledge` 缓存命中按新问题重取（F3 只测书级）；`post_classify_book` 泛词迁移；无 jpg 仅 txt 的 purge 场景；并发重复提交幂等；`/api/settings/ai/test[-vision|-selector]` 路由测试。

### 前端
- **I-9 F15 文本书签段落定位从未生效**：`components/BookmarkDrawer.vue:103` `addCurrent` 固定 `para_pos: null`，`scrollToPara` 跳转链路完备但创建端无段落捕获 → 书签只到章节头，需求 3.3/3.5「位置书签（章节+段落）」落空。修法：ReaderView 维护 `topVisiblePara` 传入写入。✅ 已复核确认
- **I-10 F11 划词菜单右侧出屏**：`composables/useReaderSelection.ts:40` 钳制 `innerWidth-280`，而 7 按钮菜单实际宽 ≈520px，右侧划词时最右按钮被裁出屏幕。修法：渲染后实测 `offsetWidth` 钳制或右对齐。✅ 已复核确认
- **I-11 HomeView 卸载不清理全局 AI**：`views/HomeView.vue:46` 创建 `useGlobalAi()`，全文件无 unmount 钩子 → 离开主页后 2s 轮询/在途 SSE/flushTimer 泄漏，返回主页产生同 session 双实例并发。修法：`onBeforeUnmount(() => globalAi.dispose())`。✅ 已复核确认
- **I-12 TaskCenter 卸载后轮询不停**：`components/TaskCenter.vue:106-108` `onBeforeUnmount` 未置 `polling=false`，`while(polling)` 循环在切路由后继续每秒请求 listTasks 直到任务结束。✅ 已复核确认
- **I-13 轮询补增量与 pendingText 叠加 → 回复重复文本**：`useReaderAi.ts:178-189`、`useGlobalAi.ts:103-113` 覆盖比较未计入未 flush 的 `pendingText`，80ms 节流/公式延迟窗口内随后 `flushDelta` 再追加同一段。修法：比较加 `pendingText.length` 或先 flush 再比。✅ 静态确认
- **I-14 openMindmap 无序号守卫**：`composables/useReaderMindmap.ts:29-46` 请求乱序时旧章节响应覆盖新章节脑图；手册 §21.4.6 自认「已有漏网处」再添一处。修法：模块内 seq 防护。
- **I-15 F16 裁定 + 加固建议**：重建按钮 `:loading="pageCacheBusy"` 已禁用重复点击（UI 不可达，Sartre 判误报），但 `rebuildPageCache` 无 `pageCacheBusy` 防重守卫（Beauvoir 判存在），且轮询闭包读**实时** `bookId.value`（切书窗口期查错书；当前 `:key="fullPath"` 重挂载使其实际不可达）。建议低成本防御式加固：入口 `if (pageCacheBusy.value) return` + `startPolling` 捕获提交时 bookId 快照。✅ 已复核确认（裁定见 §4）
- **I-16 前端测试缺口**：F8/F9/F14 修复无回归测试（上轮「随 P2 补」未兑现）；`utils/task.ts::waitForTask`（abort/超时）与 `utils/graphOption.ts` 零覆盖。

### 需求-实现背离（昨日 D4~D7 复核全部属实，待拍板）
- D4 批量导入队列 + 真实进度（决策 32）：`HomeView.onFilePicked` 单文件、无 `onUploadProgress`。✅ 属实
- D5 书签拖拽归组/分组管理（决策 24）：无拖拽、无分组重命名/删除。✅ 属实
- D6 阅读页调书位/文件夹排序（决策 21）：仅书架 `reorderBooks` 换位。✅ 属实
- D7 任务失败可重试（技术栈-后端 §3.5）：全库无重试入口。✅ 属实
- D2（决策 22/32 与代码相反）：导入仍自动预提取、无卡片手动入口。✅ 属实（昨日已报）

## 3. Minor（可选，可随重构轮清理）

- **死代码**：`rag_input.build_llm_input`（生产零引用）、`ai_context.page_image_data_uri`（仅测试引用）、`schemas/common.py` `BookOut`/`ChapterOut`、`import_service._upload_dir`、`api/vision.ts:10 getPageText`、`RagView.vue:42 mergedCount`（昨日已报未清）；仅文件内使用的 export 若干（graphLabel/graphOption/graphEdges/math）。
- **F13 按钮乱码**：`ProfileView.vue:114` `?? ??`（损坏 emoji），一行可修（`✏️ 编辑冷画像`）。✅ 已复核确认
- **F7 残留边缘**：`vision_image.py:330-331` 参数变更 purge 仅在存在旧 jpg 时执行，全 original 源的书 OCR txt 不清（影响小）。
- **epub.py:112-117 注释乱码**（`???????spine?????`）。✅ 已复核确认
- **任务时间戳时区混用**：`tasks/__init__.py:171,246,264` 本地时间 vs `created_at` UTC，同任务行 `finished_at - created_at` 计算错误；统一 `core/time.utcnow()`。
- **状态码语义**：assets/folders/tasks 若干 200+code 0 应为 400/404。
- **路由绕层直连模型**：`reading.py:56`、`folders.py:31`、`graph.py:108` 未走仓储（昨日已列未清）。
- **`repositories/graph.py:84` 与 `repositories/notes.py:10` `list_notes` 双实现**，语义易漂移。
- **参数校验缺口**：`search?limit` 无下界（负值=无上限）、`position` 无 clamp、`NoteUpdate.note_type` 无 pattern、`crop_image` 无长度上限。
- **`reorder_books` 计数虚报**：返回 `len(ordered_ids)` 而仓储静默忽略不存在 id。
- **`asset_to_dict` 与 `read_asset_content` 不一致**：/asset 带出 `merged_book_ids` 元数据。
- **`main.py:60-64` migrate_book_media 静默吞异常**；health 无 DB 探活。
- **`_blank_ratio` 纯 Python 逐像素**（每页约 1~2s），可换 numpy/抽样。
- **`clear_session_cache` 前缀匹配**：session_id 含冒号可能误清（当前低风险）。
- **流式编排重复（架构债）**：`useReaderAi`/`useGlobalAi` 约 120 行逐行相同 + `api/chat.ts` 两段 SSE 解析 60 行重复 + `hasUnclosedMath` 双份 → 抽 `useStreamingChat(sendStream, onEvent)` 工厂 + 共享 SSE 解析器。
- **流式健壮性**：`chatAbort` 无身份校验（终态窗口旧流清新流句柄）；AbortError 判定过窄（`instanceof DOMException`）；无前端看门狗（N 秒无数据 abort）。
- **`waitForTask` 监听器累积**；`formulaCache` 无容量上限（与 splitCache LRU 不一致）。
- **`saveDoodleNote` 数组下标定位**（当前 el-dialog 遮罩下不可达，脆弱设计）；**MdRender** img 未 `complete` 时仍发图。
- **杂项**：`types.ts:82-83` 注释错位；`useReaderAi.ts:124` `switchMode` 返回类型 `void`；`BookItem` `total_chapters`/`chapter_count` 双字段需核对；待办清单 C-2 行数 529→587 过时；手册 §21.1「8 件套」实为 9 个 composable。

## 4. 交叉验证裁定（代理分歧项）

| 项 | 分歧 | 裁定 |
| --- | --- | --- |
| F16 重建页缓存重复点击 | Beauvoir=确认存在；Sartre=误报 | **机制存在、UI 不可达**：按钮 `:loading="pageCacheBusy"` 禁用 + ElMessageBox 模态阻断；缺 `pageCacheBusy` 防重守卫与 bookId 快照属防御式加固（低成本，建议做） |
| F11 划词菜单出屏 | Beauvoir=Minor；Sartre=Critical | 交互缺陷非数据损坏 → **Important（P1）** |
| F15 书签段落定位 | 两端一致=存在 | **Important（P1）**（需求核心承诺落空） |
| 待办清单 §3 `term_aliases` 损坏 | Sartre 报告损坏 | **未复现**：当前文件控制字符 0（book_keywords 粘连已修复），`term_aliases` 检索不到 |
| 后端测试数 | Harvey=258 | 实际 **259 passed**（统计口径差异，无实质问题） |
| HomeView 行数 | Sartre=587 | 实测 **587 行**（待办清单 C-2 写 529 过时） |

## 5. 文档对账表（关键项）

| 文档 | 声称 | 代码实际 | 判定 |
| --- | --- | --- | --- |
| 使用手册-后端核心 §2.3 | 导入「内容 hash 去重」 | 仅计算/存储 content_hash 供资产合并，无导入去重 | ❌ 夸大（新发现） |
| 使用手册-后端核心 §2.1 | 密钥只放 .env 不入库 | API Key 写入 settings 表（掩码回显） | ⚠️ 口径不符 |
| 使用手册-后端核心 §20.4 | 缓存无上限/超大文件无校验/导入无测试 | 均已实现（200 条上限/分块拦截/test_import_service 存在） | ❌ 过时 |
| 使用手册-后端核心 §2.8 | API 端点表 | 缺 /search /reorder /media /pages /chat /tasks /ai 等 | ❌ 不完整 |
| 使用手册-AI与资产 §19.1/19.4 | 会话缓存整包回放 | F3 后只复用挑选结果、chunks 重取 | ❌ 滞后 |
| 使用手册-图谱与画像 §11.5 | 簇合并规则 | 未记 F6 泛词剔除 | ❌ 滞后 |
| 技术栈-AI.md :25 | 注入最近 6~10 轮历史 | responses 模式不成立（C-1） | ❌ 夸大 |
| 技术栈规范 ADR-009 | 任务并发化「未落地待实现」 | 任务系统已落地（v1.93） | ❌ 过时 |
| 技术栈规范/重构规范 头部 | 关联 需求 v1.69 / 手册 v1.108 | 实为 v1.86 / v1.119 | ❌ 过时 |
| README.md | 需求 v1.72 / 手册 v1.110 | 实为 v1.86 / v1.119 | ❌ 过时 |
| 需求文档 v1.37 | 相同内容重复导入共享资产 | 需手动 dedupe，导入不自动合并 | ⚠️ 部分背离 |
| 需求文档 v1.86 | 参数变更 meta 签名整目录重建 | 仅 VLM 三参数，OCR 参数不触发 | ⚠️ 泛化不符 |
| 多语言重构评估/书籍缓存方案 | 关联 docs/性能优化路径.md | 文件不存在 | ❌ 断链（新发现） |
| 需求/手册/待办清单主版本 | 变更记录 | 与最近提交（v1.86/v1.81/v1.92）逐条对得上 | ✅ 一致 |
| 知识图谱聚类算法.md §10 | 标「待定稿」 | 已被改进方案 O1~O10 定稿取代 | ❌ 过时 |

## 6. 昨日未复核项复核结论

| 项 | 结论 |
| --- | --- |
| F11 划词菜单右侧裁屏 | ✅ 确认存在（Important I-10） |
| F13 ProfileView 按钮乱码 | ✅ 确认存在（Minor，一行可修） |
| F15 文本书签 para_pos 恒 null | ✅ 确认存在（Important I-9） |
| F16 重建重复点击轮询早退 | ⚠️ 机制存在、UI 不可达；建议防御式加固（I-15） |
| F17 dispose 不清 pending 防抖 | ✅ 已修复（useReaderDoodle dispose 先 flush 再清定时器） |
| D4 批量导入队列/进度 | ✅ 属实（待拍板） |
| D5 书签拖拽归组 | ✅ 属实（待拍板） |
| D6 阅读页调书位/文件夹 | ✅ 属实（待拍板） |
| D7 任务失败可重试 | ✅ 属实（待拍板） |


## 6.5 运行时复核复现（2026-08-10 终审后二轮）

> 方法：临时复现测试（后端 pytest 8 例 + 前端 vitest 4 例），断言语义为「断言缺陷现象存在」——PASSED = 缺陷复现成功，FAILED = 未复现；另对 5 项做静态复核。全部运行于当前工作区（含未提交 F1~F7 修复）。

### 后端运行时复现 8/8 成功

| 项 | 复现证据 |
| --- | --- |
| C-1 responses 丢历史 | `body["input"]` 仅含两条 user 文本，assistant 的 `"42"` 不在；thinking/reasoning_effort 未写入 |
| I-1 F6 泛词残留 | 仅共享 定义/定理/引理/推论/证明 5 个泛词，`post_classify_book` 仍返回错误簇（矩阵簇） |
| I-2 画像跨书污染 | 归档 B 书后，warm 条目 `highlights` 带上 A 书划线 |
| I-3 TOCTOU | Barrier 同步双线程均通过 `find_active`，产生 2 个不同 task_id |
| I-4 OCR 参数不入签名 | 改 `vision_ocr_engine/lang` 后 `_signature()` 不变 |
| I-5 folder_id 500 | FK 约束异常穿透（TestClient 直接抛异常，生产即 500） |
| I-6 损坏 PDF 500 | 解析异常穿透未包装为 400 |
| I-7 get_db 无 commit | 请求级 session close 后新增行静默丢失 |

### 前端运行时复现 4/4 成功

| 项 | 复现证据 |
| --- | --- |
| F16 双重建丢轮询 | `rebuildPageText` 调 2 次，`getPageTextTask` 仅查 T1 一次，`pageCacheBusy` 提前复位 |
| I-14 脑图乱序覆盖 | 旧章节响应后到覆盖新章节 `mindmapData` |
| I-10 划词菜单出屏 | left=1500，1500+520=2020 > 1920 视口 |
| I-13 轮询重复文本 | `ab$x` 未闭合期间轮询覆盖 content，flushDelta 追加 → `ab$x$x` |

### 静态复核 5 项（代码级证据，无需运行）

| 项 | 证据 |
| --- | --- |
| F11 菜单钳制不符 | `useReaderSelection.ts:40` 钳制 `innerWidth-280`，菜单实宽 520 |
| F13 ProfileView 按钮乱码 | `ProfileView.vue:114` 字面 `?? ??` |
| F15 书签段落定位恒 null | `BookmarkDrawer.vue:103` 恒 `para_pos: null` |
| I-11 HomeView 泄漏 | 无 `onUnmounted` 钩子、无 `globalAi.dispose()` 调用 |
| I-12 TaskCenter 轮询不停 | `onBeforeUnmount` 未置 `polling=false`，`while(polling)` 继续 |

### 判定

- **全部属实，无一项误报**；D4~D7 终审已确认属实（待拍板）。
- 临时复现测试断言语义反向（修复后变 FAILED），**已删除不入库**；修复时按 F 系列惯例补正式回归测试。

### 修复状态（2026-08-10 完成）

- **P0 C-1** ✅ 已修复：`ai/client.py` responses 分支转官方 `input` 数组保留 assistant 轮次，thinking 参数映射 `reasoning:{effort}`；新测试 `test_build_body_responses_keeps_assistant_history` / `test_build_body_responses_thinking_params`。
- **后端 I-1~I-7** ✅ 已修复：`_posterior_keywords` 剔除泛词（`lexicon.py`）；归档画像校验 `current_book_id`（`profile_service.py`）；`submit_dedupe` 原子防重（`tasks/__init__.py` + `assets.py` 两个端点）；OCR 参数入 meta 签名（`vision_image.py`）；PATCH folder_id 校验 400（`books.py`）；损坏 PDF 包装 ValueError→400（`parsers/pdf.py`）；`get_db` 请求级 commit（`core/database.py`）。
- **前端 I-9~I-15** ✅ 已修复：书签 `para_pos` 真实段落定位（`BookmarkDrawer.vue` + `ReaderView.vue` 视口段落计算）；划词菜单按实宽钳制（`useReaderSelection.ts`）；HomeView 卸载 `dispose()`、TaskCenter 卸载停轮询；轮询差量前缀对齐（`useGlobalAi.ts`/`useReaderAi.ts`）；脑图请求序号守卫（`useReaderMindmap.ts`）；页缓存重建防重 + bookId 快照（`useReaderPageCache.ts`）。
- **验证**：后端 pytest **269 passed**（含新增 9 例）；前端 vitest **54 passed**（含 `tests/review-fixes-20260810.test.ts` 7 例）+ vue-tsc + vite build + ruff 全绿。
- **测试覆盖口径（修正）**：有自动化回归的项为 C-1（`test_ai_params` 2 例）、后端 I-1~I-7（各对应新增 pytest）、前端 I-10/I-13/I-14/I-15（review-fixes vitest）；I-9 于复审轮下沉纯函数后补单测（`viewportTopPara`）；I-11/I-12 为静态复核 + 代码修复、未补单测（计入 I-16 测试缺口，见 §6.6）。

### 6.6 复审修复轮（2026-08-10 第三轮收尾，代码复审后）

> 终审 §6.5 落地后复读代码，修复下列残留；本轮全部经全量验证。

- **前端 Critical**：I-13 修复后轮询补增量只写 `pendingText` 未调 `scheduleFlush` —— SSE 静默时界面冻结不渲染；修复：差量补入后立即 `scheduleFlush(assistant)`，新增 I-13b 回归（flush 链静默后轮询增量在下一拍渲染，`review-fixes-20260810.test.ts`）。
- **前端 Important**：I-9 定位逻辑下沉 `src/utils/viewport.ts::viewportTopPara`（纯函数，`.offsetTop - el.offsetTop` 同参照系），新增 2 例单测；I-15 快照改实例字段 `pollBookId` + `disposed` 标志（可见性恢复路径不再丢快照、dispose 与提交在途竞态封闭）。
- **后端 Important**：`graph.py::_submit_graph_task` 5 个调用点由 `find_active→submit` 改 `submit_dedupe` 原子防重（消除 TOCTOU）；`parsers/pdf.py` 文本抽取异常包装 ValueError（路由 400）、`extract_pdf_cover` 尽力而为容错；`books.py` 错误契约统一（上传失败由 `fail(400)` HTTP200 改 `HTTPException(400)`，同步 3 例测试断言）+ `update_book` 捕获 IntegrityError → 400；`tasks/__init__.py` docstring 注明「submit_dedupe 仅进程内防重，多进程需 DB 唯一索引」。
- **测试工程**：conftest 同步化夹具适配新增 `submit_dedupe_sync`；I-3 并发测试改事件门控 + 收尾等待（消除后台线程跨测试污染）；test_db_contract 补异常回滚路径测试 + 直插行清理（消除污染书架排序测试）；test_import_service 3 例错误断言随契约统一更新。
- **验证**：后端 pytest **269 passed**；前端 vitest **54 passed** + vue-tsc + vite build + ruff 全绿。
### 6.7 第 2 轮四域审查修复轮（2026-08-10 第四轮，代码复审后）

> 第 2 轮四域并行复审（Anscombe 后端核心 / Cicero 接口+测试 / Dewey 前端 / Lagrange 文档），**无 Critical**；本轮全部落地并全量验证。

**后端修复（I-2 收敛缺口 + Minor）**
- `tests/conftest.py`：同步化夹具补 `assets_route.submit_dedupe` + `vision_route.submit_dedupe`（防测试内真实后台线程与 teardown drop_all 竞态）。
- `api/routes/vision.py`：页缓存重建由裸 `submit` 改 `submit_dedupe`（name_prefix="vision-rebuild"），与 assets/graph 收敛。
- `api/routes/folders.py`：唯一残留 `fail(404)`（HTTP200+code404）改 `HTTPException(404)`；create/delete 并发 FK 冲突（IntegrityError）→ 400。
- `api/routes/assets.py`：非法 kind / section / 索引越界由 `ok(None)`（code 0 伪装成功）改 `HTTPException(400)`；同步 3 例测试断言。
- `services/graph/clustering.py`：初始吸收判定（`Σmin(kb,ko) ≥ 2`）剔除 `_GENERIC_DOMAIN_TERMS`——仅共享「定理/定义」的书不再误并（F6 只修了 merge 阶段，此为收敛）；新增回归测试。
- `services/vision_image.py`：`prep_page_image` 接线 `_detect_spread`（双页扫描中间空带永不裁，`_trim_bbox` 增 `is_spread` 参数）；`_load_gray` try/finally 防句柄泄漏。
- `tasks/__init__.py`：`submit_dedupe_sync` 锁内仅「检查+落库」、执行移出锁外（慢 fn 不再阻塞防重路径、嵌套防重不死锁）；`executor.submit` 池 shutdown RuntimeError 降级独立线程；新增嵌套防重回归测试。
- `ai/client.py`：`chat()` JSON 解析失败包装 `LLMError`；responses 流补 `response.reasoning_text.delta` 思考映射。
- `services/rag_router.py`：`clear_session_cache` 补清 `global:{session_id}`（精确键 + 前缀）；`vision_extract.py` 进度回调移出锁；`lexicon.generic_domain_terms` 返回 `frozenset`；`parsers/pdf.py` 文档口径与实际下限（1.0x / 2.5x 兜底）一致；`_upload_dir` 保留为测试辅助（`test_import_service` 引用）。

**前端修复（Dewey Important）**
- `useGlobalAi.ts` / `useReaderAi.ts`：流代际守卫 `streamSeq`——abort/切书/模式切换后在途轮询响应直接丢弃（不写共享 `pendingText`）；SSE 活跃期（<500ms 有 delta）不做 DB 补差，消除「DB 已含尾部 + SSE 迟到重放」重复文本；abort 且无内容时移除空气泡；错误消息统一 `instanceof Error` 守卫。
- `useReaderPageCache.ts`：重建轮询终态按**任务快照书**刷新（当前书 ≠ 快照书时不覆盖新书状态）；`reExtractCurrentPage` 书/页快照 + disposed 守卫。
- `useReaderDoodle.ts`：加载在途用户已绘制 → 保留本地绘制不覆盖（服务端数据下次进入再载）；dispose 停止两个 watch。
- `useReaderMindmap.ts`：`watch(currentChapterId)` 自增代际——切章未重开脑图时旧响应丢弃且关闭旧脑图。

**回归测试（+6）**
- 后端 3：`test_assign_clusters_does_not_absorb_on_generic_terms_only`（test_graph）、`test_submit_dedupe_sync_nested_no_deadlock`（test_tasks）、`test_vision_rebuild_uses_submit_dedupe`（test_vision_extract）。
- 前端 3：I-13c 阅读页 SSE 静默轮询补增量渲染、I-13d 流代际守卫（abort 后在途轮询不污染新流）、I-15b 页缓存重建快照书刷新（`review-fixes-20260810.test.ts`，mock 队列 resetAllMocks 防跨测试泄漏）。

**验证**：后端 pytest **272 passed** + ruff 全绿；前端 vitest **57 passed** + vue-tsc + vite build（exit 0）全绿。

**文档同步（Lagrange 10 Important）**：使用手册 v1.121（header + 变更行）、README（索引/变更行/`||` 表格修复/移出跟踪口径）、技术栈 v1.73（ADR-009 并发已落地更正、ADR-010 聚类 §10 已定稿）、聚类算法 §10 定稿、后端核心（§2.1 密钥口径、§2.7 submit_dedupe_sync、§2.8 端点表扩充、§13.4/13.5 与 §20 口径）、AI与资产 §19.1/19.4 F3、图谱与画像 §11.5 F6、断链 2 处、待办清单 v2.8（272/57）。

### 6.8 终审收尾轮（2026-08-10 第五轮，代码复审后）

**F7 · 图谱空关系重复提交（全库最有价值残留）**
- 现象：`count_relations == 0 and count_books > 0` 时每次 `GET /api/graph/books` 都提交 `graph-global-build`；`submit_dedupe` 只防「进行中」任务，成功后（单书库等关系数为 0 的真值场景）每次 GET 都会重复全量重建 + LLM 打分。
- 修复：`tasks/__init__.py` 新增 `find_recent_success`（查询最近一次成功任务，支持时间窗与 name_prefix）；`services/graph/tasks.py::lazy_global_build` 结果携带构建时书籍数；`api/routes/graph.py` 空关系分支先查最近成功构建——书籍数未变则空图谱为真值直接返回不重提；新书导入（书籍数变化）自动触发重建。失败任务不受影响（无 success 记录，访问时仍自动重试）。
- 回归测试：`test_global_graph_no_resubmit_when_empty`——单书库首次 GET 触发构建，成功后连续 GET 均不 building 且 `graph-global-build` 任务仅 1 条。

**F8 · 收尾清理**
- `frontend/src/views/HomeView.vue`：`onUnmounted` 补 `window.clearTimeout(searchTimer)`（搜索防抖定时器卸载泄漏）。
- `backend/app/schemas/common.py`：删除死代码 `fail` / `BookOut` / `ChapterOut`（全库无引用，仅 `ok` 被 17 处路由引用）。
- `backend/app/api/routes/notes.py`：热画像回写裸吞 `except Exception: db.rollback()` 补 `logging.warning`（笔记已由仓储先 commit，不丢失，仅回写失败静默）。
- 待办清单：补登记 P2「流式内核抽取 `useStreamSession`」（`useGlobalAi.ts` / `useReaderAi.ts` 流式差量 / `scheduleFlush` / pendingText 轮询内核重复，此前误记已入清单，实为未登记）。

**验证**：后端 pytest **273 passed** + ruff 全绿；前端 vitest **57 passed** + vue-tsc + vite build 全绿（实测于 2026-08-10）。

### 6.9 第 3 轮四域复审修复轮（2026-08-10 第五轮，代码复审后）

> 第 3 轮四域并行复审（Schrodinger 后端核心 / Meitner 接口+测试 / Pauli 前端 / Kepler 文档），**后端无 Critical，前端 1 个 Critical（C1）**；本轮 Critical + Important 全部落地并全量验证。

**C1 · 阅读器换书时旧书涂鸦被覆盖写入新书（数据损坏，前端）**
- 证据链：`App.vue:20` 按 `route.fullPath` 重挂载 → 重挂载时 `ReaderView.vue:46` 的 `bookId` computed 已读到新书 → 卸载钩子 `disposeDoodle()`（`ReaderView.vue:393`）内 `useReaderDoodle.ts` 的 `dispose` 用 `bookId.value`（新书）+ 旧书 `doodleReadyPage/doodleElements` → `PUT /books/{新书}/annotations` 写入旧书涂鸦；`scheduleDoodleSave` 定时器窗口内切书同型。
- 修复：`useReaderDoodle.ts` 引入 `doodleBookId` 书级快照——`loadDoodle` 记录所属书；`dispose`/`scheduleDoodleSave`/`switchPage` 三处保存前校验 `bookId.value === doodleBookId`（与进度域 F14 同模式）；`loadDoodle` finally 无条件复位 `doodleLoading`（切到无页章节后不再永久卡死致涂鸦丢失）。
- 回归测试：`frontend/tests/doodle-switch.test.ts` 4 例（dispose 切书不写 / 同书正常 flush / 迟到加载不覆盖 / 防抖窗口切书不写错）。

**后端修复（Schrodinger 7 Important + Meitner 6 Important）**
- 时区统一：`tasks/__init__.py` 的 `finished_at`/`mark_interrupted`/`_cleanup_old_tasks`/`find_recent_success` 由本地 `datetime.now()` 改 `utcnow()`（任务中心创建时间偏差 8h）；`recommendation_service._review_reminders` 同步改 UTC（复习提醒提前 8h 触发）。
- `tasks/__init__.py`：`_row_to_dict` JSON 解析加 `_safe_load_json` 防护（单条脏 result 不再 500 任务接口）；`_run_task` 嵌套任务执行后恢复外层 `task_id`（进度路由）；`_cleanup_old_tasks` 移出 `submit_dedupe` 锁外。
- F7 收口：懒构建结果由「书籍数量」改「书籍 id 集合指纹」（`list_book_ids`），删书+导入同数量书不再误判「未变化」；`repositories/graph.py` `list_books` 补 `selectinload(Book.chapters)` 防 N+1。
- 重建保留人工反馈：`compute_cross_book_graph` 清空前快照 `user_feedback` 边，重建后回填（「修改」保留用户强度）——用户「忽略」的关联重建后不再复活。
- 资产写并发：`upsert_asset` 加同书进程内写锁（归档 vs 手动总结并发防 version/内容丢失）；`POST /assets/dedupe` 加互斥锁；`DELETE /books/{id}/asset` 不存在改 404（全库契约统一）。
- 导入视觉预提取改独立 vision 任务（`submit_dedupe`，name_prefix="vision-" 与重建共用防重键）——不再占用 render 配额线程阻塞导入、同书不再双路提取；conftest 同步化补 `import_service.submit_dedupe`。
- 契约/复用：`NoteUpdate.note_type` 加 `pattern`（PATCH 非法类型 422）；`books.py` 6 处内联「查书+404」收敛为 `_get_book` 注入包装（复用 `require_book`）；无效 `folder_id` 400→404；`chat_service` 跨书 chunk 缺 `chapter_index` 防 KeyError；`deps.py` 删除无引用 `get_db` 再导出；`main.py` 启动迁移异常补日志；vision 重建 `force` 纳入防重名。

**前端修复（Pauli）**
- `useGlobalAi.ts`：abort/报错且无内容时移除空气泡（与阅读页行为对齐，消除双流式实现漂移）。
- `ProfileView.vue`：冷画像「编辑」按钮损坏文案 `?? ??` 恢复为 `✏️ 编辑`。
- `utils/task.ts`：`waitForTask` 将 `not_found` 视为终态立即抛错（任务 7 天清理后旧 task_id 不再白等至超时）；abort 监听收敛为循环外单监听（原 180s 轮询累积约 120 个）。
- `HomeView.vue`：搜索防抖加 `searchSeq` 代际守卫（旧响应不覆盖新结果）；失焦定时器纳入卸载清理。
- `MindMapPanel.vue`：脑图 tooltip 节点名/出处转义（`escapeHtml`，消除 LLM 输出注入面）。

**文档修复（Kepler 6 Important）**：README 补 F1~F7+C-1+I-1~I-15 与 §6.8/§6.9 更新记录、索引版本（技术栈 v1.73/重构 v1.41）；过期 §7 移除并更新为 §8 下一步建议；§6.8 悬空「提交前复跑」数字补实；待办清单补 §6.9 行与剩余项登记。

**验证**：后端 pytest **274 passed**（+2：书籍集合指纹回归 / 重建保留反馈）+ ruff 全绿；前端 vitest **61 passed**（+4 涂鸦跨书）+ vue-tsc + vite build（exit 0）全绿。

## 8. 下一步建议（第 3 轮复审后）

1. **提交收尾**：62+ 未提交文件待本地 git 提交；推送需网络恢复（此前 github 443 不通）。
2. **P1 遗留**：资产页「移除该书资产」前端入口（后端 404 契约已统一，前端未接线）；页级提取锁未做（导入预提取已改独立任务，收敛大半）。
3. **P2**：`useStreamSession` 流式内核抽取（useGlobalAi/useReaderAi 双份实现已造成行为漂移）；参数校验补强；死代码清理/接线（`rag_input.build_llm_input` 删除、`ai_context.page_image_data_uri`/`rag_router.clear_session_cache` 接线或删除）；前端 M5/M7/M8/M9 交互细节。
4. **P3**：notes 热画像回写改 savepoint、`reorder_books` 实际落位数、annotations 页号越界、vision force 跳过 OCR 缓存。
5. 聚类正式版（O1~O10）、归档链路增强等后续排期见 `docs/待办清单.md`。

# 使用手册

> 状态：随开发持续更新（规则见 `技术栈规范.md` §8.1：每轮任务后必须更新）
> 关联文档：`需求文档.md`（v1.33）、`技术栈规范.md`（v1.39）、`重构规范.md`（v1.23）

---

## 1. LLM 简易对话 Demo（第 1 轮任务产出）

### 1.1 chat_demo.py —— 对话客户端（`demo/chat_demo.py`）

| 函数 | 说明 |
| --- | --- |
| `load_env_file(path)` | 读取 KEY=VALUE 的 .env 文件，返回 dict；跳过注释与空行 |
| `parse_args()` | 解析命令行参数（base-url/api-key/model/mode/timeout/no-verify-ssl/prompt/mock） |
| `build_payload(mode, model, messages)` | 按接口模式构造请求体：responses（instructions/input）或 chat（messages） |
| `extract_reply(mode, data)` | 从响应 JSON 中提取回复文本（两种模式的兼容解析） |
| `call_llm(base_url, api_key, model, mode, messages, timeout, verify_ssl)` | 发送一次对话请求，返回回复文本；失败抛异常 |
| `mask_key(key)` | 隐藏 API Key 中间部分，避免日志泄露 |
| `main()` | 入口：单轮（--prompt）或交互式多轮对话 |

**如何使用**
```bash
python demo/chat_demo.py --mock --prompt "请生成本书的思维导图"   # 本地 mock 验证
python demo/chat_demo.py --prompt "你好"                          # 使用 demo/.env 的真实配置
python demo/chat_demo.py --mode chat --prompt "1+1=?"             # 切换 chat 模式
python demo/chat_demo.py                                          # 交互式多轮对话（exit 退出）
```
- 配置来源优先级：命令行参数 > 环境变量 > `demo/.env`。
- `demo/.env` 为敏感文件（已加入 `.gitignore`），模板见 `demo/.env.example`。

**如何修改**
- 新增接口模式：在 `build_payload` / `extract_reply` 中按模式分支，并在 `--mode` 的 choices 中注册。
- 调整默认超时/模型：改 `demo/.env` 的 `AI_TIMEOUT` / `AI_MODEL`。
- 注意：`call_llm` 当前为非流式请求；后续接入流式输出（SSE）时保持该函数签名稳定，另加 `stream=True` 分支。

### 1.2 _mock_llm.py —— 本地模拟 LLM 服务（`demo/_mock_llm.py`）

- 功能：OpenAI 兼容 mock，同时支持 `/v1/chat/completions` 与 `/v1/responses`，默认端口 `18999`。
- 回复规则：`pick_reply(sys_text, user_text)`——含「知识整理专家」返回固化知识 JSON；含「阅读辅导专家」返回画像 JSON；否则按关键字命中 `MOCK` 表，未命中返回提示语。
- 如何使用：`python demo/_mock_llm.py` 启动；demo 用 `--mock` 自动指向 `http://127.0.0.1:18999/v1`。
- 如何修改：新增模拟回复在 `MOCK` 字典与 `pick_reply` 中追加；修改端口改 `PORT` 常量（需同步 `chat_demo.py` 的 `DEFAULT_MOCK`）。

---

## 2. 后端 M2 骨架（第 2 轮任务产出）

### 2.1 配置与数据库（`backend/app/core/`）

- `config.py: settings`——全局配置单例（pydantic-settings，读 `.env`）：`app_host/app_port/data_dir/db_url`、AI 接口参数（`ai_base_url/ai_api_key/ai_model/ai_mode/ai_timeout/ai_verify_ssl/ai_enable_body_send/ai_temperature` + 精细参数 `ai_max_tokens/ai_thinking_type/ai_reasoning_effort/ai_top_p/ai_frequency_penalty/ai_presence_penalty/ai_stop`）、多模态视觉参数（`vision_*`，含 `vision_temperature/vision_top_p/vision_frequency_penalty/vision_presence_penalty/vision_enable_thinking/vision_thinking_budget`）。
  - 修改：新增配置项时在 `Settings` 加字段，并在 `backend/.env.example` 同步模板；密钥只放 `.env`，不入库不打印。
- `database.py: init_db()`——首次启动建表（create_all；后续模型变更走 Alembic）。
- `database.py: get_db()`——FastAPI 依赖，提供请求级 SQLAlchemy 会话。
- `time.py: utcnow()`——返回 naive UTC 当前时间（兼容 SQLite），替代已弃用的 `datetime.utcnow`。

### 2.2 解析器（`backend/app/parsers/`）

| 函数 | 功能 | 输入 | 输出 |
| --- | --- | --- | --- |
| `parse_book(path, title_hint=None)` | 按扩展名分发 | 文件路径 | `ParsedBook` |
| `split_by_headings(raw, title_fallback, is_heading)` | 公共切章助手（`parsers/_split.py`）：按标题行切分，空章节不占序号，无标题时整本一章 | 全文/标题/标题匹配函数 | `ParsedBook` |
| `parse_markdown(path, title_hint=None)` | MD 按 `#`/`##`/`###` 切章；书名取首个 `#` | 路径 | `ParsedBook` |
| `parse_txt(path, title_hint=None)` | TXT 按「第X章/Chapter N」切章 | 路径 | `ParsedBook` |
| `parse_pdf(path, title_hint=None)` | PDF（含文本型）统一按原始页切章（每页一章，content 为空，`page_index=页号`）；本地抽取文本按页放入 `page_texts`（仅作全文检索索引） | 路径 | `ParsedBook` |
| `parse_epub(path, title_hint=None)` | EPUB 解包提取正文（HTML→文本） | 路径 | `ParsedBook` |

- 使用：`ParsedBook.title/author/chapters[]`，章节为 `ParsedChapter(index, title, content)`；空内容章节不占序号。
- 修改：新增格式在 `parsers/` 下加模块，并在 `parse_book` 注册、`SUPPORTED_EXT` 增加扩展名；解析器内 `title_hint` 用于优先使用原始文件名，不要用落盘临时文件名。

### 2.3 导入服务（`backend/app/services/import_service.py`）

- `import_book(db, file_bytes, filename, title=None, author=None) -> Book`——保存文件到 `data/books/{uuid}{suffix}` → 解析 → 建 `Book` + `Chapter` 记录。
- 修改：格式校验在 `SUPPORTED_EXT`；文件存储目录由 `settings.data_dir` 控制；`total_chapters` 由解析结果回填。

### 2.4 数据访问层（`backend/app/repositories/books.py`）

- 书籍：`list_books(db, folder_id=None)` / `get_book(db, id)` / `create_book(db, **kw)` / `delete_book(db, id)`（连同章节级联删除，文件删除由 API 层负责）。
- 章节：`list_chapters(db, book_id)` / `add_chapters(db, book_id, [(index, title, content)])`。
- 书籍更新：`update_book(db, book_id, title=None, author=None, status=None, progress=None, folder_id=None, tags=None)`——字段级更新，tags 以 JSON 文本存储；返回 None 表示书籍不存在。
- 文件夹：`list_folders` / `create_folder` / `rename_folder` / `delete_folder`（其中书籍转为未归类）。
- 修改：新增查询在此层扩展；禁止在 API 层直接拼 SQL。

### 2.5 书籍服务（`backend/app/services/books_service.py`）

- `delete_book(db, book_id) -> bool`——删除书籍记录并清理磁盘文件（跨仓储与文件系统的编排，API 层不再直接操作文件）。

### 2.6 AI 客户端（`backend/app/ai/client.py`）

- `LLMClient(base_url, api_key, model, mode, timeout, verify_ssl, temperature, max_tokens, top_p, frequency_penalty, presence_penalty, stop, thinking_type, reasoning_effort, enable_thinking, thinking_budget)`——OpenAI 兼容客户端（stdlib urllib 实现，零额外依赖）。
- `client.chat(messages) -> str`——messages 为 `[{role, content}]`；mode=responses 走 `/responses`，mode=chat 走 `/chat/completions`；失败抛 `LLMError`。
- 使用：默认值来自 `settings`（文本 AI 取 `ai_*`，未传参数回退 `.env`）；真实 DeepSeek `deepseek-v4-flash` 已通过。
- **请求体（chat 分支）**：`temperature/max_tokens/top_p/frequency_penalty/presence_penalty/stop` 与 DeepSeek 思考模式 `thinking:{"type":...}`（`thinking_type`）+ `reasoning_effort`（`thinking=disabled` 时不发）仅在设置时写入；`enable_thinking/thinking_budget` 供 SiliconFlow 推理模型。**responses 分支**仅把 `max_tokens` 映射为 `max_output_tokens`（DeepSeek responses 命名，含思考 token），thinking 参数待定稿。
- 修改：新增接口模式在 `_build_body` / `_extract_reply` / `_extract_delta` 分支；新增请求参数在 `__init__` 与 `_build_body` 同步。

### 2.7 后台任务（`backend/app/tasks/__init__.py`）

- `submit(task_name, coro) -> task_id`——独立线程中 `asyncio.run` 执行协程，内存态状态（queued/running/success/failed）。
- `get_status(task_id) -> str`——查询任务状态。
- 修改：M4/M7（多模态批量预提取等）需要持久化任务记录与进度回调时，替换内存 `_STATUS` 为数据库表。

### 2.8 API 端点（`backend/app/api/routes/`）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/health` | 健康检查 |
| GET | `/api/books` | 书架列表（`?folder_id=` 过滤、`?q=` 搜索书名/作者/标签；按 `position` 排序） |
| POST | `/api/books` | 上传导入（multipart `file`，可选 `title/author`） |
| GET | `/api/books/{id}` | 详情 + 章节列表 |
| PATCH | `/api/books/{id}` | 更新 title/author/status/progress/folder_id/tags |
| DELETE | `/api/books/{id}` | 删除书籍与文件 |
| GET/POST | `/api/folders` | 文件夹列表/新建 |
| PATCH/DELETE | `/api/folders/{id}` | 重命名/删除（书籍转未归类） |

- 响应统一 `{code, message, data}`；分页与错误码扩展见 `技术栈规范.md` §3.3。

### 2.9 前端骨架（`frontend/`）

- 技术栈：Vue 3.5 + TypeScript（严格模式）+ Vite 6 + Element Plus + Pinia + Vue Router 4 + ECharts 5 + axios + markdown-it / KaTeX / DOMPurify。

**目录结构**

| 文件 | 说明 |
| --- | --- |
| `src/main.ts` | 入口：注册 Pinia / Router / Element Plus，引入全局主题 |
| `src/router/index.ts` | 路由：`/`（书架主页）、`/reader/:bookId`（三栏阅读）、`/graph` / `/settings` / `/profile`（占位页） |
| `src/api/client.ts` | axios 实例 + 统一 `{code, message, data}` 解包与错误处理 |
| `src/api/books.ts` | 书籍/文件夹 API 封装 |
| `src/types.ts` | 前端类型定义（Book / Folder 等） |
| `src/stores/book.ts` | 书架状态（Pinia） |
| `src/views/` | 五个页面组件：`HomeView`（左近期阅读右书架）、`ReaderView`（三栏：左 25% 书架+目录 / 中 45% 正文 / 右 30% AI 助手）、`GraphView`、`SettingsView`、`ProfileView` |
| `src/styles/theme.css` | 全局主题样式 |

**构建与运行（已验证）**
```bash
pnpm install          # pnpm 11：allowBuilds 配置见 frontend/pnpm-workspace.yaml
pnpm dev              # 开发服务器 http://127.0.0.1:5173，代理 /api → http://127.0.0.1:8321
pnpm build            # vue-tsc --noEmit && vite build → dist/（已通过，EXITCODE=0）
```
- pnpm 11 不再读取 `package.json#pnpm` 字段，构建脚本与依赖放行统一在 `frontend/pnpm-workspace.yaml`（`allowBuilds: esbuild/vue-demi`、`verifyDepsBeforeRun: false`）。
- 冒烟验证：后端启动在 `8321` 后，`pnpm dev` 可访问书架主页（近期阅读 + 书架，数据来自 `/api/books`）与 `/reader/:bookId` 三栏布局。

**如何修改**
- 新增页面：在 `src/views/` 新建组件并在 `src/router/index.ts` 注册路由。
- 新增接口：在 `src/api/` 下按 `client.ts` 的解包约定扩展，禁止在组件内直接拼 axios。
- M3 任务：在 `ReaderView` 接入章节正文渲染、进度记录、笔记与「不理解」标记；Markdown/LaTeX 渲染按 `技术栈规范.md` §4.6。
- 构建提示：主包约 1 MB（含 Element Plus + ECharts），路由已按需懒加载；后续若告警持续，用 `build.rollupOptions.output.manualChunks` 拆分 vendor。

---

## 3. 外部资料 RAG/Skill 与一键启动（第 3 轮任务产出）

### 3.1 任务模块升级（`backend/app/tasks/__init__.py`）

- `submit(task_name, fn) -> task_id`——支持同步函数 / async 函数 / 协程对象，结果存入内存状态。
- `get_status(task_id) -> dict`——返回 `{status, result, error}`（status：queued/running/success/failed/not_found）。
- 修改：M4/M7（多模态批量预提取等）需要持久化任务记录时，把内存 `_STATUS` 换成数据库表即可，调用方签名不变。

### 3.2 资产仓储（`backend/app/repositories/assets.py`）

| 函数 | 说明 |
| --- | --- |
| `get_asset(db, book_id, kind)` | 读取单条资产（rag / skill） |
| `upsert_asset(db, book_id, kind, content)` | 写入/更新资产，已存在则 `version + 1`（符合技术栈规范 AI 接入规范） |
| `list_assets(db, book_id)` | 列出书籍全部资产 |
| `delete_assets(db, book_id)` | 删除书籍全部资产（删除书籍时级联） |

### 3.3 RAG/Skill 提示词（`backend/app/ai/prompts/rag_skill.py`）

- `SYSTEM_PROMPT`——知识整理专家角色：输出 `summary` / `key_points`（要求标注章节段落出处）/ `skills` 的 JSON。
- `build_user_prompt(book_title, chapters_text)`——书名 + 分章正文。
- 修改：提示词集中存放，新增能力在同一目录分文件；修改走版本记录（技术栈规范 AI 接入规范）。

### 3.4 RAG 服务（`backend/app/services/rag_service.py`）

| 函数 | 说明 |
| --- | --- |
| `chunk_chapter(chapter, chunk_chars=1600)` | 章节按段落切 RAG 片段，记录章节号/段落号出处 |
| `chunk_book(chapters)` | 整本书切块（按章节顺序） |
| `_parse_llm_json(text)` | 容错解析 LLM JSON（去代码围栏、取首个 `{...}`） |
| `_normalize_skills(raw)` | 技能列表归一化（兼容字符串/字典两种返回） |
| `_build_llm_input(chapters, chunks)` | 按章节组织发送正文（chunks 由调用方一次性切好；上限 `SEND_BUDGET=8000` 字；隐私开关关闭时仅章节标题） |
| `page_chunks(page_texts)` | PDF 页缓存 → RAG 片段列表（`chapter_index`=页号、`para_pos`=页），供 PDF 归档资产使用 | 片段粒度/页标题格式在此调整 |
| `_build_page_input(page_texts)` | PDF 页缓存 → LLM 输入正文（隐私开关关闭仅页标题；超 `SEND_BUDGET` 截断） | 页文本拼接格式在此调整 |
| `generate_rag_skill(db, book_id, *, page_texts=None)` | 总结并落库，返回 `{book_id, version, rag, skill}`；未配置 `AI_API_KEY` 或 AI 返回非 JSON 时报错并透出。`page_texts` 传 PDF 页缓存时以页文本为正文与 RAG 片段（出处「第 X 页」）；成功后触发 `post_classify_book` | 页输入分支看 `page_chunks`/`_build_page_input` |
| `archive_book_task(book_id)` | **M9 读完归档后台任务**：PDF 先 `rebuild_book_caches` 视觉通读全书补齐页缓存（命中不重复调用），再以缓存全文总结 RAG/Skill；随后 `set_all_chapters_read_flag(True)` 标记读完 | 归档成功后如需写入暖/冷画像在此追加 |

- 修改：切块阈值 `CHUNK_CHARS`、发送上限 `SEND_BUDGET`；模型/接口切换在 `backend/.env`；提示词调整在 `app/ai/prompts/`。

### 3.5 资产 API（`backend/app/api/routes/assets.py`）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/books/{id}/summarize` | 提交总结任务，返回 `{task_id}`（后台执行） |
| POST | `/api/books/{id}/archive` | 读完归档（M9）：PDF 先视觉通读全书并缓存 → 文本模型总结 RAG/Skill → 标记读完；返回 `{task_id}`（轮询 `/api/tasks/{task_id}`） |
| GET | `/api/tasks/{task_id}` | 轮询任务状态 `{status, result, error}` |
| GET | `/api/books/{id}/asset` | 读取 RAG/Skill 资产（含各自 version 与更新时间） |

### 3.6 前端资料页（`frontend/src/views/RagView.vue` + `src/api/rag.ts`）

- 路由 `/rag`（顶部导航「资料」）：上传 Markdown/PDF/TXT/EPUB → 导入 → 自动总结（轮询任务）→ 展示 RAG 摘要 / 关键知识点 / Skill 技能。
- API 封装：`src/api/rag.ts` 的 `summarizeBook` / `archiveBook` / `getTask` / `getBookAsset`；阅读页「📥 归档并总结 RAG/Skill」按钮走 `archiveBook` + `getTask` 轮询；类型见 `src/types.ts`（`RagContent` / `SkillContent` / `BookAssetView` / `TaskStatus`）。
- 修改：资产内容渲染可复用 `技术栈规范.md` §4.6 的 Markdown/LaTeX 组件（M4 接入）；轮询间隔在 `pollTask` 中调整。

### 3.7 一键启动（`start.bat`）

- 双击根目录 `start.bat`：检查依赖（node / 后端 venv / 前端 node_modules）→ 后台窗口启动后端（8321）与前端（5173）→ 自动打开浏览器。
- 前端启动命令解析顺序（避免系统 PATH 无 pnpm 时报「不是内部或外部命令」）：PATH 上的 `pnpm` → `node node_modules\vite\bin\vite.js`（node 兜底，离线可用）→ `%LOCALAPPDATA%\pnpm\pnpm.cmd` → Codex 运行时 pnpm。
- 修改：端口改批处理内 `--port`；前端如需用 `vite preview`，改 `:front_vite` 分支里的启动命令。

### 3.8 阅读闭环 M3（第 3 轮任务产出）

#### 3.8.1 阅读进度（`backend/app/repositories/reading.py` + `backend/app/api/routes/reading.py`）

| 函数/端点 | 说明 |
| --- | --- |
| `get_latest_log(db, book_id)` | 取该书最近一条 ReadingLog（按 updated_at 倒序），用于重开恢复位置 |
| `upsert_log(db, book_id, chapter_id, position)` | 记录阅读位置；同一章节内重复记录只更新 position |
| `update_book_reading(db, book, progress, chapter_id)` | 更新书籍整体 progress 与 last_opened_at；chapter_id 对应章节置 read_flag=True |
| GET `/api/books/{id}/chapters/{cid}` | 章节正文（Markdown 原文 + word_count + read_flag），供前端渲染 |
| GET/POST `/api/books/{id}/progress` | 读取/保存阅读位置；POST 未传 progress 时按 `(章节序号 + position) / 总章节` 计算整体进度 |

- 修改：进度计算公式、已读阈值等都在 `routes/reading.py` 的 `save_progress` 与 `repositories/reading.py`。
- 数据结构：`ReadingLog`（book_id/chapter_id/position/updated_at）在 `app/models/activity.py`；`Chapter.read_flag` 在 `app/models/book.py`。

#### 3.8.2 笔记（`backend/app/repositories/notes.py` + `backend/app/api/routes/notes.py`）

| 函数/端点 | 说明 |
| --- | --- |
| `list_notes / get_note / create_note / update_note / delete_note` | 笔记 CRUD；类型固定为 `高亮 | 批注 | 思考 | 不理解`（Pydantic pattern 校验） |
| GET/POST `/api/books/{id}/notes` | 列表/新建笔记 |
| PATCH/DELETE `/api/notes/{id}` | 修改内容或类型 / 删除 |
| GET `/api/books/{id}/notes/export?fmt=md\|pdf` | 导出全书笔记（默认 md；pdf 为 M10 新增，含章节定位与引用原文，LaTeX 源码保留，见 §13.1） |

- 修改：新增笔记类型需同步 `NoteIn.note_type` pattern、前端 `NoteType` 类型与 `typeTag` 颜色映射。
- 注意：`update_note` 只允许改 `note_text` / `note_type`，章节与引用不可变（保持出处可追溯）。

#### 3.8.3 前端阅读页（`frontend/src/views/ReaderView.vue`）

- 布局：左栏 25%（竖向书架 + 目录）、中栏 45%（阅读主体）、右栏 30%（AI 助手，M4/M5 接入）。
- 左栏内部：书架固定在左上角（`shelf-section`，`flex:none; max-height:40%`），目录占左下剩余空间（`toc-section`，`flex:1`），两者各自独立滚动条，互不共享、互不覆盖。
- 正文：按空行分段渲染（`splitBlocks`，保留 ``` 代码块合并）；滚动防抖 600ms 保存进度；重开自动恢复上次章节与滚动位置。
- 交互：
  - 段落 hover 出现侧边按钮：❓ 不理解 / ✍ 高亮 / 批注 / 思考（前两者即时建笔记，后两者弹对话框补充内容）。
  - 划词弹出浮动菜单（同一组操作）；点击菜单按钮后清除选区。
  - 右抽屉「笔记」：列表（类型/章节/定位/编辑/删除）+「⬇ 导出 Markdown / ⬇ 导出 PDF」链接（`exportNotesUrl` / `exportNotesPdfUrl`，PDF 为 M10 新增）。
  - 工具栏「标记读完」：PATCH `status=读完, progress=1`（归档与 RAG/Skill 沉淀在 M9 接入）。
- API 封装：`frontend/src/api/reading.ts`（`getChapterContent/getProgress/saveProgress/listNotes/createNote/updateNote/deleteNote/exportNotesUrl/exportNotesPdfUrl`）；类型在 `src/types.ts`（`ChapterContent/ReadingProgress/NoteType/NoteItem`）。

#### 3.8.4 Markdown/LaTeX 渲染组件（`frontend/src/components/MdRender.vue`）

- 渲染链：`markdown-it(html:false)` → DOMPurify 消毒 → KaTeX auto-render（`katex/dist/contrib/auto-render.mjs` 动态 import）。
- 支持：`$…$` `$$…$$` `\(…\)` `\[…\]`；表格/代码/引用样式已内置。
- 已知坑与修复：`breaks:true` 会把块级公式内的换行变成 `<br>`，导致 KaTeX 无法跨文本节点匹配；`preprocessMath` 在渲染前将 `$$…$$` 内部换行压缩为空格。
- 修改：新增公式语法在 `renderMath` 的 `delimiters` 中追加；markdown-it 选项在组件顶部 `new MarkdownIt({...})` 调整；类型声明在 `src/env.d.ts`（markdown-it / auto-render.mjs）。

#### 3.8.5 API 公共依赖（`backend/app/api/deps.py`）

- `require_book(db, book_id)`——按 ID 取书并抛 404，`routes/reading.py` / `routes/notes.py` / `routes/assets.py` 复用（消除三处重复实现）。新增接口若需"书不存在"校验，直接复用该依赖。

#### 3.8.6 冒烟要点

1. `cd backend && .venv\Scripts\python.exe -m uvicorn app.main:app --port 8321` 启动后端。
2. `cd frontend && pnpm dev --host 127.0.0.1` 启动前端（无 pnpm 时用 `node node_modules\vite\bin\vite.js --host 127.0.0.1`）。
3. 打开 `/reader/1`：验证三栏布局、章节切换、滚动保存进度、刷新恢复、hover/划词建笔记、抽屉编辑删除导出、「标记读完」。
4. 回归：`cd backend && .venv\Scripts\python.exe -m pytest`（16 项）+ `cd frontend && pnpm build`（vue-tsc + vite）。

### 3.9 正文精确高亮与书架进度修复（第 3 轮补充迭代）

#### 3.9.1 正文精确高亮（`frontend/src/views/ReaderView.vue`）

- 需求：划词「高亮 / 不理解」不再整段，而是**精细到所划线的部分**，并在正文可视化标记。
- `wrapQuoteInElement(el, quote, cls)`：在段落渲染 DOM 中按文本精确匹配 quote 并包裹 `<mark class="note-hl ...">`；支持跨文本节点、合并命中为单个 mark、嵌套（整段标记内再高亮精确片段）。
- `applyHighlights()`：章节加载与笔记增删改后调用；先循环展开全部旧 `.note-hl` 再按 **quote 长度降序**重新包裹（长整段先包裹、短划词嵌套在内），保持幂等。
- `NOTE_HL_CLASS`：类型→样式类映射（高亮=黄底、不理解=红波浪、批注=绿线、思考=蓝线）；样式见 `.reading-scroll :deep(.note-hl-*)`。
- `quickNote` 改进：hover 侧边按钮的 quote 改用段落渲染后文本（`textContent`），保证与正文 DOM 可匹配。
- 注意：quote 为渲染后文本（划词 `selMenu.text` / 段落 `textContent`），与 markdown 源文本（`blocks[i]`）不同；公式等特殊渲染元素可能无法精确匹配（自动跳过，不影响其他高亮）。

#### 3.9.2 书架进度显示修复（`frontend/src/views/HomeView.vue`）

- 近期阅读：`Math.round(b.progress)` 漏乘 100（0.33 显示为 0%）→ 改为 `Math.round((b.progress ?? 0) * 100)`。
- 书架卡片：新增进度条 `el-progress` 与百分比文本（`progress > 0` 时显示），满足需求 3.1「书格显示阅读进度」。
- 阅读页左侧书架进度依赖 `saveNow → store.fetchBooks()` 实时刷新（已有逻辑，未改动）。


### 3.10 读完判定与书架进度展示（第 3 轮补充迭代 2）

#### 3.10.1 章节读完判定（自动 + 手动）

- 自动判定（前端 `frontend/src/views/ReaderView.vue`）：`AUTO_READ_MS = 10_000`，每 1s 定时器 `checkAutoRead()` + 滚动事件触发；条件为 **当前章节停留（打开后累计）≥10s 且该页滚动到底**（`scrollHeight - scrollTop - clientHeight <= 4`）时，调用 `saveProgress(..., { mark_read: true })` 将本章标记已读；`autoMarkedChapter` 集合保证每章只自动标记一次。
- 后端（`backend/app/api/routes/reading.py`）：`ProgressIn` 新增 `mark_read: bool = False`；`repositories/reading.py` 的 `update_book_reading(..., mark_read=False)` 仅当 `mark_read=True` 时才置 `Chapter.read_flag`（修复旧逻辑"打开章节即标记已读"的问题）；当某书**全部章节已读**时自动将 `Book.status` 置为 `读完`。
- 手动标记（阅读页工具栏按钮）：书未读完显示「标记读完」（`status=读完, progress=1`），书已读完显示「标记为在读」（`status=在读`），由 `toggleFinished()` 切换，本地 `book` 与书架 store 同步刷新。
- 章节列表 `✓`、阅读页工具栏「已读 x/y 章 · 进度 z%」与书架进度均基于 `read_flag` 实时计算。

#### 3.10.2 书架进度展示（`frontend/src/views/HomeView.vue`）

- 书架卡片进度区改为三段式：**上方**「已阅读 xx/yy 章」，**中间**「最新章节：第n章 标题」，**下方**放置 `el-progress` 阅读进度条（始终显示）。
- 进度百分比由 `read_chapters / chapter_count` 计算（`chapterPercent(b)`），不再使用滚动位置进度；阅读页工具栏与左侧书架同步按章节数计算。
- 近期阅读面板同步显示「已阅读 x/y 章」信息行与进度条。
- 数据来源：后端 `book_to_dict` 新增 `read_chapters`（已读章节数）与 `latest_chapter`（`{index, title}`，取最近阅读日志章节，兜底已读最高章节）；`api/routes/books.py` 通过 `book_reading_summary(db, book)`（`repositories/reading.py`）统一注入，列表/详情/上传/更新接口均返回。

#### 3.10.3 窗口最小化/还原显示修复（`frontend/src/App.vue` + `styles/theme.css`）

- 视口高度由 `100vh` 改为百分比链（`html/body/#app` 高度 100%）+ `100dvh` 兜底（`min-height: 100vh; min-height: 100dvh`），避免浏览器 UI 高度变化/窗口缩放时 `100vh` 超出可视区导致页面底部被裁切、还原后布局错乱。
- 已用 Playwright（Edge）在 1440×900 / 1280×720 / 1024×640 / 800×600 下验证：`documentElement.scrollHeight == clientHeight`、`body.scrollWidth == innerWidth`，无页面级滚动条。

#### 3.10.4 冒烟要点

1. 打开长章节，停在顶部停留 11s：章节不应被标记已读。
2. 停留 10s 后滚动到底：出现「第n章…已读完」提示，书架已读章节数 +1；全书章节读完时书籍自动变为「读完」。
3. 工具栏按钮在「标记读完 / 标记为在读」间切换，切换后书架状态同步。
4. 书架卡片显示「已阅读 x/y 章 · 最新章节：第n章 …」与底部进度条。
5. 窗口缩放到 800×600 再还原，页面无裁切/滚动条。
6. 回归：`cd backend && .venv\Scripts\python.exe -m pytest`（16 项）+ `cd frontend && pnpm build`。

### 3.11 章节标题数学公式渲染（第 3 轮补充迭代 3）

#### 3.11.1 问题与根因

- 现象：章节标题含 LaTeX 公式（如 `$-\Delta u = f$`）时，目录与工具栏标题显示原始 `$...$` 源码而非渲染后的公式。
- 根因：`ReaderView.vue` 的目录项 `.chapter-title` 与工具栏 `.chapter-heading` 使用纯文本插值 `{{ c.title }}`，未经过数学渲染管线。

#### 3.11.2 修复方案

- `components/MdRender.vue` 新增 `inline?: boolean` 属性：`inline=true` 时改用 `md.renderInline()` 渲染并以 `<span class="md-render-inline">` 输出，其余复用同一管线（`preprocessMath` → markdown-it → DOMPurify → KaTeX auto-render），保证块级/行内数学渲染行为一致。
- 新增样式 `.md-render-inline`（`display:inline; line-height:inherit`）与 `.md-render-inline :deep(.katex) { font-size: 1em }`，避免 KaTeX 默认 1.21em 放大标题字号。
- `ReaderView.vue` 三处改为 `<MdRender :source="..." inline />`：目录章节标题、工具栏章节标题、笔记抽屉定位行（`第n章 标题`）。
- `env.d.ts` 的 markdown-it 类型补丁补充 `renderInline(src: string): string`。
- 顺带修复：`C\*` 这类 markdown 转义（`\*` → `*`）在标题中也会被正确还原（由 markdown-it 内联规则处理）。

#### 3.11.3 已知限制

- 与正文同一限制：若公式内部出现会被 markdown-it 解析为强调的 `_`/`*`（如 `$x _1$` 中 `_` 两侧均为空格），`$...$` 会被拆到不同文本节点导致 KaTeX 无法匹配；常见的 `$a_i$`（下划线两侧为字母，CommonMark 视为单词内下划线，不触发强调）不受影响。极端情况可在标题中改用 `\(...\)` 分隔符。

#### 3.11.4 冒烟要点

1. 打开含公式标题的章节（如书 2 第 9.2 章 `9.2 全流程演示：$-\Delta u = f$，$u|_{\partial\Omega} = 0$`）。
2. 检查目录项与工具栏标题：可见文本无残留 `$`，公式渲染为 KaTeX（DOM 中 `.katex` 数量与公式数一致，含 MathML）。
3. 回归：正文块级公式仍正常（`pnpm build` + 阅读页冒烟）。

### 3.12 章节已读手动开关 + 书架三段式进度（第 3 轮补充迭代 4）

#### 3.12.1 章节已读手动开关

- 目录每行右侧新增开关按钮：`✓`（已读，点击取消）/ `○`（未读，点击标记），`@click.stop` 不影响行点击跳转。
- 后端新增 `PATCH /api/books/{book_id}/chapters/{chapter_id}/read`，body `{ read: bool }`（`app/api/routes/reading.py` + `repositories/reading.py#set_chapter_read_flag`）。
- 书籍状态同步规则统一为共享助手 `_refresh_status`：全部章节已读→`读完`；部分已读→`在读`；全部未读→`未读`。仅当章节标记实际变化时同步（位置保存不改变 status，保留手动「标记读完」覆盖）；自动读完路径（`mark_read=True`）同样走该规则。

#### 3.12.2 书架进度显示（三段式）

- 上方：`已阅读 xx/yy 章`；中间：`最新章节：第n章 标题`（无日志时显示「最新章节：暂无」）；下方：`el-progress` 进度条。
- 进度 = `已读章节数 / 总章节数`：`HomeView.chapterPercent(b)` 与 `ReaderView.percent(b)` 统一按此计算，主页卡片、近期阅读、阅读页左侧书架与工具栏百分比一致。

#### 3.12.3 冒烟要点

1. 目录点击 `○` → 变为 `✓`，书架已读章节 +1，书籍状态 未读→在读；再点 `✓` → 取消，状态回退。
2. 全部章节标记已读 → 状态自动变「读完」；取消其中一章 → 回退「在读」。
3. 书架卡片三段式（已阅读 x/y 章 / 最新章节 / 进度条），百分比 = x/y×100。
4. 回归：`pytest`（17 项）+ `pnpm build` + 阅读页冒烟。
### 3.13 书架排序/搜索/标签（第 16 轮任务产出，HomeView 打磨）

#### 3.13.1 后端能力（`books.py` 路由 + `repositories/books.py`）

- `Book.position`（INTEGER，默认 0）：书架排序位。新书导入自动追加到末尾（全局最大位 + 1，`create_book`）；旧库由 `database._ensure_columns` ALTER 补齐。
- `GET /api/books?q=关键词`：搜索书名/作者/标签（`or_(title.contains, author.contains, tags_json.contains)`），可与 `folder_id` 组合。
- `POST /api/books/reorder`：body `{ordered_ids: [书id...]}`，按期望顺序批量重排（position 1..N，未知 id 忽略），供拖拽换位持久化。
- `PATCH /api/books/{id}` 支持 `position` 单本调整。

#### 3.13.2 前端（`frontend/src/views/HomeView.vue` + `src/api/books.ts`）

- **布局**：书架工具栏（搜索框 + 标签下拉筛选 + 导入按钮）sticky 置顶；卡片显示封面/书名/标签 chips/阅读进度三段式；悬停显示操作按钮（🏷 编辑标签、🗑 删除）。
- **拖拽换位**：卡片 `draggable`，`dragstart` 记录拖拽书、`dragover/drop` 高亮并交换目标书（按 id 在全量列表内互换），随后 `reorderBooks(全量顺序)` 持久化；失败回滚重取列表。
- **手动标签**：卡片 🏷 弹层 `el-select multiple filterable allow-create`（候选 = 全部书的标签去重），保存走 `updateBook(id, {tags})`（后端清洗去重）；点击卡片标签 chip 直接切换该标签筛选。
- **搜索/筛选**：`searchQuery` 对书名/作者/标签做本地过滤；`activeTag` 标签筛选；二者可组合，无结果时显示空态提示。

#### 3.13.3 冒烟要点

1. 导入 3 本书 → 书架按导入顺序排列（position 1/2/3）。
2. 拖动第 3 本到第 1 本位置 → 释放后顺序更新并持久化（刷新页面仍在）。
3. 卡片 🏷 添加「数学」「概率」→ 保存后卡片显示标签；顶部标签下拉出现新标签；点击标签筛选只剩对应书。
4. 搜索框输入书名/作者/标签关键词 → 列表即时过滤；清空恢复。
5. 回归：`pytest`（125 项）+ ruff + vue-tsc + vite build。

### 3.14 前端功能测试与标签编辑器修复（书架测试轮）

#### 3.14.1 前端功能测试结果（浏览器实测，http://127.0.0.1:5173/）

| 功能 | 结果 |
| --- | --- |
| 书架渲染（6 本、封面/格式徽标、近期阅读 5 条） | ✅ 通过 |
| 搜索书名/作者/标签（`物理`→1 本，清空恢复） | ✅ 通过 |
| 标签 chip 展示与点击筛选（`物理`/`数学分析` chip） | ✅ 通过 |
| 标签下拉筛选（选择 `数学分析` → 只剩对应书） | ✅ 通过 |
| 搜索 + 标签筛选组合（无结果时显示空态） | ✅ 通过 |
| 拖拽换位 | ✅ 后端 `POST /api/books/reorder` 实测持久化（id 顺序→position 1..N→刷新保持）；前端 `onDrop` 逻辑经代码审查正确，拖拽手势无法用自动化模拟 |
| 删除书籍确认弹窗（取消不删除） | ✅ 通过 |
| 标签编辑（创建/保存/取消） | ✅ 修复后通过（见 3.14.2） |

#### 3.14.2 标签编辑器 bug 修复（`frontend/src/views/HomeView.vue`）

测试发现并修复 3 个问题：

1. **共享 `tagPopVisible` 导致一次点击弹出全部弹层**：`v-for` 内 6 个 `el-popover` 共用一个 `v-model:visible`，点任一 🏷 会同时打开全部 6 个弹层，且 `@show` 依次触发使 `tagEditBook` 指向最后一本书（保存到错误的书）。
   - 修复：改为 `tagEditId = ref<number|null>` 按书籍 id 独立控制可见性（`:visible="tagEditId === b.id"`）。
2. **保存目标错乱**：改为按钮 `@click.stop="toggleTagEdit(b)"` 在点击时显式 `prepareTagEdit(b)`（设置 `tagEditBook`/`tagDraft`），不再依赖受控 `:visible` 下不可靠的 `@show` 事件。
3. **弹层被下拉/点击关闭**：`el-select` 下拉默认 teleport 到 body，点击「创建标签」选项会被 popover 判为外部点击而关闭编辑；同时 teleport 的下拉会遮挡「保存/取消」按钮。
   - 修复：`trigger="manual"` 完全受控显隐（取消/保存按钮负责关闭）；`el-select :teleported="false"` 使下拉渲染在弹层内；select 与操作按钮之间加 `.tag-select-spacer`（44px）占位，避免空下拉遮挡按钮。

修复后实测全流程（含中文标签）：点 🏷 → 仅打开 1 个弹层 → 输入新标签 → 点击创建选项 → 点「保存」→ 后端 `tags` 落库（`["泛函分析"]`）→ 卡片显示标签 chip → 提示「标签已更新」；「取消」正常关闭。

#### 3.14.3 回归验证

- `vue-tsc --noEmit` ✅；前端 `vitest` 23 项 ✅；后端 `pytest tests/test_books_shelf.py` 6 项 ✅。
- 测试产生的临时标签已清理（book 8/3 tags 置空），书架保持原 6 本原顺序。
## 4. M4 AI 接入（第 4 轮任务产出）

### 4.1 设置 API（`backend/app/api/routes/settings.py`）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/settings/ai` | 读取当前 AI 配置（API Key 掩码后返回） |
| PATCH | `/api/settings/ai` | 保存 AI 配置（空值忽略、保留旧值），返回掩码视图 |
| POST | `/api/settings/ai/test` | 用当前（或请求体临时覆盖）配置发起最小对话，验证连通性与鉴权 |

- 请求体字段：`base_url / api_key / model / mode / timeout / verify_ssl / enable_body_send / temperature` + 精细参数 `max_tokens / thinking_type / reasoning_effort / top_p / frequency_penalty / presence_penalty / stop` + 多模态 `vision_base_url / vision_api_key / vision_model / vision_timeout / vision_verify_ssl / vision_max_tokens / vision_temperature / vision_top_p / vision_frequency_penalty / vision_presence_penalty / vision_enable_thinking / vision_thinking_budget`；PATCH 时 `api_key` 留空 = 不修改。
- 前端字段名 → 仓储键（`ai_*`）映射由 `FIELD_TO_KEY` 定义；保存与掩码视图复用 `app/repositories/settings.py`。
- 测试连接返回 `{ok, message}`；失败不抛 500，以 `ok=false` 返回友好信息（网络/鉴权错误不泄露 Key）。

### 4.2 对话流式问答（`backend/app/api/routes/chat.py` + `backend/app/services/chat_service.py`）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/books/{id}/chat` | 按当前章节上下文问答，返回 SSE 流（start/delta/end/error 事件） |
| GET | `/api/books/{id}/chat/messages` | 本书对话历史（按时间正序） |
| DELETE | `/api/books/{id}/chat/messages` | 清空本书对话历史 |

- SSE 事件序列：`{"type":"start"}` → N 个 `{"type":"delta","text":...}` → `{"type":"end","text":...,"citations":[{chapter,para}]}`；失败发 `{"type":"error","message":...}`。
- 上下文组装：当前章节正文按段落编号【第N段】注入；RAG 资产片段（`retrieve_rag_chunks` 关键词检索、带出处）与 Skill 资产（`load_skills`）自动注入系统提示词；隐私开关（`ai_enable_body_send=false`）时不发送正文与片段。
- 引用出处：`extract_citations` 解析回答中 `【第X章 第Y段】` 格式标注，`end` 事件携带结构化列表供前端展示。
- 历史落库：`end` 事件后写入 `ChatMessage`（独立会话，避免流式期间请求级会话被关闭）；失败不影响已输出的回答。

**函数清单（chat_service.py）**

| 函数 | 功能 |
| --- | --- |
| `paragraph_numbered(text)` | 章节正文按行编号，每段前加【第N段】 |
| `extract_citations(text)` | 从回答中解析引用出处 → `[{chapter, para}]` |
| `retrieve_rag_chunks(db, book_id, question, top_k=4)` | 按关键词重叠从书籍 RAG 资产检索相关片段（含出处），无命中返回空 |
| `load_skills(db, book_id)` | 读取书籍 Skill 资产技能列表 |
| `build_messages(book, chapter, question, selection, rag_chunks, skills, enable_body_send)` | 组装 system/user messages；隐私开关关闭时不发正文 |
| `persist_chat(db, book_id, chapter_id, selection, question, answer)` | 写入一条 user + 一条 assistant 历史 |
| `stream_chat(job)` | SSE 事件生成器（start/delta/end/error + 落库兜底） |
| `build_client(db)` | 按运行时配置构建 LLMClient（.env + 设置页覆盖） |
| `is_configured(db)` | 是否已配置 API Key |

**如何修改**
- 提示词：`backend/app/ai/prompts/chat.py`（`SYSTEM_PROMPT` / `build_system_prompt` / `build_user_prompt`）；新增 M5 专项能力（解读/概论/脑图/思考逻辑）在此扩展预设。
- LLM 客户端：`backend/app/ai/client.py` —— `chat()` 普通请求、`stream()` SSE 流式，双模式（responses/chat）；新增接口模式在 `_build_body` / `_extract_reply` / `_extract_delta` 分支。

### 4.3 设置仓储扩展（`backend/app/repositories/settings.py`）

- `client_kwargs(db)` —— 把运行时 AI 配置（`ai_*` 键）映射为 `LLMClient` 构造参数（剔除与客户端无关的 `ai_enable_body_send`），供 `build_client` 与设置页测试连接复用；映射表 `CLIENT_KWARG_KEYS`。
- 修改：新增 LLM 参数时同步 `AI_OVERRIDE_KEYS`、`CLIENT_KWARG_KEYS` 与 `.env.example`。

### 4.4 前端 AI 配置页（`frontend/src/views/SettingsView.vue`）

- 表单字段与后端一致；API Key 输入框留空 = 保持不变；「测试连接」用当前表单值临时覆盖测试（不保存）。
- API 封装：`src/api/settings.ts` —— `getAiSettings / saveAiSettings / testAiSettings`。

### 4.5 前端 AI 助手面板（`frontend/src/views/ReaderView.vue`）

- 右栏 30% 固定宽度；顶部 4 个能力预设按钮（解读/概论/脑图/思考逻辑，M4 为提问模板，M5 升级为专项生成）；中部对话流（Markdown/LaTeX 渲染 + 引用出处 chips）；底部输入框（Enter 发送、流式中可停止）。
- 流式解析：`src/api/chat.ts` 的 `streamChat(bookId, body, onEvent)` 用 fetch + ReadableStream 解析 SSE，返回 `{promise, abort}` 支持中断。
- 上下文：发送时自动附带当前章节 id 与阅读区选中文本（`currentSelection()`）。
- 历史：进入页面 `loadChatHistory()`；「清空」调用 `clearChatMessages`；切书自动 `resetChat()`。
- 类型：`AiSettings / ChatMessageItem / ChatStreamEvent` 定义在 `src/types.ts`。

### 4.6 冒烟要点（mock LLM）

1. `python demo/_mock_llm.py` 启动 mock（18999）。
2. 设置页填 `http://127.0.0.1:18999/v1`、任意 Key、mode=responses，点「测试连接」应提示连接成功。
3. 打开任一书籍阅读页 → 点「解读」预设（输入框出现带章节上下文的提问）→ Enter 发送 → 右栏流式输出、无报错。
4. 刷新页面历史仍在；「清空」后消失。
5. 回归：`pytest`（27 项）+ `pnpm build`。

## 5. PDF 封面 / 扫描版按页阅读 / LLM 页图附件（M4 补充）

> 需求 v1.9 起已改为「**PDF 统一按页处理（含文本型）**」：文本型 PDF 不再直接抽取正文（数学符号会乱码），统一按页切章、原图阅读、多模态视觉提取（见需求 3.2/3.4.11）。本节描述当前代码实现（扫描版识别 + 按页读图），待统一处理实现后更新。
>
> 本轮补全三类能力：PDF/EPUB 封面提取并在书架渲染；扫描版 PDF 按原始页数切章、直接阅读页图；对扫描版提问时自动把当前页图片作为附件发送给 LLM（chat 模式，需模型支持视觉输入）。

### 5.1 解析器：扫描判定与页级章节（`backend/app/parsers/pdf.py`）

- 解析改用 PyMuPDF（`pymupdf>=1.28`，已同步 `pyproject.toml` 依赖）。
- 全文提取文本长度 < `SCANNED_TEXT_THRESHOLD = 30` 字符 → 判定为扫描版：
  - 每页生成一个章节（`ParsedChapter(page_index=i, title="第 N 页", content="", is_scanned=True)`），`page_count` 为总页数；
  - 文本 PDF 维持原有章节切分逻辑不变。
- 辅助函数：
  - `render_pdf_page(path, page_index, out_path)`：把指定页渲染为 `page_XXX.jpg`（默认 1000px 宽、quality 88）；
  - `extract_pdf_cover(path, out_path)`：渲染第 1 页为封面图（无元数据封面时兜底）；
  - `render_pdf_pages(path, out_dir)`：渲染全部页到 `pages/page_001.jpg`…。
- `backend/app/parsers/epub.py` 新增 `extract_epub_cover`：优先取 OPF `<meta name="cover">` / `<meta name="cover-image">` 指向的资源，写入 `cover.{ext}`。

### 5.2 数据模型与旧库迁移（`backend/app/models/` + `backend/app/core/database.py`）

- `ParsedBook` 新增 `is_scanned` / `page_count`；`ParsedChapter` 与 `Chapter` 新增 `page_index`；`Book` 新增 `is_scanned` / `page_count`。
- `database.py` 新增 `_ensure_columns()`：`init_db` 建表后执行轻量 `ALTER TABLE`（`books.is_scanned`、`books.page_count`、`chapters.page_index`），旧数据库无需重建即可升级，缺列自动补默认值。

### 5.3 导入流程（`backend/app/services/import_service.py`）

- 导入 PDF/EPUB 时提取封面并写入 `book.cover`（书架展示用 `cover_url`）。
- 每本书使用独立子目录 `data/books/<file_id>/`（书文件 + `cover.jpg` + `pages/`），避免封面/页图跨书共享覆盖。
- 扫描版 PDF 渲染 `pages/` 目录；章节按页写入，`add_chapters` 接收 `(index, title, content, page_index)` 元组。
- 书籍序列化（`backend/app/schemas/serializers.py`）新增 `is_scanned` / `page_count` / `cover_url`；章节序列化新增 `page_index`。

### 5.4 新增 API（`backend/app/api/routes/books.py`）

- `GET /api/books/{book_id}/cover`：返回封面图片文件（`FileResponse`）。
- `GET /api/books/{book_id}/pages/{page_index}`：返回扫描版指定页的 `page_XXX.jpg`（1 基页码）。

### 5.5 聊天页图附件（`backend/app/api/routes/chat.py` + `backend/app/services/chat_service.py`）

- 开关：设置页「发送页面图片」（键 `ai_send_page_image`，环境变量 `AI_SEND_PAGE_IMAGE`，默认关闭；需同时开启隐私开关「发送书籍正文」才生效）。
- `chat.py::_page_image_data_uri(book, chapter, send_enabled)`：扫描版且当前章节带 `page_index` 时读取 `pages/page_XXX.jpg`，编码为 `data:image/jpeg;base64,...`。
- `chat_service.py::build_messages(..., page_image=None)`：`enable_body_send` 为真且传入页图时，用户消息改为 parts 列表（文本 + `image_url` 部件）；隐私开关关闭时**不附带页图**。
- `LLMClient`（`backend/app/ai/client.py`）：chat 模式原样传递 messages（含图片部件）；responses 模式经 `_content_text` 提取纯文本（图片隐式退化，多模态需模型与接口支持）。

### 5.6 前端（`frontend/src/`）

- `types.ts`：`BookItem` 新增 `is_scanned/page_count/cover_url`；`ChapterItem`/`ChapterContent` 新增 `page_index`；`AiSettings` 新增 `send_page_image`。
- `HomeView.vue`：书架卡片渲染 `cover_url` 封面（`cover-img`，150px 高、object-fit: cover），无封面时回退格式徽章。
- `ReaderView.vue`：`content.page_index != null` 时进入 `pageMode`，正文区渲染 `<img class="page-img">` 显示原始页图（扫描版不渲染文本高亮/笔记）。
- `SettingsView.vue`：新增「发送页面图片」开关，保存到 `send_page_image`。

### 5.7 冒烟要点（mock LLM）

1. 导入文本 PDF 与扫描版 PDF 各一：书架显示封面；扫描书 `page_count` 为真实页数。
2. 打开扫描书：目录为「第 N 页」，正文显示页图，切换章节页图随之切换。
3. 设置页开启「发送页面图片」并保存；阅读页对扫描书提问，SSE 正常回复、无报错；关闭「发送书籍正文」后提问不再附带页图（后端单测覆盖）。
4. 回归：`pytest`（31 项）+ `ruff check app tests` + `pnpm build`。

### 5.8 封面回填、旧布局迁移与书架删除（补充修复）

- **封面串书根因**：早期封面/页图写入共享的 `data/books/cover.jpg` 与 `data/books/pages/`，后导入的书会覆盖前书的封面。v1.17 起每本书独立子目录存储（见 5.3）。
- **封面回填**（`backend/app/services/media_service.py`）：
  - `ensure_book_cover(db, book)`：缺封面或封面文件丢失时按需重新提取（PDF 渲染第 1 页 / EPUB OPF 封面）并更新 DB；`GET /api/books/{id}/cover` 会自动触发。
  - `migrate_book_layout(db, book)`：把旧扁平布局迁移到 `data/books/<file_id>/`；共享 `pages/` 页数与本书一致时整目录搬入，避免逐页重渲染。
  - `migrate_all_books(db)`：启动时（`database.py::_migrate_book_media`）自动执行迁移 + 回填，幂等，失败不阻塞启动。
- **书架删除**：
  - 前端 `HomeView.vue`：书架卡片悬停显示 🗑 按钮，确认后调用 `DELETE /api/books/{id}` 并刷新列表。
  - 后端 `books_service.py::delete_book`：先显式清理 `chat_messages / reading_logs / notes / book_assets / knowledge_points / book_relations`（部分外键无 CASCADE，直接删书会触发外键冲突），再删书籍记录（章节随 ORM 级联），最后删除整个书籍子目录（含 PDF、封面、页图）。
  - 页图端点 `GET /api/books/{id}/pages/{page_index}` 缺文件时按需渲染单页（`render_pdf_page`）。


### 5.9 扫描版页图高清化与阅读缩放（v1.22）

- **问题**：页图按 72 DPI（`page.rect` 原始点尺寸）渲染，实际扫描 PDF 内嵌图多为 300 DPI 级（如 2024×2953），阅读时被大幅降采样，正文发虚。
- **原图渲染**（`backend/app/parsers/pdf.py`）：
  - `_auto_page_zoom(page)`：取页内嵌图片原生宽度与页面宽度之比作为渲染倍率（即按「原图」分辨率渲染，上限 6x 防超大内存）；无内嵌图片（纯矢量页）时用 2.5x（≈180 DPI）兜底。
  - `render_pdf_page`：新增 `zoom` 参数；`max_width=0` 且未给 `zoom` 时走自动原图倍率；封面仍走 `max_width=600` 旧路径。`render_pdf_pages` 默认改为自动倍率（新导入即高清）。
  - `pdf_page_target_width(path, page_index)` / `jpeg_width(path)`：计算目标宽度与读取已渲染页图实际像素宽（读像素宽须用 `pymupdf.Pixmap`，JPEG 自带 DPI 元数据会让 `pymupdf.open` 按点换算失真）。
  - 页图端点 `GET /api/books/{id}/pages/{page_index}`：文件缺失或像素宽 < 目标宽 ×0.85（旧低清页）时按需重渲染单页；响应加 `Cache-Control: no-cache` 防浏览器缓存旧图。
- **阅读缩放**（`frontend/src/views/ReaderView.vue`）：
  - 扫描版阅读区上方新增缩放条：`适配宽度` / `原始大小`（1 像素 = 1 CSS 像素，页面可横向滚动）/ `＋` `－`（0.5x~3x）。
  - 实现：`pageZoom`（`'fit' | number`）+ `pageNaturalWidth`（img `naturalWidth`）+ `pageImgStyle`（fit 用 `max-width:100%`，缩放用显式像素宽度）；`.page-scroll` 横向滚动 + `justify-content: safe center` 防止放大后左侧被裁切。
- 冒烟：书 7 第 1 页由 467×666（37KB）升级为原图分辨率（1487px 宽、约 280KB）；放大到「原始大小」可横向滚动查看清晰正文。

### 5.10 PDF 页图多模态视觉提取与页级缓存（统一适用于全部 PDF）

> 状态：**已实现（M7，见 §9）**——设计要点如下，实现细节见 §9（需求 3.4.11 / 技术栈规范 §6）。

- **链路**：PDF 页图（含文本型，文本抽取数学符号会乱码）→ 多模态 LLM 逐页提取完整页面信息（Markdown 文本，含公式/表格/图注描述）→ 缓存 `data/books/<书目录>/page_text/page_XXX.md` → 文本大模型基于缓存解读（引用出处「第 X 页」）。
- **触发时机**：手动导入 PDF（含文本型）作为知识库时**批量预提取**；阅读时不预提取，**用户在当前页对 AI 提问**时按需提取（窗口 `[P-1,P,P+1]`，第 1 页/末页裁剪，仅补缺失页，需求 9.1 决策 22）。
- **缓存命中**：`page_XXX.md` 存在且非空即命中，不再调用多模态 API；缓存缺失回退「当前页原图附件」或提示先提取。
- **预期落点**：后端 `app/ai/vision_client.py` + 任务模块提取任务 + 页图相关 API（提取/重建/读取页缓存）；前端设置页多模态配置（base_url/api_key/model 独立）与阅读页「提取本页 / 重建本书页缓存」入口。
- **依赖与约束**：多模态 API **独立配置**（`vision_base_url / vision_api_key / vision_model`，无需额度管理，需求 9.1 决策 23）；受「发送书籍内容至模型」隐私开关约束（关闭不提取、不发送）；随书删除清理页缓存。

### 5.11 位置书签与 PDF 页图涂鸦（M6 已实现）

> 状态：**已实现**（需求 3.3/3.5 / 技术栈规范 §4.7/§5；决策 22~26）。

- **位置书签（适配所有格式）**：
  - 后端：`Bookmark` 表（`book_id/chapter_id/page_index/para_pos/title/note/group_name/created_at`，随书删除级联清理）；仓储 `repositories/bookmarks.py`；路由 `api/routes/bookmarks.py`。
  - API：`GET/POST /api/books/{id}/bookmarks`（列表默认时间倒序 / 新增）、`PATCH/DELETE /api/bookmarks/{id}`（改标题/备注/分组 / 删除）。
  - 前端：`BookmarkDrawer.vue` 书签抽屉（倒序平铺 + 分组视图、新增当前章节/页书签、编辑/删除、点击定位跳转）；阅读页工具栏「🔖 书签」按钮。
  - 跳转：PDF 书签按 `page_index` 找到对应页章节加载；文本书签按 `chapter_id + para_pos` 加载章节后滚动到 `[data-para]` 段落。
- **PDF 页图涂鸦（按页阅读时）**：
  - 后端：`api/routes/annotations.py`——`GET/PUT /api/books/{id}/annotations?page_index=N`，读写 `data/books/<书目录>/annotations/page_XXX.json`；删除书籍时 `books_service._remove_book_files` 清理 `annotations/` 目录。
  - 前端：`PageDoodleCanvas.vue`（canvas 叠加层，坐标 0~1 归一化；笔刷/高亮/橡皮/文本，颜色/线宽可调；`undoStack` 上限 5 会话内撤销；橡皮命中擦除笔画/文本；划线点击弹出「💬 批注 / 🤖 就此划线提问」）；`DoodleToolbar.vue` 工具栏；ReaderView `pageMode` 下集成，切页自动保存上一页并加载当前页。
  - 存储格式：元素数组——`stroke`（tool/color/line_width/points[[x,y],...]/可选 `note`+`note_meta`）、`text`（text/color/font_size/x/y）；仅保存最终元素，不持久化历史版本。
  - **划线批注**：选中划线 → 弹窗编辑 `note`（Markdown/LaTeX），随元素保存。
  - **划线区域提问**：选中划线 → 按归一化 bbox 裁剪原图页（`cropDataUrl`，JPEG data URI）→ 复用 `POST /api/books/{id}/chat` 的 `crop_image`/`crop_label` 字段（受隐私开关约束）→ 流式回答进入 AI 面板。
- **chat 扩展**：`ChatIn` 新增 `crop_image`（划线区域裁剪图）与 `crop_label`（区域说明）；`chat_service.build_messages` 支持整页图 + 划线图多图片消息（chat 模式）。

## 6. M5 AI 增强：脑图图像组件 + 划词 AI 操作（第 5 轮任务产出）

> 升级四类生成中的「脑图」为 ECharts 图像组件（同一张图展示大纲、细节与重要定理三层），并为划词菜单增加 AI 操作。

### 6.1 脑图服务（`backend/app/services/mindmap_service.py`）

- `generate_mindmap(db, book, chapter, selection="", focus="")`：以当前章节（可叠加选中段落/关注重点）为输入生成脑图，返回 `{title, tree, markdown, citations}`。
- 提示词要求 LLM 输出严格 JSON 树（`MINDMAP_SYSTEM`）：节点含 `name / nodeType(大纲|细节|重要定理) / ref({chapter, para}) / children`。
- `parse_mindmap_json(text)`：支持裸 JSON 与 ` ```json ` 代码块；解析失败回退 `markdown_to_tree`（Markdown 列表/纯文本缩进行 → 树，含「定理/公式/定义」关键词自动标记重要定理）。
- `tree_to_markdown(node)`：树 → Markdown 层级列表（复制/导出用）。
- 兼容既有能力：隐私开关关闭时不发正文；扫描版开启「发送页面图片」时自动附带当前页图；RAG 片段与 Skill 技能注入；生成结果以 `persist_chat` 写入对话历史（问题：`生成脑图：<焦点>`）。

### 6.2 脑图 API（`backend/app/api/routes/mindmap.py`）

- `POST /api/books/{book_id}/mindmap`：body `{chapter_id?, selection?, focus?}`，返回 `ok({title, tree, markdown, citations})`；未配置 API Key 返回 400，LLM 调用失败返回 502。

### 6.3 脑图组件（`frontend/src/components/MindMapPanel.vue`）

- 基于 ECharts `tree` 系列渲染：`nodeType` 着色（大纲蓝 / 细节绿 / 重要定理红），图例提示，`tooltip` 显示类型与出处 `【第X章 第Y段】`。
- 节点点击 → 跳转阅读区对应段落（复用 `[data-para]` 锚点，可跨章节）。
- 工具栏：复制大纲（Markdown）、导出图片（`chart.getDataURL` PNG）。

### 6.4 阅读页集成（`frontend/src/views/ReaderView.vue`）

- 「脑图」预设改为弹出脑图对话框（不再填入输入框）；「解读 / 概论 / 思考逻辑」维持文本生成。
- 划词菜单新增 AI 操作：🤖 解释选中段（带选中内容提问）、🧠 该段脑图（以选中段为输入生成）、📌 加入思考清单（创建「思考」类型笔记）。
- 聊天消息新增「复制」按钮；对话历史/引用出处展示不变。

### 6.5 冒烟要点

1. 阅读页点「脑图」→ 弹出对话框 → 生成中提示 → ECharts 树渲染（三层着色 + 图例），无报错。
2. 划词 → 菜单含 3 个 AI 操作；「加入思考清单」落库为思考笔记；「解释选中段」带选中文本发送并流式回复。
3. 脑图节点带出处时可点击跳转原文；「复制大纲」「导出图片」可用。
4. 回归：`pytest`（38 项）+ `ruff check app tests` + `pnpm build`。

## 7. 阅读页解耦重构（第 7 轮任务产出）

> 目标：消除重复、拆分 1054 行 ReaderView、修复书架列表 N+1 查询，行为不变。

### 7.1 后端：书架列表批量聚合（消除 N+1）

| 函数 | 位置 | 说明 |
| --- | --- | --- |
| `books_reading_summary(db, books)` | `backend/app/repositories/reading.py` | 一次查询全部书籍的最新阅读日志后内存统计，返回 `{book_id: (read_chapters, latest_chapter)}`；章节复用 `books` 上已加载的 chapters |
| `list_books(db, folder_id)` | `backend/app/repositories/books.py` | 改为 `selectinload(Book.chapters)` 预载章节，避免序列化时逐本懒加载 |
| `GET /api/books` | `backend/app/api/routes/books.py` | 改用批量汇总，响应契约不变（`read_chapters` / `latest_chapter` / `chapter_count`） |

- 使用：书架接口无需改动前端；新增书籍相关批量接口时优先在 repositories 层做聚合查询。
- 修改：若 `latest_chapter` 兜底规则变化，同步改 `book_reading_summary` 与 `books_reading_summary` 两处（保持单本/批量行为一致）。

### 7.2 前端：纯函数工具（`src/utils/`）

| 函数 | 文件 | 说明 |
| --- | --- | --- |
| `chapterPercent(readChapters, totalChapters)` | `utils/progress.ts` | 按已读/总章节算整书百分比（无章节返回 0） |
| `percentOf(progress)` | `utils/progress.ts` | 0..1 进度值 → 整数百分比 |
| `splitBlocks(source)` | `utils/content.ts` | 章节正文按空行切渲染块，保持 ``` 代码块不截断 |

- 使用：阅读页/书架共用 `chapterPercent`；正文加载用 `splitBlocks`。
- 修改：均为纯函数，可直接补 `frontend/tests/utils.test.ts` 用例后修改。

### 7.3 前端：阅读页组合式函数（`src/composables/`）

| 组合式函数 | 职责 | 关键返回 |
| --- | --- | --- |
| `useReaderSelection(scrollEl)` | 划词菜单定位/显隐/取走选区 | `selMenu`、`onMouseUp`、`onDocMouseDown`、`takeSelection` |
| `useReaderNotes({...})` | 笔记 CRUD、正文精确高亮、笔记抽屉/对话框状态 | `notes`、`noteDialog`、`applyHighlights`、`jumpToNote` 等 |
| `useReaderProgress({...})` | 滚动防抖保存、位置恢复、自动读完（≥10s+滚到底） | `onScroll`、`saveNow`、`checkAutoRead`、`resetForBook`、`dispose` |
| `useReaderMindmap({...})` | 脑图生成/弹窗状态、节点跳转原文 | `openMindmap`、`jumpToMindmapPos` |
| `useReaderAi({...})` | 流式问答、预设能力、划词追问、清空/复制 | `sendChat`、`presetPrompt`、`resetChat`、`askSelection` |
| `useReaderDoodle({...})` | PDF 页图涂鸦（v1.56 从 ReaderView 抽出）：画板状态、按页加载/防抖保存、划线批注与划线区域提问 | `doodleElements`、`loadDoodle`、`switchPage`、`askCropOnDoodle`、`dispose` |

- 使用：ReaderView 作为组合根，按依赖顺序创建组合式函数并解构出模板所需状态/方法；`ReaderView.vue` 从 1054 行降至 427 行（v1.56 再抽 `useReaderDoodle`，813→741 行）。
- 修改：
  - 划词交互改 `useReaderSelection`；笔记/高亮改 `useReaderNotes`；进度/自动读完改 `useReaderProgress`；AI 对话改 `useReaderAi`；脑图改 `useReaderMindmap`。
  - 组合式函数通过 opts 接收共享依赖（`bookId`/`scrollEl`/`currentChapterId` 等 Ref 或回调），禁止直接 import 单例 store 于组合式函数内。
  - `useReaderAi` 的 `scrollChat` 回调由父级传入（指向 `ReaderChatPanel` 暴露的 `scrollToBottom`）。

### 7.4 前端：阅读页展示子组件（`src/components/`）

| 组件 | Props | Events |
| --- | --- | --- |
| `ReaderLeftPanel.vue` | `books`、`book`、`bookId`、`currentChapterId`、`loadError` | `select-book`、`select-chapter`、`toggle-read` |
| `ReaderChatPanel.vue` | `messages`、`streaming`、`streamError`、`contextTitle` + `v-model:input` | `preset`、`send`、`abort`、`clear`、`copy`；`defineExpose({ scrollToBottom })` |

- 使用：容器组件（ReaderView）管理状态，子组件只收 props、发事件。
- 修改：调整书架/目录 UI 改 `ReaderLeftPanel`；调整 AI 面板 UI 改 `ReaderChatPanel`（样式 scoped 随组件迁移）。

### 7.5 测试基线

- 后端：`pytest` 39 项全绿（新增 `test_list_books_bulk_summary` 覆盖批量汇总契约），`ruff check app tests` 全绿。
- 前端：`vitest run` 17 项通过（`tests/utils.test.ts` 5 项 + `tests/math.test.ts` 8 项 + `tests/text.test.ts` 4 项）；`pnpm build`（vue-tsc + vite）全绿。

### 7.6 LLM 输出数学公式定界符归一化（`src/utils/math.ts`）

LLM 常用裸括号/方括号写公式（如 `(\operatorname{ext}(K))`、独占一行的 `[ ... ]`），
标准渲染管线只认 `$...$` / `$$...$$`，导致公式原样输出。新增归一化工具：

| 函数 | 说明 |
| --- | --- |
| `normalizeMath(src)` | 组合入口；代码围栏内不转换 |
| `normalizeBlockBrackets(src)` | 独占一行且含 LaTeX 命令的 `[ ... ]` → `$$...$$` |
| `normalizeDelimiters(src)` | `\(...\)` / `\[...\]` → `$...$` / `$$...$$` |
| `normalizeInlineParens(src)` | 含 LaTeX 命令的裸 `( ... )`（支持嵌套）→ `$...$`；跳过已定界公式 |

- 使用：`MdRender.vue` 在 protectMath 每个非代码围栏段前调用 `normalizeMath`；同时为 `\(...\)` / `\[...\]` 增加占位符保护，避免内部 `* _` 被 markdown-it 转义。
- 修改：新增定界符规则改对应函数并补 `frontend/tests/math.test.ts` 用例；提示词侧已要求模型输出标准 `$...$` / `$$...$$`（`backend/app/ai/prompts/chat.py` §3），新输出走标准路径，归一化仅作兼容兜底。


### 7.7 脑图渲染清洗 + 下载/插入批注（`src/utils/text.ts`、`MindMapPanel.vue`）

脑图节点名来自 LLM 结构化 JSON，常含 `$...$` / `\operatorname{...}` / Markdown 记号，
ECharts 树标签不支持 LaTeX/Markdown，原样显示会乱码。新增纯文本清洗与两类输出：

| 函数/功能 | 说明 |
| --- | --- |
| `toPlainDisplayText(text)` | 纯函数：去 `$` 定界符与转义定界符、LaTeX 命令名与花括号、Markdown 记号（`* _ `` #`），返回可读纯文本 |
| 下载大纲 | 工具栏「下载大纲」：把 markdown 大纲存为 `<标题>.md`（Blob 下载） |
| 插入为批注 | 工具栏「插入为批注」：把当前章节 markdown 大纲写入该章批注（quote 为 `【思维导图：标题】` 占位，不与正文误匹配） |
| 笔记正文渲染 | 笔记抽屉正文从纯文本改为 `MdRender`，插入的 Markdown/LaTeX 大纲可正常渲染 |

- 使用：`MindMapPanel.vue` 的 `buildTree` 对节点名调用 `toPlainDisplayText` 后再交给 ECharts；
  点击「插入为批注」→ `ReaderView.insertMindmapAsNote()` → `useReaderNotes.addNote('批注', ...)`（复用保存/排序/提示逻辑）。
- 修改：清洗规则改 `frontend/src/utils/text.ts`（新增 `frontend/tests/text.test.ts` 用例）；
  新增按钮在 `MindMapPanel.vue` 工具栏，插入目标为当前章节的笔记表。

## 8. M6 阅读体验补全：位置书签 + PDF 页图涂鸦（第 8 轮任务产出）

### 8.1 位置书签（全格式 + 分组）

**后端**

| 文件 | 说明 |
| --- | --- |
| `app/models/activity.py` | 新增 `Bookmark` 表：`book_id/chapter_id/page_index/para_pos/title/note/group_name/created_at`（外键 CASCADE） |
| `app/repositories/bookmarks.py` | `list_bookmarks`（时间倒序）/ `get_bookmark` / `create_bookmark` / `update_bookmark`（标题/备注/分组）/ `delete_bookmark` |
| `app/api/routes/bookmarks.py` | `GET/POST /api/books/{id}/bookmarks`、`PATCH/DELETE /api/bookmarks/{id}`；章节归属校验；`BookmarkIn/BookmarkUpdate` Pydantic 模型 |
| `app/schemas/serializers.py` | `bookmark_to_dict` 序列化 |

- 修改：新增字段需同步 `BookmarkIn/BookmarkUpdate`、仓储函数与 `bookmark_to_dict`；新表由 `Base.metadata.create_all` 自动建（旧库无需 ALTER）。
- 删除书籍时 `books_service.delete_book` 显式 `delete(Bookmark)` 清理。

**前端**

| 文件 | 说明 |
| --- | --- |
| `src/api/annotations.ts` | `listBookmarks/createBookmark/updateBookmark/deleteBookmark`（复用 `client.ts`，新增 `put` 封装） |
| `src/components/BookmarkDrawer.vue` | 书签抽屉：时间倒序平铺 + 分组视图、添加当前章节/页书签（书名/分组）、编辑/删除、`jump` 事件 |
| `src/views/ReaderView.vue` | 「🔖 书签」按钮 + `BookmarkDrawer` 集成；`jumpToBookmark`（PDF 按 `page_index` 定位页章节；文本书按 `chapter_id+para_pos` 滚动 `[data-para]`）；`scrollToPara` 段落滚动 |

- 修改：`BookmarkDrawer` 的分组由 `group_name` 字符串表达；如需多级分组/文件夹树，在 `Bookmark` 表加 `parent_group_id` 并扩展抽屉分组树。

### 8.2 PDF 页图涂鸦（画板 + 撤销/擦除/文本 + 划线批注/提问）

**后端**

| 文件 | 说明 |
| --- | --- |
| `app/api/routes/annotations.py` | `GET /api/books/{id}/annotations?page_index=N` 读取、`PUT /api/books/{id}/annotations`（`{page_index, elements}`）整页覆盖保存；上限 `MAX_ELEMENTS=2000` |
| `app/services/books_service.py` | `_remove_book_files` 清理 `annotations/` 目录（旧布局分支） |
| `app/api/routes/chat.py` | `ChatIn` 新增 `crop_image`（划线区域 data URI）/ `crop_label`（区域说明）；组装 `build_messages` |
| `app/services/chat_service.py` | `build_messages` 支持整页图 + 划线图多图片消息（chat 模式，受隐私开关约束） |

- 存储：`data/books/<书目录>/annotations/page_XXX.json`（元素数组）；`stroke` 含 `tool/color/line_width/points/note/note_meta`，`text` 含 `text/color/font_size/x/y`；坐标 0~1 归一化。
- 修改：如需历史版本，把「会话内撤销栈」升级为「每页版本快照列表」存储到同一 JSON 的 `history` 字段并调整 `undo`。

**前端**

| 文件 | 说明 |
| --- | --- |
| `src/components/PageDoodleCanvas.vue` | canvas 叠加层：尺寸以页图实际渲染尺寸同步（ResizeObserver + 兜底轮询），仅缓冲区尺寸变化时重置（避免清空画布）；点坐标兼容 `[x,y]`/`{x,y}`（`src/utils/annotations.ts`）；笔刷/高亮/橡皮/文本工具；`undoStack` 上限 5 + 切页 `resetUndo`；橡皮命中擦除；划线点击选中菜单（`edit-note` / `ask-crop`）；`cropDataUrl` 按 bbox 裁剪原图为 JPEG data URI |
| `src/components/DoodleToolbar.vue` | 工具栏：工具切换 / 颜色 / 线宽 / 撤销 / 清除本页 |
| `src/api/annotations.ts` | `getPageAnnotations/savePageAnnotations` |
| `src/views/ReaderView.vue` | `pageMode` 下集成画板；涂鸦工具栏置于阅读页顶部工具栏「第 X 页」与「第 X 章/共 XX 章」之间（仅 PDF 按页阅读显示，文本书不显示）；切页保存上一页并加载当前页（防抖 800ms）、加载时迁移旧数据点格式；划线批注弹窗；划线提问（写入 AI 面板并发起 chat） |

- 修改：工具扩展（如箭头/矩形）在 `PageDoodleCanvas` 增加 tool 分支与 `AnnotationElement.type` 联合类型；撤销栈上限调整 `undoStack.length > 5` 常量。

### 8.3 冒烟要点

- 书签：文本书添加 → 抽屉分组显示 → 点击跳转回到对应段落；PDF 书（`is_scanned=true`）添加整页书签 → 跳转加载对应页章节。
- 涂鸦：PDF 页图画笔划线 → 刷新页面仍叠加显示；撤销 5 步内恢复；橡皮擦除；划线点击 → 批注弹窗保存 → 重开仍在；划线提问 → chat 请求带 `crop_image`（无 API Key 时报配置错误而非 422）。
- 删除书籍：书签与 `annotations/` 文件一并清理（测试 `test_delete_book_cleans_bookmarks_and_annotations`）。

### 8.4 涂鸦修复轮：渲染与工具栏重排（第 8 轮补充迭代）

- 画布 1×1 / 无法绘制 / 文本不渲染根因：旧版标注把 `points` 存为 `{x,y}` 对象，`drawStroke` 用数组解构 `([x, y])` 遍历直接抛 `TypeError: object is not iterable`，`onMounted` 中断导致 ResizeObserver 未注册、画布永远 1×1。
- 修复一：新增 `src/utils/annotations.ts`（`toPoint` / `normalizeAnnotationPoints`），读取与渲染均兼容 `[x,y]` 与 `{x,y}`，旧数据自动迁移为数组并回写持久化（幂等，无对象点时不触发更新）。
- 修复二：`syncSize` 仅在实际尺寸变化时重置 canvas 缓冲区（`el.width/height` 赋值会清空画布），配合 retry 轮询修正"尺寸稳定后不再重绘"导致的清空时序 bug；mounted 内 `redraw` 异常不再阻塞画板初始化（try/catch 兜底）。
- 修复三：涂鸦工具栏从页底移入阅读页顶部工具栏，位于「第 X 页」与「第 X 章/共 XX 章」之间，仅 `pageMode`（PDF 按页阅读）显示；文本书仍显示「已读 x/y 章 · 进度 z%」。
- 修复四：切页时 `resetUndo` 清空撤销栈，避免上一页涂鸦可被"撤销"进当前页；撤销按钮可用状态由画板 `can-undo` 事件驱动（`doodleCanUndo`）。
- 冒烟：旧数据页打开即渲染高亮；笔刷/高亮/橡皮/文本均可绘制且刷新后仍存在；工具切换有 active 反馈；第 20 页无数据不残留上一页内容；`/reader/2`（文本书）无涂鸦工具栏。

### 8.5 阅读区单一滚动条（第 8 轮补充迭代）

- 问题：PDF 按页阅读时 .page-scroll（overflow: auto）与外部 .reading-scroll（overflow-y: auto）叠加产生两个滚动条，阅读区视觉割裂。
- 修复（src/views/ReaderView.vue）：.page-scroll 移除 overflow: auto，改为 width: 100%; display: flex; justify-content: safe center（仅负责水平居中页图）；缩放条 .page-zoombar 增加 position: sticky; top: 0; z-index: 2; background，外层滚动时保持可见。
- 冒烟：阅读区仅剩外层 .reading-scroll 一个滚动条，滚动到底正常，缩放条不随内容滚走；左侧书架/目录独立滚动不受影响。

## 9. M7 PDF 统一处理与多模态视觉提取（第 9 轮任务产出）

> 状态：**已实现**（需求 3.2/3.4.11 / 技术栈规范 §6；决策 2/21~23）。

### 9.1 PDF 解析统一按页（`backend/app/parsers/pdf.py`）

| 函数 | 功能 | 输入 | 输出 |
| --- | --- | --- | --- |
| `parse_pdf(path, title_hint=None)` | PDF（含文本型）统一按原始页切章：每页一章（标题「第 N 页」，content 为空，`page_index=N`），`is_scanned=True`；本地抽取文本按页放入 `ParsedBook.page_texts`（仅作全文检索索引） | 路径 | `ParsedBook` |
| `render_pdf_page(path, page_index, out_path, max_width=0, quality=90, zoom=None)` | 渲染指定页为图片（默认按内嵌原图分辨率自动放大；封面用 max_width） | 路径/页号/输出路径 | `Path` |
| `render_pdf_pages(path, out_dir, max_width=0, quality=90)` | 渲染全部页为 `page_XXX.jpg` | 路径/目录 | 页数 |
| `pdf_page_target_width / jpeg_width` | 目标像素宽度（原图）/ 已渲染页图宽度（低清升级判断） | — | int |

**如何使用**：导入 PDF 时 `import_service.import_book` 自动渲染全部页到 `<书目录>/pages/`，并把本地抽取文本写入 `<书目录>/local_text/page_XXX.txt`；阅读页按「第 N 页」章节展示原图。
**如何修改**：调整页图渲染质量/倍率改 `PAGE_AUTO_ZOOM_MIN/MAX` 或 `render_pdf_page` 的 `quality/zoom`；旧版文本型 PDF 书籍仍按旧行为展示，删除后重新导入即可升级为按页模式。

### 9.2 多模态视觉配置

- 配置项（`.env` 或设置页，独立于文本 AI，无需额度管理）：`vision_base_url / vision_api_key / vision_model / vision_timeout / vision_verify_ssl / vision_max_tokens` + 精细参数 `vision_temperature / vision_top_p / vision_frequency_penalty / vision_presence_penalty / vision_enable_thinking / vision_thinking_budget`（`backend/app/core/config.py`）。默认模板指向 SiliconFlow：`https://api.siliconflow.cn/v1` + `Qwen/Qwen2.5-VL-72B-Instruct`（可用 `deepseek-ai/DeepSeek-OCR` 等），`vision_max_tokens=4096`。
- **接口约束（按 SiliconFlow 文档）**：视觉客户端**强制 `mode="chat"`**（SiliconFlow 等仅支持 `POST /chat/completions`，`responses` 模式会 404）；`LLMClient` chat 分支按设置写入 `max_tokens / temperature / top_p / frequency_penalty / presence_penalty`，以及 SiliconFlow 推理模型专用的 `enable_thinking / thinking_budget`（`VISION_ENABLE_THINKING / VISION_THINKING_BUDGET`；Qwen 等非推理模型勿开）；页图 `image_url` 固定带 `"detail": "high"` 保证识别精度。
- **请求格式（按 SiliconFlow 多模态文档 api-docs.siliconflow.cn/docs/userguide/capabilities/multimodal-vision）**：多模态模型统一走 `POST /chat/completions`，`messages[].content` 为 parts 列表（文本 + 图片部件）；图片部件 `{"type":"image_url","image_url":{"url":..., "detail":"high"}}`，`url` 支持 base64 data URI（`data:image/jpeg;base64,...`），`detail` 取 `auto/low/high`。页提取（`vision_extract._extract_page_text`）、页图附件与划线区域裁剪图（`chat_service.build_messages`，前端裁剪图由 `canvas.toDataURL('image/jpeg', 0.85)` 生成）统一该格式。
- **配置优先级**：设置页保存的值（DB `settings` 表）优先于 `.env`；`.env` 仅在对应项未在设置页保存时生效。当前正式库已把原有设置页覆盖项迁移进 `backend/.env` 并清除 DB 覆盖，`.env` 为唯一配置源（改 `.env` 后需重启后端）。
- 后端：`repositories.settings.vision_client_kwargs(db)` 返回 LLMClient 构造参数（未覆盖项取 .env，不回退到文本 AI 配置）；`vision_configured(db)` 判断是否可用；设置视图/保存/测试走 `api/routes/settings.py`（`GET/PATCH /api/settings/ai`、`POST /api/settings/ai/test-vision`）。
- 前端：`SettingsView.vue`「多模态视觉接入（M7）」卡片（Base URL/API Key/模型/超时/校验 SSL/生成上限/温度/Top P/频率惩罚/存在惩罚/思考模式开关/思维链上限 + 测试视觉连接）；`types.ts` `AiSettings` 扩展 `vision_*` 字段（含精细参数）。

### 9.3 视觉提取服务（`backend/app/services/vision_extract.py`）

| 函数 | 功能 | 使用/修改 |
| --- | --- | --- |
| `page_text_path(book, page_index)` | 页缓存文件路径 `<书目录>/page_text/page_XXX.md` | 调整目录名改 `PAGE_TEXT_DIR` |
| `read_page_cache(book, page_index)` | 读缓存；缺失/空返回 None | 缓存命中判定唯一入口 |
| `ensure_page_cache(db, book, page_index, force=False)` | 单页提取：命中（非空且非 force）直接返回，否则调用多模态 LLM 提取并落盘；隐私开关关闭抛 ValueError | 修改提取提示词改 `EXTRACT_SYSTEM`；切换视觉模型改配置 |
| `ensure_window_caches(db, book, page_index, force=False)` | `[P-1,P,P+1]`（首/末页裁剪）增量缓存，返回 {页号: 文本} | 窗口大小改 `start/end` 计算 |
| `rebuild_book_caches(db, book, force=False, progress=None)` | 全书重建/补齐；返回 {total, extracted, cached, failed, errors} | 单页失败不中断 |
| `extract_book_pages_task(book_id, force=False)` | 后台任务入口（独立会话） | 供导入预提取与重建路由调用；**M9 读完归档全书提取将复用该入口**（归档时先全书补齐页缓存，再以缓存全文调 `generate_rag_skill` 总结） |

### 9.4 页缓存 API（`backend/app/api/routes/vision.py`，prefix `/api/books`）

- `GET /{book_id}/page-text/status`：缓存覆盖 {total, cached}。
- `GET /{book_id}/page-text/{page_index}`：读单页缓存文本（未缓存 text=null）。
- `POST /{book_id}/page-text/{page_index}`：重新提取本页（force 覆盖）。
- `POST /{book_id}/page-text/rebuild`：提交后台重建任务（body `{"force": bool}`），返回 `{task_id}`。
- `GET /{book_id}/page-text/tasks/{task_id}`：任务状态。
- 注意：字面量路由（status/rebuild）必须注册在 `/{page_index}` 之前，避免被 int 路径参数抢先匹配。

### 9.5 提问链路（对话 + 脑图）

- `services/ai_context.py`：新增 `build_page_context_block(window_texts, enable_body_send)`（组装「【第 N 页】+ 文本」块）；`extract_citations` 支持【第X页】（para='页'）。
- `api/routes/chat.py`：PDF 按页章节提问时，若已配置多模态且隐私开启，先 `ensure_window_caches` 提取窗口并注入页缓存文本（出处「第 X 页」）；提取失败/未配置时回退当前页原图附件（`page_image_data_uri`）。
- `services/mindmap_service.py`：脑图生成同样优先页缓存文本。
- 前端 `ReaderChatPanel.vue` 引用 chip 兼容「第X页」。

### 9.6 前端入口（`frontend/src/api/vision.ts` + `ReaderView.vue`）

- `api/vision.ts`：`getPageTextStatus / getPageText / reextractPage / rebuildPageText / getPageTextTask`。
- 阅读页工具栏（PDF 页模式）：页缓存 x/y 状态、「🔄 重提本页」、「📄 重建页缓存」（确认后提交任务并轮询刷新）。

### 9.7 冒烟要点

1. 导入 PDF（含文本型）→ 书籍 `is_scanned=true`、章节为「第 N 页」、`/pages/N` 返回原图。
2. 设置页配置多模态并「测试视觉连接」；`GET /api/settings/ai` 返回 `vision_*` 字段（Key 掩码）。
3. 无多模态配置时 `POST /page-text/{n}` 与 `/rebuild` 返回 400「未配置多模态视觉 API」。
4. 配置后重提本页 → `page-text/status` cached 递增；`page-text/{n}` 返回文本。
5. PDF 页提问 → 回答引用【第X页】；页缓存生成后同窗口提问不再触发多模态（增量缓存）。

---

## 10. M8 知识图谱（第 10 轮任务产出）

### 10.1 后端图谱服务（`backend/app/services/graph/` 包）

> v1.56 拆分：`graph_service.py` → `services/graph/` 包（v1.63 已删除拆分后遗留的 `graph_service.py` 死代码，全库无引用；LLM 输出 JSON 容忍解析统一到 `app/ai/parsing.py` 的 `parse_llm_json`，graph_sync / llm_score / rag_service / mindmap_service 共用）。模块映射：`keywords.py`（`extract_keywords`/`sanitize_cluster_name`）、`lexicon.py`（词库与领域命名 `_domain_candidates`/`_pick_domain_name`/`_first_meaningful_term`/`_posterior_keywords`/`load_domain_lexicon`/`cache_domain_term`）、`corpus.py`（`book_corpus`/`_chapter_text`）、`clustering.py`（`assign_clusters`/`post_classify_book`/`merge_and_rename_clusters`）、`cross_book.py`（`compute_cross_book_graph`/`incremental_cross_book_graph`/`knowledge_appears_in`/`global_graph_payload`/`rebuild_all_graph`）、`intra_book.py`（`build_intra_book_graph`/`intra_graph_payload`）；`services/graph/__init__.py` 保持公共 API 兼容。

| 函数 | 功能 | 使用/修改 |
| --- | --- | --- |
| `extract_keywords(text, top_n=80)` | 关键词抽取：中文连续串拆二元组 + 英文词，停用词过滤，返回 {词: 词频} | 调整停用词改 `_STOPWORDS`；精度改 `top_n` |
| `book_corpus(book)` | 书籍文本语料：标题/作者 + 章节标题与正文；PDF 统一按页补充 `local_text/` 页文本 | 语料来源扩展（如并入 RAG 摘要）在此追加 |
| `assign_clusters(db, books=None)` | 聚类分层：**post 落盘（未失效）→ tag → 文件夹名 → 领域自动聚类**（先按相似度成簇：共享 ≥2 个加权关键词吸收；再统一用 `_pick_domain_name` 按**领域专业术语**命名，候选词见 `_domain_candidates`），仍无归属归「其他」；回写 `classify_source/classified_at/classify_version` | 聚类阈值改 `>= 2`；领域命名改 `_pick_domain_name` / 泛化词表 `_GENERIC_DOMAIN_TERMS`；算法细节见 `docs/知识图谱聚类算法.md` |
| `post_classify_book(db, book)` | 两阶段分类 post-classify（M9）：基于 RAG 资产后验关键词 top60，簇内一致性校验（重叠 ≥ `T_POST=3` 维持）/ 簇迁移 / 维持 pre 簇 / 新建簇（自动命名用 `_first_meaningful_term` 剔除泛化词）；tag/文件夹硬约束优先；落盘 `cluster_name/classify_source=post/classified_at/classify_version` | `T_POST` 初值 3，待需求决策 20 阈值函数定稿后替换；簇合并/重命名已实现（`merge_and_rename_clusters`，重命名跳过 `_GENERIC_DOMAIN_TERMS` 泛化词） |
| `merge_and_rename_clusters(db)` | post 簇合并（`T_MERGE=5` 并集重叠并入主簇）与代表性术语重命名（簇内出现书最多的后验术语众数，跳过 `_GENERIC_DOMAIN_TERMS` 泛化词，冲突跳过）；仅 post 书，归档成功后触发 | 合并阈值 `T_MERGE`；重命名候选来源/泛化词表在此调整 |
| `_clean_title_segment(seg)` | 书名/标题清洗：去掉括号内（作者/版本/出版社）与文件名下划线段，供领域命名候选词用 | 元数据格式变化时在此扩展 |
| `load_domain_lexicon()` | 读取专业术语词库：返回 (用户区术语, 系统缓存区术语)，按文件 mtime 缓存 | 词库文件位置改 `settings.domain_terms_file`；格式解析改此处 |
| `cache_domain_term(term)` | 把自动选定的专业术语写入词库【系统缓存区】（泛化词不缓存、去重、缺失自动创建文件） | 缓存策略调整改此处 |
| `_lexicon_hits(text, terms)` | 术语在文本中的命中：中文词组按子串、英文/数字词组按词边界 | 匹配规则调整改此处 |
| `_domain_candidates(book, posterior=None)` | 领域命名候选词（带权重）：章节标题×3 + 清洗后书名×2 + 正文×1 + RAG 后验（summary+key_points）×5，`extract_keywords` 抽取并剔除 `_GENERIC_DOMAIN_TERMS`；专业术语词库命中文本的术语以高权重（≥100）进入候选 | 调整权重/候选来源改此处；新泛化词加入 `_GENERIC_DOMAIN_TERMS` |
| `_pick_domain_name(candidates_by_book, members)` | 从簇成员候选词挑领域名：**用户词库命中优先** → 自动候选（含系统缓存词库，覆盖书数优先 → 词频次之）；无候选返回空串（调用方兜底取簇内最高频词） | 命名策略调整改此处 |
| `_first_meaningful_term(keywords)` | 取领域专业术语：用户词库 → 系统缓存词库 → 第一个未被 `_GENERIC_DOMAIN_TERMS` 过滤的关键词（无则最高频词），post-classify 自动命名用 | 无候选时的兜底策略改此处 |
| `compute_cross_book_graph(db)` | 重建全部书籍关联：关键词共现余弦分（0~100）+ 笔记加权（`_note_weight`，上限 15 分）；同聚类低分「主题相似」边；**有界 LLM 打分/方向/原因增强（v1.32）**：对关键词候选对调用 `llm_score.enrich_pairs_with_llm`（`MAX_LLM_PAIRS=40` 截断）并 `apply_llm_result` 合并（强度取 max、方向/类型/原因/from_book 以 LLM 为准，失败回退） | 强度公式在此调整；LLM 候选对数上限改 `llm_score.MAX_LLM_PAIRS`；方向/类型枚举改 `llm_score.DIRECTIONS`/`RELATION_TYPES` |
| `incremental_cross_book_graph(db, book_id)` | **导入自动增量更新（v1.28）**：只补新书与既有书的关联边（复用 `_pair_score`，同聚类低分「主题相似」边；幂等，既有边集合去重、不动既有关联），末尾 `link_graph_assets` 补本地 RAG 联动存根；**v1.32 起新增边同样走有界 LLM 打分增强**；返回 `{"relations_added", "linked"}`；由 `books.py upload_book` 导入成功后调用，失败回滚不阻塞导入 | 增量评分/阈值与全量重建保持一致；批量导入场景如需补「新书-新书」边在此扩展 |
| `knowledge_appears_in(db, kp_id)` | **跨书知识检索（v1.28）**：检索该知识点还出现在哪些其他书——其他书 KnowledgePoint（title+summary 关键词重叠）+ 其他书 RAG `key_points` 命中；返回 `{"source", "books": [{book_id, title, matched_kps, rag_hits, matched_count}], "total"}`，来源书不计 | 匹配规则调整改此处；可作为「相关领域判定」的补充信号 |
| `build_intra_book_graph(db, book)` | 重建单书内部图谱：章节级 + 重要段落（定理/定义/引理等模式行）+ 用户标记（笔记）；章节顺序「前置依赖」、同章重要段落「承接」 | 重要段落识别模式改 `_THEOREM_RE`；关系类型/强度在此调整 |
| `global_graph_payload / intra_graph_payload` | 全局/书内图谱 JSON 组装（含聚类、节点、边、章节索引）；**边字段含 `from_book`（v1.32）**：有向边理论源头书 id，前端据此画箭头 | 新增字段（封面、进度等）在此扩展 |
| `rebuild_all_graph(db)` | 重建全部（先逐书内部图、再跨书关联），返回统计 | 大批量时改为后台任务（`app/tasks`） |
| `score_pair_llm(db, a, b, ka, kb)`（`graph/llm_score.py`，v1.32） | 对一对书调用 LLM 打分：输出 `{strength, from_book(A/B/null), direction(承接/发展/批判/无), relation_type, reasons}`；未配置 AI / 调用失败 / 非法枚举一律返回 None（回退关键词分） | 提示词改 `ai/prompts/graph_edge.py`（`SYSTEM_PROMPT`/`build_edge_user_prompt`）；解析/校验在 `_parse_json` 与返回值规范化处调整 |
| `enrich_pairs_with_llm(db, books_by_id, keywords, candidates)`（`graph/llm_score.py`，v1.32） | 对候选书对做**有界** LLM 打分：`candidates=[(a_id, b_id, 关键词分)]` 按分降序截断 `MAX_LLM_PAIRS=40`，返回 `{pair_key: llm_result}` | 上限改 `MAX_LLM_PAIRS`；候选来源（如纳入同簇对）在此扩展 |
| `apply_llm_result(rel, a_id, b_id, result)`（`graph/llm_score.py`，v1.32） | 把 LLM 结果合并进 `BookRelation`：强度取 max、direction/relation_type/reasons 以 LLM 为准、`from_book_id` 按 A/B 回填（null 清空） | 合并策略（加权/平均）调整改此处 |

**专业术语词库文件**（`backend/domain_terms.txt`，与 `.env` 同级，用户可编辑）：每行一个术语（中文词组/英文词组），`#` 开头为注释；上部为【用户区】（最高优先级），`系统缓存区` 标记行之后由系统自动追加学习到的术语（可编辑/删除）；命名优先级 = 用户词库 → 自动候选（含系统缓存词库，按覆盖书数优先）；修改文件即时生效（后端按 mtime 重读）。

### 10.2 图谱 API（`backend/app/api/routes/graph.py`，prefix `/api/graph`）

- `GET /books`：书籍级谱系图（懒构建：尚无关联时自动计算一次）→ `{clusters, nodes, edges}`；边字段含 `strength / direction / from_book / relation_type / reasons / user_feedback`（`from_book` 为有向边理论源头书 id，v1.32）。
- `GET /books/{book_id}`：书内知识图谱（懒构建）→ `{nodes, edges, chapters}`。
- `POST /rebuild`：重建全部图谱（同步）。
- `POST /books/{book_id}/rebuild`：重建单书内部图谱。
- `POST /relations/{relation_id}/feedback`：人工反馈 `{action: 确认/忽略/修改, strength?}`，回写 `user_feedback` 与强度。
- `GET /knowledge/{kp_id}/appears-in`（v1.28）：跨书知识检索——该知识点还出现在哪些书（命中知识点 + RAG 要点），来源书不计；404 表示知识点不存在。

### 10.3 前端（`frontend/src/views/GraphView.vue` + `src/api/graph.ts`）

- 全局谱系图：ECharts force 关系图，节点=书（颜色按聚类）、边按强度粗细/颜色、最强关联边红色加粗并显示「X分 原因」、「与哪本书关系最密切」由前端按最高强度边计算；**有向边（v1.32）按 `from_book` 设置箭头 source→target**（`edgeEndpoints`），方向非「无」才显示箭头；tooltip 与关联详情弹窗展示「A → B」方向与原因（`edgeDirLabel`）；聚类筛选 chips（全部 + 各领域）；关联详情弹窗（强度/类型/原因/方向 + 确认/忽略/修改强度反馈）。
- 书内知识图谱：节点按层级着色（章节级蓝 / 重要段落红 / 用户标记橙）、大小按重要度；带箭头有向边；层级筛选 chips；**点击知识点节点打开「知识点详情」弹窗（v1.28）**：展示「还出现在 N 本书」（书名 / 命中知识点 / RAG 要点），可跳转对应书的书内图谱（`switchKpBook`）或阅读原文；原「确认跳转」交互由弹窗内按钮承接。
- 重建按钮（全局/单书）+ 导出 PNG。

## 11. M9 三层画像（第 11 轮任务产出）

### 11.1 三层画像服务（`backend/app/services/profile_service.py`，M9）

| 函数 | 功能 | 使用/修改 |
| --- | --- | --- |
| `get_hot / get_warm / get_cold / get_all_profiles(db)` | 读取单层/三层画像（`UserProfile`：layer + dimension + value_json） | 新增维度改 `_load/_save` 的 dimension 约定 |
| `update_hot_profile(db, book, progress=, chapter_title=, highlight=, question=)` | 热画像实时回写：当前书进度/章节脉络/高亮与「不理解」/进行中的问题 | 阅读进度（`routes/reading.py` `save_progress`）与笔记（`routes/notes.py` `create_note`）已挂钩 |
| `migrate_profiles_on_archive(db, book, rag=None)` | 读完归档迁移：热→暖（recent_books 新条目 + 主题聚合）→ 跨 N 本暖→冷（domain_preferences/long_term_interests）→ >N 本全部沉淀冷（暖只留最近 1 本）；相关领域书入暖记忆（v1.59 起走 `relatedness.compute_relatedness`，条目含 `score`），并记录相关度行为样本 | 阈值改 `WARM_TO_COLD_THRESHOLD`/`KEEP_RECENT`（N 由 `get_thresholds()['warm_threshold']` 学习）；相关度判定见 §11.6；归档任务 `archive_book_task` 已挂钩 |
| `reset_profiles(db)` | 清空三层画像 | 重置后从阅读行为重新积累 |

- 相关领域判定（决策 20 定稿，v1.59）：见 §11.6 `services/relatedness.py`。
- 迁移阈值说明：跨 1 本 → 热转暖；跨 3 本 → 暖转冷；> 3 本 → 全部沉淀冷（需求 3.4.1）。

### 11.2 画像 API（`backend/app/api/routes/profile.py`，prefix `/api`）

- `GET /profile`：三层画像 `{cold, warm, hot}`（热>暖>冷读取优先级）。
- `POST /profile/reset`：重置三层画像。
- `GET /profile/recommendations`：阅读建议基础版（习惯统计 / 薄弱概念 / 复习提醒 / 阅读节奏，需求 3.4.6 增强项）。

### 11.3 前端（`frontend/src/views/ProfileView.vue` + `src/api/profile.ts`）

- 画像页 `/profile`：三层卡片（热=当前书/进度/章节脉络/高亮与问题；暖=近期书目/相关领域书/归档计数；冷=领域偏好 tag/长期兴趣）+ 重置按钮（确认后清空）。
- API 封装：`src/api/profile.ts` 的 `getProfile` / `getRecommendations` / `resetProfile`；类型 `ProfileData` / `RecommendationsData`（`src/types.ts`）。
- 画像页还新增「阅读建议」卡片：习惯统计（归档/已读章节/笔记/不理解/对话）、薄弱概念 tag（「不理解」聚合）、复习提醒列表（到期标红）、节奏文案。

### 11.4 阅读建议与复习提醒（`backend/app/services/recommendation_service.py`，M9 第三轮）

| 函数 | 功能 | 使用/修改 |
| --- | --- | --- |
| `generate_recommendations(db)` | 生成阅读建议 `{stats, weak_concepts, review, rhythm}`：习惯统计（归档数/笔记/不理解/对话/已读章节/总书数）、薄弱概念（「不理解」笔记关键词聚合 top3）、复习提醒（暖画像 `recent_books` 归档 ≥ `REVIEW_AFTER_DAYS=1` 天标 due）、节奏文案（归档 0/3+/稳定三档本地规则，稳定档取冷画像领域偏好 top3） | 规则阈值改 `REVIEW_AFTER_DAYS`/`WEAK_TOP_N`；后续可叠加 LLM 个性化建议与间隔重复参数 |

- 薄弱概念：`_weak_concepts` 对 `note_type=不理解` 的笔记 `quote_text+note_text` 执行 `services.graph.keywords.extract_keywords` 聚合，取出现次数最多前 3。
- 数据源接口：`GET /api/profile/recommendations`（`api/routes/profile.py`），前端 `ProfileView.vue` 已挂载。

### 11.5 资产增量增改与簇合并/重命名（M9 第三轮）

- **增量增改**：`rag_service.generate_rag_skill` 检测到已有 `rag` 资产时走增量提示词（`app/ai/prompts/rag_skill.py` `INCREMENTAL_SYSTEM_PROMPT` / `build_incremental_user_prompt`），输入 = 旧资产概要 + `_collect_new_material(db, book)`（全书笔记/划线/「不理解」+ 最近 20 条对话）+ 全书正文；输出仍为同结构 JSON 整体覆盖落库（`upsert_asset` version+1、id 不变）。再次读完同一本书归档即触发。
- **簇合并/重命名**：`services.graph.clustering.merge_and_rename_clusters(db)`——两 post 簇「后验关键词并集」重叠 ≥ `T_MERGE=5` 合并进书更多的主簇（被并入的书并入主簇成员列表，保证重命名覆盖全部书）；簇名取簇内出现于最多书的后验术语众数，与现名/他簇名冲突则跳过；只处理 `classify_source=post` 的书（tag/文件夹硬约束不受影响）；`archive_book_task` 成功后自动触发，结果写入 `result["cluster_merge"]`，失败仅回滚不阻塞归档。

### 11.6 相关度阈值函数（`backend/app/services/relatedness.py`，M9 第五轮，决策 20 落地）

| 函数 | 功能 | 使用/修改 |
| --- | --- | --- |
| `compute_relatedness(db, book)` | 相关度判定：score = max(跨书谱系边强度, 0.5×暖画像术语覆盖率×100 + 0.5×冷画像术语覆盖率×100)（0~100 裁剪，同 post 簇抬升到阈值）；返回 `{score, threshold, related, same_cluster, signals}`（signals 含 跨书关联/暖画像主题/冷画像领域/同簇 分值，供展示与解释） | 信号权重改 `W_PROFILE_WARM/W_PROFILE_COLD`；术语来源（RAG key_points → 书名+章节标题）改 `_book_terms`；相关度可并入 LLM 打分时在此叠加 |
| `is_related_book(db, book)` | 快捷判定（原 `profile_service._is_related_book` 语义） | 其他模块需要「是否相关」时复用 |

- **信号说明**：谱系为**主信号**（0~100 直接可比，历史阈值语义）；暖/冷画像为**辅助信号**（书术语对画像术语的覆盖率，画像为空则 0）；同 post 簇**直接判定相关**。
- **学习与覆盖**：归档时 `migrate_profiles_on_archive` 调用 `record_relatedness_sample`（行为样本），`learn_thresholds` 以行为样本 25% 低分位与确认边 25% 低分位之**较小者**学习 `related_strength`（样本 ≥6，裁剪 45~60）；设置页 PATCH 可手动覆盖。
- **NULL 过滤注意**：谱系边查询必须用 `or_(user_feedback.is_(None), user_feedback != "忽略")`，否则 NULL 反馈的边会被 SQL 排除（v1.17 同款缺陷，本次已修复）。

## 12. 图谱联动 RAG/Skill 沉淀与画像阈值自动学习（第 12 轮任务产出）

### 12.1 图谱联动增量增改（`backend/app/services/graph_sync.py` + `app/ai/prompts/rag_link.py`）

| 函数 | 功能 | 使用/修改 |
| --- | --- | --- |
| `rag_book_input(db, book, budget=3000)` | 轻量 RAG 素材：章节标题+正文截断（联动上下文/存根用，无 AI 也可用） | 调整预算改 `budget` |
| `link_domain_terms(db)` | RAG 术语补水：书名+key_points 经 `extract_keywords` 抽取、跳过 `_GENERIC_DOMAIN_TERMS`，写 `rag.domain_terms/linked_terms`（幂等，内容未变化不写库） | 术语来源字段改此处 |
| `attach_linked_book_stub(db, book, other, strength, reasons)` | 本地联动存根：`rag.linked_books` 补 `{book_id,title,strength,reasons,linked_at}`（保留最近 20 条，幂等） | 条目结构/上限改 `MAX_LINKED_BOOKS` |
| `link_relation_stubs(db, rel)` | 为单条关联的两本书补存根（忽略/强度 < `LINK_MIN_STRENGTH=50` 跳过） | 阈值改 `LINK_MIN_STRENGTH` |
| `link_graph_assets(db)` | 本地联动总入口：全部未忽略关联补存根 + `link_domain_terms`，返回 `{stubs, domain_terms}` | 挂载点：谱系重建、懒构建（`GET /api/graph/books`）、关联反馈（确认/修改） |
| `sync_assets_for_relations(db, relation_ids=None, use_llm=None)` | 显式联动：本地存根 + 已配置 AI 时对强度 ≥50 关联执行 `_llm_link_update`，返回 `{stubs, llm_updated}` | `use_llm=False` 强制只做本地存根 |
| `_llm_link_update(db, book, other, rel, reasons)` | LLM 增量增改：`rag_link` 提示词（旧资产 + 关联描述 + `rag_book_input`）→ 合并 JSON 整体覆盖（保留 `chunks/linked_books/domain_terms`），version+1，随后 post-classify，失败回滚 | 提示词改 `app/ai/prompts/rag_link.py` |

- **触发时机**：跨书谱系重建/懒构建/关联反馈（确认/修改）自动补本地存根；谱系图页「💾 联动沉淀」按钮调 `POST /api/graph/sync` 做显式 LLM 联动（未配置 AI 时仅本地存根不报错）。
- **幂等约定**：内容未变化不写库、不 bump 版本，避免图谱重算反复失效 post-classify。
- **API**：`POST /api/graph/sync` 返回 `{stubs, llm_updated, domain_terms}`；`POST /api/graph/rebuild` 返回新增 `linked` 计数。
- **前端**：`src/api/graph.ts` `syncGraphAssets()`；`GraphView.vue` 工具栏「💾 联动沉淀」按钮（并修复「重建图谱」按钮原先只刷新未重建的问题）。

### 12.2 画像阈值自动学习（`backend/app/services/profile_learning.py`）

| 函数 | 功能 | 使用/修改 |
| --- | --- | --- |
| `get_thresholds(db)` | 读取当前阈值 `{warm_threshold, related_strength, review_days}`（`settings` 表持久化优先，未设置回默认常量 3/60/1） | `profile_service._is_related_book`/`migrate_profiles_on_archive`、`recommendation_service` 统一走此函数 |
| `save_thresholds(db, warm_threshold=, related_strength=, review_days=)` | 手动覆盖阈值（裁剪 2~5 / 45~60 / 1~7） | 画像页「保存阈值」按钮 |
| `record_archive(db, archived_at)` | 归档时记录节奏样本（相邻归档间隔天数，保留最近 24 条） | `migrate_profiles_on_archive` 自动调用 |
| `record_relatedness_sample(db, book_id, score, same_cluster, archived_at)` | 归档时记录相关度行为样本（related_samples，保留最近 48 条；非簇样本参与阈值学习） | `migrate_profiles_on_archive` 自动调用；样本字段变化改此处 |
| `learn_thresholds(db)` | 自动学习：归档样本 ≥6 → 间隔中位数定 `review_days`、节奏快慢定 `warm_threshold`；`related_strength` 取**行为样本 25% 低分位与确认边 25% 低分位之较小者**（行为样本 ≥6 或确认边 ≥3，裁剪 45~60）；样本不足仅记录时间戳不改阈值 | 画像页「立即按样本学习」按钮 / 归档自动触发；学习规则改此处 |
| `learning_state(db)` | 学习状态视图（归档样本数/相关度样本数/上次学习时间/已学阈值/样本下限） | 随 `GET /api/profile/thresholds` 返回 |

- **持久化键**（`settings` 表）：`profile.warm_threshold` / `profile.related_strength` / `profile.review_days` / `profile.learning_state`（JSON）。
- **API**：`GET /api/profile/thresholds`（阈值+学习状态）、`PATCH /api/profile/thresholds`（手动覆盖）、`POST /api/profile/learn`（立即学习）。
- **前端**：`src/api/profile.ts` `getThresholds/saveThresholds/learnProfileThresholds`；`ProfileView.vue`「画像阈值」卡片（三个数字输入 + 保存 + 立即学习 + 学习状态说明，v1.59 展示相关度样本数/下限）；类型 `ProfileThresholds`（`src/types.ts`，learning 含 `related_sample_count/related_samples_min`）。


## 13. M10 打磨：笔记导出 + 性能优化 + 验收测试（第 13 轮任务产出）

### 13.1 笔记导出服务（`backend/app/services/note_export.py`，M10）

- **`build_notes_markdown(book, notes, chapters) -> str`**：生成全书笔记 Markdown（沿用 M3 格式：`# 书名 笔记导出` → 每条 `## [类型]（第X章 标题）` + 引文 `>` + 正文），供 `?fmt=md` 与默认导出使用。
- **`build_notes_pdf(book, notes, chapters) -> bytes`**：生成 A4 PDF。使用 PyMuPDF 内嵌 CJK 字体（`pymupdf.Font("cjk")` → Droid Sans Fallback），按宽度逐字换行（`_wrap`，兼容中文与长 LaTeX），Markdown 记号转纯文本、LaTeX 公式以源码保留；文件约 1.7MB（含内嵌字体）。
- **API**：`GET /api/books/{id}/notes/export?fmt=md|pdf`（`fmt` 缺省为 md，非法值 422）；`Content-Disposition: attachment; filename="notes.md" / "notes.pdf"`。
- **前端**：`src/api/reading.ts` `exportNotesUrl(bookId)`（md）与 `exportNotesPdfUrl(bookId)`（`?fmt=pdf`）；阅读页笔记抽屉 `drawer-actions` 两个下载链接。

### 13.2 性能优化（M10）

- **后端页图目标宽度缓存**：`parsers/pdf.py` `pdf_page_target_width` 委托 `_target_width_cached`（`functools.lru_cache(maxsize=4096)`，按 `(文件路径, 页号)` 缓存），避免每次页图请求重新打开 PDF；书籍导入后 PDF 不变，页图仅在分辨率不足时按需升级，缓存安全。
- **页图缓存头**：`GET /api/books/{id}/pages/{page_index}` 响应 `Cache-Control: private, max-age=600`（页图不可变，10 分钟内翻页走浏览器缓存，不再每次全量重传）。
- **前端相邻页预加载**：`ReaderView.vue` PDF 页模式渲染 `.page-preload` 隐藏 img（`position: absolute; left: -9999px`）预热当前页 ±1 页浏览器缓存，翻页更跟手。
- **vite vendor 分包**：`vite.config.ts` `manualChunks` 拆分 `vendor-vue / vendor-element / vendor-echarts / vendor-markdown`；echarts 仅图谱/脑图路由按需加载；ReaderView 主 chunk 189KB → 56KB。

### 13.3 验收测试（M10）

- **`tests/test_export_m10.py`**：MD 默认导出内容（含 [类型]/引文/LaTeX 源码）、PDF 导出（content-type、`%PDF-` 头、PyMuPDF 回读含中文与 LaTeX 源码）、`fmt` 非法值 422 / `markdown` 别名 200。
- **`tests/conftest.py` 测试隔离**：屏蔽真实 `.env` 的 `AI_API_KEY / AI_BASE_URL / AI_MODEL / AI_MODE / AI_THINKING_TYPE / AI_REASONING_EFFORT / VISION_*`，修复 9 项环境性失败（导入 PDF 的后台视觉预提取与测试 monkeypatch 竞争、未配置 API 类断言、思考模式默认参数）。后端全量 **128 通过**、ruff 全绿、vue-tsc + vite build 通过。
## 14. 变更记录


| 版本 | 日期 | 变更内容 | 说明 |
| --- | --- | --- | --- |
| v1.63 | 2026-08-03 | 重构轮：删除死代码 + 统一 LLM JSON 解析 | 删除 `services/graph_service.py`（889 行遗留死代码）；新增 `app/ai/parsing.py`（`parse_llm_json`）并替换 graph_sync/llm_score/rag_service/mindmap_service 四处重复解析（`test_assets` 改引新模块）；后端 135 项 pytest + ruff 全绿；关联文档升需求 v1.33、技术栈 v1.39、重构 v1.23 |
| v1.62 | 2026-08-03 | M8 完成：LLM 打分与方向/原因增强 | 新增 §10.1 `graph/llm_score.py`（`score_pair_llm`/`enrich_pairs_with_llm`/`apply_llm_result`，有界 `MAX_LLM_PAIRS=40`，提示词 `ai/prompts/graph_edge.py`）与 `compute_cross_book_graph`/`incremental_cross_book_graph` 的 LLM 增强说明；§10.2 谱系边字段补 `from_book`；§10.3 前端方向箭头按 `from_book`（`edgeEndpoints`/`edgeDirLabel`）展示 A→B；关联文档升需求 v1.32、技术栈 v1.38、重构 v1.22 |
| v1.61 | 2026-08-03 | M10 打磨：笔记导出（Markdown+PDF）+ 性能优化 + 验收测试 | 新增 §13：`services/note_export.py`（`build_notes_markdown`/`build_notes_pdf`，PyMuPDF 内嵌 CJK 生成 A4 PDF、LaTeX 源码保留）、`GET /api/books/{id}/notes/export?fmt=md|pdf`、前端 `exportNotesPdfUrl` + 笔记抽屉「导出 PDF」；性能优化（`parsers.pdf._target_width_cached` 页图目标宽度缓存、页图 `Cache-Control: private, max-age=600`、前端相邻页预加载、vite vendor 分包）；验收测试（`test_export_m10.py` 3 项 + conftest 隔离真实 .env 的 AI/视觉配置修复 9 项环境性失败，后端 128 通过、ruff 全绿、vue-tsc + vite build 通过）；关联文档升需求 v1.31、技术栈 v1.37、重构 v1.21 |
| v1.60 | 2026-08-02 | 书架 UI 打磨：拖拽换位 + 手动标签 + 搜索/筛选 | 新增 §3.13：`Book.position` 排序位（导入自动追加末尾、`POST /api/books/reorder` 批量重排、`_ensure_columns` 迁移）、`GET /api/books?q=` 搜索、HomeView 布局优化（sticky 工具栏/标签 chips/悬停操作）、拖拽换位（按 id 交换 + reorderBooks 持久化）、手动标签编辑（el-select allow-create）+ 标签筛选；新增 6 项 pytest（后端 125 通过、ruff 全绿、vue-tsc + vite build 通过）；关联文档升需求 v1.30、技术栈 v1.36、重构 v1.20 |
| v1.59 | 2026-08-02 | M9 第五轮：相关度阈值函数（决策 20 落地） | 新增 §11.6 `services/relatedness.py`（compute_relatedness/is_related_book，谱系主信号 + 暖/冷画像覆盖率辅助 + 同簇直判，signals 可解释）；`profile_service` 归档入暖记忆改走相关度函数（related_books 含 score）并记录行为样本；`profile_learning` 新增 `record_relatedness_sample`、`learn_thresholds` 取行为样本/确认边低分位 min 学习 related_strength、`learning_state` 返回 related_sample_count；画像页展示相关度样本数；修复相关度查询 NULL 反馈边被过滤缺陷；新增 8 项 pytest（后端 119 通过、ruff 全绿、vue-tsc + vite build 通过）；关联文档升需求 v1.29、技术栈 v1.35、重构 v1.19 |
| v1.58 | 2026-08-02 | M8 第 12 轮：导入增量更新 + 跨书知识检索 | §10.1 补 `incremental_cross_book_graph`/`knowledge_appears_in` 说明（`cross_book.py` 模块映射同步）；§10.2 补 `GET /api/graph/knowledge/{kp_id}/appears-in`；§10.3 书内图谱节点点击改「知识点详情」弹窗（跨书出现列表 + 跳转对应书图谱/阅读原文，`GraphView.vue` `openKpDetail`/`switchKpBook`）；`books.py upload_book` 导入成功后自动增量补边；新增 4 项 pytest（后端 111 通过、ruff 全绿、vue-tsc + vite build 通过）；关联文档升需求 v1.28、技术栈 v1.34、重构 v1.18 |
| v1.57 | 2026-08-02 | 图谱联动 RAG/Skill 沉淀 + 画像阈值自动学习 | 新增 §12：`services/graph_sync.py`（本地存根 linked_books/domain_terms、`POST /api/graph/sync` LLM 增量增改、`ai/prompts/rag_link.py` 提示词、幂等不 bump 版本）+ `services/profile_learning.py`（`settings` 表 `profile.*` 键、归档节奏样本、确认关联低分位学习、阈值 API）；`profile_service`/`recommendation_service` 统一走 `get_thresholds`；谱系图页「💾 联动沉淀」按钮 + 画像页「画像阈值」卡片；新增 10 项 pytest（后端 107 通过、ruff 全绿、vue-tsc + vite build 通过）；关联文档升需求 v1.27、技术栈 v1.33、重构 v1.17 |
| v1.56 | 2026-08-02 | 耦合审查与解耦重构（图谱包 + AI 上下文 + 涂鸦组合式函数） | §10.1 图谱服务改 `backend/app/services/graph/` 包（模块映射见小节）；AI 上下文拆分（`ai/factory.py`、`services/citations.py`、`repositories/assets.py`、`repositories/chat.py`）；新增 `repositories/books.book_tags` 消除 tag 清洗重复；chat 路由改用仓库；ReaderView 涂鸦逻辑抽 `useReaderDoodle`（813→741 行）；后端 97 项 pytest + ruff 全绿，前端 vue-tsc + vite build 通过；关联文档升需求 v1.26、技术栈 v1.32、重构 v1.16 |
| v1.55 | 2026-08-02 | 专业术语词库（用户可编辑） | `graph_service` 新增 `load_domain_lexicon`（读取词库文件，用户区+系统缓存区）、`cache_domain_term`（自动选定术语写入缓存区）、`_lexicon_hits`（中文子串/英文词边界匹配）；命名优先级：用户词库 → 自动候选（含缓存词库，覆盖书数优先）；词库文件位于 `backend/domain_terms.txt`（与 .env 同级）；新增 5 项 pytest（后端 97 通过、ruff 全绿）；真实库验证：自动缓存 阅读/空间/变分/概率；用户词库添加「概率论」后书 7 优先命名为「概率论」；关联文档升需求 v1.26、技术栈 v1.31 |
| v1.54 | 2026-08-02 | 领域专业术语命名 | `graph_service` 新增 `_GENERIC_DOMAIN_TERMS`（泛化/出版元词过滤）、`_clean_title_segment`（书名去作者/版本/出版社）、`_domain_candidates`（章节×3+书名×2+正文×1+后验×5 加权候选）、`_pick_domain_name`（覆盖书数优先→词频次之）与 `_first_meaningful_term`；`post_classify_book` 自动命名、`assign_clusters` 领域自动聚类、`merge_and_rename_clusters` 重命名统一走专业术语命名；新增 2 项 pytest（后端 92 通过、ruff 全绿）；真实库验证：领域名 阅读/空间/变分/概率（书 1 高效阅读术→「阅读」，不再出现「定理/茆诗」）；关联文档升需求 v1.25、技术栈 v1.30 |
| v1.53 | 2026-08-02 | 自动总结领域清洗 | `rag_service.generate_rag_skill` 对 LLM 返回的 `tags` 逐个 `sanitize_cluster_name` 后写入 skill `domains`（空项丢弃、去重）；`assign_clusters` 领域自动聚类去掉 `name#书id` 标点后缀（重名关键词书并入同一簇，领域名只含汉字/英文/数字）；新增 2 项 pytest（后端 90 通过、1 项既有失败）、ruff 全绿；真实冒烟：自动领域名（阅读/定理/变分/茆诗）无标点；关联文档升需求 v1.24、技术栈 v1.29 |
| v1.52 | 2026-08-02 | 聚类 tag 清洗（只保留汉字/英文词组） | `graph_service.sanitize_cluster_name`：去特殊标点、全角字母/数字转半角、中文间不留空格，保留汉字/英文字母/数字与单词空格；`books.py` 保存 tag 时清洗（空 tag 丢弃、去重）；`_write_classify`/`post_classify_book`/`assign_clusters`/`merge_and_rename_clusters` 写入簇名前统一清洗，并兜底清洗历史遗留脏簇名；新增 5 项 pytest（后端 88 通过、1 项既有失败）、ruff 全绿；真实冒烟：PATCH 脏 tag 落库清洗、图谱簇名无标点；关联文档升需求 v1.23、技术栈 v1.28 |
| v1.51 | 2026-08-02 | M9 第三轮：增量增改 + 簇合并/重命名 + 阅读建议基础版 | `rag_service.generate_rag_skill` 已有资产走增量提示词（`rag_skill.py` `INCREMENTAL_SYSTEM_PROMPT`/`build_incremental_user_prompt`，素材 `_collect_new_material`=笔记/划线/「不理解」+最近20条对话），原资产 version+1、id 不变；`graph_service.merge_and_rename_clusters`（T_MERGE=5 并集重叠合并进主簇 + 众数重命名，仅 post 书，归档后触发）并修复合并漏并入成员导致重命名不覆盖的 bug；新增 `recommendation_service.generate_recommendations` 与 `GET /api/profile/recommendations`（stats/weak_concepts/review/rhythm）；前端画像页新增「阅读建议」卡片（`getRecommendations` + `RecommendationsData`）；新增 4 项 pytest（后端 83 通过、1 项既有失败），ruff/vue-tsc/vitest 全绿；真实冒烟验证两次归档 version 1→2、weak_concepts 聚合；关联文档升需求 v1.22、技术栈 v1.27 |
| v1.50 | 2026-08-02 | M9 第二轮：三层画像实现 | 新增 `services/profile_service.py`（热/暖/冷三层画像读写、`update_hot_profile` 阅读行为回写、`migrate_profiles_on_archive` 归档迁移：跨1本热→暖/跨3本暖→冷/>3本全部沉淀冷、相关领域书入暖记忆=同 post 簇或关联边 ≥60）；`archive_book_task` 成功后触发画像迁移；`save_progress`/`create_note` 回写热画像；新增画像 API（GET /api/profile、POST /api/profile/reset）与前端画像页（ProfileView 三层卡片+重置）；新增 7 项 pytest；关联文档升需求 v1.21、技术栈 v1.26 |
| v1.49 | 2026-08-02 | M9 第一轮：读完归档链路 + post-classify 基础版 | 新增 `rag_service.archive_book_task`（归档后台任务：PDF 视觉通读补齐页缓存 → 缓存全文总结 RAG/Skill → 标记读完）与 `page_chunks`/`_build_page_input`；`generate_rag_skill` 支持 `page_texts` 参数并在成功后触发 post-classify；`graph_service.post_classify_book`（T_POST=3 一致性/迁移，落盘 cluster_name/classify_source/classified_at/classify_version）与 `assign_clusters` 采用未失效 post 结果；`_STOPWORDS` 扩充文档噪音词；资产 API 新增 `POST /books/{id}/archive`；前端 `rag.ts` 新增 `archiveBook`、阅读页「归档并总结 RAG/Skill」按钮；新增 5 项 pytest；关联文档升需求 v1.20、技术栈 v1.25 |
| v1.48 | 2026-08-02 | 同步需求补登记决策 27 | 需求文档 v1.19 补登记 9.1 决策 27（文本思考模式与精细参数），§6 配置章节所引决策现已有出处；技术栈 v1.24 同步；关联文档升需求 v1.19、技术栈 v1.24 |
| v1.47 | 2026-08-02 | 固化「读完归档 → 视觉通读全文 → 文本模型总结 RAG/Skill」 | 需求 v1.18 / 技术栈 v1.23 新增链路：PDF 书读完归档时先由多模态图像模型通读全书（`extract_book_pages_task` 全书提取、补全缺失页缓存、命中不重复调用），再以缓存全文为输入由文本模型总结 RAG 与 Skill（`generate_rag_skill`）；§9.3 与 §3.4 标注 M9 复用入口；实现待 M9 |
| v1.46 | 2026-08-02 | 固化两阶段聚类（pre/post-classify） | 聚类算法文档 `docs/知识图谱聚类算法.md` 新增 §9 两阶段分类设计：pre-classify（tag/文件夹/关键词共现粗略聚类，已实现）→ 获得 RAG/Skill 资产后 post-classify（后验特征抽取、簇内一致性校验、簇迁移/合并、代表性术语重命名，tag/文件夹硬约束优先，资产 version 变化失效重算，落盘 classify_source/classified_at/classify_version）；需求 §3.6.1 与技术栈 §5 同步；post-classify 待 M9 资产生成后接入 |
| v1.45 | 2026-08-02 | 新增聚类算法独立文档 | 新增 `docs/知识图谱聚类算法.md`：聚类分层流程（tag → 文件夹 → 领域自动聚类）、关键词抽取（CJK 二元组+英文词+停用词）、贪心成簇 + 相似吸收（加权词频和 ≥ 2）、与跨书关联补边关系、真实数据示例、参数修改点与局限/后续增强（LLM 命名、阈值函数）；使用手册 §10.1 `assign_clusters` 行补充链接 |
| v1.44 | 2026-08-02 | M8 知识图谱第一轮 | 新增 §10：图谱服务函数表（`graph_service`：关键词/关联/聚类/书内知识点）、图谱 API（全局谱系/书内图谱/重建/人工反馈）、前端 GraphView（ECharts 谱系图与书内图谱、聚类筛选、最密切高亮、层级筛选、关联详情反馈、导出 PNG）；后端新增 6 项 pytest；关联文档升需求 v1.16、技术栈 v1.21、重构 v1.15 |
| v1.43 | 2026-08-02 | `.env` API 真实调用验证 | 用 `backend/.env` 配置完成真实 API 测试：①多模态视觉（SiliconFlow `Qwen/Qwen3.5-35B-A3B`，chat 模式）直接调用成功，并经后端 `POST /books/{id}/page-text/{n}` 提取第 1 页落盘 `page_text/page_001.md`（命中后不再重复调用）；②文本模型（DeepSeek `deepseek-v4-flash`，responses 模式）直接调用与 `POST /books/{id}/chat` 按页问答成功（页缓存注入 + 引用【第X页】）。注意：后端进程若在受限网络环境（如防火墙/沙箱）启动，外呼会报 WinError 10013，需放行或改在可联网环境运行 |
| v1.42 | 2026-08-02 | 多模态读图对齐 SiliconFlow 多模态文档 | 核对并统一全部发图链路格式（api-docs.siliconflow.cn/docs/userguide/capabilities/multimodal-vision）：`POST /chat/completions` + `messages[].content` parts（文本+图片）；图片部件 `image_url` 参数 `url`（支持 base64 data URI）+ `detail`（auto/low/high）；页提取 `vision_extract` 已带 `detail=high`，补上对话链路 `chat_service.build_messages` 页图/划线裁剪图附件的 `detail=high` 并同步测试断言；前端裁剪图统一 `image/jpeg` data URI；§9.2 补请求格式说明；关联文档升技术栈 v1.20 |
| v1.41 | 2026-08-02 | 文本/多模态模型精细参数 | 按 DeepSeek 思考模式文档（api-docs.deepseek.com/guides/thinking_mode）新增 `.env` 精细参数并全链路传入：文本 AI（`AI_MAX_TOKENS/AI_THINKING_TYPE/AI_REASONING_EFFORT/AI_TOP_P/AI_FREQUENCY_PENALTY/AI_PRESENCE_PENALTY/AI_STOP`，chat 分支写入 `thinking/reasoning_effort/max_tokens/top_p/...`，responses 分支仅映射 `max_output_tokens`）与多模态视觉（`VISION_TEMPERATURE/VISION_TOP_P/VISION_FREQUENCY_PENALTY/VISION_PRESENCE_PENALTY/VISION_ENABLE_THINKING/VISION_THINKING_BUDGET`，SiliconFlow）；`config.py/LLMClient/设置仓储/路由/前端类型与设置页` 同步；新增 7 项后端测试；关联文档升技术栈 v1.19 |
| v1.40 | 2026-08-02 | AI/视觉配置迁移到 backend/.env | 正式库 `backend/.env` 补齐全部可配置参数（文本 AI + 多模态视觉，含 `VISION_MAX_TOKENS`），并把原设置页保存的 DB 覆盖项（AI Key/视觉 Key/模式/超时等）迁移进 `.env`、清除 DB `settings` 覆盖，使 `.env` 成为唯一配置源（改 `.env` 后重启后端生效）；已验证设置接口返回全部 `.env` 值、视觉客户端自动携带 SiliconFlow 参数（`mode=chat`、`max_tokens`、`detail=high`）；文档补配置优先级说明 |
| v1.39 | 2026-08-02 | 多模态接口对齐 SiliconFlow 文档 | 视觉客户端强制 `mode="chat"`（SiliconFlow 仅支持 `/chat/completions`）；`LLMClient` 新增 `max_tokens` 参数并在 chat 分支写入请求体；`vision_max_tokens` 全链路（config/.env.example/设置页/视图/测试）默认 4096；页图 `image_url` 带 `detail="high"`；`.env.example` 视觉默认指向 `https://api.siliconflow.cn/v1` + `Qwen/Qwen2.5-VL-72B-Instruct`；设置页视觉卡片补「生成上限」，并修复保存时遗漏 `vision_base_url / vision_model` 持久化的问题；新增 1 项 pytest（后端 55 项、前端 23 项全绿，ruff/vue-tsc 通过）；关联文档升技术栈 v1.17 |
| v1.38 | 2026-08-02 | M7 PDF 统一处理与多模态视觉提取 | PDF（含文本型）统一按页切章与渲染（`parse_pdf` 全部返回按页章节 + `local_text/` 本地抽取文本）；设置页新增多模态独立配置（`vision_*`，含测试视觉连接）；新增 `services/vision_extract.py`（逐页提取/`[P-1,P,P+1]` 滑动窗口增量缓存/整书重建后台任务）与 `api/routes/vision.py`（缓存状态/读取/重提本页/重建）；对话与脑图链路优先注入页缓存文本（出处「第 X 页」），缓存缺失回退页图附件；导入 PDF 后台批量预提取；阅读页页缓存入口；引用解析支持【第X页】；新增 9 项 pytest（共 54 项）；关联文档升 v1.15 / v1.16 / v1.14 |
| v1.37 | 2026-08-02 | 后端 AI 上下文解耦重构 | 新增 `app/services/ai_context.py`（客户端构建/页图附件/段落编号/引用解析/RAG 检索/Skill 读取/上下文组装/会话落库）；`chat_service` 只保留问答拼装与 SSE；mindmap/rag 服务改为共享模块，消除 `mindmap→chat` 横向耦合；页图路径统一 `media_service.page_image_path`；rag 总结改用运行时配置（设置页 API Key 生效）；45 项 pytest / ruff 全绿，接口冒烟通过；关联文档升 v1.15 / v1.13 |
| v1.36 | 2026-08-02 | 本页已读开关 + 整本标记一致性 | 顶栏新增「标记本页已读/✓ 本页已读」开关（仅 PDF 按页阅读，复用章节已读接口，当前页即时反馈）；整本「标记读完」改为后端一键 `PATCH /books/{id}/read-all` 联动全部章节已读（读完/100%），「标记为在读」反向清除全部已读（在读/0%）并加二次确认；修复此前整本标记只改 status/progress 导致书架进度与状态不一致的问题；后端新增 `set_all_chapters_read_flag` 与 1 项测试（后端 45 项、前端 23 项全绿） |
| v1.35 | 2026-08-02 | 阅读页顶部 UI 重排与去重 | 扫描 PDF 页模式合并重复的「第 X 页」与「第 X/XXX 章」为单个「第 X/XXX 页」指示；涂鸦工具栏移出主行、独占第二行全宽（压缩间距/按钮内边距后单行显示，顶部总高 161px→90px）；删除与左栏书架重复的进度文字（已读 x/y 章 · 进度 z%）及冗余的 pageZoomText「适配宽度」字样（fit 时改显「自适应」）；清理未使用的 readCount/chapterPercent |
| v1.34 | 2026-08-02 | 缩放 UI 移入顶部工具栏 | 适配宽度/原始大小/缩放按钮从阅读区内的独立 `.page-zoombar` 行移到顶部工具栏「第 X/XXX 章」右侧（仅 PDF 按页阅读显示），阅读区不再有第二行缩放条；前端生产构建通过 |
| v1.33 | 2026-08-02 | 阅读页书架只显示最近 5 本 | 阅读页左栏书架由全部书籍改为最近打开过的 5 本（按 `last_opened_at` 倒序，复用 store `recentBooks()`），当前打开的书籍恒在列表内避免刚打开尚未保存进度时消失；前端生产构建通过 |
| v1.32 | 2026-08-02 | 阅读区单一滚动条 | PDF 按页阅读时移除内部 .page-scroll 滚动条（改仅水平居中），只保留外层 .reading-scroll 一个滚动条；缩放条 sticky 跟随；新增 §8.5 说明；Vitest 23 项 / 后端 44 项全绿；前端生产构建通过 |
| v1.31 | 2026-08-02 | M6 修复轮：涂鸦渲染与工具栏重排 | 修复 PDF 页图涂鸦无法绘制/文本不渲染（旧数据 `{x,y}` 点兼容 + 画布尺寸同步仅变化时重置）；涂鸦工具栏移入阅读页顶部（第 X 页 与 第 X 章/共 XX 章 之间，仅 PDF 显示）；切页清空撤销栈、撤销可用态由 `can-undo` 事件驱动；新增 §8.4 修复说明；Vitest 23 项 / 后端 44 项全绿；关联文档升 v1.14 / v1.14 / v1.12 |
| v1.30 | 2026-08-02 | M6 实现完成 | §5.11 状态改「已实现」并补后端/前端实现细节与 chat `crop_image` 扩展；新增 §8 M6 章节（书签 + 涂鸦函数清单/修改指南/冒烟要点）；关联文档升 v1.13 / v1.13 / v1.11 |
| v1.29 | 2026-08-02 | 同步涂鸦划线批注与提问 | §5.11 涂鸦补划线选中菜单（加批注 `note`+`note_meta` / 就此划线提问），提问复用 §5.10 页缓存链路（页缓存文本 + 划线区域裁剪图，缓存缺失回退图片附件，出处「第 X 页」）；依赖说明改为划线提问受隐私开关约束；关联文档升 v1.12 / v1.12 / v1.10 |
| v1.28 | 2026-08-02 | 同步 M6 起里程碑重新规划 | 需求 §7 重排为 M6 阅读体验补全（书签+涂鸦）/ M7 PDF 统一处理与多模态提取 / M8 知识图谱 / M9 个性化与沉淀 / M10 打磨；本文档中里程碑引用同步（任务持久化 M7、笔记 PDF 导出 M10、RAG/Skill 沉淀 M9）；关联文档升 v1.11 / v1.11 / v1.9 |
| v1.27 | 2026-08-02 | 同步书签组织与涂鸦功能定稿 | §5.11 书签列表改「默认时间倒序平铺 + 分组/文件夹归类」（`group_name`/`created_at`），涂鸦补撤销（最多 5 步会话内）/擦除/文本工具与 `stroke`+`text` 元素存储；关联文档升 v1.10 / v1.10 / v1.8 |
| v1.26 | 2026-08-02 | 同步 PDF 统一按页处理 | §5.10 扩展为全部 PDF（含文本型，避免数学乱码）；§5.11 涂鸦范围改「PDF 按页阅读」；关联文档升 v1.9 / v1.9 / v1.7 |
| v1.25 | 2026-08-02 | 同步书签/涂鸦与提取触发细化 | §5.10 触发时机更新为「当前页提问触发 + [P-1,P,P+1] 滑动窗口增量缓存」（决策 22）；新增 §5.11（规划中）位置书签（全格式）与扫描件涂鸦画板设计；关联文档升 v1.8 / v1.8 / v1.6 |
| v1.24 | 2026-08-02 | 开放项定稿同步 | 需求 9.1 新增决策 17~23（结束对话优先主动归档 / Skill 自动匹配 / 版本≥3 整条目回滚 / 相关领域按谱系关联强度 / 文件夹多级嵌套批量操作 / 多模态导入批量+首读前后页 / 独立配置无需额度管理）；§5.10 设计同步触发时机与配置约束；关联文档升 v1.7 |
| v1.23 | 2026-08-02 | 固化扫描件多模态视觉提取设计 | 新增 §5.10（规划中）：扫描版页图经多模态 LLM 提取完整信息、按页缓存到书籍目录、命中不重复调用，文本大模型基于缓存解读；关联文档同步为需求 v1.6 / 技术栈 v1.6 / 重构 v1.3 |
| v1.22 | 2026-08-02 | 扫描版页图高清化 + 阅读缩放 | 页图按内嵌原图分辨率渲染（`parsers/pdf.py` `_auto_page_zoom`，2.5x 兜底）；页图端点缺文件或低清时按需重渲染并 `no-cache`；阅读页新增「适配宽度/原始大小/＋/－」缩放条（ReaderView `pageZoom`+`pageNaturalWidth`）；新导入扫描书即高清 |
| v1.21 | 2026-08-02 | 脑图渲染修复 + 下载/插入批注 | 新增 `src/utils/text.ts` `toPlainDisplayText`（脑图节点名去 LaTeX/Markdown 记号，保留可读内文）；MindMapPanel 节点渲染清理；工具栏新增「下载大纲」（.md 下载）与「插入为批注」（写入当前章节批注，复用 `useReaderNotes.addNote` 逻辑）；笔记抽屉正文改 `MdRender` 渲染（Markdown/LaTeX 大纲可读）；Vitest 17 项 |
| v1.20 | 2026-08-02 | 修复 LLM 输出公式不渲染 | LLM 输出用裸 `( ... )` / `[ ... ]` 写公式导致 KaTeX 不识别；新增 `src/utils/math.ts` 定界符归一化（裸括号含命令→`$...$`，独占一行裸方括号→`$$...$$`，兼容 `\(...\)`/`\[...\]`），MdRender 渲染前归一化并补齐转义定界符保护；提示词要求模型输出标准 `$...$`/`$$...$$`；新增 math 测试 8 项 |
| v1.19 | 2026-08-02 | 阅读页解耦重构 | 书架列表 N+1 批量聚合（books_reading_summary + selectinload，pytest 39 项）；抽 utils/progress.ts、utils/content.ts 纯函数；ReaderView 1054→427 行（useReaderSelection/Notes/Progress/Mindmap/Ai 5 个组合式函数 + ReaderLeftPanel/ReaderChatPanel 2 个子组件）；Vitest 基线 5 项 |
| v1.18 | 2026-08-02 | M5 AI 增强：脑图图像组件 + 划词 AI 操作 | 新增 POST /api/books/{id}/mindmap（LLM 结构化 JSON → ECharts 树，Markdown 回退解析，兼容隐私开关/扫描版页图/RAG/Skill，结果入对话历史）；前端 MindMapPanel ECharts 树（大纲/细节/重要定理三层着色、出处 tooltip、点击跳转原文、复制大纲、导出 PNG）；脑图预设改弹窗；划词菜单新增解释选中段/该段脑图/加入思考清单；聊天消息复制按钮；新增 7 项后端测试 |
| v1.17 | 2026-08-02 | 修复封面串书 + 书架删除书籍 | 根因：封面/页图原写入共享 cover.jpg 与 pages/，后导入书覆盖前书封面；改为每本书独立子目录，启动自动迁移旧布局并回填缺失封面（media_service）；封面/页图端点缺文件时按需渲染；书架卡片新增 🗑 删除（确认框），后端级联清理章节/笔记/进度/资产/图谱/聊天记录与本地文件，并修复删除有进度/笔记的书时的外键冲突；新增 4 项后端测试 |
| v1.16 | 2026-08-02 | PDF 封面 / 扫描版按页阅读 / LLM 页图附件 | PDF/EPUB 导入提取封面并在书架渲染；PDF 解析器改 PyMuPDF，文本 <30 字符判为扫描版并按页切章；新增封面/页图 API；设置页「发送页面图片」开关，扫描书提问自动附带当前页图片（chat 模式、受隐私开关约束）；旧库 ALTER TABLE 迁移；mock LLM 兼容多模态 parts；新增 4 项后端测试 |
| v1.15 | 2026-08-02 | 修复正文数学渲染 | markdown-it 把内联数学 `$V^*$` / `$X^*=\{0\}$` 中的 `*` 解析为 `<em>` 强调，导致 KaTeX 无法识别；MdRender 新增 protectMath：渲染前把 `$...$` / `$$...$$` 整体替换为占位符，markdown-it 渲染后再还原（HTML 转义）交给 KaTeX auto-render；修复后 4.1 章 18 个公式全部渲染，正文/目录/块级公式无回归 |
| v1.14 | 2026-08-02 | 修复真实 API 连接报错 + 错误提示优化 | 排查确认 WinError 10013 为运行环境出站 TCP 被拦截（沙箱/防火墙/代理）导致，非代码缺陷；后端改为在普通环境启动后真实 DeepSeek 连接与流式对话验证通过；LLMClient 对 10013 增加中文提示（含排查建议），README 补充排查说明 |
| v1.13 | 2026-08-02 | M4 AI 接入 | 新增设置 API（查看/保存/测试连接）、对话 SSE 流式（章节上下文 + RAG/Skill 注入 + 引用出处解析）、对话历史 API 与前端设置页/AI 助手面板；修复 build_client 的 ai_* 键名映射（提取 client_kwargs 统一转换） |
| v1.12 | 2026-08-02 | 章节已读开关 + 书架三段式进度 | 目录行新增已读开关（PATCH chapters/{id}/read）；书籍状态同步抽共享 _refresh_status（全部读完→读完/部分→在读/全未读→未读）；书架卡片改为上（已阅读x/y章）/中（最新章节）/下（进度条）三段式，进度按已读章节/总章节计算 |
| v1.11 | 2026-08-02 | 左栏书架/目录独立滚动 | 书架固定在左上角、目录占左下剩余空间，各自独立滚动条（ReaderView 左栏改为上下两个 overflow 区域） |
| v1.10 | 2026-08-02 | 章节标题数学公式渲染 | MdRender 新增 inline 模式（md.renderInline + span 输出）并应用到目录/工具栏/笔记定位行；标题中 `$...$` 公式与 markdown 转义可正常渲染；env.d.ts 补 renderInline 类型 |
| v1.9 | 2026-08-01 | 读完判定 + 书架进度重构 + 窗口还原修复 | 章节读完自动判定（≥10s 且滚动到底，mark_read 显式标记，全部读完自动读完）与工具栏手动切换「标记读完/标记为在读」；书架卡片改为「已阅读x/y章 · 最新章节」+ 底部进度条（后端新增 read_chapters/latest_chapter 字段）；视口高度改百分比链 + 100dvh，修复最小化/还原后页面显示不全 |
| v1.8 | 2026-08-01 | 正文精确高亮 + 书架进度修复 | ReaderView 新增 wrapQuoteInElement/applyHighlights（划词精确高亮、整段与精确嵌套、增删改幂等）；HomeView 修复近期阅读进度漏乘 100、书架卡片补进度条与百分比 |
| v1.7 | 2026-08-01 | 阅读闭环 M3 | 新增章节正文/进度保存恢复/已读标记、笔记 CRUD 与 Markdown 导出、阅读页三栏交互（hover/划词/抽屉）、MdRender Markdown+LaTeX 渲染组件（修复块级公式渲染）、env.d.ts 类型声明；API 层抽公共 `require_book` 依赖（`app/api/deps.py`）消除三处重复 |
| v1.6 | 2026-08-01 | start.bat 兼容无 pnpm 环境 | 前端启动按 pnpm → node vite.js → 本地 pnpm → 运行时 pnpm 顺序解析，修复系统 PATH 无 pnpm 时的启动失败 |
| v1.5 | 2026-08-01 | 解耦重构 + start.bat 修复 | 解析器抽公共切章助手、统一实体序列化（修复 tags 契约）、API 层瘦身、RAG 服务消除重复切块；start.bat 换行符改为 CRLF 并固定前端 --host 127.0.0.1 |
| v1.4 | 2026-08-01 | 外部资料 RAG/Skill + 一键启动 | 任务模块结果存储、资产仓储、RAG 服务与提示词、资产 API、前端资料页（/rag）、`start.bat` 一键启动 |
| v1.3 | 2026-08-01 | 阅读页三栏布局与左侧书架 | 三栏改为 25%/45%/30%（border-box），左侧新增竖向书架并支持点击切换书籍；新增根目录 `README.md` 启动说明 |
| v1.2 | 2026-08-01 | 前端构建验证通过 | 补充前端目录结构、构建/运行命令与 pnpm 11 配置说明；书架主页与三栏阅读页冒烟测试通过 |
| v1.1 | 2026-08-01 | 新增 M2 后端与前端骨架 | 收录解析器、导入服务、repositories、AI 客户端、任务模块、API 端点与前端结构 |
| v1.0 | 2026-08-01 | 初稿 | 收录第 1 轮任务产出：chat_demo.py 与 _mock_llm.py 的使用与修改指南 |
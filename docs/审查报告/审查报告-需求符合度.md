# 需求审查与待办

> **审查日期**：2026-08-05
> **审查方式**：只读代码审查 + 需求文档逐条核对，未修改任何代码
> **数据来源**：
> - 响应速度优化审查报告（Sam）
> - 需求符合度审查报告（Jack）
> - 未完成需求与后续拓展报告（Eric）

---

## 一、总览

### 1.1 整体符合度

项目整体需求符合度约 **94.2%**。138 条需求项中，130 条已完全满足，4 条部分满足，4 条未满足。M1~M10 里程碑文档标记全部 ✅，但 §8.1 审查遗留仍有 3 项未解决（Markdown 内嵌图片、EPUB 图文混排、画像内容编辑）。

### 1.2 需求符合度统计

| 类别 | ✅ 已满足 | ⚠️ 部分满足 | ❌ 未满足 | 总计 |
|------|----------|------------|----------|------|
| 功能需求（3.1~3.6） | 48 | 0 | 1（MD 图片） | 49 |
| 决策（1~35） | 32 | 1（#19 版本回滚） | 2（#15 EPUB, #31 MD 图片） | 35 |
| 验收标准（1~25） | 22 | 1（#5 画像编辑） | 1（#13 EPUB） | 25 |
| 里程碑（M1~M10+） | 11 | 0 | 0 | 11 |
| 非功能需求 | 14 | 1（重试/限流） | 0 | 15 |
| 使用手册一致性 | 3 | 1（状态标注过期） | 0 | 4 |
| **合计** | **130** | **4** | **4** | **138** |

**符合率**：约 94.2%（已满足），2.9% 部分满足，2.9% 未满足

### 1.3 优化点优先级分布

| 优先级 | 后端优化点 | 前端优化点 | 合计 |
|--------|-----------|-----------|------|
| 高 | 3（#1 RAG N+1、#2 笔记 N+1、#9 批量接口） | 2（#23 资料页 N 请求、#28 MdRender 高频） | 5 |
| 中 | 11（#3~#8, #11~#12, #14~#15, #17~#18, #20） | 4（#25~#26, #29, #32） | 15 |
| 低 | 8（#9~#10, #13, #16, #19, #21~#22, #24 前端） | 4（#24, #27, #30~#31） | 12 |
| **合计** | **22** | **10** | **32** |

### 1.4 待办优先级分布

| 优先级 | 数量 | 代表项 |
|--------|------|--------|
| 高 | 4 | EPUB 图文混排、MD 内嵌图片、画像编辑、ReaderView 拆分 |
| 中 | 6 | graph_sync 并发、FTS5 前端入口、聚类算法决策、§11 失真修正、使用手册状态更新、RAG 版本回滚 |
| 低 | 8 | 本地向量检索、DeepSeek responses 参数、任务分级模型、间隔重复算法、聊天摘要压缩等 |
| **合计** | **18** | — |

---

## 二、响应速度优化点清单

> 来源：Sam 响应速度优化审查报告。已登记的优化项（../性能优化路径.md 中记录的）基本均已落地，以下为**尚未登记的新发现优化点**。

### 2.1 后端优化点（22 项）

#### 2.1.1 N+1 查询与循环内数据库调用

| # | 优化点 | 位置 | 当前问题 | 优化手法 | 预期收益 | 实施难度 |
|---|--------|------|----------|----------|----------|----------|
| 1 | **RAG 候选目录构建 N+1** | `services/rag_router.py:78-98` `build_catalog()` | 遍历全部书籍，每本书调用 `_has_assets()`（2 次查询）+ `read_asset_content()` 2 次 + `_book_domain()`，N 本书产生约 5N-6N 次查询 | 一次性批量加载全部有资产书籍的 `BookAsset` 行，在内存中构建目录；`Folder` 用 `selectinload` 或一次性 `dict` 缓存 | 高 | 低 |
| 2 | **跨书关联笔记加权 N+1** | `services/graph/cross_book.py:22-33` `_note_weight()` | 每对书籍组合（O(N²)）各自查询两本书的全部笔记，N=50 时约 5000 次查询 | 在入口一次性加载全部笔记按 `book_id` 分组到内存 dict，`_note_weight` 直接查 dict | 高 | 低 |
| 3 | **`knowledge_appears_in` 全表扫描** | `services/graph/cross_book.py:196-218` | 遍历全部 KP（排除当前书），对每条调用 `extract_keywords()`；再遍历全部 Book 读 RAG 资产再提取关键词 | 预先加载并缓存 KP 关键词；RAG key_points 批量读取后在内存匹配 | 中 | 中 |
| 4 | **`_find_shared_asset` 全表扫描** | `repositories/assets.py:86-88` | 每次查找共享资产时全量加载后 Python 循环匹配 `merged_book_ids` | 改为 SQL 查询或新增 `merged_book_ids` 关联表 | 中 | 中 |
| 5 | **`_selection_payload` 循环查询** | `services/rag_router.py:127-155` | 对每个选中 book_id 调用 `db.get(Book)` + `retrieve_rag_chunks()` | 批量 `Book.id.in_(ids)` 一次查询；资产内容批量预加载 | 中 | 低 |
| 6 | **`get_all_profiles` 三次独立查询** | `services/profile_service.py:76-77` | 每次调用发 3 条 SELECT（hot/warm/cold 各一条），该函数被多处频繁调用 | 合并为一条 `WHERE layer IN (...)` 查询，内存组装 dict | 中 | 低 |
| 7 | **`generate_recommendations` 6 次独立 COUNT** | `services/recommendation_service.py:28-35` | 6 个独立 COUNT 查询 + `_weak_concepts()` 全量加载 | 合并为条件聚合查询或物化统计缓存 | 低 | 低 |
| 8 | **`book_to_dict` 触发 `len(book.chapters)`** | `schemas/serializers.py:28` | 若 book 未用 `selectinload` 预载 chapters，此处触发隐式懒加载 | 使用 `book.total_chapters` 字段替代 `len(book.chapters)` | 低 | 低 |

#### 2.1.2 缺批量接口

| # | 优化点 | 位置 | 当前问题 | 优化手法 | 预期收益 | 实施难度 |
|---|--------|------|----------|----------|----------|----------|
| 9 | **资产批量查询接口缺失** | `api/routes/assets.py` | 前端资料页对 N 本书发 N 个独立请求；后端无批量端点 | 新增 `GET /api/assets/batch?book_ids=1,2,3` | 高 | 低 |
| 10 | **`book_reading_summary` 单本版本仍被使用** | `api/routes/books.py:43` `_book_out()` | 单书详情路径仍逐本查章节+日志，而批量版已存在 | `_book_out` 改为复用批量版或缓存结果 | 低 | 低 |

#### 2.1.3 同步阻塞点

| # | 优化点 | 位置 | 当前问题 | 优化手法 | 预期收益 | 实施难度 |
|---|--------|------|----------|----------|----------|----------|
| 11 | **聊天流式端点为同步 `def`** | `api/routes/chat.py:43` | SSE 流式响应使用同步函数，LLM 流式调用阻塞线程池工作线程 | 改为 `async def` + `run_in_executor`，或使用 `httpx.AsyncClient` | 中 | 中 |
| 12 | **全部 IO 密集路由均为同步 `def`** | `api/routes/` 全部 17 个路由文件 | 所有端点（除 `upload_book` 外）均为 `def`，高并发时线程池耗尽 | 对含 DB + 文件 IO 的端点逐步改 `async def`（优先改高频端点） | 中 | 中 |
| 13 | **`settings.py` 连接测试为同步 HTTP** | `api/routes/settings.py:108` | AI 设置连接测试在路由线程内直接发 HTTP 请求 | 已有 `run_in_executor` 包装（需核实），否则改为异步 | 低 | 低 |

#### 2.1.4 重复计算

| # | 优化点 | 位置 | 当前问题 | 优化手法 | 预期收益 | 实施难度 |
|---|--------|------|----------|----------|----------|----------|
| 14 | **`extract_keywords` 在 `knowledge_appears_in` 中重复调用** | `services/graph/cross_book.py:182-218` | 同一 KP 的标题+摘要被提取关键词后无缓存复用 | 利用已有的 `book_keywords()` 缓存或为 KP 级别增加关键词缓存 | 中 | 低 |
| 15 | **`_note_weight` 中 `_note_keywords` 重复提取** | `services/graph/cross_book.py:19-20` | 每对书比较时对其笔记重新调用 `extract_keywords` | 预计算全部笔记关键词并缓存（按 note.id） | 中 | 低 |
| 16 | **`get_all_profiles` 同一请求内多次调用** | `services/rag_router.py:101,237` + `services/chat_service.py` | 一次聊天请求中可能多次调用 `get_all_profiles` | 在请求入口取一次，通过参数传递 | 低 | 低 |

#### 2.1.5 大对象传输

| # | 优化点 | 位置 | 当前问题 | 优化手法 | 预期收益 | 实施难度 |
|---|--------|------|----------|----------|----------|----------|
| 17 | **`chapter_content_to_dict` 返回完整正文** | `schemas/serializers.py:60-69` | 章节正文（可达数百 KB）每次请求完整传输 | 支持 `If-None-Match` / ETag（按 content_hash + chapter_id） | 中 | 中 |
| 18 | **`asset_to_dict` 返回完整 `content_json`** | `schemas/serializers.py:51-57` | 资产内容可能很大，资料页列表只需摘要 | 列表接口只返回 `{version, summary_preview}`，详情页再拉完整内容 | 中 | 低 |
| 19 | **`list_books` 无分页** | `api/routes/books.py:47-52` | 书架全部书籍一次返回，数百本时 JSON 体积可观 | 增加分页或 `limit/offset` 参数；或至少移除不必要的 `chapter_count` | 低 | 低 |

#### 2.1.6 索引与数据库

| # | 优化点 | 位置 | 当前问题 | 优化手法 | 预期收益 | 实施难度 |
|---|--------|------|----------|----------|----------|----------|
| 20 | **`reading_logs.updated_at` 缺索引** | `core/database.py:56-80` `_INDEX_DDL` | `get_latest_log` 按 `book_id + updated_at DESC` 排序，无复合索引 | 添加 `CREATE INDEX ix_reading_logs_book_updated ON reading_logs(book_id, updated_at DESC)` | 中 | 低 |
| 21 | **`user_profiles` 表缺 `(layer, dimension)` 复合索引** | `services/profile_service.py:39,49` | `get_all_profiles` 按 `layer + dimension` 查询，无显式索引 | 在 `_INDEX_DDL` 中补充 | 低 | 低 |
| 22 | **`book_assets.content_json` 无全文索引** | `repositories/assets.py:86` | 共享资产查找需扫描 `content_json` 内的 `merged_book_ids` | 可考虑新增 `merged_book_ids` 规范化关联表 | 低 | 中 |

### 2.2 前端优化点（10 项）

| # | 优化点 | 位置 | 当前问题 | 优化手法 | 预期收益 | 实施难度 |
|---|--------|------|----------|----------|----------|----------|
| 23 | **资料页 N 个独立资产请求** | `views/RagView.vue:29` | `books.map(b => getBookAsset(b.id))` — N 本书发 N 个请求 | 后端提供批量接口后，前端改为单次请求 | 高 | 低 |
| 24 | **`pollTask` 重复实现** | `views/RagView.vue:92-100` + `views/RagDetailView.vue:48-56` | 两处完全相同的 `pollTask` 函数（180 次轮询 × 1 秒间隔） | 抽取为 `composable`（如 `useTaskPoll`）或 `utils/task.ts` 通用函数 | 低 | 低 |
| 25 | **AI 对话流式期间双重轮询** | `composables/useReaderAi.ts:220-222` | SSE 流式期间每 2 秒轮询聊天历史，与 SSE 主通道并行 | SSE 连接稳定时可降低轮询频率或仅在 SSE 静默超过阈值时启用 | 低 | 低 |
| 26 | **Pinia store 利用率低** | `stores/book.ts` 仅 24 行 | `useBookStore` 仅 `HomeView` 和 `ReaderView` 使用；其他页面各自独立请求 | 统一通过 store 获取书籍列表，避免多页面重复 `listBooks()` | 中 | 低 |
| 27 | **资料页长列表无虚拟滚动** | `views/RagView.vue:257` | 每本书渲染完整 DOM（含 MdRender 组件），12+ 本书时大量 DOM 节点 | 若数据量增长可引入虚拟滚动 | 低 | 中 |
| 28 | **MdRender 组件高频创建** | `views/RagView.vue:271` + `RagDetailView.vue` 多处 | 每个知识点、chunk、skill 都创建独立 MdRender 实例（含 markdown-it + DOMPurify + KaTeX） | 对列表项使用 `v-once` 或延迟渲染（`IntersectionObserver`） | 中 | 中 |
| 29 | **`mathjax-full` 动态导入但体积大** | `utils/graphLabel.ts:48-53` | `mathjax-full` 约 500KB+；仅谱系图标签渲染使用 | 评估是否可用 KaTeX 替代（已有 KaTeX 依赖），移除 mathjax-full | 低 | 中 |
| 30 | **axios 超时 120 秒过长** | `api/client.ts:4` | 请求挂起 2 分钟才超时，用户体验差 | 对非 SSE 请求缩短超时（如 30 秒），SSE 单独配置 | 低 | 低 |
| 31 | **阅读页预加载前后页图片** | `views/ReaderView.vue:471-472` | 扫描版 PDF 阅读时 `loading="eager"` 预加载前后页图片 | 改为 `loading="lazy"` 或仅在翻页时按需加载 | 低 | 低 |
| 32 | **HTTP 缓存策略几乎缺失** | 后端仅 `books.py:164` 页图设了 `Cache-Control` | 书籍封面、静态资源等无缓存头 | 为封面、静态文件添加 `Cache-Control`；API 响应添加 `ETag` | 中 | 低 |

### 2.3 Top 10 优先优化项

| 排名 | 优化点 | 位置 | 预期收益 | 实施难度 | 理由 |
|------|--------|------|----------|----------|------|
| **1** | **RAG 候选目录构建 N+1 → 批量加载** | `services/rag_router.py:78-98` | 高 | 低 | 每次 RAG 聊天触发 5N-6N 次 DB 查询；改为 1-2 次批量查询，响应时间从秒级降至毫秒级 |
| **2** | **资产批量查询接口 + 前端合并请求** | 后端 `api/routes/assets.py` + 前端 `RagView.vue:29` | 高 | 低 | 资料页 N 个 HTTP 请求 → 1 个批量请求；后端只需新增一个端点 |
| **3** | **跨书关联计算笔记预加载** | `services/graph/cross_book.py:22-33,54-108` | 高 | 低 | O(N²) 对书籍，每对 2 次笔记查询 → 一次预加载，重建时间可缩短一个数量级 |
| **4** | **`get_all_profiles` 合并为单条查询** | `services/profile_service.py:38-77` | 中 | 低 | 被多处频繁调用，每次 3 条 SELECT → 1 条，减少 2/3 查询开销 |
| **5** | **`_find_shared_asset` 全表扫描优化** | `repositories/assets.py:84-89` | 中 | 中 | 全表扫描后 Python 循环匹配；改为 SQL 条件查询或新增关联表 |
| **6** | **`reading_logs` 补充复合索引** | `core/database.py:56-80` | 中 | 低 | 缺复合索引时随日志增长退化为全表排序；一行 DDL 即可修复 |
| **7** | **章节正文 ETag / 条件请求** | `api/routes/reading.py` + `schemas/serializers.py:60-69` | 中 | 中 | 章节正文（可达数百 KB）每次完整传输；添加 ETag 后浏览器缓存命中可完全避免传输 |
| **8** | **聊天流式端点异步化** | `api/routes/chat.py:43` | 中 | 中 | SSE 流式端点为同步 `def`，LLM 调用阻塞线程池；改为 `async def` + `run_in_executor` |
| **9** | **MdRender 延迟渲染（IntersectionObserver）** | `RagView.vue:257-281` + `RagDetailView.vue` | 中 | 中 | 大量 MdRender 实例同时渲染，进入视口时再渲染可减少首屏 DOM 开销 |
| **10** | **HTTP 缓存策略补齐** | 后端全部 GET 端点 | 中 | 低 | 仅页图有 `Cache-Control`；封面、书籍列表等稳定数据应添加缓存头 |

---

## 三、需求符合度审查

> 来源：Jack 需求符合度审查报告。审查范围：全量代码 vs 需求文档体系（v1.66）。后端测试 196 项 pytest 全绿。

### 3.1 功能需求

#### 3.1.1 主页（书架首页）

| 条目 | 状态 | 证据 |
|------|------|------|
| 左右两栏（近期阅读+书架） | ✅ | `frontend/src/views/HomeView.vue` |
| 谱系图视图切换 | ✅ | `HomeView.vue` 顶部视图切换 + `GraphView.vue` |
| 近期阅读按时间倒序 | ✅ | `HomeView.vue` 近期阅读区域 |
| 搜索/标签筛选 (v1.30) | ✅ | `GET /api/books?q=` + 前端标签 chips |
| 文件夹归类+多级嵌套 | ✅ | `api/routes/folders.py` + `HomeView.vue` 拖拽 |
| 书籍拖拽换位 (v1.30) | ✅ | `POST /api/books/reorder` + `Book.position` |
| 手动标签编辑 | ✅ | `HomeView.vue` el-select allow-create |
| 导入新书（PDF/MD/TXT/EPUB/拖拽） | ✅ | `upload_book` 路由 + 前端上传组件 |
| 删除书籍（级联清理） | ✅ | `DELETE /api/books/{id}` + `books_service.delete_book` |
| 查看本书知识图谱入口 | ✅ | `GraphView.vue` 书内图谱 |

#### 3.1.2 书籍导入与解析

| 条目 | 状态 | 证据 |
|------|------|------|
| 支持 PDF/MD/TXT/EPUB | ✅ | `parsers/` 目录含 pdf.py/text.py/epub.py |
| 两段式导入（上传秒回+后台） | ✅ | `import_service.py:64-139` 同步入架 + 后台任务 |
| 进度上报 (progress/stage) | ✅ | `import_service.py:154-171` 多阶段 `update_progress` |
| 流式写盘（1MB 分块） | ✅ | `books.py:80` `while chunk := await file.read(1024*1024)` |
| 新书自动增量更新图谱 | ✅ | `import_service.py:165` `incremental_cross_book_graph` |
| **Markdown 内嵌本地图片（决策 31）** | ❌ | 导入未扫描 `![](src)` 复制图片到 `images/`；无媒体端点。需求标注"v1.45 定稿，待实现"，代码确认未实现 |

#### 3.1.3 阅读视图（三栏工作台）

| 条目 | 状态 | 证据 |
|------|------|------|
| 三栏布局 | ✅ | `ReaderView.vue`（左栏/阅读区/AI 面板） |
| 章节切换+进度记录 | ✅ | `useReaderProgress.ts` + `reading.py` 路由 |
| 笔记（高亮/批注/思考/不理解） | ✅ | `useReaderNotes.ts` + `notes.py` 路由 |
| 不理解双入口 | ✅ | 划词菜单 + 阅读区侧边按钮 |
| 笔记导出 Markdown+PDF | ✅ | `services/note_export.py` |
| 位置书签（全格式+分组） | ✅ | `BookmarkDrawer.vue` + `bookmarks.py` 路由 |
| 页图涂鸦（画板/撤销/擦除/文本） | ✅ | `PageDoodleCanvas.vue` + `DoodleToolbar.vue` |
| 划线批注+划线提问 | ✅ | `useReaderDoodle.ts` + `PageDoodleCanvas.vue` |
| 划词 AI 操作 | ✅ | `useReaderSelection.ts` + `ReaderChatPanel.vue` |

#### 3.1.4 AI 助手能力

| 条目 | 状态 | 证据 |
|------|------|------|
| 三层画像（冷/暖/热） | ✅ | `services/profile_service.py` |
| 画像归档迁移 | ✅ | `profile_service.migrate_profiles_on_archive` |
| 相关度阈值函数（决策 20） | ✅ | `services/relatedness.py` |
| 阈值自动学习 | ✅ | `services/profile_learning.py` |
| 画像注入对话（隐私开关控制） | ✅ | `chat_service.py:157` + `ai/prompts/chat.py` |
| 解读 | ✅ | `chat_service.py` mode="interpret" |
| 概论 | ✅ | `chat_service.py` mode="overview" |
| 脑图（ECharts 三层） | ✅ | `MindMapPanel.vue` + `mindmap_service.py` |
| 脑图下载大纲/插入批注 | ✅ | `MindMapPanel.vue` 工具栏 |
| 思考逻辑 | ✅ | `chat_service.py` mode="thinking" |
| 阅读建议 | ✅ | `services/recommendation_service.py` |
| 跨书谱系+关联强度+笔记加权 | ✅ | `services/graph/` 包 |
| 图谱联动 RAG/Skill 沉淀 | ✅ | `services/graph_sync.py` |
| 引用出处 | ✅ | `services/citations.py` |
| RAG/Skill 归档+增量增改 | ✅ | `services/rag_service.py` + `rag_input.py` |
| 外部资料直投 | ✅ | `RagView.vue` + `assets.py` 路由 |
| 资产两级结构+折叠展示 | ✅ | `RagView.vue` + `RagDetailView.vue` |
| 资产去重合并 | ✅ | `repositories/assets.py` `merge_duplicate_assets` |
| 资产删除（整条/单条） | ✅ | `DELETE /api/books/{id}/asset` |
| Markdown/LaTeX 渲染 | ✅ | `MdRender.vue`（KaTeX + DOMPurify） |
| 数学硬规则（决策 29） | ✅ | `ai/prompts/chat.py` `MATH_RULE` |
| 多模态视觉提取+页缓存 | ✅ | `services/vision_extract.py` + `api/routes/vision.py` |
| 滑动窗口 [P-1,P,P+1] | ✅ | `vision_extract.py` `ensure_window_caches` |
| AI 三接口格式 | ✅ | `ai/client.py` responses/chat/anthropic |
| base_url 双写法 | ✅ | `ai/client.py` `resolve_endpoint` |
| 浏览器 UA | ✅ | `ai/client.py` `_headers` |
| 会话分池+历史注入 | ✅ | `repositories/chat.py` |
| LLM 自主挑选 RAG（决策 34） | ✅ | `services/rag_router.py` |

#### 3.1.5 数据与记录

| 条目 | 状态 | 证据 |
|------|------|------|
| 阅读进度三级记录 | ✅ | `repositories/reading.py` |
| 笔记持久化 | ✅ | `repositories/notes.py` |
| 书签持久化+分组 | ✅ | `repositories/bookmarks.py` |
| 涂鸦持久化 | ✅ | `repositories/annotations.py` |
| AI 历史 | ✅ | `repositories/chat.py` |
| 图谱数据持久化 | ✅ | `models/graph.py` + `services/graph/` |

#### 3.1.6 跨书谱系与书内图谱

| 条目 | 状态 | 证据 |
|------|------|------|
| 书籍级谱系图 | ✅ | `GraphView.vue` + `services/graph/cross_book.py` |
| 聚类分层（tag→文件夹→领域） | ✅ | `services/graph/clustering.py` |
| 两阶段分类 pre/post-classify | ✅ | `services/graph/clustering.py` + `rag_service.py` |
| 书内知识图谱（三层粒度） | ✅ | `services/graph/intra_book.py` |
| 跨书知识检索 | ✅ | `GET /api/graph/knowledge/{kp_id}/appears-in` |
| LLM 打分+方向/原因 | ✅ | `services/graph/llm_score.py` |
| 专业术语词库 | ✅ | `services/graph/lexicon.py` + `domain_terms.txt` |
| 人工反馈（确认/忽略/修改） | ✅ | `api/routes/graph.py` |
| 图谱重建（全局/单书） | ✅ | `POST /api/graph/rebuild` |

### 3.2 决策符合度（决策 1~35）

| 决策# | 内容 | 状态 | 证据/备注 |
|-------|------|------|-----------|
| 1 | 本地单用户 | ✅ | 无账号体系，127.0.0.1 监听 |
| 2 | PDF 统一按页+多模态 | ✅ | `parsers/pdf.py` 按页切章 + `vision_extract.py` |
| 3 | 脑图 ECharts | ✅ | `MindMapPanel.vue` ECharts 树图 |
| 4 | 三层画像 | ✅ | `profile_service.py` |
| 5 | 仅桌面端 | ✅ | 无移动端适配代码 |
| 6 | PDF/MD/TXT/EPUB | ✅ | `parsers/` 全格式 |
| 7 | 引用出处 | ✅ | `citations.py` |
| 8 | 关联强度三合一+笔记加权 | ✅ | `graph/llm_score.py` + `cross_book.py` |
| 9 | 图谱三层粒度 | ✅ | `graph/intra_book.py` |
| 10 | ECharts | ✅ | `GraphView.vue` |
| 11 | tag→领域聚类→分层 | ✅ | `graph/clustering.py` |
| 12 | 画像迁移规则+自动学习 | ✅ | `profile_service.py` + `profile_learning.py` |
| 13 | 笔记导出 MD+PDF | ✅ | `note_export.py` |
| 14 | 不理解双入口 | ✅ | 划词菜单+侧边按钮 |
| 15 | EPUB 保留原排版 | ❌ | `parsers/epub.py` 仅 `_TextExtractor` 提取纯文本，**未保留图文混排** |
| 16 | 边方向箭头 | ✅ | `GraphView.vue` `edgeEndpoints` |
| 17 | 结束对话三组合触发 | ✅ | `rag_service.py` `archive_book_task` |
| 18 | Skill 自动按任务匹配 | ✅ | `rag_router.py` LLM 挑选 |
| 19 | RAG/Skill ≥3 版本+整条目回滚 | ⚠️ | 版本递增已实现，去重合并已实现；但**未见版本回滚 API 或版本历史列表接口** |
| 20 | 相关度阈值函数 | ✅ | `relatedness.py` |
| 21 | 文件夹多级嵌套+批量操作 | ✅ | `folders.py` + `HomeView.vue` |
| 22 | 多模态提取触发时机 | ✅ | `vision_extract.py` + `import_service.py:168` |
| 23 | 多模态独立配置 | ✅ | `config.py` `vision_*` + `SettingsView.vue` |
| 24 | 书签分组/文件夹 | ✅ | `bookmarks.py` + `BookmarkDrawer.vue` |
| 25 | 涂鸦撤销 5 步+擦除 | ✅ | `PageDoodleCanvas.vue` |
| 26 | 划线批注+提问 | ✅ | `PageDoodleCanvas.vue` + `useReaderDoodle.ts` |
| 27 | 思考模式+精细参数 | ✅ | `ai/client.py` `_build_body` |
| 28 | AI 三接口+base_url 双写法 | ✅ | `ai/client.py` |
| 29 | 数学硬规则 | ✅ | `ai/prompts/chat.py` `MATH_RULE` |
| 30 | 会话分池+历史注入 | ✅ | `repositories/chat.py` |
| 31 | Markdown 内嵌本地图片 | ❌ | **未实现**：无 `images/` 复制逻辑，无媒体端点 |
| 32 | 两段式导入+进度 | ✅ | `import_service.py` + `tasks/__init__.py` + `TaskCenter.vue` |
| 33 | 整体并发+流畅度 | ✅ | `ThreadPoolExecutor` + `Semaphore` |
| 34 | LLM 自主挑选 RAG | ✅ | `rag_router.py` |
| 35 | 异步/并行改造 | ✅ | `tasks/__init__.py` 落库+线程池+配额+progress |

### 3.3 验收标准符合度（验收 1~25）

| # | 验收条目 | 状态 | 证据 |
|---|---------|------|------|
| 1 | 导入四格式+PDF 按页+封面 | ✅ | parsers/ + media_service.py |
| 2 | 主页搜索/筛选/tag | ✅ | HomeView.vue |
| 3 | 三栏+进度+笔记+双入口+导出 | ✅ | ReaderView.vue + note_export.py |
| 4 | 四类生成+脑图+出处 | ✅ | chat_service.py + MindMapPanel.vue |
| 5 | 三层画像查看/编辑/重置 | ⚠️ | 查看 ✅ + 重置 ✅ + 阈值调整 ✅；**画像内容编辑缺失** |
| 6 | API 配置可切换+流式 | ✅ | SettingsView.vue + ai/client.py |
| 7 | 隐私开关 | ✅ | config.py `ai_enable_body_send` |
| 8 | 谱系图+强度+方向+箭头 | ✅ | GraphView.vue + graph/ |
| 9 | 聚类分层 | ✅ | graph/clustering.py |
| 10 | 书内知识点+跨书跳转 | ✅ | intra_book.py + GraphView.vue |
| 11 | 图谱反馈+重建 | ✅ | graph.py 路由 |
| 12 | 画像迁移+阈值学习 | ✅ | profile_service.py + profile_learning.py |
| 13 | EPUB 保留原排版 | ❌ | epub.py 仅纯文本提取 |
| 14 | RAG/Skill 归档+增改 | ✅ | rag_service.py |
| 15 | RAG 检索引用+Skill 自动匹配 | ✅ | rag_router.py |
| 16 | 图谱联动+暖画像 | ✅ | graph_sync.py + profile_service.py |
| 17 | Markdown/LaTeX 渲染 | ✅ | MdRender.vue |
| 18 | 文件夹多级+批量操作 | ✅ | folders.py + HomeView.vue |
| 19 | start.bat 一键启动 | ✅ | start.bat |
| 20 | 外部资料直投 | ✅ | RagView.vue + assets.py |
| 21 | 多模态视觉提取+滑动窗口 | ✅ | vision_extract.py |
| 22 | 位置书签+分组 | ✅ | bookmarks.py + BookmarkDrawer.vue |
| 23 | 涂鸦+划线批注/提问 | ✅ | PageDoodleCanvas.vue |
| 24 | AI 三接口+base_url 双写 | ✅ | ai/client.py |
| 25 | 数学硬规则+画像注入+Skill 匹配 | ✅ | MATH_RULE + build_profile_block + rag_router.py |

### 3.4 里程碑符合度（M1~M10+）

| 里程碑 | 状态 | 备注 |
|--------|------|------|
| M1 需求冻结 | ✅ | 文档体系完整 |
| M2 基础框架 | ✅ | 解析器+书架+阅读页+设置页 |
| M3 阅读闭环 | ✅ | 章节切换+进度+笔记+导出 |
| M4 AI 接入 | ✅ | SSE 流式+上下文问答+引用出处+隐私开关 |
| M5 AI 增强 | ✅ | 四类预设+脑图+划词操作 |
| M6 阅读体验补全 | ✅ | 书签全格式+涂鸦+划线批注/提问 |
| M7 PDF 统一处理 | ✅ | 按页切章+多模态视觉提取+页缓存 |
| M8 知识图谱 | ✅ | 跨书+书内+聚类+LLM 打分+联动沉淀+增量更新 |
| M9 个性化与沉淀 | ✅ | 三层画像+归档+阈值学习+相关度函数+阅读建议 |
| M10 打磨 | ✅ | 笔记 PDF 导出+性能优化+验收测试 |
| M10+ 打磨轮 | ✅ | 多接口格式/资产页重设计/决策 34/异步并行/性能优化/UI 打磨 |

> **§8.1 实现状态审查表已过期**：该表列出的 8 项"待实现"中，第 2/3/7/8 项已随 v1.60（决策 35）实现，第 6 项部分实现。仅第 4（MD 图片）、第 5（EPUB 排版）项仍未达标。

### 3.5 非功能需求符合度

| 条目 | 状态 | 证据 |
|------|------|------|
| 4.1 技术栈（Python+FastAPI+SQLite+Vue3+ECharts） | ✅ | 全栈符合 |
| 4.2 API 可配置 | ✅ | SettingsView.vue + config.py |
| 4.2 失败处理+重试+降级 | ⚠️ | LLM 降级回退已实现；**LLM 客户端缺重试/限流基础设施** |
| 4.2 流式输出 | ✅ | SSE `stream_chat` |
| 4.2 异步任务+进度 | ✅ | tasks/__init__.py + TaskCenter.vue |
| 4.3 本地存储 | ✅ | data/books/ + SQLite |
| 4.3 解析缓存落盘 | ✅ | page_text/ + llm_cache + cluster cache |
| 4.3 图谱重建 | ✅ | POST /api/graph/rebuild |
| 4.4 API Key 本地保存+不前端回显 | ✅ | .env + settings.py |
| 4.4 隐私开关 | ✅ | `ai_enable_body_send` |
| 4.4 清除数据/删除单书 | ✅ | delete_book 级联 |
| 4.5 并发（视觉/打分/任务系统） | ✅ | ThreadPoolExecutor + Semaphore |
| 4.5 导入性能（流式+并发渲染） | ✅ | 分块写盘 + 后台并发 |
| 4.5 FTS5 全书搜索 | ✅ | database.py FTS5 + search_service.py |
| 4.6 一键启动 | ✅ | start.bat |

### 3.6 使用手册一致性

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 模块地图覆盖度 | ✅ | 后端 36 项 + 前端 14 项全覆盖 |
| 模块状态标注 | ⚠️ | 部分模块标注"待实现决策 32/33"但实际已实现（如 `import_service.py`、`tasks/__init__.py`、`vision_extract.py`），状态列未同步更新 |
| 跨模块流程 | ✅ | 5 条关键流程与代码一致 |
| 分册结构 | ✅ | 5 个功能域分册 + 总纲 |

### 3.7 未满足/部分满足项详细说明

#### ❌ 未满足（4 项，去重后 2 项独立需求）

| # | 需求条目 | 需求依据 | 缺失内容 | 预期代码位置 |
|---|---------|----------|----------|-------------|
| 1 | **Markdown 内嵌本地图片** | 决策 31 / 验收（隐含 3.2/3.4.10） | 导入时扫描 `![](src)` 复制到 `images/`；`GET /api/books/{id}/media/{filename}` 媒体端点；章节返回时重写 URL | `import_service.py` + `api/routes/books.py` |
| 2 | **EPUB 保留原排版（图文混排）** | 决策 15 / 验收 13 / 3.2 | `parsers/epub.py` 仅提取纯文本，前端按纯文本渲染，未保留图文混排 | `parsers/epub.py` + `ReaderView.vue` |

> **注**：功能需求 3.1.2 中 MD 图片 ❌ 与决策 31 ❌ 为同一需求项，此处合并计数。

#### ⚠️ 部分满足（4 项）

| # | 需求条目 | 已实现部分 | 缺失部分 |
|---|---------|-----------|----------|
| 1 | **RAG/Skill 版本回滚（决策 19）** | 版本递增、去重合并、条目级删除 | 无版本历史列表 API、无回滚到历史版本的端点 |
| 2 | **画像内容编辑（验收 5）** | 查看+重置+阈值调整 | 冷画像字段（领域偏好/知识水平/语言风格/长期兴趣）不可手动编辑 |
| 3 | **LLM 客户端重试/限流** | 降级回退已实现 | 缺系统化的重试+限流基础设施 |
| 4 | **使用手册模块状态标注** | 地图结构完整 | 多个模块状态列仍标注"待实现决策 32/33"，实际已实现 |

---

## 四、未完成需求与后续拓展清单

> 来源：Eric 未完成需求与后续拓展报告。调研范围：需求文档 v1.66 全部分册、docs/ 三份技术文档、重构规范 v1.38。
> 与第三章重叠的条目（EPUB 图文混排、MD 内嵌图片、画像编辑、graph_sync 并发）已合并去重，此处标注交叉引用。

### 4.1 待定稿决策（D-1~D-6）

来源：`../../需求-决策.md` §9.2 开放项 + §9.3 缺口备选方案中标记「未定稿」的条目

| # | 决策点 | 备选方案 | 当前阻塞原因 | 优先级 |
|---|--------|----------|-------------|--------|
| **D-1** | **DeepSeek responses 模式思考参数**传递方式 | chat 模式已定稿（决策 27）；anthropic 已定稿为忽略 OpenAI 专用参数；responses 模式待定 | 等待 DeepSeek 官方 responses 接口支持思考参数后才有意义 | **低**（外部依赖） |
| **D-2** | **聚类算法实现路线**：词语向量化 vs 文本共现法 | A. 共现为主+术语归一化增强+向量可选辅助（讨论倾向）；B. 纯向量化（不推荐）；C. 维持现状 | 需统一决定三个子问题：①是否采纳「共现+术语归一化+向量可选辅助」；②向量化形态；③是否建立聚类质量评估集 | **中**（影响数百本书规模后的聚类质量） |
| **D-3** | **EPUB 保留原排版（图文混排）阅读展示**（验收 13、决策 15）→ 同第三章 ❌ 未满足 #2 | A. 前端按原 HTML 片段渲染（推荐）；B. 文本+图片资源分离渲染；C. 维持纯文本并修订验收措辞 | 方案未定稿，`parsers/epub.py` 仅提取纯文本 | **高**（验收 13 不达标） |
| **D-4** | **画像内容编辑**（验收 5）→ 同第三章 ⚠️ 部分满足 #2 | A. 仅冷画像字段可编辑（推荐）；B. 三层全可编辑 | 方案未定稿；查看+重置+阈值调整已实现，但画像内容编辑缺失 | **中**（验收 5 部分不达标） |
| **D-5** | **本地向量检索**（性能优化路径 决策 5） | sqlite-vec / faiss；与聚类 §10、网络受限环境一起定 | 与 D-2 聚类算法路线关联决策；当前 RAG 为线性扫描 | **低**（当前规模线性扫描够用） |
| **D-6** | **任务分级模型**（性能优化路径 §4 第三梯队） | 简单任务用便宜模型（挑选/分类），深度解读用主模型 | 与决策 34 挑选器衔接，挑选器已独立配置轻量模型（`RAG_SELECT_*`），部分实现了任务分级思路 | **低**（挑选器已部分覆盖） |

### 4.2 未实施需求（U-1~U-4）

| # | 功能 | 出处 | 当前状态 | 阻塞原因/依赖 | 优先级 |
|---|------|------|----------|-------------|--------|
| **U-1** | **EPUB 图文混排阅读** → 同第三章 ❌ #2 | 验收 13、决策 15、§9.3 缺口 #4 | `parsers/epub.py` 仅提取纯文本，前端按文本渲染 | 依赖 D-3 方案定稿 | **高** |
| **U-2** | **Markdown 内嵌本地图片** → 同第三章 ❌ #1 | 决策 31、§9.3 缺口 #3 | 方案已定稿（v1.45），但 §8.1 审查标记「待实现」 | 需实现媒体端点 + 导入扫描复制逻辑 | **高** |
| **U-3** | **FTS5 全书搜索前端入口** | `../性能优化路径.md` 决策 3 已实施 | 后端已落地（trigram 虚表 + `GET /api/books/search`），但**前端无任何调用** | 需在 HomeView 搜索框接入后端 FTS5 API | **中** |
| **U-4** | **graph_sync 落点 #9「逐关系 LLM 并发」** → 同第三章里程碑 #8 | `../异步并行改造计划.md` 落点 #9 | 后台任务化已落地，但 `graph_sync.py:190-204` 仍为**串行** `for rel in query.all()` | 文档声称已实施但实际未落地，需补做并发改造或修正文档 | **中** |

### 4.3 部分实施需求（P-1~P-3）

| # | 功能 | 出处 | 已实现部分 | 缺失部分 | 优先级 |
|---|------|------|-----------|---------|--------|
| **P-1** | **三层画像可查看/编辑/重置** → 同第三章 ⚠️ #2 | 验收 5、§8.1 #6 | 查看 + 重置 + 阈值调整已实现 | 画像内容编辑缺失，依赖 D-4 | **中** |
| **P-2** | **阅读能力改善机制** | §3.4.6 | 阅读建议基础版已实现（stats/薄弱概念/复习提醒/节奏文案） | 间隔重复要点回顾（无 SM-2 等算法）；阅读习惯统计（时长、章节完成率、提问深度）未实现 | **低** |
| **P-3** | **对话历史滚动摘要压缩** | `../性能优化路径.md` §3 | 聊天历史裁剪（最近 N 条）已实现 | 早于 N 轮的历史摘要化（而非直接丢弃）未实现 | **低** |

### 4.4 未达标里程碑（§8.1 遗留）

来源：`../../需求-里程碑与验收.md` §8.1 实现状态审查

§8.1 中 8 项审查结论的当前状态汇总：

| # | 功能/承诺 | §8.1 状态 | 当前实际状态 | 缺失项 |
|---|----------|----------|------------|--------|
| 1 | 跨书 RAG 检索引用 + Skill 自动匹配 | ✅ 已实现（v1.63） | ✅ 已实现 | — |
| 2 | 两段式导入 | 待实现 | ✅ **已实现**（v1.60 决策 35） | — |
| 3 | 并发落地 | 待实现 | ✅ **已实现**（v1.60 决策 35） | — |
| 4 | Markdown 内嵌本地图片 | 待实现 | ⚠️ **仍待实现**（同 U-2） | 导入扫描复制 + 媒体端点 |
| 5 | EPUB 保留原排版阅读 | 不达标 | ⚠️ **仍不达标**（同 U-1） | 需方案定稿并实现 |
| 6 | 三层画像可查看/编辑/重置 | 部分实现 | ⚠️ **仍部分实现**（同 P-1） | 画像内容编辑 |
| 7 | 任务系统 | 待实现 | ✅ **已实现**（v1.60 决策 35） | — |
| 8 | 整体异步/并行 | 待实现 | ✅ **已实现**（v1.60），但 graph_sync 逐关系并发未落地（同 U-4） | 落点 #9 并发 |

**结论**：§8.1 遗留 8 项中 5 项已解决，仍有 **3 项未解决**：
1. **Markdown 内嵌本地图片**（决策 31，方案已定稿未实现）
2. **EPUB 图文混排**（验收 13，方案未定稿）
3. **画像内容编辑**（验收 5，方案未定稿）

### 4.5 文档登记待办项

#### 4.5.1 `../异步并行改造计划.md`

| 待办 | 出处 | 状态 | 优先级 |
|------|------|------|--------|
| 落点 #9 graph_sync 逐关系 LLM 并发 | §2 落点表 #9；§9.3.2 落地注记 | 文档声称已实施，实际串行（同 U-4） | **中** |
| Phase 3 步骤 3.1：LLM 挑选器接入任务/并发基础设施 | §4 Phase 3 | 挑选器已实现（v1.63），但是否走统一限流入口 `ai/limiter.py` 未确认 | **低** |

#### 4.5.2 `../性能优化路径.md`

| 待办 | 出处 | 状态 | 优先级 |
|------|------|------|--------|
| 本地向量检索（决策 5） | §4 第三梯队、§7 决策项 #5 | 开放项，与聚类 §10 同决策（同 D-5） | **低** |
| 任务分级模型 | §4 第三梯队 | 保留项，挑选器已部分覆盖（同 D-6） | **低** |
| FTS5 前端入口 | §4 第二/三梯队实施记录 | 后端已实施，前端无 UI 入口（同 U-3） | **中** |
| 聊天历史/会话清理与归档策略 | §3 全景表 | 已有裁剪（最近 N 条），但无归档/摘要化（同 P-3） | **低** |
| 跨书共现倒排索引 | §3 全景表 | 「数百本时启用」，当前未实施 | **低**（未来扩展） |

#### 4.5.3 `../知识图谱聚类算法.md`

| 待办 | 出处 | 状态 | 优先级 |
|------|------|------|--------|
| §8 局限：成簇顺序敏感 | §8 | 后续可改为建图+连通分量/社区检测，当前未实施 | **低** |
| §8 局限：阈值固定 | §8 | 吸收阈值 `≥2` 为初值，后续可引入基于画像的阈值函数 | **低** |
| §8 局限：无 LLM 参与命名 | §8 | 后续可引入 LLM 打分/命名作为修正层 | **低** |
| §10 方案选型：词语向量化 vs 文本共现法 | §10（待定稿） | 三个待决策点未定（同 D-2） | **中** |
| §9.5 合并/重命名触发：全量扫描改增量 | §9.5 | 「书量大后改为增量候选对」——当前全量扫描 | **低**（当前书量不大） |

### 4.6 后续拓展要求（E-1~E-7）

来源：需求文档各分册中明确提到的未来扩展方向

| # | 拓展方向 | 出处 | 描述 | 当前状态 | 优先级 |
|---|---------|------|------|---------|--------|
| **E-1** | **多用户支持** | 决策 1、决策 30 | 当前仅本地单用户；`session_id` 已预留 `user:{uid}:` 前缀扩展位 | 架构预留但未实现 | **低**（明确标记不需要） |
| **E-2** | **云同步 / 多端** | 决策 1、决策 5 | 仅本地单用户、仅桌面端 | 明确排除 | **不适用** |
| **E-3** | **社交分享** | 需求文档 §2 目标用户场景 | 未明确提及社交分享功能 | 无规划 | **不适用** |
| **E-4** | **更多 AI 模型接入** | 决策 28、§3.4.12 | 已支持 `responses/chat/anthropic` 三种接口格式 | ✅ 已实现 | — |
| **E-5** | **AntV G6 升级** | 决策 10 | ECharts 为常用组件；「如复杂交互超出其能力，可升级为 AntV G6」 | 当前 ECharts 够用 | **低**（条件触发） |
| **E-6** | **对话历史滚动摘要压缩** | `../性能优化路径.md` §3 | 早于 6~10 轮的历史摘要化（而非直接丢弃） | 未实施（同 P-3） | **低** |
| **E-7** | **聚类质量评估集** | `../知识图谱聚类算法.md` §10.5 | 建立 20~30 本人工标注评估集 | 未实施 | **低** |

### 4.7 重构规范 §11 记录失真项（R-1~R-5）

来源：`../../待优化的代码问题.md` §4.3（与之前审查发现的 5 项失真记录对齐）

| # | 记录表声称 | 核对结论 | 涉及文件 | 优先级 |
|---|----------|---------|---------|--------|
| **R-1** | ReaderView 已拆分至 427 行 | **已过时**：当前 **698 行**（M6~M10 功能回涨），需重新立项拆分 | `frontend/src/views/ReaderView.vue` | **高**（超 400 行上限 74%） |
| **R-2** | 延迟导入全部提升顶层 | **部分不实**：`services/graph/keywords.py:80`、`services/vision_extract.py:126-127` 残留函数内延迟导入 | `keywords.py`、`vision_extract.py` | **中** |
| **R-3** | lexicon 私有改公共访问器 | **部分不实**：`services/graph/clustering.py:23-31` 仍直引 5 个私有符号 | `clustering.py`、`lexicon.py` | **中** |
| **R-4** | `book_tags` 消除 tag 清洗重复 | **部分不实**：`rag_router.py:57`、`cross_book.py:248`、`serializers.py:23` 共 3 处仍自行 `json.loads(tags_json)` | `rag_router.py`、`cross_book.py`、`serializers.py` | **中** |
| **R-5** | chat 路由不再直连仓储 | **部分不实**：`api/routes/chat.py:10,68` 回放路径仍直调 `persist_chat` | `api/routes/chat.py` | **中** |

### 4.8 Top 10 待办清单

| 排名 | 待办项 | 类别 | 优先级 | 理由 |
|------|--------|------|--------|------|
| **1** | **EPUB 图文混排阅读**（D-3/U-1） | 待定稿决策 + 未实施需求 | **高** | 验收 13 不达标；方案需先定稿再实施 |
| **2** | **Markdown 内嵌本地图片实现**（U-2） | 未实施需求 | **高** | 决策 31 方案已定稿（v1.45），仅欠实现 |
| **3** | **画像内容编辑**（D-4/P-1） | 待定稿决策 + 部分实施 | **高** | 验收 5 不达标；需先定稿编辑粒度 |
| **4** | **graph_sync 逐关系并发**（U-4） | 文档登记待办 | **中** | 文档声称已实施但实际串行；需补做或修正文档 |
| **5** | **FTS5 全书搜索前端入口**（U-3） | 文档登记待办 | **中** | 后端已实施但前端无 UI 入口，搜索能力浪费 |
| **6** | **聚类算法路线决策**（D-2） | 待定稿决策 | **中** | 影响数百本书规模后的聚类质量 |
| **7** | **ReaderView 二次拆分**（R-1） | 重构记录失真 | **中** | 698 行远超 400 行上限；需抽 `useReaderZoom`/`useReaderBookmarks` 等 |
| **8** | **§11 记录失真项修正**（R-2~R-5） | 重构记录失真 | **中** | 4 项部分不实需修正 |
| **9** | **本地向量检索决策**（D-5） | 待定稿决策 | **低** | 与聚类算法路线关联决策；当前规模线性扫描够用 |
| **10** | **DeepSeek responses 思考参数**（D-1） | 待定稿决策 | **低** | 外部依赖，不影响当前功能 |

---

## 五、建议执行顺序

综合三份报告的建议，按批次组织如下：

### 第一批：紧急（未满足需求 + 高收益优化点）

| 序号 | 事项 | 来源 | 预期产出 |
|------|------|------|----------|
| 1 | **Markdown 内嵌本地图片实现**（U-2 / 决策 31） | Jack + Eric | 导入扫描复制 + 媒体端点 + 前端渲染联动 |
| 2 | **EPUB 图文混排方案定稿 + 实施**（D-3/U-1 / 决策 15 / 验收 13） | Jack + Eric | 先定稿方案（推荐 A：前端按原 HTML 渲染），再实施 |
| 3 | **画像内容编辑方案定稿 + 实施**（D-4/P-1 / 验收 5） | Jack + Eric | 先定稿编辑粒度（推荐 A：仅冷画像可编辑），再实施 |
| 4 | **RAG 候选目录构建 N+1 → 批量加载**（优化 #1） | Sam | `rag_router.py` 批量查询改造 |
| 5 | **资产批量查询接口 + 前端合并请求**（优化 #2/#9/#23） | Sam | 后端新增批量端点 + 前端改一行调用 |
| 6 | **跨书关联计算笔记预加载**（优化 #3） | Sam | `cross_book.py` 一次预加载全部笔记 |
| 7 | **`reading_logs` 补充复合索引**（优化 #6） | Sam | 一行 DDL 修复 |
| 8 | **§8.1 实现状态审查表更新** | Jack | 同步更新文档，避免后续审查误判 |

**验证方式**：
- 需求项：运行 pytest 196 项全绿 + 手动验证 MD 图片导入/EPUB 图文渲染/画像编辑 UI
- 优化项：对比改造前后 `build_catalog()` / `cross_book.py` 执行耗时；验证资料页 HTTP 请求数

**文档回写**：
- 更新 `../../需求-决策.md` §9.2/§9.3 标记 D-3/D-4 已定稿
- 更新 `../../需求-里程碑与验收.md` §8.1 审查表
- 更新使用手册模块地图状态列

### 第二批：部分满足需求补齐 + 中收益优化

| 序号 | 事项 | 来源 | 预期产出 |
|------|------|------|----------|
| 1 | **RAG/Skill 版本回滚 API**（决策 19） | Jack | 版本历史列表 + 回滚端点 |
| 2 | **LLM 客户端重试/限流基础设施** | Jack | 系统化重试 + 限流中间件 |
| 3 | **graph_sync 逐关系并发**（U-4 / 落点 #9） | Eric | 补做并发改造或修正文档 |
| 4 | **FTS5 全书搜索前端入口**（U-3） | Eric | HomeView 搜索框接入后端 FTS5 API |
| 5 | **`get_all_profiles` 合并为单条查询**（优化 #4） | Sam | 3 条 SELECT → 1 条 |
| 6 | **`_find_shared_asset` 全表扫描优化**（优化 #5） | Sam | SQL 条件查询替代 Python 循环 |
| 7 | **聊天流式端点异步化**（优化 #8） | Sam | `chat.py` 改 `async def` |
| 8 | **章节正文 ETag / 条件请求**（优化 #7） | Sam | 浏览器缓存命中避免重复传输 |
| 9 | **MdRender 延迟渲染**（优化 #9） | Sam | IntersectionObserver 进入视口时再渲染 |
| 10 | **HTTP 缓存策略补齐**（优化 #10/#32） | Sam | 封面、书籍列表等添加缓存头 |
| 11 | **ReaderView 二次拆分**（R-1） | Eric | 抽取 `useReaderZoom`/`useReaderBookmarks`/`useReaderReadState` |
| 12 | **§11 记录失真项修正**（R-2~R-5） | Eric | 延迟导入残留、lexicon 私有引用、book_tags 逃逸、chat 路由直连仓储 |
| 13 | **聚类算法路线决策**（D-2） | Eric | 统一决定三个子问题 |
| 14 | **使用手册模块状态标注更新** | Jack | 同步更新已过时的状态列 |

**验证方式**：
- 需求项：pytest 全绿 + 手动验证版本回滚/FTS5 搜索/graph_sync 并发
- 优化项：对比改造前后 `get_all_profiles` 调用次数、聊天端点并发能力、资料页 DOM 节点数

**文档回写**：
- 更新 `../异步并行改造计划.md` 落点 #9 状态
- 更新 `../性能优化路径.md` FTS5 前端入口已实施
- 更新 `../../重构规范.md` §11 记录表

### 第三批：长期拓展 + 低收益优化

| 序号 | 事项 | 来源 | 预期产出 |
|------|------|------|----------|
| 1 | **本地向量检索决策与实施**（D-5） | Eric | sqlite-vec / faiss 选型 + 实施 |
| 2 | **任务分级模型**（D-6） | Eric | 简单任务用便宜模型 |
| 3 | **DeepSeek responses 思考参数**（D-1） | Eric | 等待外部依赖后实施 |
| 4 | **间隔重复算法（SM-2）**（P-2） | Eric | 阅读建议增强 |
| 5 | **对话历史滚动摘要压缩**（P-3/E-6） | Eric | 早于 N 轮的历史摘要化 |
| 6 | **聚类算法改进**（顺序敏感/阈值固定/LLM 命名） | Eric | 建图+连通分量/社区检测 + 画像阈值函数 + LLM 打分命名 |
| 7 | **聚类质量评估集**（E-7） | Eric | 20~30 本人工标注评估集 |
| 8 | **后端剩余低收益优化**（优化 #7~#8, #10, #13, #16, #19, #21~#22） | Sam | 统计聚合、同步 HTTP、画像缓存、分页等 |
| 9 | **前端剩余低收益优化**（优化 #24~#25, #27, #29~#31） | Sam | pollTask 抽取、双重轮询优化、mathjax-full 替换等 |
| 10 | **跨书共现倒排索引** | Eric | 数百本时启用 |

**验证方式**：
- 各事项完成后运行全量 pytest + 前端 E2E 测试
- 性能优化项对比改造前后基准测试数据

**文档回写**：
- 更新 `../性能优化路径.md` 对应条目状态
- 更新 `../知识图谱聚类算法.md` §8/§10 局限与方案选型
- 更新 `../../需求-决策.md` §9.2 标记 D-1/D-5/D-6 已定稿

---

## 六、关键文件索引

### 6.1 需求文档

| 文件 | 路径 |
|------|------|
| 需求总纲 | `../../需求文档.md` |
| 功能需求 | `../../需求-功能需求.md` |
| 非功能与数据 | `../../需求-非功能与数据.md` |
| 决策与开放项 | `../../需求-决策.md` |
| 里程碑与验收 | `../../需求-里程碑与验收.md` |
| 技术栈规范 | `../../技术栈规范.md` |
| 重构规范 | `../../重构规范.md` |
| 待优化的代码问题 | `../../待优化的代码问题.md` |

### 6.2 技术文档

| 文件 | 路径 |
|------|------|
| 异步并行改造计划 | `../异步并行改造计划.md` |
| 性能优化路径 | `../性能优化路径.md` |
| 知识图谱聚类算法 | `../知识图谱聚类算法.md` |

### 6.3 后端关键文件（Sam 报告涉及）

| 文件 | 路径 | 关联优化项 |
|------|------|------------|
| database.py | `backend/app/core/database.py` | #20, #21 索引与 PRAGMA |
| rag_router.py | `backend/app/services/rag_router.py` | #1, #5, #16 RAG N+1 |
| cross_book.py | `backend/app/services/graph/cross_book.py` | #2, #3, #14, #15 跨书 N+1 |
| assets.py (repo) | `backend/app/repositories/assets.py` | #4 共享资产全表扫描 |
| profile_service.py | `backend/app/services/profile_service.py` | #6 画像三次查询 |
| recommendation_service.py | `backend/app/services/recommendation_service.py` | #7 统计聚合 |
| serializers.py | `backend/app/schemas/serializers.py` | #8, #17, #18 大对象序列化 |
| chat.py | `backend/app/api/routes/chat.py` | #11 同步阻塞端点 |
| assets.py (route) | `backend/app/api/routes/assets.py` | #9 缺批量接口 |
| books.py | `backend/app/api/routes/books.py` | #10, #19 单本/分页 |
| settings.py | `backend/app/api/routes/settings.py` | #13 同步 HTTP |
| import_service.py | `backend/app/services/import_service.py` | U-2 MD 图片 |
| graph_sync.py | `backend/app/services/graph_sync.py` | U-4 逐关系并发 |
| epub.py | `backend/app/parsers/epub.py` | U-1 EPUB 排版 |

### 6.4 前端关键文件（Sam + Eric 报告涉及）

| 文件 | 路径 | 关联项 |
|------|------|--------|
| RagView.vue | `frontend/src/views/RagView.vue` | #23, #24, #27, #28 N 请求/渲染 |
| RagDetailView.vue | `frontend/src/views/RagDetailView.vue` | #24, #28 pollTask/MdRender |
| ReaderView.vue | `frontend/src/views/ReaderView.vue` | #31, R-1 预加载/拆分 |
| useReaderAi.ts | `frontend/src/composables/useReaderAi.ts` | #25 双重轮询 |
| client.ts | `frontend/src/api/client.ts` | #30 超时配置 |
| book.ts | `frontend/src/stores/book.ts` | #26 Pinia 利用率 |
| MdRender.vue | `frontend/src/components/MdRender.vue` | #28 高频渲染 |
| graphLabel.ts | `frontend/src/utils/graphLabel.ts` | #29 mathjax-full |
| HomeView.vue | `frontend/src/views/HomeView.vue` | U-3 FTS5 入口 |

---

> **文档生成说明**：本文档由三份独立审查报告（Sam 响应速度优化、Jack 需求符合度、Eric 未完成需求与后续拓展）合并而成。重叠条目已合并去重，各条目保留了原始出处标注。审查基线为 2026-08-05 的全量代码与需求文档 v1.66。

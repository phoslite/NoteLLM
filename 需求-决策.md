# 需求文档 · 决策与开放项（分册）

> 本文档是《需求文档》拆分出的分册（已确认决策 1~37 / 开放项（原 §9））。
> 总纲（概述 / 目录 / 定位表 / 变更记录）见 [需求文档.md](需求文档.md)。
> 章节标题**沿用原编号**（如 3.1），正文交叉引用可按原编号在本分册或总纲定位表中查找。

## 本分册目录

| 原编号 | 标题 |
| --- | --- |
| 9. 需求决策与开放项 |
| 9.1 已确认决策（评审结论） |
| 9.2 开放项 |

---

## 9. 需求决策与开放项

### 9.1 已确认决策（评审结论）

| # | 问题 | 决策 |
| --- | --- | --- |
| 1 | 部署形态 | 仅本地单用户，无多用户/局域网共享 |
| 2 | PDF 处理方式 | 不需要本地 OCR；**PDF 统一按页渲染原图 + 多模态视觉提取**（含文本型，避免数学符号抽取乱码）；本地文本抽取仅作全文检索索引；笔记功能为标配 |
| 3 | 脑图呈现形式 | 图像组件（ECharts 树图），同时展示大纲、细节与重要定理 |
| 4 | 画像持久化 | 三层画像：冷（长期低频）/ 暖（近 1~2 本书）/ 热（当前书细节） |
| 5 | 移动端适配 | 不需要，仅桌面端 |
| 6 | 书籍格式 | 支持 PDF、Markdown、TXT、EPUB |
| 7 | AI 引用出处 | 需要，标注书名+章节+段落位置 |
| 8 | 关联强度计算 | LLM 打分 + 关键词共现 + 人工反馈，并加权用户笔记 |
| 9 | 图谱粒度 | 章节知识点必含；重要段落、用户笔记/不理解段落也纳入 |
| 10 | 图谱组件 | 常用组件：ECharts（复杂场景备选 AntV G6） |
| 11 | 大数量聚类 | 先按用户分类 tag，无 tag 按领域自动聚类，再分层展开 |
| 12 | 画像迁移规则 | 系统自动管理、阈值随用户习惯学习；初步条件：对话内容跨越 1 本→热转暖、3 本→暖转冷、>3 本→沉淀冷画像 |
| 13 | 笔记导出格式 | Markdown 与 PDF 两者都要 |
| 14 | 「不理解」交互形式 | 划词菜单按钮 + 阅读区侧边按钮，两个入口 |
| 15 | EPUB 排版 | 保留原排版（图文混排） |
| 16 | 边方向展示 | 谱系图中显式展示箭头（**领域递进深入方向**：A→B 表示 B 是 A 的深入/进阶；承接/发展/批判归入递进语义，2026-08-05 修订，原「理论传承方向」表述废弃） |
| 17 | 「结束对话」触发 | 三者组合（手动标记读完 / AI 检测对话完结 / 用户主动发起归档），**优先以用户主动发起归档为触发方式** |
| 18 | Skill 调用方式 | **AI 自动按任务匹配调用**（结合暖画像与跨书谱系关联度排序），无需用户手动指定 |
| 19 | RAG/Skill 版本策略 | **保留至少 3 个版本**；回滚粒度为**整条目**（不按分块） |
| 20 | 相关领域判定 | 基于**跨书谱系关联强度**判定（主信号）+ 暖/冷画像术语覆盖率辅助信号 + 同 post 簇；**阈值函数已定稿（v1.29）**：score = max(谱系强度, 0.5×暖画像覆盖率 + 0.5×冷画像覆盖率)，related = 同簇 或 score ≥ related_strength；阈值由自动学习（行为样本/确认边低分位取 min，45~60）产出、设置页手动覆盖 |
| 21 | 文件夹层级与交互 | 支持**多级嵌套**；支持**批量移动、批量设 tag**；书架/阅读页可调整书籍与文件夹位置 |
| 22 | 多模态提取触发 | 手动导入 PDF（含文本型）作为知识库时**批量预提取**；阅读时不预提取，**用户在当前页对 AI 提问**时按需提取；按 `[P-1,P,P+1]` 滑动窗口（第 1 页/末页裁剪），仅补提取缺失页（增量缓存）；**读完归档时全书批量提取**（补全缺失页缓存，作为文本模型总结 RAG/Skill 的全文输入）；**书架导入自动预提取已修订为手动触发（v1.46，见决策 32）** |
| 23 | 多模态配置 | 多模态使用**独立的一套 API 配置**（base_url/api_key/model），**无需额度管理** |
| 24 | 书签列表组织 | 平铺列表（默认，按时间倒序），**支持分组/文件夹归类**（可命名/重命名/删除、拖拽归组） |
| 25 | 涂鸦撤销与历史 | 支持**撤销上一步（最多 5 步）**、**擦除痕迹**、**输入文本**等基础功能；**不保留历史版本**（撤销栈仅会话内） |
| 26 | 涂鸦划线批注与提问 | 每条划线可选中后**附加批注**（Markdown/LaTeX，随划线持久化）或**发起对划线区域的 AI 提问**；提问结合该页页缓存文本或划线区域图片，出处标注「第 X 页」，受隐私开关约束 |
| 27 | 文本模型思考模式与精细参数（DeepSeek） | 文本 AI 的 `thinking`（`AI_THINKING_TYPE`：enabled/disabled）与 `reasoning_effort`（`AI_REASONING_EFFORT`：low/medium/high/max，`thinking=disabled` 时不发送）**仅 chat 模式**写入请求体；`AI_MAX_TOKENS` 默认 32K、最大 64K（含思考 token）；思考模式下 `temperature/top_p/frequency/presence` 官方说明「设置不报错但不生效」；**responses 模式**仅将 `max_tokens` 映射为 `max_output_tokens`，thinking 参数待定稿（见 9.2 开放项） |
| 28 | AI 接口格式与 base_url 写法 | **三种接口格式**：`responses`（DeepSeek 官方）/ `chat`（OpenAI 兼容）/ `anthropic`（Messages API），设置页可切换、`AI_MODE` 控制；**base_url 两种写法**：基础地址（按接口格式自动补全路径，base 以 `/v1` 结尾补相对路径）或完整接口 URL（直接使用不再补全）；统一携带浏览器 User-Agent 规避 Cloudflare 等拦截（v1.39 已实现，见 3.4.12） |
| 29 | AI 输出数学符号规范 | 涉及数学符号**只能使用 LaTeX 或 Markdown**：行内 `$...$`、块级 `$$...$$`，**禁止无定界符裸 LaTeX 与 Unicode 数学字符**（如 `Λ`、`∈`、`ℝ`、`√`），一律写成 LaTeX 命令或 Markdown 文本；JSON 输出内反斜杠双写；对话/脑图/跨书关联/跨书联动/PDF 页提取等全部 LLM 提示词统一执行（v1.41 已落地，见 3.4.10） |
| 30 | 对话会话与上下文管理 | 会话键 `book:{book_id}:{mode}`（默认/解读/概论/思考逻辑分池）；每次提问注入最近 6~10 轮历史并按约 8k 字符预算截断；本地单用户**不引入 user_id**，`session_id` 预留 `user:{uid}:` 前缀扩展位；**已实现（v1.44，见 3.4.13）** |
| 31 | Markdown 内嵌本地图片 | 导入时扫描 `![...](src)` 并把本地图片（相对 / 绝对 / `file://` 均支持）复制到该书 `images/`，章节引用改写为相对路径；后端提供 `GET /api/books/{id}/media/{filename}`（白名单 png/jpg/jpeg/webp/gif/svg、路径规范化防穿越）；章节返回时重写为可访问 URL；SVG 允许但只经 `<img>` 标签渲染（浏览器天然隔离脚本）；图片缺失显示占位提示；AI 提问时图片可作附件（受隐私开关约束）（v1.45 定稿，待实现）；**手写 `<svg>` 标签渲染（v1.72 增补，已实现）**——Markdown 中直接手写的 `<svg>…</svg>` 内嵌图形一并渲染（`MdRender` markdown-it 开启 html 透传 + DOMPurify 启用 svg profile（`USE_PROFILES: html+svg+svgFilters`），脚本/事件属性/`javascript:` URI 仍被消毒；`![x](x.svg)` 文件引用维持 `<img>` 渲染不变） |
| 32 | 导入性能与进度（v1.46 定稿） | **两段式导入**：上传仅流式写盘（分块、不整读内存）并立即返回 `task_id`；解析 / 封面提取 / PDF 页图渲染 / 章节入库 / 跨书关联增量全部后台执行，任务带 `progress`(0~100) 与 `stage` 阶段名；前端书架顶部导入任务卡片轮询 + 批量导入队列（第 i/N 本）+ 上传真实进度（onUploadProgress）。**页图渲染**：后台 4~8 线程并发渲染 + 阅读缺失页按需渲染回退 + 渲染宽度上限 1600px（页图不可变，命中复用）。**预提取时机调整（修订决策 22）**：书架导入不再自动全书预提取（书籍卡片手动「提取全书页缓存」，x/y 页进度、可取消）；资料页 /rag 知识库投喂仍自动批量预提取；阅读提问 `[P-1,P,P+1]` 按需提取与归档全书提取不变 |
| 33 | 整体并发与流畅度（v1.47 定稿） | ① **视觉全书提取并发化**：`rebuild_book_caches` 由逐页串行改为 4~8 并发线程（`.env` `VISION_CONCURRENCY`，默认 4），页级进度原子回填、单页失败不中断、命中缓存不重复调用；② **跨书 LLM 打分并发化**：`MAX_LLM_PAIRS` 候选对并发打分（`.env` `AI_CONCURRENCY`，默认 4），失败对回退关键词分；③ **任务系统升级**：`tasks.submit` 改全局线程池（默认 8 线程）+ 状态加 `progress`/`stage` + **同类型任务并发配额**（视觉提取类全局最多 4 并发，超出排队），防线程失控与 API 额度争抢；④ **前端微优化**：标记已读/读完后的 `getBook + fetchBooks` 合并为 `Promise.all`；⑤ **测试连接异步化**：设置页「测试连接 / 测试视觉连接」改后台任务 + 轮询，不再同步等待；⑥ 归档链路保持「视觉通读 → 文本总结」硬依赖串行，仅提取本身并发化；SQLite WAL 已启用（见技术栈 §5） |
| 34 | RAG/Skill 注入：**LLM 自主挑选**（v1.53 修订，v1.63 已实现） | 由 LLM 结合**冷画像（领域偏好/知识水平/语言风格/长期兴趣）+ 暖画像（近期书摘要/相关领域）+ 当前需求（问题/章节/选中内容）**，从知识库中自主挑选本次对话需要的 RAG 与 Skill；实现采用**两级混合**：① 书级挑选——资产目录（每书 RAG summary + Skill name/applicable，数百本按领域分组折叠、冷画像偏好领域优先）交 LLM 输出 JSON `{selected_books, selected_skills, reasons}`，预算 ≤3 本书 / ≤2 个 Skill（可配置 `RAG_SELECT_MAX_BOOKS` / `RAG_SELECT_MAX_SKILLS`）；② 内容注入——选中书内用规则关键词检索 chunks（`retrieve_rag_chunks` 多书版，每书 ≤3 段）与 Skill 全文注入，当前书有资产时始终选入；出处格式【《书名》第X章 第Y段】（citations 解析已支持跨书）；**降级**：LLM 挑选失败 / 未配置 / 关闭开关 → 回退规则化候选（当前书 + 暖画像相关 top3 + 谱系关联 top2 + 关键词检索）；§9.3.1 四项细节已定稿：粒度=书级+书内规则、频率=**session_id 会话内缓存**（键=session_id+chapter_id，TTL 60 分钟可配）、挑选器=**独立模型配置**（`RAG_SELECT_*`，未填项回退主模型，默认低 max_tokens=512/禁思考/低温；**v1.79 起设置页新增「挑选模型」页签可配置全部字段，并支持 `RAG_SELECT_REASONING_EFFORT`（适配 DeepSeek，空=跟随主模型），独立测试端点 `POST /api/settings/ai/test-selector`**）、目录=领域分组折叠+冷画像优先；页模式同样注入跨书 chunks；隐私关闭仅注入 Skill 不注入 chunks；实现 = `services/rag_router.py` + `ai/prompts/rag_select.py` + `prepare_chat_job` 改挑选版 + 前端会话 `session_id` 传递；不引入向量检索（ADR-010/011 一致）
| 37 | 主页全局 AI 对话（阅读之外，2026-08-05 定稿） | 主页（/）右下角**全局 AI 对话浮窗**，不绑定书籍/章节，在阅读之外使用 Skill 与 RAG 资产辅助用户（答疑、概念解释、跨书提问、思路梳理）：① 会话——`global:{session_id}`（前端每次打开面板生成 `session_id`），多轮历史复用最近 10 轮 / 8k 字符预算注入，单一会话池（不做书级解读/概论等章节绑定预设）；② 注入——冷+暖画像（`get_all_profiles`）+ **全局 Skill**（全部书籍 skill 资产按任务文本相关性排序 top8，`repositories/assets.load_all_skills`）+ **RAG 跨书**（LLM 自主挑选全库目录 ≤3 书 / ≤2 Skill，降级规则检索全库 chunks 关键词 top6；`services/rag_router.select_global_knowledge`，会话缓存同决策 34）；③ 隐私开关关闭时仅注入 Skill、不注入 chunks 与画像（与决策 34 一致）；④ 流式——复用 SSE + `stream_key` 滚动落库 + 前端 2s 轮询（方案 2 同款）；⑤ 实现——`api/routes/ai_chat.py`（`POST /api/ai/chat` SSE + `GET/DELETE /api/ai/chat/messages?session_id=`）、`chat_service.prepare_global_job`/`build_global_messages`、`repositories/chat` 会话键支持 book_id 可空；前端 `useGlobalAi.ts` + `GlobalChatPanel.vue` 主页浮窗 |
| 36 | 附件统一走视觉模型提取为文本（2026-08-05 定稿，已实现） | **未缓存的附件（划线区域裁剪图 / Markdown 正文插图）统一经多模态视觉模型提取为文本后注入，主模型永远只接收文本**：① 入口 `vision_extract.extract_image_attachment(db, image_uri, hint="")`——内容寻址缓存 `data/cache/attachment_text/<sha256(uri)前32位>.md`（跨书共享），命中非空直接返回、不重复调用多模态；未配置视觉模型（`vision_configured`）或提取空/异常返回 None 且不落盘；② 触发条件——`ai_send_page_image` 开启（chat 与脑图统一）；`chat_service.build_messages` 彻底删除 image_url 组装分支，改为纯文本拼接（`crop_text`/`media_texts`），消除纯文本端点 `unknown variant image_url` HTTP 400；③ PDF 页模式提问仍以 `[P-1,P,P+1]` 页缓存文本为主，**缓存缺失/未配置多模态不再回退直发页图**（纯文本降级）；④ 脑图链路（`mindmap_service`）同步删除 image_url 分支、只注入页缓存文本；⑤ 前端无改动（`crop_image` 仍由 chat 接口接收，后端内部提取）；测试 `test_vision_extract`（未配置返回 None / 命中缓存不重复调用）+ `test_pdf_media` 改写，相关 25 项 pytest + 全量 226 pytest + ruff 全绿 |
| 35 | 异步/并行改造实施参数（v1.59 敲定，已实现） | 任务落库（`tasks` 表，保留 7 天）；并发参数 `.env` 可配且 **=0 不限制**（文本 4 / 视觉 4 / 页图渲染 4 / 全局线程池 8）；同类型配额采用**信号量分池**（B 方案：多任务可同时、总量受限，0=不限制），预留统一限流入口兼容后期 LLM 并发（决策 34）；SQLite 写冲突=全局写锁 + 短事务重试；进度权重自适应（默认：导入 40/40/20、归档 60/40、重建 20/50/30）；前端统一任务中心面板（1s 轮询）；测试连接并入任务系统（超时 60s）；实施拆 3 个 commit（任务系统 → 后台化 → 并发化）；详见 docs/异步并行改造计划.md §8 |

### 9.2 开放项

（上一版开放项 1~7 已全部定稿，见 9.1 决策 17~23；书签列表组织与涂鸦撤销/历史已定稿，见 9.1 决策 24、25；文本思考模式已定稿，见 9.1 决策 27；附件统一走视觉模型提取为文本已定稿，见 9.1 决策 36。）当前开放项：**1 条**——

1. **DeepSeek responses 模式下思考模式参数**（`thinking/reasoning_effort`）的传递方式待定稿（chat 模式已定稿，见 9.1 决策 27；anthropic 模式已定稿为忽略 OpenAI 专用参数，见 3.4.12/决策 28；待 DeepSeek 官方 responses 接口支持后再定）。
2. **聚类算法实现路线（v1.50 登记，2026-08-05 已定稿）**：**正式版 L0~L4 已落地（2026-08-10，见待办 v2.10 / 使用手册 v1.122 / 技术栈 v1.74）；剩余 O 项见待办 §3.1**——`docs/知识图谱聚类算法-改进方案.md`（O1~O8 开放决策：自研连通分量+加权标签传播成簇引擎、IDF 加权余弦 + τ_cluster 建边、LLM 允许降分融合、jieba 分词等）先在 `demo/clustering_demo.py` 上验证效果（对照现行贪心吸收 vs 连通分量 vs 加权 LPA，示例语料复现 D1 误吸场景），**正式版代码零改动**；待 demo 验证通过且用户拍板后再按改进方案落地（届时同步技术栈 ADR，与 ADR-010 不引入其他语言约束一致）。
3. **视觉提取输入优化（v1.82 登记；压缩图/裁边/OCR 预留 v1.86 已实现并入主程序，见 技术栈-AI §6「落地实现」；乱码优化分层待定稿）**：针对书 24 归档 429 TPM 限流与提取乱码的输入侧优化（详见 技术栈-AI §6「视觉提取输入优化」）：① 压缩图缓存 `pages_vlm/`（阅读原图与提取压缩图分离、归档批量预生成 + 窗口懒生成、`meta.json` 参数签名一致性、`VISION_IMAGE_MAX_WIDTH=1400`/`VISION_IMAGE_QUALITY=82`/`VISION_IMAGE_TRIM=conservative`，**v1.86 已实现**；OCR 预留扩展 `VISION_OCR_ENGINE`：tesseract 已实现、paddle/rapidocr 占位，压缩图生成后同步产出 `pages_vlm/page_XXX.txt` 提取优先使用）；② 乱码优化分层（提示词硬规则 / 后处理校验层含变量泄漏检测与形近混淆词典 / 文本层对齐提示 / 修复请求 / 公式区特写 / 纠错闭环）；③ 二值化与轮廓提取已结论：tile 计费由分辨率决定、不降 token，仅作去噪/裁边/公式定位的版面分析信号；**裁边方案与双页扫描已定稿（v1.85）**：`VISION_IMAGE_TRIM=conservative` 默认（只裁 ≥20px 完全空白边带）/ aggressive（连通分量保护 + padding 16px）/ none；检测=灰度→阈值化→投影（平滑 5~10px、密度 0.1%、连续 ≥5 行空带）；双页扫描纳入 v1（中间空带 >页宽 1.5% 且两侧对称实质内容才判定，demo 实测修正原 30% 误写；只裁四边）；bbox 入 meta.json；裁后 <50% 回退；验证=bbox 调试图 + 文本层对齐 + 确定性测试。**剩余待定稿项**：压缩图默认宽度——demo A/B 增加 tile 边界档位（1100/1400/1024/960，先实测 tile 尺寸 512/1024 再选档）、后处理校验层是否启用与检测项范围、修复请求开关与节流、纠错闭环入口排期；demo 实验用书 24 真实页图 + 读响应 `usage.prompt_tokens` 实测验证。


### 9.3 缺口备选方案（v1.52 登记，v1.54 增补；v1.59 第 1/2/8/9 行已定稿；v1.63 第 6 行已定稿并实现）

> 对应 `需求-里程碑与验收.md` §8.1 实现状态审查。已定稿缺口（决策 31/32/33/34）在此列实现选型；未定稿缺口列备选方案；**定稿后回写 9.1 并同步技术栈 ADR**。

| # | 缺口 | 需求目标 | 备选方案 | 推荐 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 1 | 两段式导入 + 进度（决策 32） | 上传秒回 `task_id`，后台解析/页图渲染带 `progress`/`stage`，前端导入任务卡片轮询 | A. 任务落库（tasks 表持久化）+ 进度回调（推荐）；B. 内存态任务 + 前端轮询（过渡方案，重启丢失） | A | 已定稿（2026-08-04，实现选型见 异步并行改造计划 §8） |
| 2 | 并发落地（决策 33 / ADR-009） | 视觉提取 4~8 并发、跨书 LLM 打分并发、全局线程池 + 同类型配额 | A. 应用内 `ThreadPoolExecutor` + `Semaphore` + 同类型配额（demo 已验证，推荐）；B. 独立任务进程/消息队列（本地单用户过重，不采用） | A | 已定稿（2026-08-04，见 异步并行改造计划 §8） |
| 3 | Markdown 内嵌本地图片（决策 31） | 导入扫描 `![...](src)` 复制到该书 `images/`，白名单媒体端点 | A. 复制 + `GET /api/books/{id}/media/{filename}` 白名单端点（推荐）；B. 直接引用原绝对路径（路径泄露/跨书失效，不采用） | A | **已实现（2026-08-05，C 组）**：导入复制 images/ + 白名单媒体端点 + 章节返回重写 + 聊天附件（media_service / import_service / books 路由 / chat_service），媒体引用正则兼容 HTML 属性引号边界 |
| 4 | EPUB 保留图文混排（验收 13） | 以原排版（图文混排）形式阅读展示 | A. 前端按原 HTML 片段渲染（保留图片与基础样式，改造 ReaderView 排版组件，推荐）；B. 文本 + 图片资源分离渲染（折中，排版有损）；C. 维持纯文本并修订验收措辞（降级） | A | **已定稿并已实现（2026-08-05，方案 A）**：spine 解析 + 图片提取重写 + 服务端消毒（scrub_html）+ 前端 DOMPurify 渲染（epub_render 分支）；AI/RAG/图谱/搜索消费点统一 html_to_text；验收 13 达成 |
| 5 | 画像内容编辑（验收 5） | 三层画像可查看 / 编辑 / 重置 | A. 仅冷画像字段可编辑（领域偏好 / 知识水平 / 语言风格 / 长期兴趣，推荐，避免热/暖实时数据被手动覆盖冲突）；B. 三层全可编辑（热/暖可手动覆盖，但与自动更新冲突，需合并策略） | A | **已定稿并已实现（2026-08-05，方案 A）**：仅冷画像可编辑（PATCH /api/profile/cold + ProfileView 表单：领域分数 1~10 / 兴趣多选） |
| 6 | RAG/Skill 注入：**LLM 自主挑选**（v1.53 修订，v1.63 已实现） | LLM 结合冷/暖画像与当前需求，从知识库自主挑选 RAG/Skill 注入 | 见 9.1 决策 34（两级混合：书级 LLM 挑选 → 书内规则检索；失败回退规则方案） | LLM 自主挑选（需求已定） | **已定稿并已实现（2026-08-04）**：① 粒度=书级+书内规则检索；② 频率=session_id 会话内缓存（键含章节，TTL 60 分钟）；③ 挑选器=独立模型配置（`RAG_SELECT_*`，未填回退主模型）；④ 目录=按领域分组折叠 + 冷画像偏好领域优先；页模式同样注入跨书 chunks；隐私关闭仅注入 Skill |
| 7 | 聚类算法路线 | 语义增强（同义/近义关联） | 见 `docs/知识图谱聚类算法-改进方案.md`（O1~O8）与 `docs/知识图谱聚类算法.md` §10 | 正式版 L0~L4 已落地（2026-08-10） | **已定稿（2026-08-05 路线 + 2026-08-10 落地）**：demo/clustering_demo.py 对照验证三路成簇 + LLM 降分融合，正式版零改动（见 9.2-2） |
| 8 | 进度反馈：必要操作进度条（v1.54 登记） | 耗时操作用进度条 / 阶段提示提升耐心：导入解析、页图渲染、视觉全书提取、归档（视觉通读 → 总结）、资产总结、图谱重建/增量、跨书 LLM 打分；对话 LLM 挑选等秒级操作用状态提示即可 | A. 全量：任务系统落库 + `progress`(0~100)/`stage` + 前端进度条（卡片/按钮内，依赖缺口 2/3 任务系统升级，推荐）；B. 最小：内存态任务 + 前端轮询 + 阶段文案（无百分比）；C. 仅关键路径（导入 / 归档 / 全书提取）先做，其余后补 | A（分两批：先任务系统，再全量接入） | 已定稿（2026-08-04，见 异步并行改造计划 §8） |
| 9 | 整体异步 / 并行改造（v1.55 登记） | 全链路耗时点异步化 / 并行化：上传导入（两段式）、页图渲染（`render_pdf_pages` 串行逐页且每页重复打开 PDF，需并发 + 复用已打开 doc）、视觉全书提取、跨书 LLM 打分、测试连接（当前同步阻塞，决策 33 ⑤ 未落地）、图谱重建/增量与 post-classify 扫描（后台化）、任务系统（线程池 + 同类型配额）；SQLite WAL 已启用（写串行 / 读并发）；归档「视觉通读 → 总结」保留串行硬依赖；路由层同步 def 为主（长操作已后台化，上传为唯一 async） | A. 全覆盖：任务系统 + 上述全部热点并发化 + 测试连接异步（推荐，与第 1/2/8 项合并为一轮）；B. 分步：先任务系统 + 页图渲染 / 视觉提取 / LLM 打分三热点（Phase 1），测试连接与图谱后台化随 Phase 2；C. 仅热点最小改造（页图渲染 + 测试连接） | A（与第 1/2/8 项同批次落地） | 已定稿（2026-08-04，见 异步并行改造计划 §8） |

---

### 9.3.2 异步/并行审查落点清单（v1.56 登记；v1.62 已全部落地）

> **独立改造计划见 `docs/异步并行改造计划.md`**（背景 / 原则边界 / Phase 1~3 / 验收 / 风险）；**性能优化路径见 `docs/性能优化路径.md`**（索引 / 缓存 / 前端 / 检索增强，三梯队）；本节仅保留落点清单。

> 代码审查产出：按「收益 × 影响面」排序的具体改造落点；与 9.3 第 1/2/8/9 项（任务系统 / 导入 / 并发 / 进度条）合并实现。

| # | 位置 | 现状（同步 / 串行） | 改造方式 | 收益 |
| --- | --- | --- | --- | --- |
| 1 | `api/routes/books.py` `upload_book` | 同步导入 + **同步** `incremental_cross_book_graph`（含 LLM 打分候选对） | 两段式：上传秒回 task_id；增量图谱 / 打分入后台任务（决策 32） | 高：导入不再阻塞 |
| 2 | `api/routes/graph.py` `GET /graph/books` | 懒构建**同步** `compute_cross_book_graph` + `link_graph_assets`（首次打开阻塞数秒~数十秒） | 后台任务 + 前端轮询 / 进度；或预构建缓存 | 高：书多时首开谱系图 |
| 3 | `api/routes/graph.py` `POST /rebuild` | 全量重建同步（含 40 对 LLM 打分串行） | submit 后台化 + progress/stage + 前端轮询；打分并发（决策 33） | 高 |
| 4 | `api/routes/graph.py` `POST /sync` | 同步**逐关系** LLM 增量增改（`sync_assets_for_relations` → `_llm_link_update` 串行） | 后台任务 + 同类型并发配额（信号量） | 高 |
| 5 | `api/routes/settings.py` 测试连接（文本 / 视觉） | 同步阻塞请求 | 后台任务 + 轮询（决策 33 ⑤ 未落地） | 中 |
| 6 | `parsers/pdf.py` `render_pdf_pages` | 串行逐页 + 每页重复打开 PDF | 并发渲染（线程池 4~8）+ 复用已打开 doc | 高：导入大 PDF |
| 7 | `services/vision_extract.py` `rebuild_book_caches` | 串行逐页 LLM 提取 | 并发 4~8 + 页级进度原子回填（决策 33） | 高 |
| 8 | `services/graph/llm_score.py` + `cross_book.py` | 候选对串行打分 | 并发 + 失败回退关键词分（决策 33） | 中高 |
| 9 | `services/graph_sync.py` `sync_assets_for_relations` | 对每条关系串行 LLM（多书场景） | 并发（与 #4 同批） | 高 |
| 10 | 前端 `ProfileView.vue`（268~270） | `getProfile` / `getRecommendations` / `getThresholds` 串行 await | `Promise.all` 并行 | 低（毫秒级） |
| 11 | 前端 `RagDetailView.vue`（27~28） | `getBook` / `getBookAsset` 串行 | `Promise.allSettled` | 低 |
| 12 | 前端 `ReaderView.vue` 打开书 | 章节 / 对话历史 / 书签 / 笔记串行加载 | 无依赖请求并行；相邻页预载（已有） | 中：翻页体感 |
| 13 | `tasks/__init__.py` `submit` | 每任务一线程 + 内存态 | 全局线程池 + 同类型配额 + 落库 + progress（决策 32/33） | 高：全局基础 |
> **落地状态（2026-08-04，v1.62）**：落点 1~9、12、13 已随决策 35 异步/并行改造实施；落点 10、11（前端 `ProfileView` / `RagDetailView` 串行请求）已随性能优化第一梯队并行化（`Promise.all` / `Promise.allSettled`）；性能第一梯队（PRAGMA 调优 / 索引补齐 / 关键词缓存 / 聚类缓存 / 上传分块流式写盘）见 docs/性能优化路径.md §4；第二/三梯队（LLM 结果缓存 / FTS5 全书搜索 / 会话历史裁剪 / 前端懒加载与解析缓存 / 图谱渐进渲染 / 任务中心 UI）同日实施，决策 1~4 定稿，见 docs/性能优化路径.md §4/§7。

### 9.3.1 LLM 挑选实现细节（v1.54 登记，v1.63 已定稿并落地）

> 决策 34 已定需求方向（LLM 自主挑选），以下 4 个实现细节 **2026-08-04 已统一敲定并落地（v1.63）**，已回写决策 34（9.1 第 34 行）。补充敲定：会话载体=前端每次进入对话会话生成 `session_id`（换书/换模式/清空后重新生成）；页模式同样注入跨书 chunks；隐私关闭（`AI_ENABLE_BODY_SEND=false`）时挑选器照常运行、仅注入 Skill 不注入 chunks。

| 细节 | 选项 A | 选项 B | 选项 C | 推荐 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 挑选粒度 | 书级挑选 + 书内规则检索（chunks 用 `retrieve_rag_chunks` 多书版，每书 ≤3 段，成本低） | 直接 chunk/skill 级挑选（更准，每次多耗 token/延迟） | — | A | **已定稿（v1.63）** |
| 挑选频率 | 每次提问都挑选（最准） | 会话内缓存（session_id + 章节为键，TTL `RAG_SELECT_CACHE_TTL_MINUTES`=60，换章/换会话重新挑选） | 相似度命中缓存（复杂，收益小） | B | **已定稿（v1.63）** |
| 挑选器配置 | 复用文本主模型 + 轻量参数（低 max_tokens、禁思考） | 独立配置一套轻量挑选模型（`RAG_SELECT_BASE_URL/API_KEY/MODEL/MODE` 等，未填项回退主模型；`AI_RAG_SELECT_ENABLED=false` 整体降级规则） | — | B（用户敲定） | **已定稿（v1.63）** |
| 候选目录组织 | 全库摘要平铺（≤ 几十本时可用） | 按领域分组折叠（用户 tag→文件夹→聚类领域→未分类），冷画像偏好领域优先展开，每书摘要 ≤60 字 + 技能名 ≤5 个，目录硬上限 150 条 | — | B | **已定稿（v1.63）** |
